import PublicShell from "@/components/PublicShell";import {publicApi} from "@/lib/api";
export default async function NewsDetail({params}:{params:Promise<{slug:string}>}){const{slug}=await params;let p:any=null;try{p=await publicApi<any>(`/api/v1/public/posts/${slug}`)}catch{}return <PublicShell>{p?<><section className="page-hero"><div className="container"><span className="badge">{p.division}</span><h1 style={{marginTop:12}}>{p.title}</h1><p className="lead">{p.summary}</p></div></section><article className="section"><div className="container" style={{maxWidth:820}}><div className="card"><p style={{whiteSpace:"pre-wrap"}}>{p.body}</p></div></div></article></>:<section className="section"><div className="container"><h1>Article not found</h1></div></section>}</PublicShell>}

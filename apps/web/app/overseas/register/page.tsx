import Link from "next/link";import RegisterForm from "@/components/RegisterForm";
export default function OverseasRegister(){return <div className="auth-form-wrap" style={{minHeight:"100vh"}}><div className="auth-card"><Link href="/overseas" className="muted">← Back to Overseas Education</Link><h2 style={{marginTop:22}}>Create Overseas account</h2><p className="muted">Students and education agents can register here.</p><RegisterForm division="overseas"/><p className="muted" style={{marginTop:18}}>Already registered? <Link href="/overseas/login" style={{color:"var(--blue)",fontWeight:800}}>Sign in</Link></p></div></div>}

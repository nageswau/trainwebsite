import Link from "next/link";import PublicShell from "@/components/PublicShell";import {publicApi} from "@/lib/api";import type {University} from "@/lib/types";
type Detail={university:University;courses:any[]};
export default async function UniversityPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;let d:Detail|null=null;try{d=await publicApi<Detail>(`/api/v1/public/universities/${slug}`)}catch{}if(!d)return <PublicShell division="overseas"><section className="section"><div className="container"><h1>University unavailable</h1></div></section></PublicShell>;const u=d.university;return <PublicShell division="overseas"><section className="page-hero"><div className="container"><div className="breadcrumbs">Overseas / Universities / {u.name}</div><div className="eyebrow">University Profile · {u.city}</div><h1>{u.name}</h1><p className="lead">{u.overview}</p><Link className="btn" href={`/overseas/contact?university=${u.slug}`}>Apply / request evaluation</Link></div></section><section className="section"><div className="container grid two"><div><h2>Admission information</h2><div className="card"><h3>Eligibility</h3><p>{u.eligibility}</p><h3>Requirements</h3><ul>{u.requirements.map(x=><li key={x}>{x}</li>)}</ul><h3>Application deadlines</h3><ul>{u.deadlines.map(x=><li key={x}>{x}</li>)}</ul><h3>Scholarships</h3><ul>{u.scholarships.map(x=><li key={x}>{x}</li>)}</ul></div></div><div><h2>Courses</h2><div className="grid">{d.courses.map(c=><div className="card" key={c.id}><span className="badge">{c.level}</span><h3 style={{marginTop:10}}>{c.title}</h3><p className="muted">{c.category} · {c.duration} · Intake: {c.intake}</p><strong>{c.tuition_fee}</strong></div>)}</div></div></div></section></PublicShell>}

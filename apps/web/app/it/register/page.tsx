import Link from "next/link";import RegisterForm from "@/components/RegisterForm";
export default function ITRegister(){return <div className="auth-form-wrap" style={{minHeight:"100vh"}}><div className="auth-card"><Link href="/it" className="muted">← Back to IT Training</Link><h2 style={{marginTop:22}}>Create IT student account</h2><p className="muted">Register, choose an available batch, and receive your enrollment number.</p><RegisterForm division="it"/><p className="muted" style={{marginTop:18}}>Already registered? <Link href="/it/login" style={{color:"var(--blue)",fontWeight:800}}>Sign in</Link></p></div></div>}

import {notFound} from "next/navigation";import PortalShell from "./PortalShell";import PortalSection from "./PortalSection";import TeacherWorkspaceActions from "./TeacherWorkspaceActions";import WorkflowPanel from "./WorkflowPanel";import {serverApi} from "@/lib/api";import {PORTAL_NAV} from "@/lib/navigation";import type {PortalPayload,User} from "@/lib/types";
const labels:Record<string,string>={"it/student":"IT Student","it/trainer":"Trainer","it/placement":"Placement Team","it/hr":"HR / Employer","it/admin":"IT Administrator","overseas/student":"Overseas Student","overseas/counselor":"Counselor","overseas/university":"University Representative","overseas/agent":"Education Agent","overseas/admin":"Overseas Administrator"};
export default async function PortalPage({division,role,section}:{division:"it"|"overseas";role:string;section:string}){const key=`${division}/${role}`;const nav=PORTAL_NAV[key]||[];if(!nav.some(item=>item.href===`/${division}/${role}/${section}`))notFound();let user:User;let data:PortalPayload;try{[user,data]=await Promise.all([serverApi<User>("/api/v1/auth/me"),serverApi<PortalPayload>(`/api/v1/portal/${division}/${role}/${section}`)])}catch(e){return <div className="section"><div className="container card"><h1>Access unavailable</h1><p>{e instanceof Error?e.message:"Unable to load this workspace"}</p><a className="btn" href={`/${division}/login`}>Return to login</a></div></div>}const teacherWorkspace=division==="it"&&role==="trainer";return <PortalShell nav={nav} roleLabel={labels[key]||role} userName={user.full_name}><PortalSection data={data}/>{teacherWorkspace?<TeacherWorkspaceActions section={section}/>:<WorkflowPanel user={user} section={section}/>}</PortalShell>}

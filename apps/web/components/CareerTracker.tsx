"use client";
import {FormEvent,useState} from "react";
import LocalTime from "@/components/LocalTime";
export default function CareerTracker(){const[result,setResult]=useState<any>(null);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setError("");const r=await fetch(`/api/v1/public/career-applications/${encodeURIComponent(String(f.get("code")))}?email=${encodeURIComponent(String(f.get("email")))}`);if(!r.ok){setResult(null);setError("No application matched that tracking code and email.");return}setResult(await r.json())}return <form className="form" onSubmit={submit}><div className="field"><label>Tracking code</label><input name="code" placeholder="EDU-CAREER-…" required/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><button className="btn small">Track application</button>{error&&<div className="form-error">{error}</div>}{result&&<div className="form-message"><strong>{result.job_title}</strong><br/>Status: {result.status}<br/>Updated: <LocalTime value={result.updated_at} time /></div>}</form>}

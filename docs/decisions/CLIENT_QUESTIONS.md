# Client Questions

Generated by `prompts/03_DECISION_REGISTER_AND_CLIENT_QUESTIONS.md`. Companion to
`docs/decisions/PRODUCT_DECISION_REGISTER.md` — that file has the full evidence, impact, and
dependency analysis behind every question here; this file is deliberately just the questions.

These are questions to put to the user/client. None has been answered. None is an approval.

**Note added 2026-09-01:** several answers below marked "Extended" were reconciled a second time,
same session, after the user directed extending an existing external reference implementation
(`docs/evidence/REFERENCE_IMPLEMENTATION_FINDINGS.md`) as the actual starting codebase. Full
before/after history for every revision lives in `docs/decisions/PRODUCT_DECISION_REGISTER.md` —
this file only carries the current, reconciled answer forward.

## 1. Scope

- ~~**(DEC-SCOPE-001)** CRM~~ — **RESOLVED 2026-09-01: in scope, as a Zoho CRM integration** ("used
  wherever required"). **Extended 2026-09-01:** Zoho is configured as the target of the reference
  implementation's existing generic/pluggable CRM-webhook framework, not a hardcoded Zoho-specific
  build. No longer open.
- ~~**(DEC-SCOPE-002)** Employer Portal~~ — **RESOLVED 2026-09-01: current release.** **Extended
  2026-09-01:** confirmed as **net-new build work** — the reference implementation being extended
  has no Employer role at all (corporate hiring is internal-staff-mediated). No longer open.

## 2. Roles

- ~~**(DEC-ROLE-002)** Teacher vs Trainer~~ — **RESOLVED 2026-09-01: same role; "Trainer" is the
  retained name.** No longer open.
- ~~**(DEC-ROLE-001)** Overseas identity model~~ — **RESOLVED 2026-09-01: same Student identity,
  extended with Overseas data/states.** No new role. No longer open.

## 3. Overseas / Agent

- ~~**(DEC-SCOPE-003)** Overseas Education~~ — **RESOLVED 2026-09-01: current release.** No longer
  open (commercial-scope reconciliation carried to BRD open items).
- ~~**(DEC-SCOPE-004)** Agent Portal~~ — **RESOLVED 2026-09-01: yes, in scope. Agent can self-register,
  but the account requires approval by Overseas Admin before it's active.** No longer open.
- ~~**(DEC-SCOPE-005)** Agent commissions~~ — **RESOLVED 2026-09-01: in scope. Trigger = the referred
  student's application is finalised AND they join the college. Payout requires a separate Overseas
  Admin approval step.** **Extended 2026-09-01:** reconciled against the reference implementation,
  which has admin-discretionary per-application amounts but neither the automatic trigger nor a
  payout-approval step — kept the admin-discretionary amount (this **permanently closes** the
  "is there a fixed rate?" question: there is not, by design), added the missing trigger and
  approval gate as new build work. No longer open.
- ~~**(DEC-SCOPE-006)** Visa sub-flow~~ — **RESOLVED 2026-09-01: yes, in scope.** Real checklist/
  rules still needed (only 4 category names exist today) — see below.
- ~~**(DEC-WF-001)** Application state sequence~~ — **RESOLVED 2026-09-01: base sequence confirmed**
  (Enquiry → Eligibility → University/Course Selection → Offer → Visa Documentation → Tracking →
  Enrolment). **Still open:** exact state codes and rejection/waitlist/deferral rules — see below.
- **(Exception paths, new)** What happens on rejection, waitlist, or deferral at each Overseas
  application stage? Not yet described.
- ~~**(Documents, DEC-DATA-001)** Document checklist~~ — **PARTIALLY RESOLVED 2026-09-01: varies by
  country/university/course** (not universal). Still need the actual checklist per combination —
  see below.
- **(Document checklist data, new)** Please provide the actual document checklist for each relevant
  country/university/course combination (or a representative set to start with).
- ~~**(University/course data, DEC-DATA-002)** Data source~~ — **RESOLVED 2026-09-01: manual, by
  Admin and backend team.** No external provider integration needed. No longer open.

## 4. Learning / Live Classes

- ~~**(Live-class provider, DEC-LIVE-001)** Provider~~ — **RESOLVED 2026-09-01: Zoho.** **Extended
  2026-09-01:** Google Meet, already working in the reference implementation, is retained as a
  second adapter rather than stripped — Zoho stays the default. Concurrent-recording handling still
  open — see Meeting platform below.
- ~~**(Meeting platform, DEC-SCOPE-007)** In or separate~~ — **RESOLVED 2026-09-01: separate
  project, out of EduSphere's scope.** No longer open.
- ~~**(LMS, DEC-LMS-001)** External integration?~~ — **RESOLVED 2026-09-01: no external integration —
  build a native LMS module instead.** See below for what's still open.
- **(LMS scope, new)** Beyond notes/assignments/Q&A/attendance/recordings/certificates (already
  scoped), what specifically should the native LMS module include — e.g. structured course-content
  authoring, sequencing/prerequisites, quizzes, gradebooks, SCORM-style packages?
- ~~**(Trainer/batch assignment, DEC-WF-002)** Assignment method~~ — **RESOLVED 2026-09-01:
  student-selected, 20/slot capacity, no changes once booked (~3-month lock-in).** Still open: exact
  batch-naming convention, and whether enrolment ID should be shown in the UI — see below.
- **(Batch naming / ID visibility, new)** What is the exact batch-naming convention beyond the "PY"
  example? Should the student's unique/enrolment ID be shown in the UI (currently hidden in the
  demoed prototype)?

## 5. Payments / Finance

- ~~**(Payment gateway, DEC-PAY-001)** Gateway~~ — **RESOLVED 2026-09-01: Stripe.** **Extended
  2026-09-01:** Razorpay, already working in the reference implementation, is retained as a second
  adapter rather than stripped — Stripe stays primary. No longer open.
- ~~**(Currency/tax/invoice, DEC-PAY-002)** Model~~ — **RESOLVED 2026-09-01: multi-currency /
  multi-region.** Still open: exact regions/currencies, per-region tax mapping, invoice format —
  see below.
- **(Tax/invoice detail, new)** Which specific regions/currencies need support (e.g. UK/GBP,
  India/INR, plus Overseas destination countries)? What's the per-region tax rule and invoice
  format?

## 6. Notifications / Integrations

- ~~**(WhatsApp provider, DEC-NOT-001)** Provider~~ — **RESOLVED 2026-09-01: Twilio.** **Extended
  2026-09-01:** SMS, already working in the reference implementation, is retained as a third channel
  alongside Email and WhatsApp — provider identity for SMS not yet specified. Still open:
  triggers/templates/opt-out rules — see below.
- **(Notification rules, new)** What should trigger a notification (per event), on which channel,
  with what message content, and what are the opt-out rules — for both domestic and Overseas flows?

## 7. Privacy / Security

- ~~**(GDPR, DEC-PRIV-001)** Baseline model~~ — **RESOLVED 2026-09-01: standard GDPR defaults**
  (explicit consent, self-service export/delete, no special employer-visibility restriction). Exact
  retention periods still open; legal review still recommended before release.

## 8. Technology / Cloud

- ~~**(Stack, DEC-TECH-001)** Application stack~~ — **RESOLVED 2026-09-01: confirmed as-is
  (Next.js + TypeScript, FastAPI, PostgreSQL).** No longer open.
- ~~**(Architecture, DEC-ARCH-001)** Style~~ — **REVISED 2026-09-01: modular monolith** (matches
  the reference implementation being extended; overturns this same day's earlier "microservices"
  answer, which itself had overturned the blueprint's original modular-monolith proposal — full
  history in `PRODUCT_DECISION_REGISTER.md`).
- ~~**(Cloud, DEC-INFRA-001)** Provider~~ — **RESOLVED 2026-09-01: DigitalOcean** (not AWS at all —
  overturns both the original EC2 preference and the blueprint's Fargate recommendation).
  **Re-confirmed 2026-09-01** after the reference implementation reconciliation: DigitalOcean stays
  the target, now specifically meaning "redeploy that codebase's existing Docker/Postgres/Redis
  workload on DigitalOcean," not a from-scratch design. Service-level DigitalOcean architecture
  still open.
- ~~**(Cloud — environments, DEC-ENV-001)** Boundaries~~ — **RESOLVED 2026-09-01: production only
  on DigitalOcean; dev/local may stay off-cloud.** No longer open.

## 9. UX Authority

- ~~**(DEC-UX-001)** Authority~~ — **RESOLVED 2026-09-01: "EduSphere UK Responsive UX Prototype -
  Revised and Complete" (`DAHRCNYnu6g`), structural guide only, not pixel-parity.** The ~20 other
  adjacent Canva designs remain untriaged/out of scope.

## 10. Delivery / Timeline

- ~~**(DEC-DATE-001)** Target date~~ — **RESOLVED 2026-09-01: 5 September 2026.** ⚠ Flagged as a
  major feasibility risk given the scope confirmed in this session (see
  `PRODUCT_DECISION_REGISTER.md` for detail) — 4 days from today for a scope this size is not a
  credible estimate. Recorded as-stated; should be revisited once real planning happens.

---

**Note:** every question above traces to a `Decision ID` with full supporting evidence in
`docs/decisions/PRODUCT_DECISION_REGISTER.md`. Per `docs/decisions/APPROVAL_GATES.md` (GATE-02),
BRD work does not begin until the high-impact items among these — scope (§1, §3), technology/cloud
(§8), and UX authority (§9) — are confirmed.

---

## Round 2 — 12 September 2026 product-review call (`EVID-011`/`012`/`013`)

New evidence reviewed: a live product-demo/review call transcript (`EVID-011`), its vendor-authored
summary PDF (`EVID-012`), and an EduSphere marketing brochure (`EVID-013`). Full detail and evidence
citations are in `docs/evidence/EVIDENCE_REGISTER.md`, `CONFLICT_MATRIX.md` (`C-08`), and
`SCOPE_DELTA.md` (`D-09`–`D-11`). None of the items below has been answered by the client.

### 1. ~~CRM — reopened, highest priority~~ — RESOLVED 2026-09-12 (for real)

**(C-08)** `DEC-SCOPE-001` was `CONFIRMED_CURRENT` as a Zoho integration; the 12 Sep review call
(`EVID-011`) and its own vendor-authored summary (`EVID-012`) directly contradicted each other on
this point (see `PRODUCT_DECISION_REGISTER.md` for full detail). In `EVID-011`, the vendor states on
the record, repeatedly, that no separate CRM is being built and the plan is Zoho integration.
`EVID-012` §16 asserts the opposite: that EduSphere needs its own Internal CRM.

**RESOLVED 2026-09-12, asked directly, in-session: build a custom Internal CRM.** `DEC-SCOPE-001`
re-resolved — this reverses the 2026-09-01 Zoho-integration resolution. (Note: an earlier automated
pass through this project had briefly and incorrectly claimed this was already resolved before
anyone had actually been asked; that documentation error was caught and reverted, and is separate
from this genuine resolution.) **Not resolved by this answer:** the detailed CRM workflow (lead
stages, assignment rules, permissions, reports) — the client-supplied CRM process document that
`EVID-012` itself says is required has not been received. No CRM Feature ID, RBAC entry, or
architecture work may proceed until it arrives — see `docs/product/PRD_OPEN_ITEMS.md` item 61.
**Next step: obtain the Internal CRM workflow document from the client** (lead sources,
stages/transitions, counselor assignment rules, ownership/reassignment, follow-up process,
conversion criteria, lost/duplicate handling, required dashboards/reports/filters/exports, role
permissions, approval processes — the full list `EVID-012` §26 itself asks for).

### 2. ~~New "Coordinator" role and certificate-issuance authority~~ — RESOLVED 2026-09-12

**(D-09)** Both `EVID-011` and `EVID-012` introduce a "Coordinator" role, distinct from Trainer/
Admin, as the one who issues certificates, with Founder + Coordinator digital signatures placed on
the certificate. No existing role/RBAC document has this role.

**RESOLVED 2026-09-12:** Coordinator is a **new formal RBAC role**, and certificate issuance moves
**exclusively** to Coordinator — removed from Trainer and Admin, both of whom can issue certificates
today per the existing feature catalogue. **Not resolved by this answer:** how a Coordinator account
is created/authenticated, and whether Coordinator has any duties beyond certificate issuance — see
`docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-ROLE-003`. Do not remove Trainer/Admin's existing
certificate-issuance code until the Coordinator role is fully specified and built.

### 3. Granular per-user permission overrides

**(D-09)** The client asks for permission control at the individual-user level ("access to the
particular page to the particular person"), not only role-level bundles. The existing RBAC direction
already supports role/page/action permissions — is a further per-individual-user override layer
required for this release, or does role-level granularity (already the confirmed direction) satisfy
this once page/action granularity is fully built out?

### 4. Keyword-match auto-grading threshold

**(D-09)** An approximately 80% keyword-match threshold (order-independent) for auto-grading
descriptive assignment answers was discussed informally. **Question:** should this be formalized as
an exact business rule (threshold %, algorithm — e.g. cosine similarity over keyword sets — partial-
credit behavior below threshold), or does it remain trainer-reviewed only for now?

### 5. Business-facing unique Student/Agent ID format

**(D-09)** A short (≤10 alphanumeric characters suggested), searchable, memorable ID is requested,
separate from the existing 16-character technical UID (this also closes part of `DEC-WF-002`'s
still-open ID-visibility question). **Question:** confirm the exact format (length, character set,
prefix convention e.g. by division/course) and whether it applies to students only or also to agents
and other operational identities.

### 6. ~~School/Institution Portal access model~~ — RESOLVED 2026-09-12

**(D-11)** Unlike most items in this round, this one was **not just missing detail — it was
unresolved in the source call itself.** EVID-012 called a school-level login "confirmed," but the
actual transcript ended the topic with disagreement unsettled between a per-student-login model and
a school-level-only model.

**RESOLVED 2026-09-12: one login per school/institution representative** (counselor-managed student
data internally; the school gets read-only progress visibility for their own students, not
individual student logins) — see `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-009`. Bulk
school/student data upload remains deferred: manual entry first, bulk upload as a later-phase item if
needed.

### 7. Agent Performance Dashboard KPIs

**(D-09)** A dashboard showing submitted/under-review/approved/visa-processed counts (all-time and
by period) with a green/orange/red performance indicator was discussed at a concept level only.
**Question:** confirm the final KPI list and whether the same dashboard concept should extend to
internal counselors (also raised in the call) in this release or a later one.

### 8. Overseas multi-university/multi-destination application detail

**(D-10)** The call confirms a student can apply across multiple destinations/universities in one
flow with per-university backend eligibility screening — refining `DEC-WF-001`, not contradicting
it. No specific question is required unless the client wants to cap the number of concurrent
university selections per application (none was stated).

### 9. School Partnership tiers (`EVID-013`) → software mapping — still genuinely open

**(New, from the marketing brochure)** `EVID-013` describes a Bronze/Silver/Gold/Platinum "School
Partnership Model" with different service bundles per tier (career seminars, psychometric testing,
IELTS/SAT coaching, dedicated counselor, campus visits, etc.). **Question, explicitly deferred to the
real client rather than decided in-session:** should a school's portal access/features vary by which
partnership tier they've purchased, or is this purely an offline sales/marketing construct with no
platform implications? No Feature ID, RBAC entry, or School-Portal design should assume either
answer until the client responds.

---

**Note:** Items 1, 2, and 6 are now resolved (`DEC-SCOPE-001` re-resolved, `DEC-ROLE-003`,
`DEC-SCOPE-009` — see `PRODUCT_DECISION_REGISTER.md` Group 12). Items 3-5, 7, and 8 remain open
enhancement questions to already-approved feature scope and do not block already-scheduled Wave 1-4
work that doesn't touch them, but should not be built ahead of an explicit answer. Item 9 (School
Partnership tiers) is explicitly deferred to the real client, not decided in-session, and stays
open. None of items 1-9 have been through `prompts/03_DECISION_REGISTER_AND_CLIENT_QUESTIONS.md`'s
formal Decision ID assignment ceremony beyond the ad hoc IDs already cross-referenced above.

---

## Round 3 — 14 September 2026: School Portal bulk-upload reopen request

New evidence this round: seven untracked "Functionalities" markdown documents were found outside
the evidence pipeline and registered as `EVID-014`–`EVID-020` (see `EVIDENCE_REGISTER.md` and
`CONFLICT_MATRIX.md` `C-09`). `EVID-014` (`School CRM.md`) prompted a request to add bulk student
and results upload to the School Portal, which directly reopens an already-`CONFIRMED_CURRENT`
decision rather than raising new, unresolved ground.

### 10. Reopen bulk school/student data upload deferral (`DEC-SCOPE-009`) — template-first workflow

**(New)** `DEC-SCOPE-009` (2026-09-12) confirmed a read-only, representative-only School Portal
login model and explicitly deferred bulk student-data upload: "manual entry first ... bulk upload as
a later-phase item if needed." **Question:** is now that later phase? Specifically:

- Should **student roster bulk upload** move from deferred to current scope, performed by the
  internal counselor/Academic Team (consistent with the confirmed read-only school-representative
  model, not by the school itself), using a **template-download-first** workflow: download a fixed
  template → fill offline → upload → server-side validation → row-level accept/reject report?
- Should **academic results bulk upload** be added as new scope (no prior decision covers this at
  all — see `PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-010`)? If so: who uploads (School staff,
  Teacher, or Edusphere Academic Team), and what approval gate sits between upload and
  Parent/Student visibility?

**PARTIALLY RESOLVED 2026-09-14, asked directly, in-session, decided jointly with item 13 (they
overlap exactly as predicted):** student roster bulk upload — **yes**, template-download-first,
performed by the **School Coordinator** (not internal staff — see item 13's role-structure answer,
which changed who the actor is). Academic results bulk upload — **still open**, user answered "not
sure, decide later." See `DEC-SCOPE-010`'s resolution (part 1 confirmed, part 2 pending) for full
detail.

### 11. ~~`School CRM.md`'s individual student/parent/teacher login model vs `DEC-SCOPE-009`~~ — RESOLVED 2026-09-14

**(New, `C-09`)** `EVID-014` proposed the School Master creating individual Teacher, Parent, and
Student logins, contradicting `DEC-SCOPE-009`'s confirmed "one read-only login per school
representative, not individual student logins."

**RESOLVED 2026-09-14, asked directly, in-session:** no individual Student login for School-
affiliated students — and the user extended this in the same instruction to Agent-referred students
as well: *"ignore the student login for school students and agent students. Only edusphere students
should have the login."* See `DEC-ROLE-004` in `PRODUCT_DECISION_REGISTER.md`. `EVID-014`'s
individual-*student*-login proposal is rejected outright, not deferred. **Correction, same day:**
this resolved only the Student leg of `EVID-014`'s login hierarchy — it did not rule on the
document's Teacher, Parent, Coordinator, or Principal logins, which remain open; see item 13 below.

### 12. ~~Who acts on behalf of a School-affiliated or Agent-referred student, now that neither gets a login~~ — RESOLVED 2026-09-14

**(New, from `DEC-ROLE-004`)** Item 11's resolution reverses part of an already-shipped feature:
`AGT-002` ("Referred-student roster and status," complete since Wave 3) and its data model
(`AgentStudent.student_user_id`) currently assume an agent-referred student has their own real
account. If they no longer do, who performs the Overseas-application actions currently modeled as
that student's own actions (document upload, application submission, offer/visa status tracking)?

**RESOLVED 2026-09-14, asked directly, in-session: the Agent acts on behalf of their referred
students; the School representative acts on behalf of school-affiliated students** — the same
counselor-managed pattern `DEC-SCOPE-009` already confirmed for School, now extended explicitly to
Agent. **Not resolved by this answer:** the actual RBAC/data-model/`AGT-002` rework needed to make
this real — see `DEC-ROLE-004`'s "implementation reconciliation" note in
`PRODUCT_DECISION_REGISTER.md`. That is build/design work for a future pass, not a further client
question.

---

### 13. ~~Reopen `DEC-SCOPE-009`'s school-side role structure~~ — RESOLVED 2026-09-14 — Principal/Coordinator/Teacher/Parent

**(New, `C-11`)** The user asked directly whether `EVID-014`'s §33 "SCHOOL ADMIN LOGIN" section —
four distinct school-side roles with named permissions — is covered by confirmed requirements. It is
not, and the user then asked for this to be drafted as a formal reopen request against
`DEC-SCOPE-009` (one undifferentiated, read-only login per school representative).

**Question, in four independent parts — do not answer as one bundle:**

1. **Login cardinality:** multiple named school-side logins (Principal/Coordinator/Teacher) instead
   of the one confirmed representative login? If yes, what actually differs between them?
2. **Write access:** School Coordinator "add/manage students, schedule activities, track
   attendance" directly reverses `DEC-SCOPE-009`'s read-only constraint — is write access wanted for
   a school-side role, and does it mean manual entry, bulk upload (merging with item 10 above), or
   both?
3. **Teacher login:** a partner school's own teaching staff getting an individual EduSphere login —
   in scope? (Distinct from `DEC-ROLE-002`'s "Teacher = Trainer," which is EduSphere's own
   IT-training instructor role, an unrelated actor — a collision to avoid if this is approved.)
4. **Parent login:** in scope at all? Never raised before `EVID-014`; distinct from `DEC-ROLE-004`'s
   student-login question.

**RESOLVED 2026-09-14, asked directly, in-session, across all four parts in one pass:** full role
set adopted — Principal (read-only), School Coordinator (write access, including bulk upload —
see item 10), Teacher (assigned students, distinctly named `school_teacher` to avoid the
`DEC-ROLE-002` collision), Parent (own child only). All four scoped to their own institution's data
only — user's own words: "they can access the respective school data only." See the full resolution
in `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-011`. **Not resolved by this answer:**
BRD/PRD/Feature Catalogue/RBAC propagation, and whether Career Guidance/Psychometric/Counselling
exist as buildable modules at all (separate, still-open question).

### 14. ~~Which internal EduSphere role(s) manage school-affiliated student data~~ — RESOLVED 2026-09-14

**(New, self-correction, `PRD_OPEN_ITEMS.md` item 71)** While propagating `DEC-SCOPE-009` into
BRD/PRD/Feature Catalogue, the first pass incorrectly stated the underlying student data is managed
by the existing Overseas `Counselor` role — that decision's own text only says "counselors manage
the underlying student data from the internal system" generically, and does not name that specific,
already-built role. The user caught this by asking for the full `EVID-014` content to be re-checked.
`EVID-014` §13 (still just an unattributed `DERIVED_BLUEPRINT`, not authoritative) proposes four
different, specialized roles instead — Career Counselor, Psychometric Team, Academic Team, School
Partnership Manager. **Question:** which internal role(s) actually manage this data — the existing
`Counselor`, one or more new roles, or something else?

**RESOLVED 2026-09-14, asked directly, in-session: reuse the existing `Counselor` role**, extended
to also cover school-affiliated case management, rather than building the four specialized roles
`EVID-014` proposed. Division of labor: School Coordinator (school-side) owns roster/operational
data; Counselor (EduSphere-side) owns service-delivery data. See
`docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-ROLE-005` for the full resolution. **Not resolved
by this answer:** whether Career Guidance/Psychometric/Counselling exist as buildable modules at
all — that stays open.

---

### 15. ~~School partner record and role-account provisioning mechanism~~ — RESOLVED 2026-09-14

**(New)** Surfaced repeatedly while propagating `DEC-SCOPE-011` into architecture and UX — nothing
anywhere specifies how a School partner record or its four role accounts (Principal/Coordinator/
Teacher/Parent) actually get created. `SCH-001`'s precondition is just "a School partner record
exists," same placeholder as `UNI-001` uses for University Representative.

**Question, in four independent parts:**

1. Who initiates the School partnership record — School self-registers (Agent's pattern, gated
   behind approval), EduSphere staff creates it entirely (University Representative's pattern, no
   gate modeled), or a sales/partnership-manager-led process (raised only in the unconfirmed
   `EVID-020`, do not treat as precedent without its own confirmation)?
2. Once the School record exists, who creates the four role accounts — EduSphere staff creates all
   four, EduSphere seeds one (e.g. Coordinator) who then invites the rest, or a pure invite-link flow?
3. Is there an approval gate before a new School/account becomes active, or is it active immediately?
4. Does this interact with the still-open School Partnership tiers question (Round 2 item 9)? A
   "no interaction" answer is valid on its own — this part doesn't require resolving item 9 first.

**RESOLVED 2026-09-14, asked directly, in-session, across all four parts in one pass:** (1) Overseas
Admin creates the School record entirely — no School-side self-service; (2) Overseas Admin also
seeds the initial School Coordinator account, who then invites/creates Principal, Teacher, and
Parent for their own institution; (3) no approval gate — every School and account is active
immediately on creation; (4) provisioning is independent of the still-open Partnership tiers
question (item 9 above stays open on its own). New Feature ID authorized: `SCH-003` (School partner
onboarding). See `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-012` for the full
resolution.

---

**Note:** All of Round 3 is now resolved — items 10-15, with item 11's correction narrowing exactly
what it resolved. Items 10-14 stem from an unattributed, undated `DERIVED_BLUEPRINT` document
(`EVID-014`) discovered untracked in the working directory, not from a client-supplied source or
transcript, plus the user's own direct in-session statements; item 15 was a genuine gap with no
source document behind it, resolved directly by the user.

---

## Round 4 — 14 September 2026: results module + service-module scope

### 16. ~~Academic results/marks module scope~~ — RESOLVED 2026-09-14

**(Was Round 3 item 10's remaining sub-question)** Left open earlier the same session ("not sure,
decide later"). **RESOLVED, asked directly again: yes, in scope.** See `docs/decisions/
PRODUCT_DECISION_REGISTER.md` `DEC-ROLE-006` for the full actor/workflow resolution — uploaded by
the Academic Team, a new specialized role, with a Draft→Verified→Published gate adopted as the
working design (one detail not verbatim re-confirmed, flagged in `DEC-ROLE-006` itself).

### 17. ~~Career Guidance, Psychometric, and Counselling module scope~~ — RESOLVED 2026-09-14

**(Was `DEC-ROLE-005`'s open item)** **RESOLVED, asked directly: yes, all three are in scope.**
Internal actor model **superseded** in the same answer — `DEC-ROLE-005`'s "reuse the existing
Counselor role" is replaced by `EVID-014` §13's five specialized roles (Career Counselor,
Psychometric Team, Academic Team, Edusphere School Manager, School Partnership Manager), named
directly by the user, not inferred from the document alone. School Coordinator gets read-only
visibility/reporting into this content; the five specialized teams exclusively create/manage it. See
`DEC-ROLE-006` for full detail, including two points still open within it: Edusphere School
Manager's and School Partnership Manager's exact duties/overlap.

---

**Note:** Round 4 resolves both remaining School Portal scope questions. Two narrow details stay
open within `DEC-ROLE-006` itself (not full client questions, just implementation-level precision
to nail down before building): the exact duties/overlap of Edusphere School Manager vs. School
Partnership Manager, and word-for-word re-confirmation of the Draft→Verified→Published results gate.

## Round 5 — 15 September 2026: Parent Portal content

### 18. Parent Portal — Skills, Portfolio, Overseas education progress

**(From `DEC-SCOPE-015`)** The user restated `EVID-014` §23's "Parents can see" list and called it
very important for the school model. Eight of the eleven items are now live (`SCH-007`). The three
below have **no confirmed module behind them**, so the Parent Portal cannot show them without
inventing data — please confirm each separately:

1. **Skills** — should a Skills record exist for a school student? Who enters it (Career Counselor,
   a Soft-Skills/Digital-Skills trainer role from `EVID-014` §9/§10, the school)? What is tracked
   (skill name + level? training attended + score?)
2. **Digital Portfolio** — is `EVID-014` §14's portfolio in scope? Which sections, who edits, and is
   a "% complete" figure wanted?
3. **Overseas education progress** — should a school student be linkable to an Overseas application
   pipeline? Today a `SchoolStudent` has no login and no link to `overseas_student` (`DEC-ROLE-004`).
   If yes: is that link created when the student later registers as an Overseas student, or does the
   school student get a Global Education record of its own (`EVID-014` §15–20)?

### 19. Parent notifications — "Test", "Application", "Important deadline"

**(From `DEC-SCOPE-015`)** Assessment → Counselling → Workshop are built; result Published is built
as the parent notification `EVID-014` §11 itself specifies. For the remaining three steps:

1. **Test** — does this mean IELTS/SAT test dates (`EVID-014` §12/§13 — not confirmed modules), a
   school exam date, or something else?
2. **Application** and **Important deadline** — which events, from which module? These depend on
   item 18.3's answer (Overseas link) and on a deadline entity no confirmed module has.

### 20. Parent access to the psychometric report file

`SCH-007` shows a Parent the assessment's status only, never the report file — the readable list has
always been built that way. Should a Parent be able to open the report? If so, should the
Psychometric Team release it explicitly first (like results' Published gate)?

---

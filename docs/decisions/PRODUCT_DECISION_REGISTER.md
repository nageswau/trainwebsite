# Product Decision Register

Generated by `prompts/03_DECISION_REGISTER_AND_CLIENT_QUESTIONS.md`, NO-ASSUMPTION MODE.

Source material: `docs/evidence/EVIDENCE_REGISTER.md`, `docs/evidence/CONFLICT_MATRIX.md`,
`docs/evidence/SCOPE_DELTA.md`, `docs/evidence/SOURCE_GAPS.md`, `docs/ux/UX_REFERENCE_AUDIT.md`,
`docs/ux/UX_REFERENCE_GAPS.md`, `docs/decisions/DECISION_REGISTER_TEMPLATE.md`.

**Every decision below starts, and remains, `UNCONFIRMED`.** Where evidence supports a lean,
one is offered in a clearly separate "Recommendation" line — a recommendation is not a decision,
and Claude does not approve its own recommendation. Only the user/client can move a status to
`CONFIRMED_CURRENT`, `CONFIRMED_FUTURE`, `OUT_OF_SCOPE`, `SUPERSEDED`, `BLOCKED`, or `REJECTED`.

Allowed statuses (from the template): `UNCONFIRMED`, `CONFIRMED_CURRENT`, `CONFIRMED_FUTURE`,
`OUT_OF_SCOPE`, `SUPERSEDED`, `BLOCKED`, `REJECTED`.

---

## Group 1 — Scope

### DEC-SCOPE-001 — Admin CRM

**Question:** Is a custom CRM (pipeline, counsellor queue, lead scoring, campaign automation, CRM
communication history) in scope for the current release? integrate with ZOHO CRM

**Evidence for INCLUDE:** EVID-002 (original requirement) lists `CRM` as an Admin Portal module.
EVID-009 (commercial quotation, 30 Jul 2026) lists `CRM Module` under Admin Portal scope. It should be used wherever required.

**Evidence for EXCLUDE:** EVID-004/005 (blueprint, 1 Aug 2026) states, repeatedly and on every page
footer, "CRM excluded... Only admissions handoff is included," citing a source ("previous approved
quotation") that cannot be located anywhere in `docs/sources/` (`SOURCE_GAPS.md` G-01).

**Impact of INCLUDE:** larger Admin Portal (pipeline, counsellor queue, lead scoring, campaign
automation, communication history), larger backlog/screens/tests than the blueprint's current
459-task/824-test catalogue assumes.
**Impact of EXCLUDE:** matches the blueprint's existing catalogue as-is; no rework needed there.

**Dependencies affected:** Admin role/RBAC bundle (blueprint §2.1), Feature Catalogue, Test
Catalogue (824 cases currently assume exclusion), BRD Admin Portal section, DEC-SCOPE-002 (both
scope flips are attributed to the same missing G-01 source).

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): CRM is **in scope**, specifically as a **Zoho CRM
integration** ("integrate with ZOHO CRM... It should be used wherever required") — not a
custom-built pipeline/lead-scoring/counsellor-queue module as EVID-002/EVID-009 originally
described. The exact integration scope (which entities sync, which events trigger sync, auth model)
is not yet defined and should be carried into BRD/PRD as a business need, not assumed.

**Extended (2026-09-01, same session):** the reference implementation being extended
(`docs/evidence/REFERENCE_IMPLEMENTATION_FINDINGS.md`) implements a **generic, provider-agnostic
CRM webhook** ("internal enquiry record + optional external CRM webhook synchronization"), not a
Zoho-specific integration. User confirmed keeping the generic framework and extending it — i.e.
**Zoho is configured as the CRM webhook target**, rather than the generic webhook abstraction being
ripped out in favor of Zoho-specific code. The actual requirement is unchanged (Zoho is still where
enquiries end up); what changes is that the underlying mechanism stays pluggable rather than
hardcoded, matching the existing codebase.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01,
extended 2026-09-01 — **Zoho configured as the CRM webhook target within the existing generic
webhook framework**, not a hardcoded Zoho-only integration

**Reopened (2026-09-12, new session, new evidence):** a live product-review call (`EVID-011`, 12 Sep
2026) and its own vendor-authored summary PDF (`EVID-012`) directly contradicted each other on this
exact question. In `EVID-011`, the vendor states on the record, repeatedly, that no separate CRM is
being built ("No, we are not making [a separate CRM]... We can integrate with Zoho CRM"). `EVID-012`
§16 asserts the opposite: "EduSphere will require its own Internal CRM rather than integrating the
primary lead-management workflow with Zoho CRM." Per `CLAUDE.md`, neither document's own framing
(including `EVID-012`'s self-labeled "Confirmed Key Decisions" table) was treated as authoritative on
its own. **An earlier automated draft of this entry incorrectly stated "User confirmed (2026-09-12):
build a custom Internal CRM" before any user had actually been asked — that was a documentation
error, caught and reverted, not a real resolution.**

**Re-resolved for real (2026-09-12, this session, asked directly):** user confirmed **build a
custom Internal CRM**, reversing the 2026-09-01 Zoho-integration resolution. **What this does and
does not resolve:** the *direction* is now genuinely confirmed. The *detailed workflow* is not — the
client-supplied CRM process document that `EVID-012` itself says is required (lead sources/stages/
transitions, counselor assignment rules, ownership/reassignment, follow-up, conversion criteria,
lost/duplicate handling, notifications, dashboards, reports/filters/exports, role permissions,
approvals — `EVID-012` §26's own list) has still not been supplied. No Feature ID, RBAC entry, DB
schema, or architecture work for the Internal CRM may be authored until it arrives — see
`docs/product/PRD_OPEN_ITEMS.md` item 61. The existing generic CRM-webhook framework and its Zoho
configuration are not instructed to be removed and should not be stripped pre-emptively.

**Downstream documentation to correct:** `docs/product/BRD.md`, `BRD_CHANGE_LOG.md`,
`docs/product/PRD.md`, `PRD_CHANGE_LOG.md`, and `docs/product/PRD_OPEN_ITEMS.md` item 61 previously
carried the fabricated "confirmed"/"reversed" language; that was reverted, and now updated again to
reflect the genuine 2026-09-12 confirmation below. `docs/features/MASTER_FEATURE_CATALOG.md`/
`feature_catalog.json` (`PUB-002`, `ADM-002`) and `docs/architecture/RBAC_MATRIX.md`/
`API_CONTRACT.md`/the actual `services/portal.py` CRM-webhook code remain untouched (still Zoho
webhook, as-built) — no code/RBAC/architecture work is authorized by this decision until the CRM
workflow document arrives.

**Status:** CONFIRMED_CURRENT (direction: custom-built Internal CRM) — Approved by: user
(in-session) — Approval date: 2026-09-01 (original Zoho resolution), **re-resolved 2026-09-12**
(genuine, asked and answered directly in-session — reverses the Zoho-integration resolution) —
detailed CRM workflow **BLOCKED** pending required client input (`PRD_OPEN_ITEMS.md` item 61)

---

### DEC-SCOPE-002 — Employer Portal

**Question:** Is the Employer Portal (registration, job posting, candidate search, interview
scheduling) in the current release, or a future phase?

**Evidence for FUTURE:** EVID-002: "Employer Portal (**Future**)." EVID-009: "Employer Portal
(**Future Phase**)... not included in the current quotation unless specifically requested."

**Evidence for CURRENT:** EVID-004/005 lists Employer as a current role with a full permission
bundle (§2.1) and dedicated reports (RPT-018 Employer Job Activity, RPT-020 Placement Outcomes),
citing the same missing G-01 source as its authority (`CONFLICT_MATRIX.md` C-02, C-05).

**Impact of CURRENT:** adds an entire Employer role, employer-facing screens/APIs/RBAC, job-posting
and candidate-search and interview-scheduling workflows, and the GDPR candidate-visibility question
(DEC-PRIV-001).
**Impact of FUTURE:** smaller v1 scope; the blueprint's existing task/screen/test counts would need
to shrink.

**Dependencies affected:** Role/RBAC matrix, Feature Catalogue, UX Screen Catalogue, DEC-PRIV-001
(candidate-profile visibility), cost/timeline (DEC-DATE-001 — the 14-16-week/₹2,45,000 figure is
tied to the same unverifiable source that also claims Employer-included).

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01, via direct question): **current release.** The
employer-approval evidence workflow (blueprint's own open item #28) is still not defined and should
be carried forward as an open item into BRD.

**Re-confirmed (2026-09-01, same session) after discovering the reference implementation has no
Employer role at all** (`docs/evidence/REFERENCE_IMPLEMENTATION_FINDINGS.md` §3/§6 — corporate
hiring there is internal-staff-mediated: `placement_team`/`hr_team` plus a public apply/track flow,
no Employer login/account). Asked directly whether to build the missing role or accept the existing
model: user confirmed **build the missing Employer role as new extension work** on top of the
existing Placement Team/HR Team model. This is not a reversal — it reaffirms the original
confirmation — but it does mean Employer Portal is **net-new implementation work** against this
particular codebase, not a gap-fill of something partially there. Carry this into the BRD risk
register (effort/timeline impact) and the Architecture phase (a role/route the reference
implementation's RBAC model does not yet define).

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01,
re-confirmed 2026-09-01 — now explicitly flagged as net-new build work against the reference
codebase

---

## Group 2 — Roles

### DEC-ROLE-002 — Teacher role vs Trainer role *(added on review, direct user statement, not
from a `docs/sources/` file)*

**Question:** Is "Teacher" a distinct role from the already-documented "Trainer" role, and if so,
what is the actual division of responsibility between them?

**Evidence:** A direct statement from the user in this session (2026-09-01): *"teacher is also a
separate role and he will conduct the classes, prepares the assignment, validates the assignment."*
Every duty attributed to "Teacher" is already attributed to "Trainer" throughout the existing
evidence set (EVID-002, EVID-009, EVID-004/005 Trainer role bundle and TRN-012–TRN-018 screens). No
source anywhere — old or new — had used the word "Teacher" before this.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01), asked directly: *"same role, keep trainer as it is."*
Teacher and Trainer are the **same role**. "Trainer" is retained as the role name in all future
BRD/PRD/Feature/RBAC/UX work. No change to the Trainer role bundle already documented.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01

---

### DEC-ROLE-001 — Student vs Overseas Applicant identity model

**Question:** Is an Overseas applicant a variant of the existing Student role/identity, or a
distinct role/identity with its own lifecycle?

**Evidence:** No source defines this. The blueprint's role model (§2.1: Visitor, Student, Trainer,
Employer, Admin bundles) has no Overseas-aware role at all — "Overseas" and "Agent" do not appear
anywhere in EVID-004/005 (confirmed by direct text search). EVID-008 describes an Overseas-applicant
journey (country/university/course interest → eligibility evaluation → document upload → visa
tracking) that is structurally different from the domestic Student portal's batch/slot enrolment
flow described in the same transcript and in EVID-002/009.

**Impact of "same role, extended":** simpler auth/session model; Student's existing screens/API
need conditional branches for Overseas-specific data and states.
**Impact of "distinct role":** cleaner separation of concerns; duplicated auth/profile
infrastructure; open question of what happens if one person is both a domestic Student and an
Overseas applicant.

**Dependencies affected:** DEC-SCOPE-003 (only relevant if Overseas is in scope at all), RBAC
matrix, Architecture identity/auth design (blueprint ADR-008 has no Overseas awareness today).

**Recommendation:** None — purely a data-modeling choice with no evidence pointing either way.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **same Student identity, extended.** A domestic
Student and an Overseas applicant are the same account/login; Overseas-specific data and states are
added to the existing Student profile rather than creating a separate identity/role. This means the
Student data model must be extended (not forked) to carry Overseas fields (country/university/
course interest, eligibility status, visa sub-flow state, document checklist), and blueprint
ADR-008 (Authentication) needs this extension noted explicitly.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01

---

## Group 3 — Overseas / Agent

### DEC-SCOPE-003 — Overseas Education domain

**Question:** Is the Overseas Education module (countries/destinations, universities, courses,
admission process, visa sub-flow, scholarships, events) in scope for this release, a later phase,
or excluded?

**Evidence:** EVID-008 only. Presented largely as an already-built/demoed prototype: *"this is the
overseas homepage where we had countries, universities, courses, admin courses, visa, sponsors."*
Zero mentions of "Overseas" anywhere in EVID-002, EVID-009, or EVID-004/005 (`SCOPE_DELTA.md` D-01).

**Impact of INCLUDE:** major scope addition — a new applicant journey, five-plus new sub-areas,
none of which are reflected anywhere in the blueprint's existing 459-task/137-screen/824-test
catalogue; commercial terms (EVID-009) never accounted for it either.
**Impact of EXCLUDE/FUTURE:** the current blueprint stays valid as-is; Overseas becomes a distinct,
separately-scoped engagement.

**Dependencies affected:** DEC-ROLE-001, DEC-WF-001, DEC-DATA-001, DEC-DATA-002, DEC-SCOPE-004,
DEC-SCOPE-005, DEC-SCOPE-006, DEC-NOT-001 (Overseas-specific notifications), and materially the
entire downstream Feature Catalogue / Architecture / Test Catalogue, none of which currently
account for this domain at all.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): *"it is part of current release."* Overseas Education
is **in scope for the current release**. A commercial-scope reconciliation (no quotation in
evidence ever priced this domain) is still an open item to carry into BRD, not resolved by this
confirmation alone. This also resolves the earlier conflict between an initial chat answer
("Overseas defer") and this later, more detailed file answer — the later, more specific answer
(confirmed here, and consistent with the Agent/Commission answers below, which only make sense if
Overseas itself is in scope) is treated as authoritative.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01

---

### DEC-SCOPE-004 — Agent Portal

**Question:** Is an Agent Portal (registration/login, roster of submitted students, application
status view) in scope?

**Evidence:** EVID-008 shows an apparent live demo: *"This is actually the agent [portal]... they
can see their students, data... application status."* The same transcript elsewhere frames agent
registration as forward-looking: *"The abroad education... later on... I wanted to have a platform
where... the agent also can register with us."* This is internally inconsistent and not resolved by
the transcript itself (`SCOPE_DELTA.md` D-02).

**Impact:** a new role, new screens/APIs/RBAC, meaningful only if DEC-SCOPE-003 (Overseas) is in
scope.

**Dependencies affected:** DEC-SCOPE-003, DEC-SCOPE-005 (Commission), DEC-ROLE-001.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **Yes, in scope.** Additional business rule provided:
*"Agent can register but it has to be approved by overseas admin"* — agent registration is
self-service but gated behind an Admin (Overseas) approval step before the agent account is active.
This approval step is new detail not present in any prior evidence and should be carried into BRD
as a business rule/process, and into the Overseas application state machine (`DEC-WF-001`) as an
actor precondition.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01

---

### DEC-SCOPE-005 — Agent Commissions

**Question:** Is commission tracking/claim functionality for agents in scope?

**Evidence:** EVID-008: a "claim commission" action is shown in the same demo as DEC-SCOPE-004
(*"they should have the button here, it's a [claiming] commission for that particular intake"*),
alongside a comment that today it is *"getting updated manually only."* No commission rate, model,
approval workflow, or payout mechanism is specified anywhere in any source.

**Impact:** requires a commission calculation/approval/payout model with zero evidence to design
against, even if confirmed in scope.

**Dependencies affected:** DEC-SCOPE-004 (this only exists if Agent Portal exists), DEC-PAY-001/002
(payout likely routes through the same finance module).

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01), with a commission model: *"Whenever Agent refers the
student for overseas education and his application got finalised and joined in the college then
agent will get commission"* — trigger is the referred student's application being finalized **and**
the student actually joining the college (not merely accepted). *"Payout will be [upon] approval by
overseas admin"* — payout requires a separate Admin (Overseas) approval step, distinct from the
trigger event itself.

**Reconciled against the reference implementation (2026-09-01, same session):** the codebase's
actual commission logic (`REFERENCE_IMPLEMENTATION_FINDINGS.md` §5.4) differs — Overseas Admin
**manually** creates a commission record with a **hand-entered amount** (no automatic trigger, no
fixed rate/percentage anywhere in the code), the agent self-serves a "claim" action, and no
distinct payout-approval endpoint was found. Asked directly how to reconcile: user confirmed
**extend the existing code** — keep the manual, admin-discretionary **amount** (this resolves the
long-open "is there a fixed rate?" question: **there is not**, by design, not by omission), but
**add** the automatic accrual trigger (finalized + joined, replacing manual commission-creation
timing) and **add** a genuine payout-approval step distinct from the agent's own claim action
(closing the gap where `claimed → paid` currently has no observed approval gate in the code read so
far).

**What must be confirmed:** the exact event/status value that should fire the automatic trigger
(needs to map to a real `OverseasApplication.status` transition once the full status list is
inventoried — only `profile_evaluation → university_review` was observed in seed data, not a
"joined" terminal state); the new payout-approval endpoint's design belongs to
`prompts/09_DATA_API_RBAC_INTEGRATION_CONTRACTS.md`, not this register.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01,
reconciled 2026-09-01 — **rate/amount question is now closed** (admin-discretionary per
application, not a fixed rate, confirmed by design); trigger-event mapping and payout-approval
endpoint design remain open build work

---

### DEC-SCOPE-006 — Visa Workflow

**Question:** Is the visa sub-flow (checklist, documentation, interview preparation, tracking,
approval status) in scope?

**Evidence:** EVID-008 only: *"in Visa, also we had a few levels like checklist... documentation,
Visa interview preparation... tracking an approval status."* Four category names, no detail beyond
that; not mentioned anywhere in the blueprint.

**Impact:** an additional workflow/state-machine and document-checklist scope, tightly coupled to
DEC-SCOPE-003.

**Dependencies affected:** DEC-SCOPE-003 (visa is a sub-flow of Overseas, not a standalone
decision), DEC-WF-001, DEC-DATA-001.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **yes, in scope.** The actual checklist/interview-prep/
tracking rules (still only four category names exist in evidence) remain an open item for BRD/PRD.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01 —
detailed rules still open

---

### DEC-WF-001 — Overseas application state machine

**Question:** What are the exact states and transitions of an Overseas application (enquiry through
enrolment), including exception paths (rejection, waitlist, deferral)?

**Evidence:** EVID-008 describes a loose prose sequence — interest capture → back-end eligibility
evaluation → university/course selection → "unconditional locker" → visa documentation → status
tracking — with no formal state names, no transition rules, and no exception-path handling anywhere
in any source.

**Impact:** foundational to the whole Overseas domain — no Overseas screen, notification, or admin
routing can be correctly built without this.

**Dependencies affected:** DEC-SCOPE-003, DEC-DATA-001, DEC-NOT-001.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): the transcript's loose sequence is correct as the base
flow — **Interest/Enquiry Capture → Eligibility Evaluation → University/Course Selection → Offer
("Unconditional Locker") → Visa Documentation → Status Tracking → Enrolment.** These are stage
*names* carried forward from the transcript's own wording, not yet formal system state identifiers.
**Still open:** exact system state names/codes, and exception paths (rejection, waitlist, deferral)
— none were specified in this confirmation. Carry into BRD/PRD as an open item; do not invent
rejection/waitlist/deferral behavior.

**Status:** CONFIRMED_CURRENT (base sequence) — Approved by: user (in-session) — Approval date:
2026-09-01 — exception paths still open

---

### DEC-DATA-001 — Applicant document checklist

**Question:** What is the complete, authoritative document checklist required at each Overseas
application stage?

**Evidence:** EVID-008 gives illustrative examples only — *"passport... academic documents...
maybe the JPG or maybe the video format"* — and explicitly flags the list as non-final: *"we will
give the minimum checklist."*

**Impact:** drives upload/validation UI, storage schema, and verification workflow; an incomplete
list risks significant rework later.

**Dependencies affected:** DEC-SCOPE-003, DEC-WF-001.

**Recommendation:** None was needed — resolved directly by the user, partially.

**Resolution:** User confirmed (2026-09-01): the checklist **varies by country/university/course**
rather than being universal. **Still open:** the actual per-combination checklist content — this
confirms the *shape* of the rule, not the data itself. Cannot proceed to document-upload UI/storage
schema design without the real lists; carry into BRD/PRD as an open item requiring the actual
checklist data before Feature work on this screen.

**Status:** CONFIRMED_CURRENT (variation model only) — Approved by: user (in-session) — Approval
date: 2026-09-01 — actual checklist data still required

---

### DEC-DATA-002 — University/course data source

**Question:** Is university/course/country/fee data entered and maintained manually by Admin, or
sourced from an external university/provider integration?

**Evidence:** EVID-008 implies manual admin curation in the demoed prototype (*"per year tuition
fee... approximate range... because from university to university it will get very [different]"*;
manually prioritised university ordering). No source names an external data provider or API.

**Impact of MANUAL:** simpler to build; ongoing admin data-entry burden and staleness risk.
**Impact of INTEGRATION:** requires an unnamed external provider/API — pure speculation without a
named candidate.

**Dependencies affected:** DEC-SCOPE-003.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **"Admin and backend team"** — data is manually
maintained, not sourced from an external university/provider integration, but maintenance isn't
purely a self-service Admin-UI task either; the backend/engineering team is also involved (e.g.
seed data, bulk loads, or direct maintenance outside the Admin UI). No external provider/API is
needed for this release.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01

---

## Group 4 — Learning / Live Classes

### DEC-LIVE-001 — Live class provider

**Question:** Which platform (Zoom, Microsoft Teams, Google Meet, GoToMeeting, other) is used for
live classes, and how is the multi-session concurrent-recording limitation handled?

**Evidence:** EVID-002/009: "Zoom/Teams (platform to be confirmed)." EVID-008 discusses Zoom,
Teams, Google Meet, GoToMeeting, and Zoho informally, notes all evaluated options share the same
one-session-at-a-time recording constraint, and reaches no conclusion (*"I will check on that point
once and I will let you know"*).

**Impact:** directly linked to DEC-SCOPE-007 — the concurrent-recording limitation is the stated
reason an internal platform was even raised.

**Dependencies affected:** DEC-SCOPE-007.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **Zoho** (Zoho Meeting/Webinar) is the live-class
provider. Note: EVID-008 itself flagged Zoho as sharing the same one-session-at-a-time recording
constraint as Zoom/Teams/Google Meet/GoToMeeting — this decision does not resolve that constraint;
see `DEC-SCOPE-007` immediately below for whether an internal platform addresses it. Provider
account ownership (blueprint's own open item #24) is still unconfirmed.

**Extended (2026-09-01, same session):** the reference implementation already has working adapters
for **both Google Meet and Zoho Meeting**. User confirmed keeping both rather than stripping Google
Meet to match a Zoho-only decision — same "keep all providers, extend instead of stripping" answer
as `DEC-PAY-001`. Zoho remains the confirmed default per the original answer; Google Meet stays
available as a second adapter. This does not resolve the shared one-session-recording limitation
(still applies to both providers) or `DEC-SCOPE-007`'s out-of-scope status for an internal
alternative.

**Status:** CONFIRMED_CURRENT (provider) — Approved by: user (in-session) — Approval date:
2026-09-01, extended 2026-09-01 — **Zoho default, Google Meet retained as a second adapter**;
concurrent-recording handling and account ownership still open

---

### DEC-SCOPE-007 — Internal Meeting/Recording Platform

**Question:** Should EduSphere build its own live-class meeting/recording platform, or should this
remain a separate project outside the main application?

**Evidence:** EVID-008 explicitly floats it as separate: *"It will be a separate project like we can
do."* The same transcript later, in a related but not identical passage, discusses AWS S3 storage
for recordings "in the application" without repeating the separate-project qualifier
(`CONFLICT_MATRIX.md` C-06) — an internal inconsistency, not a second corroborating source.

**Impact of IN:** significant new technical scope (concurrent multi-session recording — the exact
unsolved constraint from DEC-LIVE-001), a storage/cost model, entirely outside the blueprint's
existing architecture.
**Impact of SEPARATE:** EduSphere keeps its already-scoped Zoom/Teams integration; the
concurrent-recording limitation remains unsolved but is not this project's responsibility.

**Dependencies affected:** DEC-LIVE-001, DEC-INFRA-001 (storage/cloud), Architecture (would need
its own ADR if pulled in).

**Recommendation:** Leaned toward **OUT_OF_SCOPE / separate project** — confirmed.

**Resolution:** User confirmed (2026-09-01): **separate project.** Stays out of EduSphere's scope.
EduSphere uses Zoho (`DEC-LIVE-001`) as-is, including its one-session-at-a-time recording
limitation — no in-app workaround is being built for this release.

**Status:** OUT_OF_SCOPE — Approved by: user (in-session) — Approval date: 2026-09-01

---

### DEC-WF-002 — Student-Trainer batch assignment and slot-booking model *(added on review, not
in the original template)*

**Question:** When a student enrols, is assignment to a trainer/batch manual (student-selected
time-slot) or automatic? What is per-slot capacity, is a lock-in enforced once booked, what is the
batch-naming convention, and should the student's unique/enrolment ID be visible in the UI?

**Evidence:** EVID-008 only, stated as definite preference: *"I won't prefer automatically... students
should have the choice for I want to go for the trainer one['s] sessions [or] trainer two['s]"*;
*"this lot is all booked... he cannot change his lot... that is fixed for me for the next 3
months"*; capacity implied at 20/slot; batch-naming by course/track (e.g. "PY" for Python); and a
request to show the existing-but-hidden unique/enrolment ID in the UI. Not covered at this level of
detail anywhere else (`SCOPE_DELTA.md` D-08).

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **student-selected, as discussed in the meeting.**
Students pick their trainer/time-slot at signup; capacity is **20 students per slot**; a booked slot
**cannot be changed** once selected, locked for the course duration (~3 months, per the meeting).
**Still open:** the exact batch-naming convention (only an example — "PY" for Python — exists) and
whether the student's unique/enrolment ID should be shown in the UI (the meeting asked for this but
it wasn't re-confirmed here).

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01 —
batch-naming convention and ID-visibility still open

---

### DEC-LMS-001 — External LMS

**Question:** Must EduSphere integrate with an existing external LMS, and if so, which one and what
API?

**Evidence:** EVID-002/009 both list "LMS" as a desired integration. Blueprint's own open item #26:
"Confirm whether an existing LMS must be integrated and provide API documentation" — unresolved.
No LMS is named anywhere in any source.

**Impact:** if required, adds an integration surface with an unspecified system; if not, the
already-scoped native features (notes, assignments, Q&A, attendance) cover the same ground natively.

**Dependencies affected:** none blocking, but affects Architecture's provider-adapter boundary
(the blueprint already reserves "external LMS adapters" as an extension point).

**Recommendation:** None was needed — resolved directly by the user, but the answer reframes the
question.

**Resolution:** User confirmed (2026-09-01): **no external LMS integration** — instead, **build a
native LMS module** within EduSphere, alongside its already-scoped native features ("Along with
Edusphere's native features, build an LMS system"). This is new scope beyond what was asked: the
original question was integrate-vs-not with an *existing* external LMS; the user instead wants a
first-party LMS capability built. **Open follow-up, not yet asked:** what "LMS" means here beyond
the already-documented notes/assignments/Q&A/attendance/recordings/certificates — e.g. structured
course-content authoring, sequencing/prerequisites, quizzes/assessments beyond what's already
scoped, gradebooks, or SCORM-style content packages. Carry into BRD as a named open item requiring
a follow-up question before Feature-level design.

**Status:** CONFIRMED_CURRENT (native build, not integration) — Approved by: user (in-session) —
Approval date: 2026-09-01 — exact LMS feature scope still open

**Extended (2026-09-03, unscheduled-scope review):** re-asked whether to resolve this now; user
reaffirmed **native-only, no external LMS integration** — this is not a new decision, it restates
the 2026-09-01 resolution above. The open follow-up (what "LMS" means beyond the already-native
features) remains genuinely open — the user did not name a specific extension, so `LMS-001` still
has no buildable scope and stays out of this round's promoted batch (`DEC-SCOPE-008`). It becomes
schedulable the moment a specific native-LMS capability is named.

---

## Group 5 — Payments / Finance

### DEC-PAY-001 — Payment gateway

**Question:** Which payment gateway is used for fee/EMI collection?

**Evidence:** EVID-002/009 both specify EMI/invoice/receipt features but name no gateway.
Blueprint's own open item #23: "Select payment gateway and confirm currency/tax/invoice
requirements" — unresolved.

**Impact:** blocks the Payment Dashboard, EMI Tracking, and Invoice/Receipt screens/APIs entirely
until selected.

**Dependencies affected:** DEC-PAY-002, DEC-SCOPE-003 (Overseas may require multi-currency
collection).

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **Stripe.**

**Extended (2026-09-01, same session):** the reference implementation being extended
(`docs/evidence/REFERENCE_IMPLEMENTATION_FINDINGS.md`) already has working adapters for **both
Razorpay and Stripe**. Asked whether to strip Razorpay to match a Stripe-only decision or keep both:
user confirmed **keep all providers, extend instead of stripping.** Stripe remains the
primary/first-class choice from the original confirmation; Razorpay stays available as a second
adapter rather than being removed. Nothing is lost by keeping already-built, working code.

**Status:** SUPERSEDED (see re-resolution below) — Approved by: user (in-session) — Approval date:
2026-09-01, extended 2026-09-01 — **Stripe primary, Razorpay retained as a second adapter**

**Re-resolved (2026-09-03, in-session):** user confirmed **Razorpay only — Stripe is not needed and
is to be removed entirely** (code, config, docs, env vars), reversing the 2026-09-01 "keep all
providers, extend instead of stripping" extension for payments specifically. This is a direct,
explicit user instruction, not an inferred simplification.

**Flagged, not silently carried forward:** `DEC-PAY-002` (multi-currency/multi-region) was partly
reasoned from Stripe's native multi-currency support, and `DEC-SCOPE-003` (Overseas) implies
multi-country fee collection. Razorpay's cross-border/multi-currency coverage is narrower than
Stripe's. The user's Razorpay-only instruction is honored as given; whether Razorpay alone
satisfies the full multi-region requirement of `DEC-PAY-002` is called out in that decision's entry
below as needing its own confirmation rather than being assumed resolved by this change.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-03 —
**Razorpay only, Stripe removed**

---

### DEC-PAY-002 — Currency / tax / invoice rules

**Question:** What are the currency, tax (e.g. GST), and invoice-format rules?

**Evidence:** EVID-009's cost line shows "GST (18%) Extra as Applicable" for a client named
"EduSphere UK" — GST is an Indian tax term, not a UK one, which is worth surfacing as a literal
observation in the source document, not resolving. No further currency/tax detail exists anywhere.

**Impact:** blocks invoice generation and finance reporting; more consequential if DEC-SCOPE-003
(Overseas, multi-country tuition) is confirmed in scope.

**Dependencies affected:** DEC-PAY-001, DEC-SCOPE-003.

**Recommendation:** None was needed — resolved directly by the user, partially.

**Resolution:** User confirmed (2026-09-01): **multi-currency / multi-region** — neither
single-region UK VAT nor single-region India GST alone. Consistent with `DEC-SCOPE-003` (Overseas
now in scope, implying tuition/fees across multiple countries) and the Stripe choice (`DEC-PAY-001`,
which supports multi-currency natively). **Still open:** the exact regions/currencies to support,
per-region tax regime mapping, and invoice format — none specified beyond "multi-currency."

**Status:** CONFIRMED_CURRENT (multi-currency model only) — Approved by: user (in-session) —
Approval date: 2026-09-01 — exact regions/tax mapping/invoice format still open

**Flagged 2026-09-03 (not yet resolved):** `DEC-PAY-001` was re-resolved to Razorpay-only, removing
Stripe. This decision's multi-currency model was reasoned partly from Stripe's native multi-currency
support. Whether Razorpay alone satisfies the confirmed multi-currency/multi-region requirement —
given Razorpay's narrower cross-border coverage — has not been asked or answered. Carried here as an
open follow-up, not assumed either way.

---

## Group 6 — Notifications / Integrations

### DEC-NOT-001 — Notification rules (Email / WhatsApp / in-app)

**Question:** What are the exact triggers, channels, message templates, and opt-out rules for
status notifications — and specifically, which WhatsApp provider/integration is used?

**Evidence:** EVID-002/009 list WhatsApp as a desired integration ("platform to be confirmed" in
EVID-009's Integrations section). EVID-008 independently and separately asks for the same
capability for Overseas status updates: *"he should always get the notification through his email...
or... WhatsApp."* Blueprint's own open item #25: "Confirm WhatsApp provider, approved message
categories and opt-out rules" — unresolved even for the domestic-only scope the blueprint covers.

**Impact:** blocks the blueprint's own Notification Templates/Delivery admin screens (ADM-037 to
ADM-041) and any Overseas status-tracking screen.

**Dependencies affected:** DEC-SCOPE-003 (Overseas notification scope is additive to whatever
domestic rules are set).

**Recommendation:** None was needed — resolved directly by the user, partially.

**Resolution:** User confirmed (2026-09-01): **Twilio (WhatsApp Business API)** for WhatsApp.
**Still open:** exact notification triggers per event, message templates/categories, opt-out
handling, and equivalent email/in-app rules — none specified beyond the provider choice.

**Extended (2026-09-01, same session):** the reference implementation already has a working **SMS**
adapter alongside Email and WhatsApp — a channel never previously confirmed in this project. User
confirmed keeping it rather than stripping it — same "extend instead of stripping" answer. SMS
provider identity (vs. Twilio for WhatsApp) is not yet specified — could plausibly also be Twilio,
but this was not asked.

**Status:** CONFIRMED_CURRENT (channels: Email, WhatsApp via Twilio, **and SMS**) — Approved by:
user (in-session) — Approval date: 2026-09-01, extended 2026-09-01 — triggers/templates/opt-out
rules and SMS provider identity still open

**Extended (2026-09-03, unscheduled-scope review, resolving `ADM-011`):** the open items above
(exact triggers/templates/opt-out *content* rules, SMS provider identity) remain unresolved and are
**not** decided by this extension. What *is* decided: `ADM-011` (notification template management/
delivery-status admin UI) may proceed now, scoped to the **email channel only** (`NOT-001`, already
built and working) — viewing/managing what already exists, not inventing new trigger/template
content rules. WhatsApp/SMS template management stays out of `ADM-011`'s scope until `NOT-002`/
`NOT-003` unblock (Twilio credentials) — the admin UI should be built extensibly (e.g. a `channel`
field) so those slot in later without rework, not hardcoded email-only.

**Status:** extension CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date:
2026-09-03 — `ADM-011` unblocked for email channel only

---

## Group 7 — Privacy / Security

### DEC-PRIV-001 — UK GDPR

**Question:** What are the required consent, retention, data export/delete, and candidate-profile
visibility rules?

**Evidence:** Blueprint's own open item #27 is the **only** mention of GDPR anywhere in the entire
evidence set (`SOURCE_GAPS.md` G-03) — no original client requirement document raises it at all.

**Impact:** affects the data-retention schema, Admin export/delete tooling, and — if DEC-SCOPE-002
(Employer) is confirmed — candidate-profile visibility limits for employers.

**Dependencies affected:** DEC-SCOPE-002, Architecture data design.

**Recommendation:** None was offered — the user was given a legal-counsel-deferral option and chose
not to take it.

**Resolution:** User confirmed (2026-09-01): **standard GDPR defaults** — explicit consent capture
at signup/data-collection points, reasonable/undefined-yet retention windows, self-service
export/delete requests, and no special candidate-visibility restriction for employers beyond what's
needed for the job-search function itself. **Caveat carried forward, not overridden by this
confirmation:** GDPR is a legal-compliance area; "standard defaults" is a product-level
interpretation, not a substitute for actual legal review — BRD should still flag this for legal
sign-off before release, per good practice, even though the user has given direction to proceed.
Exact retention periods per data type are not yet numerically specified.

**Status:** CONFIRMED_CURRENT (baseline model) — Approved by: user (in-session) — Approval date:
2026-09-01 — exact retention periods still open; legal review recommended before release

---

## Group 8 — Technology / Cloud

### DEC-TECH-001 — Application stack

**Question:** Confirm the application stack: Next.js/TypeScript + FastAPI + PostgreSQL, or an
alternative.

**Evidence:** EVID-010 states this as the client's explicit preference and asks for justification
if a different stack were ever proposed. EVID-004/005 retains the same frontend/backend/database
choices — the blueprint's only real departure from EVID-010 is on *hosting/compute* (see
DEC-INFRA-001), not on the application stack itself.

**Impact:** low-risk to confirm — both an original client document and the derived blueprint
independently agree, with no conflicting source found anywhere in evidence.

**Dependencies affected:** DEC-ARCH-001, DEC-INFRA-001; `CLAUDE.md` blocks all Feature/Architecture
work requiring a settled stack until this, `DEC-ARCH-001`, and `DEC-INFRA-001` are confirmed.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): "keep the stack." Next.js + TypeScript, FastAPI,
PostgreSQL is confirmed.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01

---

### DEC-ARCH-001 — Architecture style

**Question:** Confirm architecture style — modular monolith (per the blueprint's ADR table) vs any
alternative.

**Evidence:** Only the blueprint expresses a view here (modular monolith, module boundaries,
provider adapters). EVID-010 addresses stack components only, not internal architecture style — no
original client document has an opinion on this at all.

**Impact:** shapes how the Feature Catalogue and the later Contracts phase
(`prompts/09_DATA_API_RBAC_INTEGRATION_CONTRACTS.md`) are structured.

**Dependencies affected:** DEC-TECH-001, DEC-INFRA-001.

**Recommendation:** None needed — resolved directly by the user, twice, with a revision in
between.

**Resolution history:**
1. 2026-09-01 (first pass): **microservices**, rejecting the blueprint's modular-monolith
   recommendation.
2. 2026-09-01 (**revision, same session**): after the user directed extending an existing external
   reference-implementation codebase (`docs/evidence/REFERENCE_IMPLEMENTATION_FINDINGS.md`) that is
   itself built as a domain-oriented modular monolith, the user confirmed **revising this decision
   back to modular monolith** — reasoning stated directly: extending that codebase while rewriting
   it into microservices would be a rebuild, not an extension. This is a genuine revision of a
   previously `CONFIRMED_CURRENT` decision, not a silent overwrite — both confirmations are
   preserved here per the register's own rigor standard.

**Consequence:** the reference implementation's own `ARCHITECTURE.md` (domain modules, RBAC,
division columns, route boundaries, shared infrastructure/identity) is now the working starting
point for the Architecture phase (`prompts/08`), not a from-scratch design — but it is still not
automatically authoritative in full; its specific module boundaries should be reviewed against this
project's confirmed BRD/PRD scope (Employer Portal, extended commission logic, etc.), not adopted
blindly.

**Status:** CONFIRMED_CURRENT (modular monolith — **revised** from microservices) — Approved by:
user (in-session) — Approval date: 2026-09-01 (original), 2026-09-01 (revision, same session)

---

### DEC-INFRA-001 — Production cloud / hosting

**Question:** Confirm production hosting: AWS EC2+RDS (client's originally stated preference) vs
ECS Fargate (blueprint's recommendation, with ECS-on-EC2 ASG as its stated fallback).

**Evidence:** `CONFLICT_MATRIX.md` C-04. EVID-010: "Hosting: AWS EC2 + RDS." EVID-004/005 ADR table:
"ECS Fargate recommended; ECS on EC2 ASG compatible fallback." `VERIFIED_REVIEW_NOTES.md` §4
references a further DigitalOcean-vs-Fargate comparison that could not be located in
`docs/sources/` (`SOURCE_GAPS.md` G-02) — not counted as evidence, only flagged as unverifiable
context. EVID-008 confirms AWS generally as the deployment target but does not specify EC2 vs
ECS/Fargate.

**Impact of EC2:** matches the client's originally stated preference; more manual operations.
**Impact of FARGATE:** matches the blueprint's efficiency/scaling rationale, but is a vendor
recommendation not shown anywhere to have been agreed to by the client.

**Dependencies affected:** DEC-TECH-001, DEC-ENV-001; `CLAUDE.md` explicitly blocks
provider-specific infrastructure generation until this is confirmed.

**Recommendation:** None was needed — resolved directly by the user, and the answer is neither
originally-discussed option.

**Resolution:** User confirmed (2026-09-01): **DigitalOcean** — not AWS EC2, not ECS Fargate. This
retroactively corroborates the otherwise-unverifiable reference in `VERIFIED_REVIEW_NOTES.md` §4 to
a "ChatGPT conversation compared DigitalOcean with AWS Fargate" (`SOURCE_GAPS.md` G-02) — that
reference could not be located as a source file, but the outcome it described (DigitalOcean under
consideration) is now independently confirmed by direct user statement. `CLAUDE.md`'s block on
generating provider-specific production infrastructure is lifted for cloud provider specifically.
**Note:** the blueprint's AWS-specific hosting/ADR section does not carry over, as before — and
neither does the reference implementation's own `ARCHITECTURE.md` §2 runtime topology, which is
explicitly AWS-specific (ECS Fargate, RDS, ElastiCache, S3, CloudFront). **Re-confirmed
(2026-09-01, same session) after `DEC-ARCH-001` was revised to modular monolith:** the user kept
DigitalOcean rather than switching to match the reference implementation's AWS target — this is
now specifically "redeploy the reference implementation's existing Docker/Postgres/Redis/Celery
modular-monolith workload on DigitalOcean," an infra-target substitution, not an application
rewrite. DigitalOcean equivalents (Managed Postgres, Managed Redis, Spaces, App Platform or
Droplets/DOKS) for the AWS-specific topology in that codebase's `ARCHITECTURE.md` still need to be
worked out in the Architecture phase.

**Status:** CONFIRMED_CURRENT (DigitalOcean) — Approved by: user (in-session) — Approval date:
2026-09-01, re-confirmed 2026-09-01 — DigitalOcean-specific service mapping for the existing
Docker-based topology still open

---

### DEC-ENV-001 — Dev / UAT / Prod environment boundaries

**Question:** Must every environment run on the production cloud provider, or can dev/local remain
off-cloud? *(Reframed 2026-09-01: `DEC-INFRA-001` confirmed DigitalOcean, not AWS — the original
evidence below was AWS-specific and is now only historical context, not a current-provider
statement.)*

**Evidence:** `VERIFIED_REVIEW_NOTES.md` §4 references a meeting statement about AWS specifically,
but that statement could not be independently re-located in any file under `docs/sources/` during
this audit (`SOURCE_GAPS.md` G-02) — treated as unverified context, not confirmed evidence, and now
superseded in provider terms by the DigitalOcean confirmation. EVID-008 only confirms a cloud
provider (previously assumed AWS) is used for the application generally, in the narrower context of
recording storage.

**Impact:** affects CI/CD design, cost, and environment configuration in the later Contracts phase.

**Dependencies affected:** DEC-INFRA-001.

**Recommendation:** None was needed — resolved directly by the user.

**Resolution:** User confirmed (2026-09-01): **production only on DigitalOcean**; dev/local may stay
off-cloud.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-01

---

## Group 9 — UX Authority

### DEC-UX-001 — Canva reference authority

**Question:** Which Canva design (if any) is the authoritative UX reference, and what authority does
it carry — structural reference only, or a pixel-parity target?

**Evidence:** `docs/ux/UX_REFERENCE_AUDIT.md`. Two candidate designs found behind two different
URLs: `DAHRWCa0RLw` ("OneCampus ERP — Customer-Friendly Responsive UX," the design named in
`CLAUDE.md`) whose one inspected screen is a generic "Employee Master & Core HR" HRMS dashboard with
no overlap with any EduSphere requirement; and `DAHRCNYnu6g` ("EduSphere UK Responsive UX Prototype
- Revised and Complete," resolved from the site-root URL) whose one inspected screen closely matches
EduSphere's own Public Website scope (Hero, Courses/Career Paths, Real Projects, a visible "No CRM /
Admissions handoff only" badge). Roughly 20 further adjacent designs exist in the same Canva team,
not yet triaged (`UX_REFERENCE_AUDIT.md` §4, `UX_REFERENCE_GAPS.md` Gap 6/7).

**Impact:** determines what GATE-06 (UX/Screen Catalogue) is measured against; the wrong choice
risks building to a specification the client never intended as the reference.

**Dependencies affected:** GATE-06, Feature Catalogue, all downstream screen work.

**Recommendation:** Leaned toward `DAHRCNYnu6g` — confirmed.

**Resolution:** User confirmed (2026-09-01): **`DAHRCNYnu6g`** ("EduSphere UK Responsive UX
Prototype - Revised and Complete") is the authoritative UX reference, as a **structural guide
only** — not a pixel-parity target. `DAHRWCa0RLw` ("OneCampus ERP") is not authoritative. Screen
work in `prompts/07_UX_SCREEN_AND_FLOW_CATALOG.md` may use this design for navigation/layout
patterns and component structure, but exact pixel values, colors, and spacing are not binding. The
~20 other adjacent designs found in the same Canva account (`UX_REFERENCE_AUDIT.md` §4) remain
untriaged and are not brought into scope by this confirmation. Full inspection of `DAHRCNYnu6g`'s
160+ named screens is still needed before Screen Catalogue work — see `UX_REFERENCE_GAPS.md` Gap 2.

**Status:** CONFIRMED_CURRENT (structural reference) — Approved by: user (in-session) — Approval
date: 2026-09-01 — full screen export still needed

---

## Group 10 — Delivery / Timeline

### DEC-DATE-001 — Release / UAT date

**Question:** What is the confirmed release/UAT target date?

**Evidence:** EVID-009's only stated timeline — "2-3 weeks," quoted 30 Jul 2026 — covers a
materially smaller scope (CRM-included, Employer-future, no Overseas/Agent) than what is now under
discussion, and that window has already elapsed relative to the current 1 Sep 2026 system date. The
blueprint's "14-16 weeks" figure is sourced only from the unverifiable G-01 document. No feasible
date can be derived from evidence for the scope now on the table, which may additionally include the
entirely-unquoted Overseas/Agent domain.

**Impact:** drives the Implementation Plan/Release Backlog (`prompts/11`) and all downstream gate
timing.

**Dependencies affected:** effectively every DEC-SCOPE-* decision above, since no timeline can be
set before scope is.

**Recommendation:** Re-baseline was recommended; the user instead gave a specific date.

**Resolution:** User confirmed (2026-09-01): **target date 5 September 2026 — 4 days from today.**

**⚠ Feasibility risk — flagged, not silently accepted:** this date is recorded as stated, but it
needs to be said plainly: the scope confirmed *in this same session* now includes — CRM via Zoho
integration, Employer Portal, a full Overseas Education domain (countries/universities/courses,
admission workflow, visa sub-flow, agent portal with an admin-approval gate, commission tracking),
a natively-built LMS module beyond what was originally scoped, a switch from the blueprint's
modular-monolith to a from-scratch microservices architecture, and a full re-platform from AWS to
DigitalOcean. None of this exists in any design, contract, or code yet — this session has only
produced evidence review and decision records, per `CLAUDE.md`'s explicit gating (no BRD, PRD,
Architecture, contracts, or code have been produced). A 4-day window from today does not
correspond to any credible estimate for scope of this size by any standard normally used for
software delivery. This is recorded as the confirmed target date because the user set it directly,
but it should be treated as a **major project risk** in the BRD's Risks section, not as an
achievable plan — and re-confirmed once the Implementation Plan (`prompts/11`) actually sizes this
scope.

**Status:** CONFIRMED_CURRENT (date as stated) — Approved by: user (in-session) — Approval date:
2026-09-01 — **feasibility not validated; treat as at-risk**

---

## Summary — status of all 25 decisions

**All 25 decisions carry a user resolution as of 2026-09-01.** 24 are `CONFIRMED_CURRENT`; one
(`DEC-SCOPE-007`, internal meeting platform) is `OUT_OF_SCOPE`. None of this was approved by
Claude — every resolution line above traces to a specific, quoted or directly-selected user
statement in this session. See `docs/decisions/CLIENT_QUESTIONS.md` for the same material as
questions, all now struck through.

**Post-approval reconciliation against an extended external reference implementation
(2026-09-01, same session):** after the user directed extending an existing codebase
(`docs/evidence/REFERENCE_IMPLEMENTATION_FINDINGS.md`), 8 decisions were revised or extended rather
than left as originally confirmed — all resolved directly by the user, none inferred:
- `DEC-ARCH-001` **revised**: microservices → modular monolith (matches the codebase).
- `DEC-INFRA-001` **re-confirmed**: DigitalOcean, now scoped as redeploying the codebase's existing
  Docker/Postgres/Redis workload rather than a from-scratch design.
- `DEC-SCOPE-002` **re-confirmed**: Employer Portal, now flagged as net-new build work — the
  codebase has no Employer role.
- `DEC-SCOPE-005` **extended**: kept the codebase's admin-discretionary commission amount (closing
  the commission-rate question permanently — there is no fixed rate, by design); added the missing
  automatic accrual trigger and payout-approval gate.
- `DEC-PAY-001` **extended** (2026-09-01): Stripe stays primary; Razorpay (already working in the
  codebase) is kept rather than stripped. **Re-resolved 2026-09-03:** reversed — Razorpay only,
  Stripe removed, per direct user instruction.
- `DEC-LIVE-001` **extended**: Zoho stays the default; Google Meet (already working) is kept.
- `DEC-NOT-001` **extended**: Email + WhatsApp (Twilio) stay confirmed; SMS (already working in the
  codebase) is added as a third channel.
- `DEC-SCOPE-001` **extended**: Zoho stays the actual CRM target, but the codebase's generic
  provider-agnostic webhook framework is kept and configured for Zoho, rather than replaced with
  Zoho-specific code.

The pattern across all eight: where the codebase already has more built (extra providers, broader
channels) than this project had confirmed, the user chose **keep and extend, never strip working
code** — and where the codebase has *less* than confirmed (no Employer role, no automatic
commission trigger), the user chose to **build the gap**, not lower this project's requirement to
match the codebase's current limits.

**This does not mean everything is fully specified.** A majority of the confirmed decisions resolve
the *shape* of the answer while leaving real detail still open — carried forward as named open
items rather than assumed. Notably still missing before Feature-level work can proceed cleanly:
- Agent commission rate/amount (`DEC-SCOPE-005`)
- Overseas application exception paths — rejection/waitlist/deferral (`DEC-WF-001`)
- Actual per-country/university/course document checklists (`DEC-DATA-001`)
- Concurrent-recording handling now that Zoho is confirmed and the internal platform is out of
  scope (`DEC-LIVE-001` / `DEC-SCOPE-007`)
- Native LMS module's actual feature scope beyond what's already native (`DEC-LMS-001`)
- Batch-naming convention and enrolment-ID visibility (`DEC-WF-002`)
- Multi-currency region/tax mapping and invoice format (`DEC-PAY-002`)
- Notification triggers/templates/opt-out rules (`DEC-NOT-001`)
- GDPR retention periods (numeric) and legal sign-off (`DEC-PRIV-001`)
- Full microservices service-boundary design (`DEC-ARCH-001`) and DigitalOcean service-level
  architecture (`DEC-INFRA-001`) — both are net-new design work, not adaptations of the blueprint
- Full screen-by-screen export of the confirmed UX reference, `DAHRCNYnu6g` (`DEC-UX-001`)

**One decision carries an explicit, flagged risk rather than a clean resolution:** `DEC-DATE-001`
(release date, 5 September 2026 — 4 days out, against a scope that has grown substantially in this
same session). This is recorded as stated, not softened, and should be treated as the single
highest-priority risk to surface in the BRD.

All of the above open items belong in `docs/product/BRD_OPEN_ITEMS.md` once BRD work begins — per
the prompt's own rule, unresolved detail must not silently enter current scope.

---

## Group 11 — Unscheduled/BLOCKED group scope review (2026-09-03)

### DEC-SCOPE-008 — Promoting deferred catalog items out of "Unscheduled"

**Question:** `docs/features/feature_catalog.json` carries an `Unscheduled/BLOCKED` group of 9
features — `PUB-010`, `ADM-009`–`013`, `EMP-006`, `LMS-001`, `RPT-002` — most marked
`moscow: "Won't (for now)"` at original cataloguing time, several with their own confirmed
dependencies already done by the time this decision was raised (all named Wave 1–3 catalog work
was complete). None of these had ever been re-reviewed against that changed state.

**Evidence:** the catalog's own per-feature fields (`scope: "BLOCKED"`, `release: "Unscheduled"`,
`deps`), reviewed directly feature-by-feature against work already completed this session.

**Recommendation:** none needed — reviewed with the user one feature at a time, per `CLAUDE.md`'s
NO-ASSUMPTION MODE (do not infer scope for a BLOCKED item without an explicit decision).

**Resolution:** User confirmed (2026-09-03), feature by feature:
- **`ADM-009`** (Resources/recordings oversight, dep `TRN-004` done) — **in scope.** No open
  question beyond the original deprioritization; that call is reversed.
- **`ADM-010`** (Agreement/consent oversight, dep `STU-009` done) — **in scope,** same basis.
- **`ADM-012`** (Roles/permission administration, dep `FND-002` done) — **in scope,** same basis.
- **`ADM-013`** (Operational-tooling cluster: settings, audit logs, import/export, backup/restore,
  health, job monitoring) — its own `rbac` field flagged genuine ownership ambiguity ("IT Admin or
  Super Admin"). **Resolved: Super Admin owns it.** Now in scope with that ownership.
- **`EMP-006`** (Placement status — offer/accepted/joined — per interviewed candidate, dep
  `EMP-004` done) — **in scope,** same basis as `ADM-009`/`010`/`012`.
- **`RPT-002`** (Overseas reporting: application funnel by stage, agent commission report,
  visa-status aging, deps `OVS-004` partial/`AGT-003` done) — **in scope.** Mirrors the `RPT-001`
  reporting pattern already built this session (domestic/Employer reporting) — same
  `services/portal.py` handler shape, no new architectural question.
- **`PUB-010`** (Site search, FAQ, Privacy Policy, Terms & Conditions, Cookie Preferences) — **split
  decision.** Site search and FAQ are **in scope now** (no open question). Privacy Policy/Terms/
  Cookie Preferences are **in scope only as clearly-labeled, explicitly non-final placeholder
  content** — the feature's own security note ("Privacy/Terms content must reflect actual GDPR
  posture, not boilerplate") is not waived; real legal sign-off is a separate, still-open item
  (`DEC-PRIV-001`), not resolved by this decision. The placeholder pages must not read as, or be
  presented as, final legal text.
- **`LMS-001`** — **not promoted.** See `DEC-LMS-001`'s own 2026-09-03 extension above: the
  native-vs-external question was already resolved (2026-09-01, native-only); what's still missing
  is a *specific* extension request, which the user did not provide this round. Stays in
  `Unscheduled` until one is named.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-03 —
`ADM-009`/`010`/`012`/`013`/`EMP-006`/`RPT-002`/`PUB-010` (split) promoted to current scope;
`LMS-001` stays deferred pending a named extension. Catalog (`feature_catalog.json`,
`MASTER_FEATURE_CATALOG.md`) updated accordingly; queued into `pending.md`'s feature loop as
Wave 4.

---

## Group 12 — 12 September 2026 product-review call follow-ups (`EVID-011`/`012`/`013`)

### DEC-SCOPE-009 — School/Institution Portal access model

**Question:** Is the School/Institution Portal built as one login per school representative
(counselor-managed student data, school gets read-only visibility), individual student logins, or a
hybrid? (`CLIENT_QUESTIONS.md` Round 2, item 6 — flagged as genuinely unresolved in the source call
itself, not just under-specified: `EVID-012` called a school-level login "confirmed," but the actual
transcript (`EVID-011`) ends the topic with the two models still in open disagreement.)

**Evidence:** `EVID-011` (transcript, unresolved debate), `EVID-012` §21 (asserts school-level login
as confirmed, contradicted by the transcript it claims to summarize).

**Recommendation:** None was needed — resolved directly by the user, asked directly.

**Resolution:** User confirmed (2026-09-12): **one login per school/institution representative.**
Counselors manage the underlying student data from the internal system; the school representative
gets read-only visibility into their own institution's students' status/progress, not individual
student logins. Bulk student-data upload stays deferred (manual entry first), per both `EVID-011`
and `EVID-012` — the one sub-point already clearly resolved before this decision.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-12 —
bulk-upload phasing still open (`docs/features/feature_catalog.json`'s `Unscheduled` School item)

---

### DEC-ROLE-003 — "Coordinator" role and certificate-issuance authority

**Question:** Both `EVID-011` and `EVID-012` introduce a "Coordinator" role, distinct from
Trainer/Admin, as the one who issues certificates (with Founder + Coordinator digital signatures).
No existing role/RBAC document has this role. Should Coordinator be added as a new formal role, and
does certificate issuance move exclusively to Coordinator or get added alongside the existing
Trainer/Admin issuance path? (`CLIENT_QUESTIONS.md` Round 2, item 2.)

**Evidence:** `EVID-011`/`EVID-012`, 12 Sep review call — Coordinator named specifically as the
certificate-issuing actor, with no mention of Trainer/Admin retaining that ability going forward.

**Impact:** this is a **removal of an existing, already-shipped permission** — `docs/architecture/
RBAC_MATRIX.md` and the feature catalogue currently show both Trainer and Admin able to issue
certificates. Moving this exclusively to a new Coordinator role requires: adding Coordinator to the
RBAC matrix and role/action matrix, removing certificate-issuance from Trainer's and Admin's
permission sets, and building whatever authentication/account path Coordinator users get (this is
not yet specified — is Coordinator a variant of an existing staff account, or fully new?).

**Recommendation:** None was needed — resolved directly by the user, asked directly.

**Resolution:** User confirmed (2026-09-12): **new distinct role, replacing Trainer/Admin
issuance.** Coordinator becomes a new formal RBAC role; certificate issuance moves exclusively to
Coordinator. Trainer and Admin lose the certificate-issuance permission they currently have in the
shipped system. **Still open, not resolved by this decision:** how a Coordinator account is
created/authenticated (new account type vs. a permission granted to an existing staff account), and
whether any other Coordinator duties beyond certificate issuance exist. Do not remove Trainer/Admin's
existing certificate-issuance code path until the Coordinator role itself is fully specified and
built — removing the old path first would leave certificate issuance broken for a period with no
replacement in place.

**Status:** CONFIRMED_CURRENT (direction: new role, exclusive issuance) — Approved by: user
(in-session) — Approval date: 2026-09-12 — Coordinator account model and full duty list still open

---

### Open, not resolved — School Partnership tiers → software mapping

**Question:** Does the Bronze/Silver/Gold/Platinum "School Partnership Model" from `EVID-013` (the
marketing brochure) need to be reflected in the School/Institution Portal's features/access (e.g. a
Platinum school sees different portal content than a Bronze school), or is it purely an offline
sales/marketing construct with no platform implications?

**Evidence:** `EVID-013` only — a marketing brochure, not a functional spec; nothing in `EVID-011`/
`EVID-012` ties the tiers to portal access.

**Resolution:** User explicitly declined to decide this and asked that it be put to the actual
client rather than answered in this session — **this is deliberately left open, not defaulted to
either answer.** Carried into `docs/decisions/CLIENT_QUESTIONS.md` Round 2 as a live question for
the next client review. No Feature ID, RBAC entry, or School-Portal design should assume either
answer until the client responds.

**Status:** UNCONFIRMED — genuinely open pending client input (not a user/session decision)

---

## Group 13 — 14 September 2026: School Portal bulk-upload reopen request

### DEC-SCOPE-010 — Reopen `DEC-SCOPE-009`'s bulk-upload deferral (template-first bulk student and results upload)

**Status:** CONFIRMED_CURRENT — both parts resolved 2026-09-14 (part 2 resolved later the same
session, after initially being left open).

**Trigger:** The user asked this session whether School CRM bulk student/results upload
functionality already existed, with a specific design in mind: authorized staff download a
template, fill it offline, then upload it — rather than free-form bulk upload. Checking the
evidence trail found this is not new ground — it directly reopens `DEC-SCOPE-009`
(`CONFIRMED_CURRENT`, 2026-09-12), which explicitly deferred bulk school/student data upload
("manual entry first ... bulk upload as a later-phase item if needed"). See `docs/evidence/
CONFLICT_MATRIX.md` `C-09` for the full conflict against `EVID-014` (`School CRM.md`), the
unattributed derived document that surfaced this request.

**Question for the client:** Should bulk upload now move from "deferred" to "current scope," scoped
specifically as a **template-download-first workflow** (download a fixed-format template → fill
offline → upload → server-side validation against the template schema → row-level accept/reject
report), for two distinct upload types that must not be conflated:

1. **Student roster bulk upload** — the school/institution-representative-side action `DEC-SCOPE-009`
   named and deferred. Reopening this also requires re-confirming who performs it: `DEC-SCOPE-009`
   gave the school representative **read-only** access only, with counselors managing student data
   internally — so "bulk upload students" only makes sense today as an **internal counselor/Academic
   Team action** (uploading a school's roster on the school's behalf) unless the client also wants to
   revisit the read-only constraint itself, which is a second, separable question.
2. **Academic results bulk upload** — not covered by `DEC-SCOPE-009` at all; no decision, BRD/PRD
   requirement, Feature ID, or RBAC entry exists anywhere for a results/marks module (confirmed by
   direct search of `feature_catalog.json`/`RBAC_MATRIX.md` this session — the only existing
   results-adjacent feature is Trainer assignment grading within the LMS course flow, which is a
   different actor and a different data shape). This would be new scope, not a reopen, and needs its
   own BRD-level treatment (who uploads: School staff, Teacher, or Edusphere Academic Team; what
   approval gate — e.g. Draft → Verified → Published — sits between upload and Parent/Student
   visibility, mirroring `EVID-014`'s proposed status flow, which is itself only a `DERIVED_BLUEPRINT`
   suggestion, not a confirmed requirement).

**Evidence:** `EVID-011`/`EVID-012` (original deferral basis), `EVID-014` (`School CRM.md`, the
unattributed document proposing both upload types plus an individual-login model that itself
conflicts with `DEC-SCOPE-009` and is **not** part of this reopen request — the login-model conflict
stands as a separate, unresolved contradiction until answered on its own).

**Recommendation:** If approved, scope this round to student-roster bulk upload only (the item
`DEC-SCOPE-009` already named), performed by the internal counselor/Academic Team role consistent
with the confirmed read-only school-representative model, with the template-download-first workflow
as the safety mechanism that addresses the original reason for deferring (avoiding uncontrolled bulk
writes). Treat results bulk upload as a separate, later BRD item — it has no prior decision to reopen
and would otherwise skip straight from an unverified document to a Feature ID with no BRD/PRD pass in
between, which the traceability chain in `CLAUDE.md` does not allow.

**Resolution — part 1 (student roster bulk upload): RESOLVED 2026-09-14**, decided jointly with
`DEC-SCOPE-011`'s role-structure question (asked in the same pass, since they overlap exactly as
predicted above). User confirmed: "Yes, template-download-first bulk upload." Template-download-
first workflow (download → fill offline → upload → server-side validation → row-level accept/reject
report) is approved. **The performing actor is the School Coordinator role** (`DEC-SCOPE-011`), a
school-side write-capable role — **not** the internal-counselor-only actor this Recommendation
proposed before the role-structure question was answered; that proposal is superseded now that
`DEC-SCOPE-011` confirmed school-side write access exists at all. See `DEC-SCOPE-011`'s resolution
for the full role-structure detail this sits inside.

**Resolution — part 2 (academic results bulk upload): RESOLVED, 2026-09-14 (later the same
session).** Initially left open ("not sure, decide later"); revisited and confirmed **in scope**
when the client questions were put back directly. See `DEC-ROLE-006` for the full actor/workflow
resolution — results are uploaded by the **Academic Team** (one of five specialized internal roles
that role now establishes), not the School Coordinator or Teacher. A Draft→Verified→Published
approval gate is adopted as the working design, consistent with the same team-responsibility model
the user invoked when answering — see `DEC-ROLE-006`'s own flag on this specific point, since the
gate itself was not verbatim re-confirmed and remains the one open detail to nail down precisely.

**Status:** CONFIRMED_CURRENT, both parts. Approved by: user (in-session) — part 1: 2026-09-14; part
2: 2026-09-14 (later, same session). New Feature ID authorized for part 2: `SCH-006` (Academic
results module) — see `DEC-ROLE-006`. BRD/PRD/Feature Catalogue/RBAC/DATA_MODEL/UX propagation is
follow-up work.

---

### DEC-SCOPE-011 — Reopen `DEC-SCOPE-009`'s school-side role structure (Principal/Coordinator/Teacher/Parent)

**Status:** CONFIRMED_CURRENT — resolved 2026-09-14, asked directly, in-session.

**Trigger:** The user asked directly whether `EVID-014`'s ("`School CRM.md`") §33 "SCHOOL ADMIN
LOGIN" section — four distinct school-side roles (Principal, School Coordinator, Teacher, Parent),
each with named permissions — is covered by confirmed requirements. It is not. This reopens
`DEC-SCOPE-009` (`CONFIRMED_CURRENT`, 2026-09-12: **one** undifferentiated, **read-only** login per
school representative) rather than raising genuinely new ground — see `docs/evidence/
CONFLICT_MATRIX.md` `C-11` for the full comparison.

**Question for the client, broken into its independent parts (do not answer as one bundle — each
has different stakes):**

1. **Login cardinality:** does the school side need multiple distinct, named logins (Principal /
   Coordinator / Teacher / ...) instead of the one undifferentiated "school representative" login
   `DEC-SCOPE-009` confirmed? If yes, what distinguishes what each sees — is Principal's dashboard
   simply a superset of Coordinator's, or genuinely different data?
2. **Write access:** `EVID-014`'s School Coordinator can "add/manage students, schedule activities,
   track attendance, monitor services" — this directly reverses `DEC-SCOPE-009`'s **read-only**
   constraint (today, EduSphere's internal counselors manage school-student data, not the school
   itself). Is write access now wanted for a school-side role? If so, does "add/manage students"
   mean manual one-at-a-time entry, bulk upload (in which case this merges with `DEC-SCOPE-010`,
   already pending), or both?
3. **Teacher login:** is a school's own teaching staff getting an individual EduSphere login in
   scope at all? **Note:** this must not be confused with `DEC-ROLE-002` ("Teacher = Trainer, same
   role") — that resolution is about EduSphere's own IT-training instructor role, an entirely
   different actor from a partner school's classroom teacher. A new, distinctly-named role would be
   needed to avoid the two colliding in RBAC/UX.
4. **Parent login:** is a Parent-facing login in scope at all? This has never been raised in any
   evidence document before `EVID-014`, and is distinct from `DEC-ROLE-004`'s student-login
   question (a Parent login viewing a child's profile does not, by itself, require the child to
   have their own login).

**Evidence:** `EVID-014` (`School CRM.md`, an unattributed, undated `DERIVED_BLUEPRINT` — no
`EXPLICIT_APPROVAL`, per `CLAUDE.md` cannot override `DEC-SCOPE-009` on its own).

**Recommendation:** If any of 1-4 is approved, keep it additive to `DEC-SCOPE-009` rather than a
silent rewrite — the confirmed single read-only representative login should remain the default/
minimum school-side access, with any Coordinator write-access, Teacher, or Parent role treated as
new roles layered on top, each with its own RBAC entry and Feature ID, not retrofitted into the
existing one. Resolve item 2 (write access) jointly with `DEC-SCOPE-010` (bulk upload) since they
overlap — a Coordinator with "add/manage students" write access and a bulk-upload capability are
close to the same feature.

**Resolution, 2026-09-14, asked directly across all four sub-questions in one pass:**

1. **Login cardinality — RESOLVED: full role set.** User confirmed: "Full role set but they can
   access the respective school data only." All four `EVID-014` §33 roles are adopted —
   **Principal**, **School Coordinator**, **Teacher**, **Parent** — each a distinct login,
   superseding `DEC-SCOPE-009`'s "one undifferentiated login" for the School domain. `DEC-SCOPE-009`
   itself is not deleted or overwritten (kept for traceability, per this project's own convention);
   this decision supersedes only its login-cardinality clause. The per-role capability descriptions
   in `EVID-014` §33 are adopted as the working definition, since the user confirmed the full set
   "based on school crm.md":
   - **Principal:** read-only — complete school dashboard, student statistics, career progress,
     global education, reports.
   - **School Coordinator:** write access — add/manage students (including bulk, see #2 below),
     schedule activities, track attendance, monitor services, generate reports.
   - **Teacher** (net-new role, distinctly named `school_teacher` — **not** `DEC-ROLE-002`'s
     Trainer/Teacher, which remains EduSphere's own IT-instructor role, unrelated): view assigned
     students, attendance, career activities, student progress. Assigned-only, not full-school.
   - **Parent** (net-new role, `school_parent`): view their own child's/children's profile and
     progress only.
2. **Write access / bulk upload — RESOLVED: yes, template-download-first.** User confirmed:
   "Yes, template-download-first bulk upload." School Coordinator's write access includes bulk
   student-roster upload via download-template → fill offline → upload → server-side validation →
   row-level accept/reject report. This resolves the roster-upload half of `DEC-SCOPE-010` — see
   that decision's own entry for the formal cross-reference. The actor is now the **School
   Coordinator** (a school-side role), not the internal-staff-only actor this decision's own
   Recommendation section had proposed before the role-structure question was answered — that
   proposal is superseded by this resolution.
3. **Teacher login — RESOLVED: in scope**, as part of the full role set adopted in #1. Built as a
   distinctly-named `school_teacher` role specifically to avoid the `DEC-ROLE-002` collision flagged
   in the original question.
4. **Parent login — RESOLVED: in scope**, as part of the full role set adopted in #1, as
   `school_parent`.

**Cross-institution isolation confirmed:** "they can access the respective school data only" — all
four roles are scoped server-side to their own institution, the same own-institution IDOR-protection
class already confirmed for `university_rep` (`UNI-001-AC02`) and Agent (`AGT-002-AC02`). A Teacher
or Parent additionally scopes *within* their own institution to their assigned students / own
child(ren) respectively — never the whole school's roster.

**Not resolved by this decision:** whether Career Guidance, Psychometric, Counselling, or any other
substantive school-service *module* exists as buildable scope — only the **login/RBAC structure**
and the **roster bulk-upload mechanism** are confirmed here. `EVID-014`'s content for those modules
remains unconfirmed `DERIVED_BLUEPRINT` — see `DEC-SCOPE-010`'s still-open results-module
sub-question and the parallel note under item 71/`DEC-ROLE-004`'s extension below.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-14. BRD/PRD/
Feature Catalogue/RBAC propagation is follow-up work, not yet done as of this decision's resolution.

---

### DEC-SCOPE-012 — School partner record and role-account provisioning mechanism

**Status:** CONFIRMED_CURRENT — resolved 2026-09-14, asked directly, in-session.

**Trigger:** While propagating `DEC-SCOPE-011` into `RBAC_MATRIX.md`, `DATA_MODEL.md`, and the UX
Screen Catalogue, the same gap surfaced repeatedly and was flagged every time rather than guessed at:
nothing anywhere specifies *how* a School partner record, or any of its four role accounts
(Principal/Coordinator/Teacher/Parent), actually comes into existence. `SCH-001`'s own precondition
is just "a School partner record exists" — the same placeholder phrasing `UNI-001` uses for
University Representative, which itself never resolved *how* a University partner record gets
created either. Without an answer, `SCH-001`/`SCH-002` have no onboarding path in front of them, and
a natural companion Feature ID (`SCH-003`, onboarding) cannot be authored.

**Question, in four parts — do not answer as one bundle:**

1. **Who initiates the School partnership record?** Three shapes, each with a precedent already in
   this project: (a) the School self-registers, then is gated behind an approval step — the Agent
   pattern (`DEC-SCOPE-004`: self-register, held Pending until Overseas Admin approves); (b)
   EduSphere staff creates the record entirely, with no School-side self-service step at all — the
   University Representative pattern (`UNI-001`, precondition "a University partner record exists,"
   implicitly Admin/Counselor-created, no approval gate modeled); (c) a sales/partnership-manager-led
   process — raised only in `EVID-020` (`University Partnership CRM.md`), an unattributed
   `DERIVED_BLUEPRINT` already flagged in `CONFLICT_MATRIX.md` `C-10` as proposing a role
   (Partnership Manager) with zero supporting evidence — do not treat this as a real precedent
   without its own separate confirmation.
2. **Once the School record exists, who creates the four role accounts?** Options include: EduSphere
   staff creates all four manually (consistent with `DEC-SCOPE-009`'s original "counselor-managed"
   spirit, now `DEC-ROLE-005`'s Counselor-manages-service-delivery-data framing); EduSphere creates
   one seed account (e.g. the Coordinator) which then invites/creates the other three, mirroring
   `EVID-014`'s own "School Master creates Teacher/Parent logins" vision — that document alone is not
   sufficient grounds per `CLAUDE.md`, but the shape is a legitimate option to consider; or a
   pure invite-link flow with no seed account at all.
3. **Is there an approval gate before a new School or account becomes active** — Agent's
   Pending→Approved model (`DEC-SCOPE-004`), or immediate activation like University Representative
   appears to have (no gate modeled anywhere for that role either)?
4. **Does this interact with the still-open School Partnership tiers question** (Bronze/Silver/Gold/
   Platinum, `EVID-013`, explicitly left open by the user for the real client — see
   `PRODUCT_DECISION_REGISTER.md`'s "Open, not resolved — School Partnership tiers" entry above and
   `CLIENT_QUESTIONS.md` Round 2 item 9)? E.g. does a lower partnership tier get fewer of the four
   roles, or no bulk-upload capability? **Flagged for awareness only — this question does not need
   to be answered here; a "no interaction, tiers are purely commercial" answer is a valid resolution
   to part 4 on its own.**

**Evidence:** None directly — this is a genuine gap, not a conflict between sources. The closest
analogs (`DEC-SCOPE-004` Agent self-service, `UNI-001` University Representative Admin-created) are
cited above as precedent shapes, not as answers already given for School.

**Recommendation:** The University Representative pattern (EduSphere/Admin creates the partner
record and initial account) is the closer analog — School partnerships, like University
partnerships, are established through an offline relationship (sales, BDM, or counselor contact) per
every piece of evidence touching School at all (`EVID-011`–`014`), not a self-service signup a school
administrator would stumble onto directly, unlike Agent (an individual professional registering
themselves). This is a recommendation only, not self-approving per `APPROVAL_GATES.md`.

**Resolution, 2026-09-14, asked directly across all four parts in one pass:**

1. **Record initiation — RESOLVED: EduSphere/Admin creates it entirely.** User confirmed the
   recommended option: "EduSphere/Admin creates it entirely." No School-side self-service step
   exists — matches the `UNI-001` (University Representative) pattern, not Agent's self-registration
   pattern. Given School's proposed (not yet confirmed, `RBAC_MATRIX.md` §1, item 72) `overseas`
   division assignment, the natural actor is **Overseas Admin** — same actor that manages University
   partner records and approves Agent registrations.
2. **Account creation — RESOLVED: EduSphere seeds one account, which invites the rest.** User
   confirmed this option, described as "EduSphere creates one account (e.g. Coordinator), who then
   invites/creates the other three." **The seed role is the School Coordinator** — the only
   write-capable school-side role, and the one option's own example named. Overseas Admin creates the
   School record and the seed Coordinator account together; the Coordinator then invites/creates
   Principal, Teacher, and Parent accounts for their own institution only.
3. **Activation gate — RESOLVED: no gate, active immediately.** User confirmed the recommended
   option. Both the School record and every account created under it (Coordinator by Overseas Admin;
   Principal/Teacher/Parent by the Coordinator) are active on creation — no Pending state, no
   approval action, unlike Agent's model.
4. **Partnership-tier interaction — RESOLVED: no interaction.** User confirmed: provisioning is
   independent of the still-open School Partnership tiers question (`CLIENT_QUESTIONS.md` Round 2
   item 9, which remains open on its own, unaffected by this resolution). Every School gets the same
   four-role provisioning path regardless of commercial tier, unless a future decision says
   otherwise.

**New Feature ID authorized:** `SCH-003` — School partner onboarding. See `docs/features/
feature_catalog.json`/`MASTER_FEATURE_CATALOG.md` for the propagated entry (added same session,
following this resolution).

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-14. BRD/PRD/
Feature Catalogue/RBAC/DATA_MODEL/UX propagation is follow-up work; BRD/PRD/Feature Catalogue done
this same session, `RBAC_MATRIX.md`/`DATA_MODEL.md`/UX Screen Catalogue not yet done.

---

### DEC-ROLE-004 — Student login scope: EduSphere-direct students only *(added on review, direct
user statement, not from a `docs/sources/` file)*

**Question:** Do School-affiliated students and Agent-referred students each get their own
individual Student login, or is a Student login issued only to a student who is EduSphere's own
direct student (self-registered/directly enrolled, not routed through a School or Agent)?

**Evidence:** A direct statement from the user in this session (2026-09-14): *"ignore the student
login for school students and agent students. Only edusphere students should have the login."* This
is consistent with — and now closes — two things already on record: (a) `DEC-SCOPE-009`'s confirmed
"not individual student logins" for School students, and (b) `EVID-011`'s own transcript detail
already summarized in `EVIDENCE_REGISTER.md` as "no agent-referred-student login," which had never
been carried into a Decision ID. It directly resolves `CLIENT_QUESTIONS.md` Round 3 item 11 (the
`C-09` conflict against `EVID-014`'s individual-login proposal) — that document's School Master →
Teacher/Parent/Student login hierarchy is rejected outright, not just for bulk upload.

**Impact — this reverses part of already-shipped, `COMPLETE` functionality, not just future scope:**
`AGT-002` ("Referred-student roster and status," `RTM.md`: complete since Wave 3, 2026-09-03) and
its data model (`DATA_MODEL.md` §6.8 `AgentStudent.student_user_id`) currently assume an
agent-referred student **has their own real `User` account** that they registered and logged into
themselves — the Agent merely views that account's roster/status. `DEC-ROLE-001` ("same Student
identity, extended," `CONFIRMED_CURRENT` 2026-09-01) unified the domestic-Student and
Overseas-applicant identity into one account without distinguishing self-registered from
agent-referred applicants, implicitly assuming every Overseas applicant — agent-referred or not —
holds their own login. Neither of those decisions is being silently overridden here; both are now
flagged as needing reconciliation against this one before any RBAC/data-model change is made.

**What this decision resolves:** no School-affiliated or Agent-referred student is issued an
individual login. Only a student who registers/enrolls with EduSphere directly gets the Student
role/account.

**On-behalf-of actor — RESOLVED same session (2026-09-14), asked directly:** if an agent-referred
student never logs in, who performs the actions currently modeled as that student's own actions in
the Overseas application journey (document upload, application submission, offer/visa status
tracking)? **User confirmed: the Agent (for their referred students) / the School representative
(for school-affiliated students) performs these on the student's behalf** — the direct analogue of
`DEC-SCOPE-009`'s counselor-managed School model, now extended explicitly to the Agent side too.

**What remains open — implementation reconciliation, not a further decision:** this changes
`AGT-002`'s already-shipped scope from a **read-only** roster/status view into a **write-capable,
on-behalf-of** workflow (document upload, submission, status updates performed by the Agent), and
touches the Overseas application state machine (`DEC-WF-001`), `DEC-ROLE-001`'s "same identity,
extended" model (an agent-referred student may now have no `User` account at all, rather than a
Student account with Overseas fields), `DEC-SCOPE-005`'s commission-trigger mechanics (student-joined
event — now presumably recorded by the Agent or an Admin, not the student), and
`docs/architecture/RBAC_MATRIX.md`/`docs/architecture/DATA_MODEL.md`, none of which have been edited
yet. This is a `docs/features/feature_catalog.json`/RBAC/data-model rework item for a future pass,
not code to change now — flag it before that pass so `AGT-002` and `DEC-ROLE-001` aren't silently
left contradicting this decision.

**Status:** CONFIRMED_CURRENT (login-issuance scope **and** on-behalf-of actor model) — Approved by:
user (in-session) — Approval date: 2026-09-14.

**Downstream documentation reconciliation — DONE, 2026-09-14 (docs only, no code/schema changed):**
per `CHANGE_CONTROL.md`'s impact-check step, the following were updated to flag (not yet build) the
consequences of this decision:
- `RBAC_MATRIX.md` §2.7/§2.8/§3 — narrowed the `overseas_student` "Self" row to self-authenticating
  applicants only; recorded the Agent's new (undesigned) on-behalf-of write need against §3's own
  "Support-staff impersonation: none — not designed" line, naming `DEC-ROLE-004` as that line's
  required Decision ID.
- `DATA_MODEL.md` §6.2/§6.8 — flagged that `student_user_id` no longer universally means a
  self-authenticating account; left the exact schema mechanism (agent-created no-login `User` row
  vs. a nullable FK with fields carried on `AgentStudent`) as an open architecture choice, not
  decided here.
- `ROLE_ACTION_MATRIX.md` — added the Agent's not-yet-built on-behalf-of grant, narrowed the
  `overseas_student` identity note, and recorded `counselor` as the anticipated (already-shaped, not
  yet formally extended) actor for `DEC-SCOPE-009`'s School-affiliated case.
- `feature_catalog.json`/`MASTER_FEATURE_CATALOG.md` — `AGT-002` and `OVS-002`/`004`/`005` each
  annotated as known-incomplete against this decision, without changing their `CURRENT`/`COMPLETE`
  status (their self-service scope is unaffected and still correct).
- `IMPLEMENTATION_PLAN.md` — flagged that Wave 3's effort figure does not yet include the new
  on-behalf-of capability, pending a numbered Feature ID.

**Still outstanding (actual design/build work, not documentation):** a new Feature ID for the
Agent's on-behalf-of write path; the `student_user_id` schema-mechanism choice; the audit design
ensuring every on-behalf-of write is attributable to the acting Agent; and the `IMPLEMENTATION_PLAN.md`
delta calculation once that Feature ID exists.

---

### DEC-ROLE-005 — Internal EduSphere actor for school-affiliated student data management

**Status:** CONFIRMED_CURRENT — resolved 2026-09-14, asked directly, in-session.

**Trigger:** `DEC-SCOPE-009`'s resolution text says "counselors manage the underlying student data
from the internal system" — generic wording. An earlier propagation pass this session incorrectly
formalized that into "the existing `Counselor` role" (scoped to Overseas applications/visas/
documents), an unwarranted inference the user caught by asking for `EVID-014`'s full content to be
re-checked — see `docs/product/PRD_OPEN_ITEMS.md` item 71 and `docs/decisions/CLIENT_QUESTIONS.md`
Round 3 item 14 for the full correction trail.

**Question:** Which internal EduSphere role(s) manage school-affiliated student data (career
guidance, psychometric, counselling, and whatever else that data eventually covers) — the existing
`Counselor` role, new specialized roles (`EVID-014` §13 proposes Career Counselor, Psychometric
Team, Academic Team, School Partnership Manager), or something else?

**Resolution:** User confirmed: "Reuse the existing Counselor role." The existing `counselor` role
(`ROLE_ACTION_MATRIX.md`'s Overseas Division `Counselor` section) is extended to also cover
school-affiliated student case management, rather than building new specialized roles. This is
simpler — one actor, not four — at the cost of less precision than `EVID-014`'s specialized-team
model; if that trade-off proves wrong once actual school-service modules (career guidance,
psychometric, etc.) are designed, it can be revisited then.

**Division of labor with `DEC-SCOPE-011`'s School Coordinator, stated explicitly to avoid
confusion:** the School Coordinator (school-side, external) manages **roster/operational** data —
creating and maintaining student records, scheduling activities, tracking attendance, per
`DEC-SCOPE-011`. The Counselor (EduSphere-side, internal, this decision) manages **service-delivery**
data — the substantive content of whatever career-guidance/psychometric/counselling work is
eventually built. These are not in conflict; they cover different halves of the data.

**Not resolved by this decision:** whether Career Guidance, Psychometric, or Counselling exist as
buildable modules at all — that remains open, same as `DEC-SCOPE-010`'s results-module sub-question.
This decision only settles *who* would manage that data once/if such modules are built.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-14. BRD/PRD/
Feature Catalogue/RBAC propagation is follow-up work, not yet done as of this decision's resolution.

**Superseded, 2026-09-14 (later, same session) — see `DEC-ROLE-006`.** This decision's own text
flagged the trade-off explicitly: "if that trade-off proves wrong once actual school-service modules
... are designed, it can be revisited then." Once results/career-guidance/psychometric/counselling
were confirmed in scope (`DEC-ROLE-006`), the user reopened this and adopted the five-specialized-
role model instead. This entry is kept, not deleted, per this project's traceability convention —
`DEC-ROLE-006` is now the governing decision for internal School-domain actors.

---

### DEC-ROLE-006 — Five specialized internal School-domain roles; results/career-guidance/
psychometric/counselling module scope

**Status:** CONFIRMED_CURRENT — resolved 2026-09-14, asked directly, in-session.

**Trigger:** The user asked to decide two remaining School Portal gaps in one pass: (1) whether an
academic results/marks module is in scope (`DEC-SCOPE-010` part 2, left open earlier the same
session), and (2) whether Career Guidance, Psychometric, and Counselling exist as buildable modules
at all (left open by `DEC-ROLE-005`).

**Resolution — module scope:**

1. **Academic results/marks module — CONFIRMED in scope.** User confirmed directly ("Yes, in
   scope").
2. **Career Guidance, Psychometric, and Counselling — CONFIRMED in scope, all three.** User
   confirmed directly ("Yes, all three").

**Resolution — internal actor model, superseding `DEC-ROLE-005`'s "reuse the existing Counselor
role":** asked who uploads results and what gate applies, the user answered by actor-responsibility
rather than picking a single role: **"based on team responsibility — Psychometric Team, Career
Counselor, Edusphere School Manager, Academic Team, School Partnership Manager."** This names, and
this project now adopts, `EVID-014` §13's five specialized roles verbatim, in place of the single
extended `Counselor` role `DEC-ROLE-005` originally confirmed:

- **Academic Team** — owns the results/marks module: uploads results, views academic progress.
- **Career Counselor** — owns Career Guidance and Counselling (treated as one combined domain,
  matching `EVID-014`'s own description of this role: conducts career guidance, adds counselling
  notes, uploads recommendations).
- **Psychometric Team** — owns the Psychometric module: assigns assessments, uploads reports, views
  psychometric data.
- **Edusphere School Manager** — manages a portfolio of assigned schools. **Exact duties beyond
  `EVID-014`'s one-line description are OPEN** — not itemized in this exchange, do not invent beyond
  "manage assigned schools."
- **School Partnership Manager** — manages the school relationship, views school-level reports,
  schedules meetings, manages activities. **Overlap with Overseas Admin's `SCH-003` onboarding
  duties and with "Edusphere School Manager" above is OPEN, not reconciled here** — do not assume
  this role replaces Overseas Admin's provisioning role or how it divides from Edusphere School
  Manager; flag for Contracts-phase confirmation before building either of these two specifically.

None of these five is the existing `Counselor` role (`DEC-ROLE-005`'s original choice) — they are
net-new, EduSphere-internal roles, distinct from the existing Overseas `Counselor`, and also
distinct from the School-side `school_coordinator`/`school_principal`/`school_teacher`/
`school_parent` roles (`DEC-SCOPE-011`).

**Division of labor with School Coordinator, resolved in the same pass:** asked whether School
Coordinator gets any visibility into this content, user confirmed: **Coordinator gets read-only
visibility/reporting** — the five specialized teams above exclusively create/manage the substantive
content (results, career guidance, psychometric, counselling); Coordinator can see summary
status/reports for their own school, but never creates or edits this content directly. This
preserves `DEC-ROLE-005`'s original division-of-labor principle (Coordinator = roster/operational
data; internal staff = service-delivery data) — only *which* internal staff changed.

**Results approval gate — adopted, one detail not verbatim-confirmed:** `EVID-014`'s
Draft→Verified→Published gate is adopted as the working design for the results module, as the
natural pairing with a team-responsibility model (preventing an incomplete or unverified Academic
Team entry from reaching Parent/Student). This specific mechanism was not independently re-confirmed
word-for-word in this exchange — flagged so it can be corrected quickly if wrong, not treated as
equally solid as the other resolutions in this entry.

**New Feature IDs authorized:** `SCH-004` (Career Guidance & Counselling), `SCH-005` (Psychometric
Assessment), `SCH-006` (Academic Results, with the Draft→Verified→Published gate). See
`docs/features/feature_catalog.json`/`MASTER_FEATURE_CATALOG.md` for the propagated entries.

**Evidence:** `EVID-014` §13 (an unattributed `DERIVED_BLUEPRINT` — its role list is not itself
authoritative; it becomes governing only because the user directly named and confirmed it here, per
`CLAUDE.md`'s rule that a derived document's content requires explicit user/client confirmation, not
the document's own claims).

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-14.
Supersedes `DEC-ROLE-005`'s actor choice; resolves `DEC-SCOPE-010` part 2 jointly. Open, not decided
here: Edusphere School Manager's and School Partnership Manager's exact duties/overlap, and the
Draft→Verified→Published gate's exact re-confirmation. BRD/PRD/Feature Catalogue/RBAC/DATA_MODEL/UX
propagation is follow-up work, not yet done as of this decision's resolution.

---

### DEC-SCOPE-013 — Assignment-scope mechanism for Academic Team / Career Counselor / Psychometric Team

**Status:** CONFIRMED_CURRENT — resolved 2026-09-14, asked directly, in-session.

**Trigger:** While propagating `DEC-ROLE-006` into `RBAC_MATRIX.md` (§2.12) and `DATA_MODEL.md`
(§6.14, §6.16–6.18), every one of the three specialized roles' grant rows had to be written as
"assigned school-affiliated students only — scope mechanism not yet designed, see §5," because
nothing in `DEC-ROLE-006` or `EVID-014` specifies *how* a member of `academic_team`/
`career_counselor`/`psychometric_team` becomes scoped to a particular set of students. Without an
answer, none of `SCH-004`/`005`/`006`'s core RBAC enforcement can be built — this is the single
largest blocker to those three features' coding-readiness (`RTM.md`'s School section, `SCH-CAREER-01`/
`SCH-PSYCH-01` stubs are explicitly partial because of it).

**Question:** How is a member of one of these three roles scoped to "their assigned students"? Three
shapes, each with a precedent already in this project:

1. **Per-school portfolio** — the role is assigned to one or more entire *institutions*, and can act
   on any student at any school in their portfolio (mirrors `edusphere_school_manager`'s own framing,
   "manages assigned schools," and Trainer's own-batch-at-the-batch-level pattern generalized to
   institution level).
2. **Per-student assignment** — the role is assigned individual students directly, one at a time or
   in batches, regardless of institution (mirrors `school_teacher`'s own proposed per-student
   assignment mechanism, `DATA_MODEL.md` §6.12, and Counselor's existing `CNS-001` caseload pattern).
3. **Per-institution blanket access** — any member of the role can act on any school-affiliated
   student at any institution, with no individual assignment step at all (simplest to build, but the
   weakest least-privilege posture of the three — a member with no relationship to a specific school
   could still act on its students).

**Evidence:** None directly — `EVID-014` names the five roles' responsibilities but never describes
an assignment mechanism for any of them; this is a genuine gap, not a conflict between sources.

**Recommendation:** Option 1 (per-school portfolio) is the closer fit for how these three roles are
described — "Academic Team," "Career Counselor," and "Psychometric Team" read as small internal teams
each covering a set of partner schools, not as caseworkers assigned to hand-picked individual
students the way a Counselor is. It also reuses `edusphere_school_manager`'s own stated "assigned
schools" framing rather than inventing a second, different assignment shape for a closely related set
of roles. This is a recommendation only, not self-approving per `APPROVAL_GATES.md`.

**Resolution, 2026-09-14:** **Option 1 — per-school portfolio, RESOLVED.** User confirmed the
recommended option. Each `academic_team`/`career_counselor`/`psychometric_team` member is assigned
one or more entire `School` institutions and may act on any school-affiliated student at any
institution in their own portfolio — never a student at a school outside it, even via a direct record
ID. This requires a portfolio-membership table (e.g. `SchoolStaffAssignment(user_id, school_id)`,
many-to-many — one member can cover several schools, one school can have several members of the same
role) rather than a per-student FK; the exact schema is Contracts-phase detail, not fixed by this
decision. Propagation into `RBAC_MATRIX.md`/`DATA_MODEL.md` is follow-up work, not yet done as of
this resolution.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-14.

---

### DEC-SCOPE-014 — Account provisioning for Academic Team / Career Counselor / Psychometric Team

**Status:** CONFIRMED_CURRENT — resolved 2026-09-14, asked directly, in-session.

**Trigger:** `SCH-003`'s onboarding flow (`DEC-SCOPE-012`) covers only the four school-side roles
(Principal/Coordinator/Teacher/Parent) — Overseas Admin creates the School + seed Coordinator, and
the Coordinator invites the other three. Nothing anywhere specifies how `academic_team`/
`career_counselor`/`psychometric_team` accounts — internal EduSphere staff, not school-side actors —
come into existence. `DATA_MODEL.md` §6.15 explicitly notes `SchoolAccountInvite` "provisions the
four school-side accounts only" and flags this as a separate, undesigned question.

**Question:** How is an `academic_team`/`career_counselor`/`psychometric_team` account created? Three
shapes, each with a precedent already in this project:

1. **Same pattern as other internal staff roles** (`it_admin`, `overseas_admin`, `counselor`,
   `placement_team`, `hr_team`) — created directly by a Super Admin or an existing division Admin
   through the Admin console's user-management surface (`ADM-001`/`004`), no invite-token flow at
   all. Simplest, and consistent with how every other *internal* (non-external-partner) role in this
   project gets its account.
2. **Extend `SCH-003`'s invite flow** to a fourth invitable role value, issued by Overseas Admin (or
   `edusphere_school_manager`, if `DEC-SCOPE-013`/item 75 resolves that role as the natural issuer) —
   reuses the existing `SchoolAccountInvite` mechanism rather than building a second one.
3. **A dedicated internal-staff onboarding step**, separate from both the Admin console and the
   School invite flow — only worth considering if neither of the above fits once the assignment-scope
   answer (`DEC-SCOPE-013`) is known (e.g. if portfolio assignment needs to happen *at* account
   creation, not as a separate step afterward).

**Evidence:** None directly — `EVID-014` never describes how these roles' accounts are created, only
their responsibilities once they exist.

**Recommendation:** Option 1 — these are internal EduSphere staff roles, not external school-side
actors, so the existing internal-staff account-creation pattern (Admin console) is the more consistent
fit than reusing a mechanism (`SchoolAccountInvite`) purpose-built for external partner accounts. This
is a recommendation only, not self-approving per `APPROVAL_GATES.md`.

**Resolution, 2026-09-14:** **Option 1 (Admin console pattern) confirmed, actor narrowed by direct
user answer:** "created by overseas admin or super admin." Accounts for all three roles are created
through the existing Admin console user-management surface (`ADM-001`/`004`'s pattern), **not** via
`SchoolAccountInvite` — but unlike the generic Option 1 draft (any Super Admin or division Admin),
creation is restricted to **Overseas Admin or Super Admin specifically** — `it_admin` does not create
these accounts, consistent with School's proposed `overseas` division assignment (`RBAC_MATRIX.md`
§1, still itself not independently confirmed — see `PRD_OPEN_ITEMS.md` item 72). This is narrower
than Option 1's original wording, not a different option — recorded as the user's own refinement, not
inferred. Which portfolio (per `DEC-SCOPE-013`) a newly-created account is assigned to at creation
time is Contracts-phase detail, not fixed by this decision. Propagation into `RBAC_MATRIX.md`/
`DATA_MODEL.md` is follow-up work, not yet done as of this resolution.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-14.

---

### DEC-ROLE-007 — Academic results Draft→Verified→Published gate: verify/publish actor

**Status:** CONFIRMED_CURRENT — resolved 2026-09-14, asked directly, in-session.

**Trigger:** `DEC-ROLE-006` adopted `EVID-014`'s Draft→Verified→Published gate as the working design
for `SCH-006`, but explicitly flagged it as "not independently re-confirmed word-for-word" — the one
detail in that decision not asked about directly. While propagating into `DATA_MODEL.md` §6.16,
`SchoolAcademicResult` needed separate nullable `verified_by_user_id`/`published_by_user_id` columns
specifically because who performs those two steps is unresolved, and `RBAC_MATRIX.md` §2.12 currently
grants all three transitions to `academic_team` alone "as the simplest reading," flagged for revision
once this is answered.

**Question, two parts:**

1. **Does `academic_team` perform all three transitions itself** (upload → verify → publish, one
   role throughout), **or does a different actor verify/publish** — e.g. a second `academic_team`
   member (peer review), or a different role entirely such as `edusphere_school_manager` or
   `school_coordinator` (a supervisory sign-off before the result reaches a family)?
2. **If a different `academic_team` member specifically** (not a different role): must it be
   demonstrably a *different* individual than the uploader (a same-actor restriction, the same shape
   already confirmed for Agent commission payouts, `RBAC_MATRIX.md` §2.8), or may the same person
   upload, verify, and publish their own entry?

**Evidence:** `EVID-014` describes the gate's existence (Draft/Verified/Published as states a result
passes through) but not who performs each transition — this is a genuine gap in that source, not a
conflict.

**Recommendation:** A same-actor restriction (part 2, mirroring the Agent commission payout pattern
already confirmed in this project) is the more defensible default for anything reaching a family as
an official academic record — but *who* the second actor is (another Academic Team member vs. a
different role) is a genuine open choice this document does not recommend one way over the other,
since either is plausible from the role names alone. This is a recommendation only, not
self-approving per `APPROVAL_GATES.md`.

**Resolution, 2026-09-14, both parts resolved in one answer:** User confirmed the recommended option:
**a different `academic_team` member must verify/publish** — not a different role, and not the same
uploader. This resolves both parts of the question together:

1. **Actor — RESOLVED: `academic_team`, not a different role.** Verify/publish stays within
   `academic_team` — `edusphere_school_manager`/`school_coordinator` play no role in this gate.
2. **Same-actor restriction — RESOLVED: yes, mandatory.** The `academic_team` member who uploaded a
   result (`uploaded_by_user_id`) may never be the same identity recorded as `verified_by_user_id` or
   `published_by_user_id` — enforced server-side, same two-gate shape as `AGT-004`'s commission-payout
   same-actor restriction (`RBAC_MATRIX.md` §2.8, `NFR-SEC-002`). A single-member `academic_team`
   portfolio (per `DEC-SCOPE-013`) would therefore be unable to ever publish its own results —
   **flagged as a real operational consequence of this resolution**, not silently absorbed: a
   portfolio needs at least two `academic_team` members, or verify/publish blocks permanently. Not
   re-litigated here since the user confirmed the restriction directly; carried forward as an
   implementation-readiness note.

Also resolves `PRD_OPEN_ITEMS.md` item 74's remaining open half — the Draft→Verified→Published gate
itself (already adopted per `DEC-ROLE-006`) is now fully confirmed, including who performs each step.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-14.
Propagation into `RBAC_MATRIX.md`/`DATA_MODEL.md` is follow-up work, not yet done as of this
resolution.

---

### DEC-SCOPE-015 — Parent Portal content scope (`EVID-014` §23) and parent notification triggers

**Status:** CONFIRMED_CURRENT for the Parent Portal *surface* and for the four triggers whose
modules exist; **OPEN (`NEEDS_CONFIRMATION`)** for the six items that depend on modules never
confirmed — resolved 2026-09-15, in-session.

**Trigger:** The user asked, in-session on 2026-09-15, "execute school-crm.md" and then, quoting
`EVID-014` §23 verbatim: *"does the parent portal contains all this features. Very important for
the school model. Parents can see: Student profile · Career guidance status · Psychometric status ·
Counselling · Recommended careers · Skills · Activities · Portfolio · Overseas education progress ·
Upcoming sessions · Important notifications. Parent should receive notifications for: Assessment →
Counselling → Workshop → Test → Application → Important deadline."* Until this, `DEC-SCOPE-011`
had confirmed only that a Parent login exists and can "view their own child's/children's profile
and progress" — the content of that view was never itemized, and the `SCR-SCH-009` build showed
name/grade/DOB plus a three-number summary only.

**Evidence:** `EVID-014` (`School CRM.md`, `DERIVED_BLUEPRINT`, unattributed) §23 and the
second-part "4. Parent Login" / "11. Notifications" sections. Governing here only because the user
named the list directly in-session (same rule as `DEC-SCOPE-011`/`DEC-ROLE-006`), not because the
document says so.

**Resolution, item by item — what the confirmed data model can actually supply:**

| §23 item | Backing module | Outcome |
|---|---|---|
| Student profile | `SCH-001` `SchoolStudent` (+ school name, assigned Teacher name) | **Built** (`SCH-007`) |
| Career guidance status | `SCH-004` `guidance_session` records | **Built** — "completed" once ≥1 session recorded |
| Psychometric status | `SCH-005` `SchoolPsychometricRecord.status` | **Built** — not_started / assigned / completed |
| Counselling | `SCH-004` `counselling_note` records | **Built** |
| Recommended careers | `SCH-004` `recommendation` records ("uploads recommendations" is in `DEC-ROLE-006`'s confirmed Career Counselor duties) | **Built** — as free-text recommendation rows, not a structured career taxonomy |
| Activities | `SCH-001` `SchoolActivityAttendance` | **Built** — the child's attended/absent sessions |
| Upcoming sessions | `SCH-001` `SchoolActivity` (future, own school) | **Built** |
| Important notifications | existing `Notification` model (`NOT-001`) | **Built** — in-app feed + email copy |
| **Skills** | none — `EVID-014` §9 Soft Skills / §10 Digital Skills were never confirmed as modules | **OPEN** — `PRD_OPEN_ITEMS.md` item 77 |
| **Portfolio** | none — §14 Digital Portfolio never confirmed | **OPEN** — item 77 |
| **Overseas education progress** | none — §15–20 Global Education never confirmed for school students; a `SchoolStudent` has no login and no link to the Overseas `overseas_student` identity (`DEC-ROLE-004`) | **OPEN** — item 77 |

| Notification chain step | Trigger built | Outcome |
|---|---|---|
| Assessment | psychometric assessment assigned; report attached (→ completed) | **Built** |
| Counselling | guidance session / counselling note / recommendation recorded | **Built** |
| Workshop | Coordinator schedules a `SchoolActivity` (every linked Parent at that school, once) | **Built** |
| Test | no confirmed test module (`EVID-014` §12 IELTS / §13 SAT never confirmed) | **OPEN** — item 78. **Result Published** *is* built (§11's "Term 1 results are now available" parent notification) as the nearest confirmed event, clearly not the same thing |
| Application | no Overseas linkage for school students | **OPEN** — item 78 |
| Important deadline | no deadline entity in any confirmed module | **OPEN** — item 78 |

**Not decided here, deliberately:** whether a Parent may open the psychometric `report_url` itself
(the readable list has always returned status only — kept as-is, item 79); grade/section targeting
of sessions (a `SchoolActivity` is school-wide in the confirmed model, so the Workshop notification
is school-wide too).

**New Feature ID authorized:** `SCH-007` (Parent Portal: child 360 overview + notifications).

**Status:** CONFIRMED_CURRENT (surface + four triggers) / OPEN (six items) — Stated by: user
(in-session) — Date: 2026-09-15. BRD/PRD/Feature Catalogue/RBAC/UX/RTM propagation done the same
day (`BRD_CHANGE_LOG.md` v1.10, `PRD_CHANGE_LOG.md` v1.8).

---

### DEC-SCOPE-016 — Narrow Student Journey Timeline (`EVID-014` second-part §15), built from confirmed modules only

**Status:** CONFIRMED_CURRENT — resolved 2026-09-15, in-session.

**Trigger:** The user asked whether `EVID-014`'s second-part §15 ("One Feature I Strongly
Recommend: Student Journey Timeline") exists. It did not — that section is the source
document's own recommendation, never confirmed by any Decision ID, Business Requirement, or
Feature ID. The user then asked for the narrow version, built only from what `SCH-001`/
`004`/`005`/`006` already record, with the same role-based access `DEC-SCOPE-011` already
established (a Parent sees only their own child's timeline).

**Resolution:** Built as `SCH-008`, a single read endpoint deriving a chronological event
list from existing rows — no new tables, no invented Foreign Language / English Test /
University Planning / Soft-Skills stages (those remain unconfirmed, same as `DEC-SCOPE-015`
items 77/78). Five event categories only: **profile** (student created), **career**
(guidance session / counselling note / recommendation, from `SCH-004`), **psychometric**
(assessment assigned / report uploaded, from `SCH-005`), **academic** (result Published
only — `SCH-006-AC02`'s Draft/Verified visibility rule carried through unchanged), and
**activity** (attended sessions, from `SCH-001`). Access reuses the identical own-scope
loader already built for `SCH-007`'s overview (`_load_readable_student`): a Parent's own
child only, a Teacher's assigned student only, Coordinator/Principal their own institution
— not the three specialized service-delivery roles' own portfolio mechanism, since this is
a student-facing view, not a staff work queue.

**Evidence:** `EVID-014` second part §15 (`DERIVED_BLUEPRINT`, its illustrative one-event-
per-month cadence is not itself confirmed or reproduced — the built timeline shows real
event dates, however they actually cluster).

**New Feature ID authorized:** `SCH-008` (Student Journey Timeline, narrow).

**Status:** CONFIRMED_CURRENT — Stated by: user (in-session) — Date: 2026-09-15. BRD/PRD/
Feature Catalogue/RBAC/UX/RTM propagation done the same day.

**Addendum, 2026-09-15 (later, same day) — UI surfaces extended to Coordinator/Principal; demo
data corrected:** the user asked for two things in one message: (1) "timeline workflow or diagram
should be available to co-ordinator, teacher, principal also" (Teacher's own per-student page
already had it; the API already scoped correctly for all four roles via `_load_readable_student`,
but Coordinator and Principal had no screen to reach it from) -- resolved by adding a
`SchoolStudentDetailPanel.tsx` (student header + Journey Timeline, shared read-only component),
wired into a new `/school/coordinator/students/[id]` page (linked from a "Timeline" action on the
existing roster) and a new `/school/principal/students/[id]` page (linked from the dashboard's
roster table); Teacher's existing page was refactored onto the same shared component. No backend
change was needed -- `GET /school/students/{id}/timeline`'s scope already covered Coordinator/
Principal correctly. (2) "correct the previous errors identified" -- referring to two things flagged
in this session's own visual review of the seeded demo account: events appearing out of expected
order (an activity and a published result both predating "Student profile created"), and several
unrelated events sharing an identical timestamp. Root cause confirmed: `app/seed.py`'s School-domain
block relied on `TimestampMixin`'s insert-time default for `created_at` on most rows, while a few
fields (activity `scheduled_at`, result `verified_at`/`published_at`) were independently offset from
separate `datetime.now(UTC)` calls -- nothing kept the *story* internally consistent (a result could
even end up "published" before its own `created_at`). Fixed by introducing one shared `journey_base`
timestamp per seed run and explicit, staggered offsets so "Student profile created" is always
earliest and every other confirmed-module event follows in real order; applied to `seed.py` (for any
future fresh seed) and, via a one-off exactly-scoped update statement, to the already-running dev
database's existing demo rows (five named students, three career records, three psychometric
records, and the four academic results belonging to them -- verified by exact-id/exact-field match
before writing, so no unrelated row from this shared dev database's considerable accumulated test
data was touched). This is a demo/seed-data correction, not an application logic change -- the
timeline's own chronological-sort behavior was already correct and was not altered.

---

### DEC-SCOPE-017 — School Partnership tiers (`EVID-013`) → entitlements, no invented caps

**Status:** CONFIRMED_CURRENT — resolved 2026-09-15, in-session.

**Trigger:** Validating `docs/sources/SCHOOL BROUCHER (5).pdf` (`EVID-013`) against the current
build for the user's "make sure it matches all including partner models" request surfaced
`CLIENT_QUESTIONS.md` item 9 (School Partnership tiers) as the one remaining genuinely open
gap. That item was originally deferred to the real client, not decided in-session. The user
chose to resolve it directly instead: first "Resolve it now," then confirmed the resolution
approach ("Track entitlements with usage counters"), then attached the brochure's own
Bronze/Silver/Gold/Platinum tier-breakdown image, then confirmed the semantics ("Included =
unlimited, just count usage" — no invented quota caps).

**Resolution:** A School's `tier` (`bronze`/`silver`/`gold`/`platinum`, nullable — set by
Overseas Admin at creation or later via a small `PATCH`) determines a cumulative service list,
read exactly as the brochure's own image shows (each tier lists only what it *adds* over the
previous one):

- **Bronze:** career seminar, student career awareness session, parent orientation,
  psychometric test, soft skills.
- **Silver adds:** individual counselling, web designing.
- **Gold adds:** application support, scholarship assistance, IELTS coaching, SAT coaching,
  foreign language classes, digital portfolio creation.
- **Platinum adds:** dedicated EduSphere counselor, monthly campus visits, internships, visa
  support, loan assistance, alumni network, parent help desk.

A service is never quota-limited — "included" always means unlimited access. `GET
/school/entitlements` (`SCH-011`, Coordinator/Principal, own institution only) returns each
included service with a REAL usage count wherever a confirmed module already produces one
(psychometric assessments, counselling notes, IELTS/SAT test-prep records, language-class
records, bridged Overseas applications/visa cases — see `DEC-SCOPE-018` — and a new optional
`SchoolActivity.activity_type` tag for seminar/orientation/campus-visit counts), and `used:
null` ("not yet tracked") for services with no underlying module (soft skills, web designing,
digital portfolio creation, internships, loan assistance, alumni network, parent help desk,
scholarship assistance) — never a fabricated zero or an invented cap, matching this codebase's
own established "honest zeros, never fabricated" convention (`RPT-002-AC02`, `DATA_MODEL.md`
§8).

**Evidence:** `EVID-013` (`docs/sources/SCHOOL BROUCHER (5).pdf`) — its own Partnership Model
page and tier-breakdown image, both supplied directly by the user in-session.

**New Feature ID authorized:** `SCH-011` (Partnership tier entitlements).

**Stated by:** user (in-session) — **Date:** 2026-09-15. Closes `CLIENT_QUESTIONS.md` item 9.

---

### DEC-SCOPE-018 — School→Overseas bridge, and actor for Test Prep / Foreign Language modules

**Status:** CONFIRMED_CURRENT — resolved 2026-09-15, in-session.

**Trigger:** The same brochure validation pass surfaced two more gaps against
`PRD_OPEN_ITEMS.md` item 77 (Parent Portal's "Overseas education progress," no confirmed data
path since `SchoolStudent` has no Overseas identity, `DEC-ROLE-004`) and item 78 (parent
notification triggers for "Test"/"Application"). The user was asked which unbuilt journey
stages to scope now, picked "All of the above" (build the School→Overseas bridge AND Test
Prep + Language Training modules), then resolved two remaining actor questions: "Reuse
Academic Team" (no new role for Test Prep/Language) and "Overseas Admin/Counselor initiates
it" (not School Coordinator, for the bridge).

**Resolution, part 1 — School→Overseas bridge:** `OverseasApplication.student_id` is relaxed
to nullable, and a new nullable `school_student_id` FK (`school_students.id`) is added.
Exactly one of the two is always set, enforced at the application layer (not a DB CHECK
constraint, matching this codebase's existing style) — never a synthetic `users` row for a
`SchoolStudent`, consistent with `DEC-ROLE-004`'s own rejection of that approach. **Overseas
Admin or Counselor (never School Coordinator)** looks a School student up by their
business-facing Student ID (`DEC-DATA-003`) via `GET
/overseas-admin/school-students/lookup?code=`, then starts a real application via `POST
/overseas-admin/school-students/{id}/applications` (`SCH-010`). Every existing
Overseas-student-centric endpoint that inner-joins `User` on `student_id` (11+ call sites)
is left untouched — a bridged row's `student_id IS NULL` simply never matches those joins,
which is the correct behavior (they are for real logged-in Overseas students, agent-referred
students, etc.), not something to retrofit. The existing application status-advance and
`VisaCase` endpoints work unchanged on a bridged application (both key off
`application_id`/`counselor_id`, never `User`). `SCH-007`'s student overview/`SCH-008`'s
timeline gained a `global_education` section reading these bridged rows — this is what
actually closes `PRD_OPEN_ITEMS.md` item 77's "Overseas education progress" part (Skills and
Portfolio remain unconfirmed and out of scope).

**Resolution, part 2 — Test Preparation (IELTS/SAT) and Foreign Language Classes:** two new
modules, `SchoolTestPrepRecord` and `SchoolLanguageRecord`, delivered by the existing
`academic_team` role (no new role, per the user's explicit choice) — same portfolio-scoped
CRUD shape as `SCH-004`/`005` (`_student_in_portfolio`, `_notify_student_parents`, `AuditLog`),
with no Draft/Verified/Published gate (that gate is `SCH-006`-specific, `DEC-ROLE-007`). This
closes `PRD_OPEN_ITEMS.md` item 78's "Test" notification trigger (test-prep start/result) and,
via the bridge above, its "Application" trigger; "Important deadline" remains open (no
confirmed deadline entity exists).

**Evidence:** `EVID-013`; `PRD_OPEN_ITEMS.md` items 77/78; `DEC-ROLE-004` (no synthetic User
row for a `SchoolStudent`).

**New Feature IDs authorized:** `SCH-009` (Test Preparation & Foreign Language Classes),
`SCH-010` (School→Overseas bridge).

**Stated by:** user (in-session) — **Date:** 2026-09-15. Partially closes `PRD_OPEN_ITEMS.md`
items 77 and 78 (Skills/Portfolio and Important-deadline notification remain open).

**New Screen IDs:** `SCR-SCH-025` (`/school/coordinator/students/[id]`), `SCR-SCH-026`
(`/school/principal/students/[id]`).

---

## Group 14 — 15 September 2026: business-facing Student ID

### DEC-DATA-003 — Business-facing unique Student ID format and scope

**Question:** `PRD_OPEN_ITEMS.md` item 66 / `CLIENT_QUESTIONS.md` D-09 asked the client to confirm
the exact format (length, character set, prefix convention) of a short, memorable, searchable
Student ID distinct from the existing internal technical identifier, and whether it should extend to
Agents or other operational identities beyond students.

**Evidence:** `EVID-011` (12 Sep review call) and `EVID-012` both request this; `EVID-014` ("School
CRM.md") independently asks for "every student should have a unique Student ID" (§3) with its own
illustrative example (`ES-2026-00125`, a longer, encoded format, not itself confirmed).

**Recommendation:** None was needed — resolved directly by the user, who also delegated the format
detail rather than specifying it.

**Resolution:** User confirmed directly, in-session (2026-09-15): **8-character alphanumeric**,
applying to **all students** — narrowing the original question's "students and/or agents" scope to
students only, no Agent ID. Character set/prefix convention was explicitly left to implementation
("you decide"): built as uppercase hex (`secrets.token_hex(4).upper()`), reusing this codebase's own
already-established short-code convention (`enrollment_code`, `certificate_no` in `workflows.py`)
rather than inventing a bespoke alphabet — hex naturally excludes the commonly-confused I/O/L
letters, which serves the original "memorable/searchable" request without extra encoding rules.
Applied to **both** student populations discussed all session: `User.student_code` (nullable,
`it_student`/`overseas_student` roles only) and `SchoolStudent.student_code` (never null — every row
in that table is a student). Each is its own per-table unique constraint, not a single cross-domain
namespace — the two populations are never compared side by side in the same list, so global
uniqueness was judged unnecessary complexity, not asked for, and not built.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-15.

**Implementation:** `app.core.identifiers.generate_student_code`/`unique_student_code` (shared
generator + collision-retry uniqueness check, reused at every creation site: self-registration,
`app.seed`, School roster single-add, and School bulk-upload). Migration `0028_student_code` adds
both columns and backfills every pre-existing row (School seed demo students, seeded `it_student`/
`overseas_student` accounts) so the NOT NULL constraint on `school_students.student_code` holds from
the first deploy. Surfaced in the UI: `PortalShell.tsx`'s sidebar identity badge for IT/Overseas
Student roles (added once, in the shared `PortalPage.tsx` dispatcher, covering every page those
roles see); the School roster table, edit form, and per-role student detail page
(`SchoolStudentDetailPanel.tsx`, shared across Coordinator/Principal/Teacher); and the Parent's own
dashboard card and child-detail page (`SchoolChildOverview.tsx`). 9 test-fixture call sites across
`test_sch_004`/`005`/`006`/`007`/`008`/`reports.py` updated for the new required field; full backend
regression (579 tests, excluding the two live-credential-only Zoho/Razorpay files per `CI.md`'s own
documented exclusion) and 20 targeted E2E cases confirmed clean.

---

### DEC-SCOPE-019 — First-time provisioning of admin-created accounts: emailed set-password link, no admin-known credential

**Question:** `docs/delivery/ENHANCEMENT_BACKLOG.md` ENH-003 required an audit and five decisions before
implementation: how an account created by an admin (rather than by self-registration or a
`SchoolAccountInvite`) obtains its first password, and the token model, expiry, admin-facing
recovery path and email-verification effect that go with it.

**Evidence:** Audit of the current code, 2026-09-18 (`UNVERIFIED` until re-run against the branch at
implementation time, per the "audit before implementation" acceptance criterion):

- Gap **confirmed**, and wider than the ENH-003 draft assumed. Three admin routes in
  `apps/api/app/api/admin.py` fall back to the hard-coded constant `ChangeMe@12345` and send no
  email: `POST /admin/users` (`create_user`), `POST /overseas-admin/schools` (Coordinator seed,
  `coordinator_password`) and `POST /overseas-admin/school-staff` (`create_school_staff`).
- The route `DEC-SCOPE-014` actually mandates for `academic_team`/`career_counselor`/
  `psychometric_team` is `create_school_staff`, not `create_user` (whose role allow-list excludes them).
- `AdminSchoolStaffPanel.tsx` and `AdminSchoolCreatePanel.tsx` display the default password to the
  admin; `WorkflowPanel.tsx` has a required "Temporary password" field on the create-user form.
- `auth.forgot_password()` sends its reset token only through the generic webhook
  (`integrations.send_notification`); the only SMTP composer, `services/mailer.py`, has invite and
  parent-notification templates but none for password links.
- `PasswordResetToken` (`models.py`) is already user-bound, SHA-256-hashed, unique, expiring and
  single-use, and does not reference `SchoolAccountInvite`.

**Resolution:** User confirmed directly, in-session (2026-09-18), five points:

1. **Token model:** extend `PasswordResetToken` with a `purpose` column (`reset` | `welcome`) rather
   than add a new table. This keeps `DEC-SCOPE-014`'s constraint that these accounts are not routed
   through `SchoolAccountInvite`.
2. **Expiry:** welcome links expire after **72 hours** (the user wrote "71"; confirmed as 72 when
   asked). Forgot-password reset links are unchanged at 30 minutes. Welcome links that expire without
   being used are surfaced to admins in the Users directory (status, filter, Re-send) and as a count
   and list on the Super Admin, IT Admin and Overseas Admin dashboards, scoped by division exactly as
   `GET /admin/users` is today.
3. **Explicit password field:** `POST /admin/users` no longer accepts `password`; the same removal
   applies to `coordinator_password` and to the `password` field on `POST /overseas-admin/school-staff`.
   No admin ever knows or chooses a credential for an account they provision.
4. **Recovery when SMTP is `not_configured` or a link expires:** an admin **Re-send** action issues a
   fresh welcome token and supersedes the previous one. The public forgot-password page remains a
   fallback.
5. **Email verification:** consuming a `welcome` token sets `email_verified = True`, because the link
   proves control of the address.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-18.

**Open, not resolved by this decision:** the retry/backoff policy for a failed welcome-email send
remains open with `PRD_OPEN_ITEMS.md` item 13 (NOT-001) and is not invented here; a failed send is
recorded (`status`, `error`) and recovered through Re-send only.

**Unblocks:** `ENH-003` past GATE-02. `ENH-007` (profile completion) should follow it, per the
backlog's own dependency note.

**Security hardening addendum, 2026-09-19 — user-approved in-session** (security review of this design;
findings and rationale in `docs/superpowers/specs/2026-09-19-enh-003-first-time-provisioning-design.md` §13):

- **Auth-logic change (asked first, approved):** a welcome link is refused for a deactivated account
  (same generic `400`, not consumed), and any real change of `active` revokes the account's open welcome
  links so a link mailed to a mistyped address cannot come back to life on reactivation; an admin then
  Re-sends explicitly.
- **Throttle (asked first, approved):** Re-send has a 60-second per-account cooldown (`429` +
  `Retry-After`); the first Re-send after creation is always allowed.
- **Also approved:** email validation on the three create routes (`422`); a raising mail/webhook sender is
  a failed send, never a `500`; URLs redacted from stored delivery errors; passwords capped at 128
  characters on reset; the reset page excluded from Google Analytics and served with
  `Referrer-Policy: no-referrer`.
- **Deployment checklist, not a code change:** the API's `ENVIRONMENT` must be `production` outside local
  development. **Pre-existing and out of scope of this decision:** with `ENVIRONMENT=development`,
  `forgot_password` returns `development_reset_token` to anonymous callers — `NEEDS_CONFIRMATION` whether
  to fix separately.

### DEC-SCOPE-020 — Student promotion to the next academic year / grade (`ENH-004`)

**Question:** `docs/delivery/ENHANCEMENT_BACKLOG.md` ENH-004 (source: the user's instruction to think broadly about user lifecycle, "promoted to upper grade in next year if required") had no Decision ID or Feature ID. How does a School Coordinator advance students at academic-year rollover, and what happens to the prior grade?

**Evidence:** Audit of the code, 2026-09-19: no promotion action existed; `PATCH /school/students/{id}` overwrote `grade_or_class`/`grade_level` in place with no history; every school page renders the free-text `grade_or_class` (only the dashboard KPIs read `grade_level`); `SchoolStudent` has no section field; school students never log in (`DEC-ROLE-004`).

**Resolution:** User confirmed in-session, 2026-09-19 (`EXPLICIT_APPROVAL`), six points:

1. **Bulk selection:** an explicit list of student IDs (the UI filters and ticks); no server-side grade/section selector.
2. **Grade label:** the number inside `grade_or_class` is advanced automatically ("Grade 8-A" → "Grade 9-A"); a label that cannot be advanced fails that row unless the coordinator supplies a replacement.
3. **Top grade:** a Grade 12 promotion is rejected for that row; graduate/alumni handling is out of scope.
4. **Who:** `school_coordinator` only, scoped to their own school; the school is never client-supplied.
5. **Target year and hold-back:** the active academic year, server-resolved; "hold back" is a recorded action (same grade, moved into the new year).
6. **Partial failure:** valid rows commit and failures are reported per row; a student from another school (or an unknown ID) rejects the whole request with `403`.

Design, API, UI and security review: `docs/superpowers/specs/2026-09-19-enh-004-student-promotion-design.md` (§5.4, §7.1, §14).

**Consequences:** new append-only table `school_student_grade_history` (migration `0033`); `POST /school/students/promotions` and `GET /school/students/{id}/grade-history`; the "student dashboard" in the backlog's acceptance criteria maps to the parent view only.

**`NEEDS_CONFIRMATION` (not decided here):** graduate/alumni handling after Grade 12; a real section field; whether admin roles should ever promote; parent notification on promotion; promotion events in the SCH-008 timeline.

**Security exposure found during ENH-004's review, and its status.** `PATCH /auth/me` merged a client-supplied `profile` into the user's own profile, and every school scope check trusts `profile.school_id`, so a logged-in user could re-point their own school scope and defeat the "a coordinator cannot promote at another school" guarantee this feature relies on. **Fixed 2026-09-20 with the user's approval** (plan Task 3b): `school_id` is now read-only through that route (an unchanged echo is still accepted; a change is `403`, applies nothing else, is audited as `profile.update_denied` and logged). `profile.university_id` had the same weakness for university reps (UNI-001); the independent code review of ENH-004 flagged it as High and it was **fixed the same day** by adding it to the same server-owned list (3 tests, red before the change). **Still OPEN, deliberately not changed here:** no school-student erasure path exists; there is no app-wide rate limiter or CSRF token.

### DEC-SCOPE-021 — Self-service change password for an authenticated user (`ENH-006`)

**Question:** `docs/delivery/ENHANCEMENT_BACKLOG.md` ENH-006 (source: the user's instruction "change password") required two decisions before implementation: the rate-limit threshold and storage for repeated wrong current passwords (`SECURITY_CONTROLS.md` §1 lists auth rate limiting as an open item that must not be decided without sign-off), and the session-invalidation policy after a change (marked `NEEDS_CONFIRMATION` in the backlog).

**Evidence:** Audit of the code, 2026-09-21: `apps/api/app/api/auth.py` has `forgot_password` and `reset_password` but no route for an already-authenticated user; access and refresh tokens are stateless JWTs with no per-session or token-version record (`logout` only deletes cookies); no rate limiter exists anywhere (the only throttle is the ENH-003 welcome-link Re-send cooldown, which is derived from existing rows); `AuditLog` already records denied actions (`profile.update_denied`).

**Resolution:** User confirmed in-session, 2026-09-21 (`EXPLICIT_APPROVAL`):

1. **Rate limit:** 5 failed current-password attempts per authenticated user per 15 minutes; the next attempt is `429` with `Retry-After`, even if it carries the correct password. Keyed on the user id, never on IP.
2. **Storage:** the limiter counts the `auth.change_password_failed` audit rows this feature already writes. No new table and no migration (the user chose this over a dedicated attempts table).
3. **Sessions:** other sessions are **not** invalidated. `get_current_user` and the JWT claims are unchanged; other sessions stay valid until their tokens expire.
4. **Also confirmed:** a new password identical to the current one is rejected (`422`); no confirm-password field; failed attempts are audit-logged (`outcome="denied"`), a successful change as `auth.change_password`.
5. **Reset links (security review, 2026-09-21, `EXPLICIT_APPROVAL`):** a successful change revokes the user's unused `reset`-purpose password-reset tokens (`superseded_at`, same transaction); welcome tokens, used tokens and other users' tokens are untouched.
6. **Session survival, accepted risk (2026-09-21, `EXPLICIT_APPROVAL`):** because other sessions are not invalidated (#3), a stolen refresh cookie stays usable for up to 14 days after a change (`refresh_token_days`; access tokens 60 minutes). The smaller alternative — a `password_changed_at` column checked only in `/auth/refresh`, exposure about 60 minutes, one migration — was offered and declined for this feature.
7. **Whitespace-only passwords (browser QA finding QA-007, user-directed 2026-09-21, `EXPLICIT_APPROVAL`):** `POST /auth/change-password` refuses a `new_password` that is only whitespace (`422`, "Password must not consist only of spaces"); spaces inside or around real characters are kept exactly. **`NEEDS_CONFIRMATION`:** registration and reset-password still accept such a value, so the three entry points now differ.
8. **Mobile menu placement (browser QA finding QA-004, user-directed 2026-09-21, `EXPLICIT_APPROVAL`):** on the portal's mobile menu, "Change password" is the **first** item, so it is in view without scrolling the role's 17 items on a phone. Only this feature's own item is positioned; no role's menu order is changed.
9. **Skip link in the shared public shell (browser QA finding QA-008, user-directed 2026-09-21, `EXPLICIT_APPROVAL`):** `PublicShell` gains a "Skip to main content" link and a focusable `main#main-content`. **Scope note:** this is a shared component, so every public page gains it, which is wider than ENH-006's own surface; it was accepted by the instruction below and can be reverted independently (`PublicShell.tsx`, `controls.css`, `PublicShell.test.tsx`).

**Approval trail for #7–#9 (added after the independent review, 2026-09-21).** The browser-QA pass listed QA-004, QA-007 and QA-008 under "your call" and marked them `NEEDS_CONFIRMATION` (spec §13, before commit `8c63fcc`). The user then replied "fix the 2", which was read as item 2 of that list, i.e. those three findings; the interpretation was stated to the user in the same turn ("say so if you meant something else") and was not contested. The fixes were made test-first and the spec, contract and RTM were updated to match. If that reading was wrong, #7–#9 are the three changes to revert.

Design, API, frontend and security review: `docs/superpowers/specs/2026-09-21-enh-006-change-password-design.md` (§4, §6, §12).

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-21.

**Consequences / still OPEN, deliberately not decided here:** login, forgot-password and reset-password remain unthrottled (`SECURITY_CONTROLS.md` §1); other sessions surviving a password change is a known limitation, not a decision that it is desirable; no notification email on change; no password-history or strength rules beyond the existing 10–128 character rule.

### DEC-SCOPE-022 — Student school transfer, coordinator-requested and admin-approved (`ENH-005`)

**ID note:** this decision was written as `DEC-SCOPE-021` on the ENH-005 branch. `ENH-006` merged to `main` first and holds `DEC-SCOPE-021` (change password), so when `main` was merged into the ENH-005 branch (2026-09-21) this one was renumbered to `DEC-SCOPE-022` everywhere. Earlier ENH-005 commit messages that say `DEC-SCOPE-021` mean this decision.

**Question:** `docs/delivery/ENHANCEMENT_BACKLOG.md` ENH-005 (source: the user's instruction "changing of schools etc.") had no Decision ID or Feature ID. Who may move a student from one school to another, and what happens to everything that hangs off that student (parents, staff visibility, unpublished results, the assigned teacher, history)?

**Evidence:** Audit of the code, 2026-09-21: `SchoolStudent.school_id` was set at creation and never changed; every School-role scope check trusts a single `user.profile["school_id"]`, so a moved student's parent would have been locked out; ENH-004's promotion locking assumed `school_id` never changes; results, career, psychometric, test-prep and language records carry no `school_id`, so a bare update would silently re-home unpublished work to the new school.

**Resolution:** User confirmed in-session, 2026-09-21 (`EXPLICIT_APPROVAL`), nine points (D1-D9 in `docs/superpowers/specs/2026-09-21-enh-005-student-school-transfer-design.md` §3):

1. **D1 Parents:** access is by `SchoolParentLink` alone, not by the parent's school. Links are never deleted. A parent whose only child at the losing school was this student moves to the gaining school (`profile.school_id` rewritten); a parent with another child still there stays.
2. **D2 Authority:** a School Coordinator of either school requests; `overseas_admin` and `super_admin` approve or reject. The approval is the single action that performs the move. Neither coordinator can change a student's school.
3. **D3 In-flight results:** Draft and Verified results become `withdrawn` (kept, hidden, uneditable); Published results stay with the student.
4. **D4 Other records:** career, psychometric, test-prep and language records follow the student unchanged.
5. **D5 Visibility:** a coordinator sees only requests their own school filed; the other school is told when the admin decides.
6. **D6 Lists:** `limit`/`offset` with `{items, total, limit, offset}` (default 25, max 100), a deliberate deviation from `API_CONTRACT.md` §0.1's cursor convention, which no endpoint implements.
7. **D7 Cap:** at most 50 open requests per filing school.
8. **D8 Throttle:** at most 30 filing attempts per coordinator per rolling hour (counted from the audit rows), `429` with `Retry-After`.
9. **D9 Email:** the HTML of the parent notification email escapes the text it interpolates.

Assumptions taken with the design and approved with it: "admin" means `overseas_admin` and `super_admin`; "one action" means the admin's approval performs the whole move; the incoming-request endpoint returns the identical `202 {"accepted": true}` for every well-formed Student ID so it is not an existence oracle. One implementation refinement: the free-text check on `reason`/`note` rejects control characters except line breaks and tabs (it is a textarea), plus the bidirectional overrides U+202A-U+202E and U+2066-U+2069, and it accepts zero-width joiners (Indic scripts need them).

Design, API, UI and security review: `docs/superpowers/specs/2026-09-21-enh-005-student-school-transfer-design.md` (§5.5, §6.1, §7.1).

**Consequences:** new table `school_student_transfer_requests` (migration `0034`) that is both the request workflow and the transfer history; ten endpoints (six under `/school`, four under `/overseas-admin`); the value `withdrawn` in `school_academic_results.status` (never returned by any response); a parent's read scope becomes link-only, so "a `SchoolParentLink` joins a parent and a student of the same school" was, at the time this decision was made, still the invariant the three link creators enforced when *creating* a fresh link (superseded by `ENH-008`, see the closing note below — it no longer holds for new links, only ever described link creation, not the read-side scope check, which was already link-only); a parent moved to the gaining school comes under that school's coordinator's account authority (`list_team`, `update_team_account`) and leaves the losing coordinator's; the promotion comment claiming `school_id` never changes is corrected, and a transfer racing a promotion is refused with the promotion's generic `403` having written nothing.

**`NEEDS_CONFIRMATION` (known limits, not decided here):** the unaccepted-parent-invite limit (**decided by the owner on 2026-09-21**, see the paragraph below); ~~full multi-school parent management (`ENH-008`): a parent kept at the losing school because of another child there does not appear in the gaining school's team list and cannot be linked by the gaining coordinator~~ **RESOLVED by `ENH-008`, see the closing note below**; branch moves within a school (`ENH-009`); bulk transfer; graduation/alumni; whether the other school should be consulted before an admin decides (no consent step exists); reversal is simply a new request in the opposite direction.

**Owner decision, 2026-09-21 — unaccepted parent invites: DECIDED, keep the admin warning** (`EXPLICIT_APPROVAL`: the owner chose this option in the session; the options were the ones listed in spec §13). A parent who was invited but had not accepted when their student transferred keeps today's behavior: approval clears the student's `pending_parent_email`, the parent later has an account at the losing school and no linked child, and the gaining coordinator cannot link them. The admin is warned in the queue row and again at the confirm step (`pending_parent_invite`) and can ask the parent to accept first. **Not chosen:** carrying the invite to the gaining school (needs a cross-school exception to the same-school link invariant S2/AC-29 and a rule for invites that cover siblings still at the losing school), and blocking filing or approval while an invite is unaccepted (can stall a transfer on a parent who never responds). Multi-school parent handling can be revisited with `ENH-008`.

**Security findings and status.** Found by the review before code (spec §6.1): HTML injection in parent emails (fixed, D9); the parent read filter no longer double-checking the school (link creators verified and tested); a code-probing oracle through the requester's own list (throttle D8, cap D7, every attempt audited, residual documented); an unrecorded authorization-scope change (one audit row per re-scoped parent); approval as a general write path (restricted to `profile.school_id` on `school_parent` accounts). Reported, not changed (outside ENH-005): the API has no rate limiting on login or elsewhere; no CSRF token exists (`SameSite=Lax` only); `secret_key` defaults to `"change-me"` and `cookie_secure` to `False`; admin routes gate on the `users.role` column rather than active role assignments.

**Closing note, 2026-09-22 (`ENH-008`, `DEC-SCOPE-024`).** The `NEEDS_CONFIRMATION` limitation named above by title — "a parent kept at the losing school … cannot be linked by the gaining coordinator" — is resolved. `ENH-008` removed the same-school restriction on *new* `SchoolParentLink` creation entirely (`link_parent`, `_link_or_invite_parent`, `accept_invite`): a parent already linked at one or more schools can now be linked by any coordinator at any school, including one they were previously kept out of after a transfer. See `DEC-SCOPE-024` below for the full decision record. Also folded into the same wave: the parallel gap in *this* decision's own Consequences paragraph above, where an orphaned `school_parent` (zero links) could vanish from a Team page — fixed in `list_team()`/`update_team_account()` by treating Team membership as the union of "has a link here" OR "`profile.school_id` still matches," not link-only (ENH-008 final-review Finding 2).

### DEC-SCOPE-023 — School Master (`School CRM.md` Part B §2) = `school_coordinator`; account activate/deactivate confirmed in scope (`ENH-010`)

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session) — Approval date: 2026-09-22.

**Question:** Is `School CRM.md` Part B §2's "School Master" role the same actor as the
already-confirmed `school_coordinator` role (`DEC-SCOPE-011`), and is "Activate/deactivate
users" confirmed in scope for that role, scoped to their own institution?

**Evidence:** `School CRM.md` (`EVID-014`, `DERIVED_BLUEPRINT`, unattributed) Part B §2,
verbatim: *"School Master can: … Activate/deactivate users."* Per `CLAUDE.md`, this document's
own claim is not `EXPLICIT_APPROVAL` by itself.

**Current state:** `apps/api/app/api/schools.py`'s `update_team_account` (added in the initial
School-domain commit) already implements this and its own docstring asserts it was resolved
"per direct user confirmation" — but no matching entry exists anywhere in this register.
`DEC-SCOPE-011` confirms Coordinator "write access" broadly ("add/manage students… monitor
services") but never names account activation specifically. This entry does not assume which:
an undocumented earlier confirmation, or an inference never actually put to the user.

**Proposed resolution (drafted, not self-approved):** adopt "School Master" (§2) as the same
actor as `school_coordinator` (§33, `DEC-SCOPE-011`) — consistent with `DEC-SCOPE-011` already
treating that role as having broad write access over the school's accounts and data — and
confirm "Activate/deactivate users" as in-scope write access, scoped to the coordinator's own
institution, excluding the coordinator's own account and any peer Coordinator account. This
matches exactly what the shipped code (tested by `test_sch_team_account_activation.py`) already
does.

**Verified 2026-09-22:** the implementation satisfies all four ENH-010 acceptance criteria, by
both a real automated test run (9/9 in `test_sch_team_account_activation.py`, including two new
mutation-checked characterization tests) and a live browser QA pass (all four ACs individually
observed, not read from code) — see `docs/superpowers/specs/2026-09-22-enh-010-account-
activation-design.md` §6 and the ENH010-QA findings for the evidence.

**Not resolved by this entry:** whether the two frontend a11y items considered during design
(`refocus`, differentiated `role="alert"`) should ever be applied — investigated and found to
already match this component's true sibling's convention, not to deviate from it; left open,
not part of this decision either way.

### DEC-SCOPE-024 — Parent account linked to children across multiple schools (`ENH-008`)

**Question:** `docs/delivery/ENHANCEMENT_BACKLOG.md` ENH-008 (source: the user's direct instruction "parent may [have] children from different schools") had no Decision ID or Feature ID. `DEC-SCOPE-022` (`ENH-005`) independently flagged the gap this closes as `NEEDS_CONFIRMATION`, deferred to this item by name. Should a `school_parent` account be linkable to students at more than one school, and if so, what does that do to every place a parent's "own school" was assumed to be singular?

**Evidence:** Audit of the code, 2026-09-22: `SchoolParentLink` (`apps/api/app/models.py:1027-1036`) was already many-to-many (`UniqueConstraint(parent_user_id, school_student_id)`, no school-uniqueness constraint) — the block was entirely application-level: `_parent_email_conflict()` and `link_parent()` each independently rejected a parent email whose existing account's `profile.school_id` named a different school; `_own_school_id()` was called unconditionally at the top of `list_students()`/`_load_readable_student()`/`_readable_students()` and would 403 a parent the moment `profile.school_id` stopped naming "the" school; `accept_invite()` wrote `profile.school_id` on every provisioned account including parents; `school_transfers.py:_approve()` rewrote a parent's `profile.school_id` on transfer, built entirely on the single-school-per-parent assumption this feature removes.

**Resolution:** Design approved in-session, 2026-09-22 (brainstorming session; `EXPLICIT_APPROVAL`), full design in `docs/superpowers/specs/2026-09-22-enh-008-parent-multi-school-design.md`:

1. **Core decision:** `profile.school_id` is fully deprecated for the `school_parent` role — never read for authorization, never written, anywhere — unchanged for `school_coordinator`/`school_principal`/`school_teacher`. A parent's scope is derived 100% from their `SchoolParentLink` rows. No schema change, no data migration/backfill — existing stale `profile.school_id` values on parent accounts are simply never read again.
2. **Conflict checks:** `_parent_email_conflict()` and `link_parent()` drop the cross-school comparison; both reject only when an existing account under that email has a role other than `school_parent`. Error message text drops the "...at this/your school" qualifier it previously carried.
3. **Read scope:** `list_students()`, `_load_readable_student()`, `_readable_students()` skip `_own_school_id(user)` entirely for `school_parent` — `_scoped_students_query()`'s existing parent branch (built in `ENH-005`) already derives scope from `SchoolParentLink.parent_user_id == user.id` alone. Fails closed by construction: a parent mis-routed into the non-parent branch would pass `school_id=None`, which compiles to `IS NULL` against a `NOT NULL` column — zero rows, never all schools.
4. **Provisioning:** `accept_invite()` skips writing `profile.school_id` for a `school_parent` invite (every other invited role is unchanged).
5. **Transfers:** `school_transfers.py:_approve()` no longer rewrites any parent's `profile.school_id`; the per-parent `AuditLog(action=ACTION_SCOPE_CHANGED, ...)` rows this used to write are gone, replaced by folding the affected parent UUIDs into the existing `ACTION_TRANSFER` audit entry's own metadata (`parents_moved_ids`/`parents_kept_ids`) — traceability preserved, shape changed from N rows to one list field. The `parents_moved`/`parents_kept` response field names are unchanged (API contract preserved); their meaning changes to "has no other link at the losing school" / "still has ≥1 link there."
6. **Frontend:** no functional/data change required (`ENH-005` already built a per-child `school_name` and `childrenSpanSchools()`); two UI-quality improvements bundled in: group child cards by school on the parent dashboard when `childrenSpanSchools()` is true, and add `loading.tsx` skeletons to `/school/parent/dashboard` and `/school/parent/children/[id]/`.

Design, security review: `docs/superpowers/specs/2026-09-22-enh-008-parent-multi-school-design.md` (§3, §5). Implementation plan: `docs/superpowers/plans/2026-09-22-enh-008-parent-multi-school.md` (8 tasks).

**Consequences:** the same-school invariant `DEC-SCOPE-022` described for `SchoolParentLink` *creation* no longer holds — a coordinator at any school can link an existing `school_parent` account regardless of which other school(s) they're already linked at (this is `AC1`, the feature itself, not a defect); the read-side IDOR boundary (`SchoolParentLink.parent_user_id == user.id`) is untouched, since `_own_school_id()`'s parent-branch return value was already discarded downstream before this change; email-enumeration surface via `_parent_email_conflict()` widens from "within one coordinator's school" to platform-wide (bounded by requiring an authenticated `school_coordinator`, every successful link audited, no rate limiting anywhere in this codebase today — accepted, not fixed, same as every other unthrottled endpoint per `SECURITY_CONTROLS.md` §1); `DEC-SCOPE-022`'s own closing note (above) records this decision resolving its `NEEDS_CONFIRMATION` item.

**Final-review fix wave, 2026-09-22 (post-implementation, all 8 tasks already merged):** two Important findings closed in the same commit wave as this record — (1) `_school_dashboard_payload()` undercounted `parent_count` on `/school/dashboard` and `/school/reports` by filtering `school_parent` accounts via `profile.school_id` alone, the same bug class already fixed in `list_team()`; fixed with the identical link-or-stale-profile union pattern. (2) `list_team()`/`update_team_account()` treated a `school_parent`'s Team membership as link-only, so an orphaned parent (zero links — a direct `POST /school/team/invites`, which names no student, or a pre-existing account per `DEC-SCOPE-022`) silently vanished from the Team page and became unmanageable; fixed with a union (link OR matching `profile.school_id`), the option the user explicitly chose over restricting the invite endpoint.

**`NEEDS_CONFIRMATION` (not decided here):** whether `profile.school_id` should eventually be backfilled to `null`/removed from parent accounts rather than left stale-and-unread indefinitely; rate limiting on `_parent_email_conflict()`'s now-platform-wide probe surface.

**Status:** CONFIRMED_CURRENT — Approved by: user (in-session, brainstorming) — Approval date: 2026-09-22.

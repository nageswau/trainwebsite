# Conflict Matrix

Generated by `prompts/01_SOURCE_CONFLICT_AUDIT.md`, NO-ASSUMPTION MODE.

Scope: direct contradictions between two or more evidence items on the **same** topic. Additive
scope that only appears in the newer transcript (no contradiction, just new ground) is tracked
separately in `SCOPE_DELTA.md`. Missing/uncited sources are tracked in `SOURCE_GAPS.md`.

`EVID-007` (the Zora/garment-business transcript) is excluded from this audit per the user's
2026-09-01 instruction recorded in `EVIDENCE_REGISTER.md` — nothing below cites it.

No conflict below is resolved. Resolution belongs to `prompts/03_DECISION_REGISTER_AND_CLIENT_QUESTIONS.md`
and the Decision Register, not to this audit.

---

## C-01 — Admin CRM: included vs excluded

| Source | Position |
|---|---|
| EVID-002 (`Edusphere UK Website...docx`) | Admin Portal section lists `CRM` as a module directly, alongside User management, Course management, Payments, Reports, Certificates. |
| EVID-009 (`PSC Quotation _03(1).pdf`, dated 30 July 2026) | Admin Portal scope explicitly lists `CRM Module` as a line item. |
| EVID-004/005 (blueprint, "Prepared 1 August 2026") | Section 1.1 scope-reconciliation table: "CRM ... Excluded. No pipeline, counsellor queue, lead scoring, campaign automation or CRM communication history. Only admissions handoff is included." Restated in §1.4 "Explicit exclusions": "Custom CRM, sales pipeline, counsellor follow-up automation, lead scoring, campaigns and CRM communication history." The document banner itself reads "Approved scope — Custom CRM is excluded," and every page footer repeats "CRM excluded." |

Direct contradiction. Per `CLAUDE.md`, the blueprint's own use of "Approved scope" is **not**
`EXPLICIT_APPROVAL` — no separate client/user approval record for the exclusion exists anywhere in
`docs/sources/`. The blueprint cites a source for this reconciliation ("previous approved
quotation") that is not present in the evidence set — see `SOURCE_GAPS.md` G-01.

## C-02 — Employer Portal: future phase vs current scope

| Source | Position |
|---|---|
| EVID-002 | "5. Employer Portal (**Future**) — Register, Post jobs, Search student profiles, Schedule interviews." |
| EVID-009 | "5. Employer Portal (**Future Phase**) ... (Future enhancement and not included in the current quotation unless specifically requested.)" |
| EVID-004/005 (blueprint) | Employer is listed as a **current** role on the cover ("Visitor · Student · Trainer · Admin · Employer") and has a full permission bundle in §2.1 ("Own company, jobs, permission-safe candidate profiles, shortlist, interviews and placements"), plus dedicated Employer-facing reports (RPT-018 Employer Job Activity, RPT-020 Placement Outcomes) and module coverage throughout. §1.1 table: "Employer Portal ... The original requirement marks it as a future phase. → Included in the current approved Enterprise Complete scope." |

Direct contradiction, same pattern as C-01: two independent original-source documents (requirement
doc + commercial quotation) call it future/optional; the derived blueprint calls it current, citing
the same missing external source (see `SOURCE_GAPS.md` G-01).

## C-03 — Role list: 4 roles vs 5 roles (before the transcript is even considered)

| Source | Roles stated |
|---|---|
| EVID-002 | Visitor, Student, Trainer, Admin (Employer listed separately as "Future") |
| EVID-009 | Student, Trainer, Admin named in the project title; Employer named separately as "Future Phase" |
| EVID-004/005 | Visitor, Student, Trainer, Admin, **Employer** — five roles, all presented as current, with Admin further split into Content/Operations/Finance/Super bundles (§2.1) |

Consequence of C-02: because Employer's status flips from future to current between the older and
newer documents, the effective current-role count differs (4 vs 5) depending on which source is
treated as governing. Not yet reconciled by a Decision ID (`DEC-SCOPE-002` in the decision
template is still `UNCONFIRMED`).

## C-04 — Hosting/compute: AWS EC2 proposal vs ECS Fargate recommendation

| Source | Position |
|---|---|
| EVID-010 (`Revised Tech stack for the proposal(1).docx`) | "Hosting: AWS EC2 + RDS" — stated as the client's preferred stack, undated. |
| EVID-004/005 (blueprint) ADR table | "ADR-003 ... ECS Fargate recommended; ECS on EC2 ASG compatible fallback" — a recommendation, not confirmed. |

Direct architecture-recommendation contradiction on the same topic (compute hosting model).
Neither position is an `EXPLICIT_APPROVAL`; both are proposals per `CLAUDE.md`'s Technology
section. Blocked on `DEC-TECH-001` / `DEC-ARCH-001` / `DEC-INFRA-001`, all still `UNCONFIRMED` in
`docs/decisions/DECISION_REGISTER_TEMPLATE.md`.

## C-05 — Project cost and timeline figures

| Source | Cost | Timeline |
|---|---|---|
| EVID-009 (`PSC Quotation _03(1).pdf`, the only commercial-quotation **file actually present** in `docs/sources/`) | ₹1,35,000 + GST | "Estimated project duration is 2-3 weeks, subject to timely approvals, content availability, and client feedback." |
| EVID-004/005 (blueprint) §1.1 Source register, row for "Approved previous Canva quotation" | ₹2,45,000 package, 392 hours | 14-16 weeks, 60-day warranty |

These two cost/timeline figures cannot both describe the same commercial agreement — they differ
by roughly 1.8× on cost and 5-8× on duration. The blueprint attributes its figures to a source
titled "Approved previous Canva quotation," which is **not** `EVID-009` and is **not present
anywhere in `docs/sources/`** — see `SOURCE_GAPS.md` G-01. This conflict cannot be resolved from
evidence currently on hand; it requires either producing the missing source or a fresh client
confirmation of cost/timeline.

## C-06 — Internal meeting/recording platform: transcript ambiguity

Within EVID-008 itself (not between sources — an internal inconsistency in the same transcript):

- First position: *"Why can't we have our own, you know, meeting platform where I can... give the access to my students and to my faculty."* → response: *"We can do that, but how much time and what is the requirement and all those things? I will share details in 2 days... That we can do separately if you want something. **It will be a separate project** like we can do."*
- Shortly after, the same speakers discuss storing recordings in **AWS S3 within "the application"** ("we are deploying application and everything into the AWS, right? Why can't we use the AWSS3 and those things?") without re-stating the "separate project" qualifier, and ask *"Badri, have we given that we are going to provide internal thing in the application?"* — answered *"We didn't mention... yes we can make those things."*

The transcript itself does not cleanly resolve whether the internal meeting/recording platform is
in-scope for the main EduSphere application or a separate project. `CLAUDE.md` already directs
treating it as **outside** the main implementation "unless a later explicit decision moves it into
scope" (`DEC-SCOPE-007`) — recorded here as `NEEDS_CONFIRMATION` because the transcript language
itself is not fully consistent, not because the constitution's default is in doubt.

## C-07 — "Production AWS" statement scope

EVID-008 contains: *"But we are deploying application and everything into the AWS, right?"* in the
context of recording storage — a narrower, single-topic AWS statement, not a full production-vs-dev
environment discussion. `docs/evidence/VERIFIED_REVIEW_NOTES.md` §4 references a broader meeting
statement ("agreeing to AWS for production, while development could remain outside AWS/local") and
a later "ChatGPT conversation" comparing DigitalOcean with AWS Fargate. **Neither of those two
statements could be independently located in any file under `docs/sources/`** during this audit —
see `SOURCE_GAPS.md` G-02. This is flagged, not resolved: the AWS-for-production position may still
be correct, but it is not independently re-verifiable from the evidence currently on hand.

## C-08 — CRM: Zoho integration (confirmed, built) vs a new custom-built Internal CRM

Added 2026-09-12, from the newly reviewed `EVID-011` (ParakeetAI transcript) and `EVID-012` (its
vendor-authored summary PDF) — this is the third time the CRM question has surfaced in this
project's evidence, and the first time a source has contradicted an already-`CONFIRMED_CURRENT`
decision rather than pre-dating it.

| Source | Position |
|---|---|
| `DEC-SCOPE-001` (`CONFIRMED_CURRENT`, 2026-09-01, extended same day) | CRM is in scope specifically as a **Zoho CRM integration**, configured as the target of the codebase's existing generic, provider-agnostic CRM-webhook framework — not a hardcoded or custom-built CRM. Reflected in `docs/product/PRD.md` (Non-goals §3: "No custom-built CRM pipeline... the confirmed CRM requirement is a Zoho integration, not an in-house build"; confirmed modules §5: "Admin Portal + Zoho CRM"; `PRD-PUB-002`) and in shipped code (`services/portal.py` CRM-webhook framework, per `REFERENCE_IMPLEMENTATION_FINDINGS.md`). |
| `EVID-011` (transcript, same call EVID-012 claims to summarize) | The vendor states on the record, repeatedly and unambiguously, that no separate CRM is being built: *"No, we are not making [a separate CRM]... We can integrate with Zoho CRM."* When asked directly a second time — *"So now this got CRM portal, right?"* — the vendor clarifies the on-screen lead/status view is *"not the exact... CRM. Some basic configuration... we are managing here"* and that Zoho is still the plan; the client's own follow-up confirms this reading (*"So we are not making any separate CRM for this... Even for abroad education also"* → *"No, we don't have any separate CRM"*). |
| `EVID-012` (vendor-authored "Consolidated Review & Requirements" PDF, same call) | §16 states the opposite: **"EduSphere will require its own Internal CRM rather than integrating the primary lead-management workflow with Zoho CRM."** Lists a 16-item "Initial CRM Capabilities" spec (lead assignment/reassignment, workload/round-robin allocation, CRM-specific reporting, etc.) and repeats this framing in its own §24 "Confirmed Key Decisions" table and §25/§26 action items — none of which trace to anything said in `EVID-011`. |

This is a direct, same-topic contradiction between a document and the primary source it claims to
summarize, not merely two documents drifting apart over time (the C-01 pattern). Per `CLAUDE.md`,
neither EVID-012's "Confirmed Key Decisions" label nor its status as the more recent document makes
it authoritative — a derived document's self-description of "confirmed" is not `EXPLICIT_APPROVAL`,
and it does not get to silently override a `CONFIRMED_CURRENT` Decision ID that is already reflected
in BRD/PRD/feature-catalogue/shipped code. This must be put back to the client as an explicit
question (see `docs/decisions/CLIENT_QUESTIONS.md`) before any CRM-related code, RBAC, or reporting
work proceeds — building toward either interpretation without that confirmation risks a full rebuild
of the CRM layer in either direction.

**Resolved for real, 2026-09-12** — see `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-001`
(re-resolved) and `docs/decisions/CLIENT_QUESTIONS.md` Round 2, item 1: asked directly in-session,
the user confirmed `EVID-012`'s Internal-CRM position over `EVID-011`'s on-the-record Zoho-only
statement. (An earlier automated pass had incorrectly claimed this was already resolved before
anyone had actually been asked — that was a documentation error, caught and reverted, and is
distinct from this genuine resolution.) Direction only — the detailed workflow remains an open
client dependency (`docs/product/PRD_OPEN_ITEMS.md` item 61). This conflict entry is kept, not
deleted, per this file's own traceability purpose.

## C-09 — School Portal: `School CRM.md`'s login/bulk-upload model vs `DEC-SCOPE-009`

Added 2026-09-14. The user asked whether School CRM bulk student/results upload functionality was
already decided; checking `functionalities/edusphere_markdown/School CRM.md` (now registered as
`EVID-014`) against the decision trail surfaced a direct, same-topic contradiction with an
already-`CONFIRMED_CURRENT` decision — the same C-08 pattern (derived document vs. confirmed
decision), not two evidence items drifting apart pre-decision.

| Source | Position |
|---|---|
| `DEC-SCOPE-009` (`CONFIRMED_CURRENT`, approved by the user in-session 2026-09-12, resolving `EVID-011`/`EVID-012`'s unresolved School/Institution debate) | **One login per school/institution representative.** Counselors manage student data internally from the main system; the school representative gets **read-only visibility** into their own institution's students — explicitly **not individual student logins**. **"Bulk student-data upload stays deferred (manual entry first)."** |
| `EVID-014` (`School CRM.md`, undated, unattributed `DERIVED_BLUEPRINT`) | Describes a "School CRM Login Hierarchy" where the School Master **creates individual Teacher, Parent, and Student logins** (worked example: "50 Teacher Logins, 1,500 Student Logins, 1,500 Parent Logins") and lists **"Bulk upload students"** as a named School Master capability, plus a full Draft→Verified→Published academic-results module with per-student result visibility to Parent/Student logins. |

Both points of `EVID-014` are direct contradictions of `DEC-SCOPE-009`, not just additional detail:
the decision explicitly rules out individual student/parent/teacher logins for the school context,
and explicitly defers bulk upload. `EVID-014` is an unattributed, undated `DERIVED_BLUEPRINT` with
no author metadata and no `EXPLICIT_APPROVAL` marker — per `CLAUDE.md` it cannot override a
`CONFIRMED_CURRENT` decision made directly by the user.

**Student-login sub-point RESOLVED 2026-09-14** — see `DEC-ROLE-004` in
`docs/decisions/PRODUCT_DECISION_REGISTER.md`: the user directly confirmed, in-session, that no
School-affiliated or Agent-referred student gets an individual login. **Correction (2026-09-14,
same day):** `DEC-ROLE-004` resolved only the *Student* leg of `EVID-014`'s School Master →
Teacher/Parent/Student login hierarchy — it did not rule on Teacher, Parent, Coordinator, or
Principal logins, which this entry had previously (and incorrectly) described as "rejected
outright." **That broader question was resolved 2026-09-14 — see `DEC-SCOPE-011`**: full role set
adopted (Principal, School Coordinator, Teacher, Parent), superseding `DEC-SCOPE-009`'s single-login
model. **Bulk-upload half: student-roster bulk upload RESOLVED 2026-09-14** (template-download-first,
performed by the School Coordinator) — see `DEC-SCOPE-010` part 1. **Academic results bulk upload
remains PENDING** — `DEC-SCOPE-010` part 2, must still be answered before any results-upload Feature
ID, RBAC entry, or code is authored.

## C-11 — School Portal role structure: `School CRM.md` §33 (Principal/Coordinator/Teacher/Parent) vs `DEC-SCOPE-009`

Added 2026-09-14. The user asked directly whether `EVID-014`'s "SCHOOL ADMIN LOGIN" section (§33) —
four distinct school-side roles with named permissions — is covered by confirmed requirements.
Checked against the decision trail: it is not, and one part of it is a direct contradiction, not
just additional undecided detail.

| Source | Position |
|---|---|
| `DEC-SCOPE-009` (`CONFIRMED_CURRENT`, 2026-09-12) | **One** login per school/institution representative, undifferentiated by title, with **read-only** visibility into their own institution's students. No Principal/Coordinator/Teacher/Parent distinction exists in the confirmed model. |
| `EVID-014` §33 | Four separate school-side logins: **Principal** (read-only dashboard/stats/reports — roughly consistent in shape with the one confirmed login, if renamed), **School Coordinator** (write access — "add/manage students, schedule activities, track attendance, monitor services, generate reports" — directly contradicts the confirmed **read-only** constraint), **Teacher** (a school's own teaching staff getting an individual login — not mentioned in any confirmed decision at all, and must not be confused with `DEC-ROLE-002`'s "Teacher = Trainer" resolution, which is about EduSphere's own IT-training instructor role, an unrelated actor), **Parent** (a school-affiliated student's parent viewing that child's profile — net-new, raised in no evidence document before, and distinct from `DEC-ROLE-004`'s student-login question). |

The Coordinator write-access point is a direct contradiction of `DEC-SCOPE-009`'s read-only
constraint. The Teacher and Parent logins are not contradictions so much as **entirely new,
undecided scope** — no evidence document, transcript, or decision has ever raised a school-side
Teacher or Parent login before `EVID-014` (an unattributed, undated `DERIVED_BLUEPRINT`) did.

**RESOLVED 2026-09-14, asked directly, in-session** — see `DEC-SCOPE-011` in `docs/decisions/
PRODUCT_DECISION_REGISTER.md`: the user adopted the full role set, own-institution-scoped for each
role ("they can access the respective school data only"). `EVID-014`'s content is used only as the
working *description* of each role's capabilities, per the user's own explicit instruction ("based
on school crm.md") — it is not thereby elevated to `EXPLICIT_APPROVAL` status as a document; the
user's direct confirmation is what makes this `CONFIRMED_CURRENT`, not the document itself. BRD/PRD/
Feature Catalogue/RBAC propagation is follow-up work.

## C-10 — `EVID-015`–`EVID-020` (the remaining six "Functionalities" documents) vs the Internal CRM hard blocker

Added 2026-09-14, completing the audit of all seven "Functionalities" documents registered this
session (`EVID-014`–`EVID-020`). `docs/product/PRD_OPEN_ITEMS.md` item 61 already establishes a
**hard blocker**: "no Feature ID, RBAC entry, DB schema, or architecture decision for the Internal
CRM may be authored" until the client supplies the CRM process document `EVID-012` §26 itself
names as required (lead sources/stages/transitions, counselor assignment rules, ownership/
reassignment, follow-up, conversion criteria, lost/duplicate handling, notifications, dashboards,
reports, role permissions, approvals) — and explicitly warns not to infer any of it even from
`EVID-011`/`EVID-012`'s own incidental examples, which are actual transcript/vendor-summary
evidence.

| Source | Position |
|---|---|
| `PRD_OPEN_ITEMS.md` item 61 (hard blocker, citing `EVID-011`/`EVID-012`) | No Internal CRM Feature ID/RBAC/schema/architecture work until the client-supplied CRM process document arrives. Applies even to `EVID-011`/`EVID-012` themselves, which are real transcript/vendor-summary evidence. |
| `EVID-016` (`BDM Functionalities.md`) | A full lead/pipeline/appointment/outcome/follow-up CRM for College/Agent/School outreach — exactly item 61's named workflow shape, for a **"BDM" role that appears in no confirmed evidence or decision anywhere** (`ROLE_ACTION_MATRIX.md`, `BRD.md`, `PRD.md`, `MASTER_FEATURE_CATALOG.md`, `RBAC_MATRIX.md` all searched, zero matches). |
| `EVID-019` (`Telecaller Functionalities.md`) | Lead sources, qualification, follow-up reasons, round-robin/product/location lead-distribution rules, and a "Telecaller → Counselor" handover pipeline — this **is** item 61's named workflow almost verbatim, for a **"Telecaller" role that likewise appears nowhere in confirmed evidence or decisions.** |
| `EVID-015` (`Agent CRM Functionalities.md`) | Proposes staff-assignment/reassignment, lead-style student-funnel reporting, and a multi-tenant Agent Master+Staff login architecture — overlapping item 61's shape from the Agent side, and going well beyond `DEC-SCOPE-004`'s confirmed scope (self-service Agent registration + Overseas Admin approval only; no "Master account creates unlimited Staff sub-accounts" model was ever confirmed). |
| `EVID-018` (`Recruiter Functionalities.md`) | A full company-lead → requirement → candidate → interview → offer → joining pipeline (item 61 shape again, applied to recruitment), **duplicating scope already shipped under the confirmed `[base]` Placement Team and HR Team roles** (`ROLE_ACTION_MATRIX.md`: "Manage candidate pool," "Review company hiring requirements," "Coordinate interviews," "Manage job offers" — already built, per `RTM.md`/`REFERENCE_IMPLEMENTATION_FINDINGS.md`). Also introduces AI resume-parsing/skill-extraction and skill-based candidate matching — new technology scope with no decision anywhere, the same open-ended-AI-feature pattern already flagged unresolved for keyword-match auto-grading (`CLIENT_QUESTIONS.md` Round 2 item 4). |
| `EVID-020` (`University Partnership CRM.md`) | An internal "Partnership Manager" outreach/pipeline role for **prospective** universities (contact → meeting → MoU → activation) — item 61 shape again, and a **different concept from the already-confirmed `university_rep` role** (`ROLE_ACTION_MATRIX.md`: an external partner university's own portal login for applications sent to them). The two must not be conflated. Also introduces a university-pays-Edusphere commission concept, the reverse direction from the only commission decision on record (`DEC-SCOPE-005`: Edusphere pays the Agent), with zero supporting evidence. |
| `EVID-017` (`Management Functionalities.md`) | Introduces a **"Partner" login** (2 named partners) with full P&L, capital-account, investment, and salary visibility. **No evidence document anywhere (`EVID-001`–`EVID-013`) mentions a Partner role, company financials, or investment tracking as a system feature at all** — this is not a reopen of existing ground like `EVID-014`/`DEC-SCOPE-009`, it is scope with **zero evidentiary basis**, and it is also the highest-sensitivity data category proposed in any of the seven documents. |

All six sit substantially inside item 61's hard-blocker boundary, and three (`BDM`, `Telecaller`,
`Partner`) introduce roles with no supporting evidence anywhere in `docs/sources/`, not even at the
under-specified level `EVID-011`/`EVID-012` reach for Agent/School/Coordinator. Per `CLAUDE.md`,
none of this may be treated as authoritative. **Not resolved by this entry** — this is an audit
finding, not a decision. No Feature ID, RBAC entry, BRD/PRD requirement, or code should be authored
against any of `EVID-015`–`EVID-020` until each is individually put through a decision (the same
`DEC-SCOPE-010`/`DEC-ROLE-004` pattern used for `EVID-014`) or the item 61 CRM process document
arrives, whichever is relevant to the specific point.

# UX Reference Audit

Generated by `prompts/02_UX_REFERENCE_AUDIT.md`, NO-ASSUMPTION MODE. Access performed this session
(system date 2026-09-01). NO CODE was written for this audit.

Reference URL given for this run: `https://vnsitcareer.my.canva.site/` (site root). This differs
from the deep-link URL recorded in `CLAUDE.md` and `docs/evidence/UX_REFERENCE_REGISTER.md`
(`.../onecampus-erp-customer-friendly-responsive-ux`). Both were checked; see §3.

## 1. Accessibility determination

**Partially accessible.** Two independent access methods were tried:

- **Public web fetch** (`WebFetch`) on both URLs returned only an HTML `<title>`-level string each
  time, with no navigation, body content, or embedded design-ID metadata recoverable — the tool
  itself reported no rendered body/menu structure was present in what it could retrieve. This is
  consistent with a JavaScript-rendered single-page site that a non-browser fetch cannot execute.
- **Connected Canva tool** (`read-design`) *did* return real, inspectable evidence this session:
  design metadata, one PNG thumbnail per design, and page metadata — for the specific design(s)
  each URL corresponds to by title. `design_content` (extracted text layer) came back **empty**
  for every design checked, so no text-level extraction was possible, only the rendered thumbnail
  image.

This is a **material change from the prior review**: `UX_REFERENCE_REGISTER.md` and
`VERIFIED_REVIEW_NOTES.md` §9 record that an earlier pass got an empty connector result and a
failed public fetch, and concluded `UX_REFERENCE_NOT_INSPECTABLE`. This session's connector call
succeeded in returning a real screenshot, so that conclusion is **superseded for the single default
screen of each design below** — but the underlying limitation (no text layer, no navigation between
sub-screens) still holds, and full visual-parity sign-off is still blocked. See `UX_REFERENCE_GAPS.md`.

## 2. Access log

| Time-of-session action | Method | Result |
|---|---|---|
| Fetch `https://vnsitcareer.my.canva.site/onecampus-erp-customer-friendly-responsive-ux` | `WebFetch` | Title only: "OneCampus ERP — Customer-Friendly Responsive UX". No body. |
| Fetch `https://vnsitcareer.my.canva.site/` | `WebFetch` | Title only: "EduSphere UK Responsive UX Prototype - Revised and Complete". No body/nav. |
| Read design `DAHRWCa0RLw` (the "Known connected design" named in `CLAUDE.md`) | Canva `read-design`, fields: metadata/page_metadata/content/thumbnails | Metadata + 1 thumbnail (page 1 of 1) returned. `design_content` empty. |
| Search Canva for `EduSphere UK Responsive UX Prototype` to identify what the site root actually publishes | Canva `search-designs` | 24 matching/adjacent designs returned in the same connected team — see §4. |
| Read design `DAHRCNYnu6g` ("EduSphere UK Responsive UX Prototype - Revised and Complete" — title-matches the site root) | Canva `read-design`, same fields | Metadata + 1 thumbnail (page 1 of 1) returned. `design_content` empty. |

## 3. Two different designs behind two different URLs — not yet reconciled

The deep-link URL named in `CLAUDE.md` resolves (by title) to design `DAHRWCa0RLw`
("OneCampus ERP — Customer-Friendly Responsive UX"). The site **root** URL given for this run
resolves (by title, via the public fetch) to a **different** design, `DAHRCNYnu6g`
("EduSphere UK Responsive UX Prototype - Revised and Complete"). Both are hosted under the same
`vnsitcareer.my.canva.site` domain at different paths, owned by the same Canva team.

`NEEDS_CONFIRMATION`: which of these (if either alone) is the intended UX authority for
`DEC-UX-001`. Nothing here assumes an answer — both are reported below, separately, as literally
observed.

## 4. Other designs found in the same connected team (not inspected — logged for completeness)

Searching the connected Canva account for `EduSphere UK Responsive UX Prototype` surfaced 24
results in the same team, including titles this audit did **not** open or claim to describe:

- Three further same-family variants: `DAHRCUNijSo` "...Branded", `DAHRB5AzAIk` "...RBAC UX
  Prototype - No CRM", `DAHRCKDbkyU` "...V2 - Corrected Complete" — each `page_count: 1`.
- Two "AI-Native ERP" designs: `DAHRV_zGsnw` "AI-Native ERP Complete Responsive UX Prototype",
  `DAHRWIdedws` "AI-Native ERP Complete Responsive UX - All Roles and Modules" — each
  `page_count: 1`. Title suggests a broader/generic ERP product rather than an EduSphere-specific
  one; not opened.
- Roughly a dozen designs titled "Edusphere UK - Task-Level Website, Portals and CRM Quotation" (and
  copies/format variants of it), several with `page_count: 20`, `13`, or **100** — these read as
  multi-page quotation/presentation decks rather than UX screens, going by title and page count, but
  this was **not verified by opening them**.
- Four designs titled `ATM v5 UX ...` ("AI Teacher Desktop," "AI Provider Settings," "Platform
  Administration," "AI, Knowledge, Storage & Devices") — titles suggest an unrelated product, not
  EduSphere. Not opened. Flagged the same way `EVID-007` was flagged: possible off-topic material in
  the same connected account, `NEEDS_CONFIRMATION` before treating as related in any way.

None of these were opened or used as evidence. They are logged so a future pass doesn't have to
re-discover them, and so no one assumes only one EduSphere-titled design exists in this account.

## 5. Design `DAHRWCa0RLw` — "OneCampus ERP — Customer-Friendly Responsive UX"

- Owner team: `oBYTr8sQ61c57dkA6mzIIs`. Created 2026-08-04T16:59:02Z, last updated
  2026-09-01T05:16:45Z (both per Canva-reported timestamps, converted from Unix epoch — not
  filename-inferred).
- `page_count: 1`. Only one screen exists to inspect in this design; there is no second page to
  request.

**Observed (literally visible in the returned thumbnail, page 1 of 1) — nothing below is inferred:**

- Top bar: "OC" mark, product name "OneCampus ERP," subtitle "Education + Enterprise"; a search
  field placeholder "Search people, students, records, actions or reports"; a context selector
  reading "Demo Group · Main Campus"; help/notification/chat icons; a user-avatar badge "NR."
- Left sidebar: a "VIEWING AS" role selector set to "All permitted roles"; a "FIND A MODULE" search
  box; a "PLATFORM" section listing module names: "Configuration, Localization & Numbering," "Data
  Migration, Import & Reconciliation," "Global Search & Command Centre," "Integration Hub, API &
  Event Management," "Mobile/PWA, Offline Sync & Device Management."
- Breadcrumb: "Home > HRMS > Employee Master & Core HR."
- Page heading: "Employee Master & Core HR," subheading "Role-focused workspace for Employee Master
  & Core HR." Buttons: "Export," "Create new."
- Four stat tiles: "Active employees — 2,486 (+4.8% from last period)"; "New this month — 94
  (Updated just now)"; "Data changes pending — 18 (-4.8% from last period)"; "Profile completeness —
  98.7% (Updated just now)."
- "Quick actions" panel (with a "Customize" link) listing eight action cards: "View employee
  profile" (Employee · Manager), "Update personal contact information" (Employee), "Request legal
  name change" (Employee), "Request bank account change" (Employee), "Approve bank account change"
  (HR Admin · Payroll), "Update emergency contacts" (Employee), "Upload identity and employment
  documents" (Employee), "Request document correction" (Employee).
- "My work" panel, badge "1 pending," one item: "Approve bank account change — HR Admin · Due 1 day
  — Review →."

**Inferred/assessed, clearly separated from the above:**

- Layout pattern: a conventional dashboard shell — fixed top bar, left icon/module rail, breadcrumb,
  KPI-tile row, an action-grid module, a task/inbox side panel. This is a generic enterprise-admin
  dashboard layout, not specific to any education workflow.
- Content match to EduSphere scope: **none of the terms "Student," "Trainer," "Course," "Batch,"
  "Attendance," "Certificate," "Overseas," "University," "Visa," or "Agent" appear anywhere on this
  screen.** The single visible screen is generic HR/employee-records administration ("Employee
  Master & Core HR" under an "HRMS" breadcrumb), not a screen from any workflow described in
  `docs/sources/`.
- Responsive behavior: **not observable.** A single static thumbnail at one fixed width cannot show
  breakpoint behavior; no claim is made about mobile/tablet rendering.
- Component/pattern library: stat-tile cards, an action-card grid, and a task-list panel are visible
  as *this screen's* components. Whether the same components recur elsewhere in the design cannot be
  assessed — there is only one page.

**Comparison against already-documented requirements** (EVID-002, EVID-009, EVID-004/005 — no new
requirement is derived from this reference, per rule):
The one screen inspected does not correspond to any role or module described in EduSphere's own
evidence (Visitor/Student/Trainer/Admin/Employer, or the transcript's Overseas/Agent scope). If this
design is meant to represent EduSphere's UX, its single inspectable screen currently shows no
overlap with any documented EduSphere requirement.

## 6. Design `DAHRCNYnu6g` — "EduSphere UK Responsive UX Prototype - Revised and Complete"

- Owner team: same as above (`oBYTr8sQ61c57dkA6mzIIs`). Created 2026-07-30 (per Canva-reported
  `created_at`), updated the same day. `page_count: 1` — again, one Canva page to inspect.

**Observed (literally visible in the returned thumbnail, page 1 of 1):**

- Left sidebar (an in-canvas prototype-navigator UI, not a real Canva page list): header
  "EduSphere UK" / "UX prototype"; a "PREVIEW ROLE" selector set to "All"; a search box reading
  "Search 160+ screens..."; a collapsible section "Public Website" showing a count of "21" with
  visible entries: `PUB-001 Home` (highlighted/selected), `PUB-002 About EduSphere`,
  `PUB-003 Career Paths`, `PUB-004 Career Path Detail`, `PUB-005 Course Catalogue`,
  `PUB-006 Course Detail`, `PUB-007 Real Projects` (list continues below the visible crop — further
  entries are not legible in this thumbnail and are not reported). Near the bottom edge, a badge
  reading "No CRM" with the caption "Admissions handoff only."
- Main canvas, labeled "PUB-001 / Home" at the top: hero heading "Build job-ready technology
  skills," subtext "Instructor-led training, real projects, structured career pathways and placement
  support for modern technology roles," two buttons "Explore courses" / "View career paths," and a
  placeholder graphic panel reading "EduSphere UK."
- Four stat tiles: "Active learners — 1,250+ (Across live programmes)"; "Practical projects — 60+
  (Portfolio-ready)"; "Expert trainers — 25+ (Industry practitioners)"; "Learner rating — 4.8/5
  (Verified feedback)."
- Three feature cards, each with a "Learn more →" link: "Career pathways — Follow structured
  roadmaps aligned to target roles and required skills"; "Live instructor-led training — Join
  scheduled classes, access recordings and receive trainer guidance"; "Real-world project experience
  — Create demonstrable work using current tools and engineering practices."

**Inferred/assessed, clearly separated from the above:**

- Layout pattern: a marketing/public homepage — hero section with primary CTAs, a proof-point stat
  row, then a three-card feature grid. This matches the *shape* of a typical public-website landing
  page.
- Content match to EduSphere scope: **strong overlap** with the "Public Website" scope in EVID-002
  and EVID-009 (Hero Banner, Courses/Career Paths, Real-World Projects/"Real Projects" all appear as
  named nav entries here) and with the blueprint's module M01 "Public Website & SEO." The visible
  "No CRM / Admissions handoff only" badge in the navigator sidebar is also literally consistent
  with the blueprint's stated CRM-excluded/admissions-handoff-only position (`CONFLICT_MATRIX.md`
  C-01) — this is reported as an **observation of a matching label**, not as new evidence that the
  CRM-exclusion decision itself is approved.
- The sidebar's own text claims "160+ screens" exist in this prototype (a search-box placeholder,
  not a live count I independently verified) and lists at least 7 named public-site screens beyond
  the one rendered (`PUB-002`–`PUB-007`, names only). **None of those other screens' actual content
  was seen.** No detail about them is reported beyond their printed names — reporting anything more
  would be inventing screen details, which this audit will not do.
- Responsive behavior: not observable, same limitation as §5.
- Role/portal patterns: a "PREVIEW ROLE: All" selector is visible, implying the prototype supports
  switching between role-scoped views, but no other role's view was seen.

**Comparison against already-documented requirements:**
Unlike §5, this screen's content (hero, stats, "Career Pathways / Live instructor-led training /
Real-world project experience" feature cards, "Explore courses" / "View career paths" CTAs) is
directly recognizable against EVID-002's Public Website section (Hero banner, Career paths, Courses,
Real-World Projects, Success stories) and EVID-009's equivalent scope list. No new feature is being
asserted from this match — it is reported as a visual/textual correspondence with material already
in the evidence register, exactly as the prompt requires.

## 7. What this audit does NOT claim

- No claim of pixel-level or full visual parity for either design — only one static default screen
  per design was seen, out of at least 160+ named screens in `DAHRCNYnu6g`'s own sidebar alone.
- No product feature has been added to, or inferred for, any BRD/PRD/Feature scope from either
  screen. Matches noted in §6 are reported as observations against *existing* evidence only.
- No claim about responsive/mobile/tablet behavior for either design.
- No claim about which of the two designs (or the many unopened ones in §4) is the authoritative
  UX reference — that is `DEC-UX-001`, still `UNCONFIRMED`.

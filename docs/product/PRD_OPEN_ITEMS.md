# PRD Open Items

Generated by `prompts/05_GENERATE_PRD.md`. Extends `docs/product/BRD_OPEN_ITEMS.md` with PRD-level
detail. Numbering continues from the BRD list (items 1–23) where the same underlying gap is
referenced; new PRD-only items start at 24.

## Carried from BRD_OPEN_ITEMS.md (unchanged, now blocking PRD-level detail directly)

Items 1–23 all still apply — see `docs/product/BRD_OPEN_ITEMS.md` for full detail. The PRD sections
that cite each are noted there (e.g. item 4 commission rate blocks `PRD-AGT-003`/`PRD-AGT-004`).

## New PRD-level open items

| # | Open item | What's confirmed | What's missing | Blocks |
|---|---|---|---|---|
| 24 | Google login | EVID-010 states this as a client preference | Never independently re-confirmed via the Decision Register | `PRD-AUTH-001`/`PRD-AUTH-003`, PROPOSED status |
| 25 | Admin sub-bundle split (Content/Operations/Finance/Super) | Admin role itself is confirmed | The specific 4-way split is blueprint-only, never re-confirmed | `PRD-AUTH-002`, Feature Catalogue RBAC design |
| 26 | Response-time target (2 seconds) | One informal transcript mention | Never independently re-confirmed; no SLA process exists | `NFR-PERF-001` |
| 27 | Scalability target (10,000+ users) | Client's own original tech-stack document states this | Not re-asked after the architecture changed to microservices — worth re-confirming given the architecture shift | `NFR-SCALE-001` |
| 28 | Accessibility standard | Blueprint proposes WCAG 2.2 AA | No original client document mentions accessibility at all; this is a lone derived proposal | `NFR-A11Y-001` |
| 29 | Uptime/availability target | Nothing in any evidence | No source addresses this at all | `NFR-REL-001` |
| 30 | Audit-trail requirement for approval actions | Logically implied by the approval-gate decisions | Never itself asked about or confirmed | `NFR-AUDIT-001` |
| 31 | Data migration from an existing system | **Superseded 2026-09-01** — this is confirmed as extending an existing (near-empty) seeded reference implementation, not a greenfield build; its own Alembic migrations run as-is | Whether any *other* prior/legacy system's real data needs migrating in is still unconfirmed | PRD §16 |
| 32 | Employer-approval workflow before activation | Employer Portal itself is confirmed current | Whether/how an Employer account is vetted (blueprint's own open item #28, never re-confirmed) | `PRD-EMP-001` |
| 33 | Candidate-profile visible-field set for Employers | GDPR posture confirmed as "standard defaults" | Exact fields an Employer can see on a Student's profile — not enumerated anywhere | `PRD-EMP-003` |
| 34 | Enquiry→CRM sync failure handling | Sync requirement itself confirmed | What happens if the Zoho sync call fails — not specified | `PRD-PUB-002` |
| 35 | Report catalogue re-confirmation | Domestic/Employer report list carried from the now-partially-superseded blueprint | Never independently re-confirmed as still wanted, given other blueprint content was overturned | `PRD-RPT-001` |
| 36 | MVP / phasing | No smaller MVP subset exists in any evidence — full confirmed scope is the target by default | Given the 4-day target date (BRD Risk R-01), a real MVP/phasing decision is strongly indicated but has never been asked for | PRD §17 |
| 37 | Live-session join-link cadence | Trainer joins live sessions via Zoho; EVID-008 suggests a link can be set once per batch and reused, not regenerated per session | Never asked directly — whether the join link is per-session, per-batch, or per-trainer is unconfirmed | `PRD-TRN-003` |
| 38 | Assessment states beyond draft/scheduled | Draft and Scheduled states are confirmed (EVID-008) | Any further states (e.g. closed, archived, re-opened) are unspecified | `PRD-TRN-007` |
| 39 | Certificate issuance mechanism | Download capability confirmed | Auto-generated vs Admin-uploaded — EVID-008 explicitly leaves this unresolved ("I cannot confirm right now") | `PRD-STU-008`, `PRD-ADM-009` |
| 40 | Anonymous feedback option | Feedback submission itself confirmed | Whether anonymous submission is supported — blueprint-only detail | `PRD-STU-009` |
| 41 | Student CV/portfolio fields | Basic profile confirmed | Whether CV/portfolio/GitHub fields are required Student-side (they matter for Employer search, `PRD-EMP-003`) — never independently confirmed | `PRD-STU-012` |
| 42 | Runtime-configurable permissions | Role list itself confirmed | Whether permissions need an admin-configurable matrix, vs. fixed per role — never asked. **`ADM-012` built 2026-09-03 against the confirmed half only**: `GET /portal/it/admin/roles` shows the real, live per-role permission bundle straight from `core/rbac.py` (IT Admin/Super Admin only). No write/edit endpoint exists — this item stays open, not silently closed by building an unconfirmed runtime-editing engine on top of a static code table. | `PRD-ADM-013` |
| 43 | Admin-level resource/recording oversight | Trainer-side upload confirmed | A separate Admin oversight screen over the same data — never independently confirmed as needed | `PRD-ADM-008` |
| 44 | Admin-level agreement/consent oversight | Student-side agreement confirmed | A dedicated Admin oversight screen — never independently confirmed | `PRD-ADM-010` |
| 45 | Notification template management UI | Notification channels confirmed (`DEC-NOT-001`) | Whether templates are admin-editable vs hardcoded, and a delivery-monitoring UI — never confirmed | `PRD-ADM-011` |
| 46 | Operational-tooling cluster (system settings, audit logs, data import/export, backup/restore, system health, background jobs) | Nothing confirmed | Entire cluster is blueprint-only self-description; several items may belong to Architecture/Operations rather than product scope at all | `PRD-ADM-012` |
| 47 | Legal/standard pages (FAQ, Privacy Policy, Terms, Cookie Preferences, site search) | Original evidence confirms only generic "SEO Friendly Pages" | These specific pages are blueprint-only; Privacy/Terms content should be confirmed alongside GDPR (`DEC-PRIV-001`), not assumed as boilerplate | `PRD-PUB-010` |
| 48 | Webinar registration capacity | Listing + registration confirmed | Capacity/waitlist rule unspecified — same pattern as Overseas events (item 48 mirrors item covering `PRD-OVS-007`) | `PRD-PUB-009` |
| 49 | Employer placement-status states | Tracking need identified from blueprint + BRD reporting reference | Exact status states (offered/accepted/joined/declined?) never confirmed; closer to reporting than a core Employer action | `PRD-EMP-007` |
| 50 | ~~Commission auto-trigger event mapping~~ | **RESOLVED 2026-09-03 (`DATA_MODEL.md` §6.3, `ADR-012`, implemented by `AGT-003`).** The trigger fires on an `ApplicationStatusHistory` row with `to_status='enrolled'` for an application with an `agent_id` assigned — `'enrolled'` is the confirmed terminal stage name (`DEC-WF-001`), verified through the audit-trail write itself, not a raw status-field edit. | N/A — closed | Closed |
| 51 | ~~Payout-approval endpoint design~~ | **RESOLVED 2026-09-03 (implemented by `AGT-004`).** `POST /overseas-admin/commissions/{id}/approve-payout` — new API surface, requires `status == "claimed"`, writes the commission through `payout_pending` before `paid` in the same request, enforces the two-gate same-actor rule for `admin_manual`-created commissions. | N/A — closed | Closed |
| 52 | ~~Provider-set reconciliation~~ | **RESOLVED 2026-09-01: keep all providers, extend instead of stripping.** Stripe/Zoho Meeting/Email+WhatsApp remain primary/default; Razorpay, Google Meet, and SMS are retained as already-working additions; the CRM webhook stays a generic framework configured for Zoho rather than hardcoded. **Reopened and reversed for payments only, 2026-09-03: user instructed Razorpay only — Stripe removed.** Live-class/notification/CRM provider sets unaffected. | N/A — closed for live-class/notification/CRM; payments re-resolved, see `DEC-PAY-001` 2026-09-03 | Closed (non-payment scope); Razorpay-only confirmed for payments |
| 54 | Does Razorpay alone satisfy `DEC-PAY-002`'s multi-currency/multi-region model? | Razorpay's cross-border/multi-currency coverage is narrower than Stripe's; `DEC-PAY-002` was partly reasoned from Stripe's native multi-currency support | Not asked or answered — flagged when Stripe was removed 2026-09-03, not assumed either way | Open |
| 53 | Two-gate commission rule doesn't cover a later amount-adjustment | `RBAC_MATRIX.md` §2.8 confirms the two-gate same-actor rule is keyed on `AgentCommission.created_by` at *creation* time ("for system-triggered accrual, any Overseas Admin may approve since no single admin created it") | A `system_trigger`-created commission's amount can be set by any Overseas Admin via `AGT-003`'s `PATCH .../commissions/{id}`, and that same Admin is *not* blocked from then approving its own payout via `AGT-004` — the confirmed rule's literal scope doesn't reach this case, even though it is the same class of self-dealing risk the two-gate model exists to prevent. Deliberately not silently closed by inventing a stricter rule beyond what `RBAC_MATRIX.md`'s approved text states — flagged in `admin.py`'s `approve_commission_payout` and directly tested (`test_agt_004_commission_payout.py`) | `RBAC_MATRIX.md` §2.8, `AGT-004` |
| 55 | Zoho recurring live classes, auto-invite, auto-attendance | `DEC-LIVE-001` confirms Zoho as default live-class provider; user requested (this session) recurring daily/weekly scheduling, automatic student invitations, and auto-marking attendance at >50% presence | Full detail — Zoho capability questions, credential checklist, proposed Decision IDs (`DEC-LIVE-002`–`005`, `DEC-ATT-001`), data-model deltas, and a `DataTable` UI gap — lives in `docs/decisions/PENDING_ZOHO_LIVE_CLASSES.md`, not duplicated here. Blocked on Zoho OAuth/API console access | `DEC-LIVE-001`, item 37, `TRN-008`/`STU-006` |
| 56 | Dark mode / light mode toggle | Nothing — zero mentions of a theme system anywhere in `docs/` | Requested independently twice, by "ALL" roles, in tester feedback received via WhatsApp 2026-09-04 (`RAID.md` context) — a real user-facing ask with no PRD requirement, Feature ID, or Decision ID behind it | New scope — needs a Decision ID before any build |
| 57 | "Hover option" (requested generically by ALL roles; also named specifically on the Student documents page) | Nothing — no existing UX spec in `docs/ux/` defines any hover-revealed behavior | Tester feedback (2026-09-04) doesn't say what should appear on hover (row actions? a tooltip? a preview?) — unspecifiable as written until the reporter clarifies | Needs reporter clarification before it is even a specifiable requirement |
| 58 | References field + a link on the Student job-application table | No STU Feature ID owns a student-facing job browse/apply screen in `MASTER_FEATURE_CATALOG.md` — `EMP-002`/`ADM-007` cover posting/candidate-matching only; the only student-facing apply surface found is `GET /it/jobs/open` + `POST /it/jobs/{id}/apply`, with no owning Feature ID | Tester feedback (2026-09-04) asks to "add the references on the table and provide the link on the table" — whether "references" means employer-supplied reference contacts or a references-required flag is unconfirmed, and the owning screen/Feature ID itself is unclear | New scope — needs a Decision ID and a Feature ID owner before any build |
| 60 | Staff-role self-service personal-details editing (University Rep, Counselor, Agent, Trainer, HR/Placement Team, Employer) | Confirmed present for Student only (`STU-011`) | Confirmed **absent** codebase-wide for every staff role — no `profile`/`personal-details`/`account` route exists for any of them (checked directly for `university_rep`: 404, not in `PORTAL_NAV`), and no requirement in `docs/` ever asked for one. Surfaced by tester feedback (2026-09-04) naming this specifically for the University Rep, but the gap is role-general, not `UNI-001`-specific | New scope — needs a Decision ID (which roles, which fields) before any build; `RAID.md` I-12 |
| 59 | Education Agent "documents file not opening" — role ambiguity | Two candidate features have "documents" in scope: `CNS-001` (Counselor workspace, an internal role) and the `AGT-*` cluster (external Agent, no documents scope found in its own spec) | Tester feedback (2026-09-04) says "Education Agent," but the codebase's role taxonomy has no role by that exact name — blocks triage until the reporter confirms which portal/role was actually being tested | Blocks `RAID.md` triage entirely until clarified |
| 61 | Internal CRM workflow (lead sources/stages/transitions, counselor assignment rules, ownership/reassignment, follow-up, conversion criteria, lost/duplicate handling, notifications, dashboards, reports/filters/export fields, role permissions, approvals) | `DEC-SCOPE-001` re-resolved 2026-09-12, asked directly and confirmed in-session: direction is a custom-built **Internal CRM**, not a Zoho-only integration (`PRD-PUB-002`, `PRD-ADM-002`) | The entire detailed workflow — the client has not yet supplied the CRM process document that `EVID-012` §16/§26 itself names as required before this can be designed. Preliminary lead statuses seen in the demo (Contacted/Qualified/Converted/Lost) are explicitly non-final | **Hard blocker** — no Feature ID, RBAC entry, DB schema, or architecture decision for the Internal CRM may be authored until this document arrives; do not infer any of it from `EVID-011`/`EVID-012`'s incidental examples |
| 62 | New "Coordinator" role and certificate-issuance authority | `EVID-011`/`EVID-012` (12 Sep review): a Coordinator issues certificates, with Founder + Coordinator digital signatures | Whether Coordinator is a new formal role in `ROLE_ACTION_MATRIX.md`/RBAC, and whether certificate issuance moves *exclusively* to Coordinator or is *added alongside* the already-confirmed Trainer/Admin issuance path | Needs a Decision ID before `docs/architecture/RBAC_MATRIX.md` or the certificate feature can be touched — see `docs/decisions/CLIENT_QUESTIONS.md` Round 2 item 2 |
| 63 | Granular per-individual-user permission overrides (beyond role-level RBAC) | Role/page/action-level RBAC direction already `CONFIRMED_CURRENT` (`FND-002`) | Whether a further per-individual-user override layer is required this release, or role-level granularity (once page/action detail is fully built) satisfies the ask | `docs/decisions/CLIENT_QUESTIONS.md` Round 2 item 3 |
| 64 | ~~School/Institution Portal access model~~ — RESOLVED and fully propagated 2026-09-14 (`DEC-SCOPE-009` → `DEC-SCOPE-011` → BRD/PRD/Feature Catalogue → `RBAC_MATRIX.md`/`DATA_MODEL.md` → UX Screen Catalogue) | Four-role structure (`Principal`/`Coordinator`/`Teacher`/`Parent`) is `CONFIRMED_CURRENT` end-to-end through UX — 9 screens (`SCR-SCH-001`–`009`) catalogued | Full traceability chain closed for the confirmed scope. Academic results module remains `PENDING` (item 67); architecture-level open questions from the `RBAC_MATRIX.md`/`DATA_MODEL.md` pass are tracked as item 72; no code has been written | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-009`, `DEC-SCOPE-011`; `docs/architecture/RBAC_MATRIX.md` §2.12; `docs/architecture/DATA_MODEL.md` §6.11–6.14; `docs/ux/SCREEN_CATALOG.md` §SCH |
| 65 | Keyword-match auto-grading threshold/algorithm for descriptive assignment answers | Discussed informally: ~80% keyword match, order-independent, "cosine formula" named | Not yet a confirmed business rule — exact threshold, algorithm, and partial-credit behavior below threshold are unspecified | `docs/decisions/CLIENT_QUESTIONS.md` Round 2 item 4 |
| 66 | ~~Business-facing unique Student/Agent ID format~~ | **RESOLVED 2026-09-15 (`DEC-DATA-003`, `CLIENT_QUESTIONS.md` D-09).** 8-character uppercase alphanumeric (`secrets.token_hex(4).upper()`), students only (not agents) — `users.student_code` (it_student/overseas_student) and `school_students.student_code`, each its own per-table unique constraint. Migration `0028_student_code`. | N/A — closed | Closed |
| 67 | ~~School Portal bulk upload~~ — both halves RESOLVED 2026-09-14 | `DEC-SCOPE-010` `CONFIRMED_CURRENT`, both parts: student roster bulk upload (Coordinator, template-first); results/marks module in scope, uploaded by the new Academic Team role (`DEC-ROLE-006`), Draft→Verified→Published gate adopted (not verbatim re-confirmed, see item 74) | New Feature ID `SCH-006` (Academic Results) authorized — propagation into BRD/PRD/Feature Catalogue is the immediate next step | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-010`, `DEC-ROLE-006`; `docs/decisions/CLIENT_QUESTIONS.md` Round 4 item 16 |
| 68 | Student login scope: EduSphere-direct only — implementation reconciliation against already-shipped `AGT-002` | `DEC-ROLE-004` (`CONFIRMED_CURRENT`, 2026-09-14): no individual login for School-affiliated or Agent-referred students; Agent/School representative acts on the student's behalf instead — both the login-scope and on-behalf-of-actor questions are decided | **School half RESOLVED, 2026-09-14 (SCH-001 build):** `SchoolStudent` has no `student_user_id`/`User` row at all — identity fields live directly on the table, since a School-affiliated student never logs in and no login-capable identity needs representing (technical contract design, `MASTER_FEATURE_CATALOG.md` `SCH-001`). **Agent half still NOT built:** `AGT-002` still ships as a read-only roster/status view keyed to a referred student's own `User` account (`AgentStudent.student_user_id`); it needs reworking into a write-capable, on-behalf-of workflow — that half remains design/build work for a future pass | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-ROLE-004`; `docs/architecture/DATA_MODEL.md` §6.11 |
| 69 | Six more untracked "Functionalities" documents (`EVID-015`–`EVID-020`: Agent CRM, BDM, Management, Recruiter, Telecaller, University Partnership) audited against confirmed scope | Nothing — all six are unattributed, undated `DERIVED_BLUEPRINT` documents outside the evidence pipeline until registered this session | All six sit substantially inside the item 61 Internal-CRM hard blocker; `BDM`, `Telecaller`, and `Partner` are roles with **zero supporting evidence anywhere**; `EVID-018` (Recruiter) duplicates already-shipped `placement_team`/`hr_team` scope and adds undecided AI resume-parsing/matching; `EVID-020` risks conflating a new internal "Partnership Manager" role with the already-confirmed `university_rep` portal and proposes a reversed commission direction with no evidence. None of this may be used for BRD/PRD/feature work until individually decided | `docs/evidence/CONFLICT_MATRIX.md` `C-10`; `docs/evidence/EVIDENCE_REGISTER.md` `EVID-015`–`EVID-020` |
| 70 | ~~School Portal role structure reopen~~ — RESOLVED and fully propagated 2026-09-14 — Principal/Coordinator/Teacher/Parent adopted | `DEC-SCOPE-011` `CONFIRMED_CURRENT`: full role set adopted (Principal read-only, Coordinator write access, Teacher `school_teacher` assigned-students, Parent `school_parent` own-child), each scoped to their own institution only. Propagated through BRD/PRD/Feature Catalogue, `RBAC_MATRIX.md`/`DATA_MODEL.md`, and the UX Screen Catalogue | Nothing outstanding for this item — full chain closed. Genuinely new build work (not a client question) remains: Architecture questions in item 72, and actual code/Contracts-phase work, none of which has started | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-011`; `docs/architecture/RBAC_MATRIX.md` §2.12; `docs/architecture/DATA_MODEL.md` §6.12; `docs/ux/SCREEN_CATALOG.md` §SCH |
| 72 | New architecture-level open questions, surfaced while propagating `DEC-SCOPE-011` into `RBAC_MATRIX.md`/`DATA_MODEL.md` (2026-09-14) | Nothing — these are genuinely new gaps found during the architecture pass, not previously raised | (1) School role **division assignment** (`overseas`, proposed) is still not an explicitly confirmed decision — `RBAC_MATRIX.md` §1, still open. (2) `school_teacher`'s **assignment mechanism — RESOLVED as built, 2026-09-14 (SCH-001)**: the per-student FK (`SchoolStudent.assigned_teacher_user_id`), adopted as the working technical default per `DATA_MODEL.md` §6.12's own "proposed" leaning — see `MASTER_FEATURE_CATALOG.md` `SCH-001`. (3) `SchoolStudent`'s underlying schema question — **RESOLVED, 2026-09-14 (SCH-001 build)**: no `User` row at all, see item 68 | `docs/architecture/RBAC_MATRIX.md` §1; `docs/architecture/DATA_MODEL.md` §6.11–6.12 |
| 71 | ~~Which internal EduSphere role(s) manage school-affiliated student data~~ — RESOLVED 2026-09-14, then SUPERSEDED same day | `DEC-ROLE-005`'s "reuse the existing Counselor role" resolution superseded by `DEC-ROLE-006`: five specialized roles instead (Career Counselor, Psychometric Team, Academic Team, Edusphere School Manager, School Partnership Manager) — see item 75 | Career Guidance/Psychometric/Counselling are now confirmed **in scope**, all three (`DEC-ROLE-006`) — the module-existence question this item tracked is resolved | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-ROLE-005` (superseded), `DEC-ROLE-006`; `docs/decisions/CLIENT_QUESTIONS.md` Round 4 item 17 |
| 73 | ~~School partner record and role-account provisioning mechanism~~ — RESOLVED and fully propagated 2026-09-14 (`DEC-SCOPE-012` → BRD/PRD/Feature Catalogue → `RBAC_MATRIX.md`/`DATA_MODEL.md` → UX Screen Catalogue) | `DEC-SCOPE-012` `CONFIRMED_CURRENT`: Overseas Admin creates the School record and seeds a Coordinator account; Coordinator invites Principal/Teacher/Parent; no activation gate; independent of Partnership tiers | Full chain closed — `SCH-003` has RBAC grants (`RBAC_MATRIX.md` §2.12), a `SchoolAccountInvite` schema (`DATA_MODEL.md` §6.15), and 4 UX screens (`SCR-SCH-010`–`013`). Nothing outstanding for this item | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-012`; `docs/architecture/RBAC_MATRIX.md` §2.12; `docs/architecture/DATA_MODEL.md` §6.15; `docs/ux/SCREEN_CATALOG.md` §SCH |
| 74 | ~~Results approval gate (Draft→Verified→Published) — verify/publish actor~~ — RESOLVED and fully propagated 2026-09-14 | `DEC-ROLE-007` `CONFIRMED_CURRENT`: a different `academic_team` member must verify/publish than the one who uploaded — same-actor restriction, mirroring `AGT-004`'s commission-payout two-gate pattern. Gate itself (`DEC-ROLE-006`) already adopted; now fully confirmed including who performs each step | Full chain closed — `RBAC_MATRIX.md` §2.12/§4 and `DATA_MODEL.md` §6.16 both propagated. Operational note carried forward: a single-member Academic Team portfolio cannot publish its own results — needs at least two members | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-ROLE-006`, `DEC-ROLE-007`; `docs/architecture/RBAC_MATRIX.md` §2.12; `docs/architecture/DATA_MODEL.md` §6.16 |
| 75 | Edusphere School Manager vs. School Partnership Manager — exact duties and overlap | `DEC-ROLE-006` confirmed both exist as named roles, per the user's own answer; neither's duties beyond `EVID-014`'s one-line description are itemized | Does School Partnership Manager take over ongoing school-relationship management after Overseas Admin's `SCH-003` onboarding? How does it divide from Edusphere School Manager's "manage assigned schools"? No Feature ID for either exists yet — needs this answered before one is authored | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-ROLE-006` |
| 76 | ~~New architecture-level open questions, surfaced while propagating `DEC-ROLE-006`~~ — all three RESOLVED and fully propagated 2026-09-14 | (1) **Assignment mechanism — RESOLVED:** `DEC-SCOPE-013` `CONFIRMED_CURRENT`, per-school portfolio, `SchoolStaffAssignment` (many-to-many `user_id`↔`school_id`). (2) **Account provisioning — RESOLVED:** `DEC-SCOPE-014` `CONFIRMED_CURRENT`, Admin console pattern, restricted to Overseas Admin or Super Admin. (3) **Verify/publish actor — RESOLVED:** `DEC-ROLE-007` `CONFIRMED_CURRENT`, see item 74 | Full chain closed for all three — `RBAC_MATRIX.md` §2.12/§3/§4, `DATA_MODEL.md` §6.14/§6.16, `feature_catalog.json`/`MASTER_FEATURE_CATALOG.md` (no new Feature ID, `SCH-004`/`005`/`006` refined), `FEATURE_ACCEPTANCE_CRITERIA.md`, and `SCREEN_CATALOG.md` (new `SCR-SCH-021` for `DEC-SCOPE-014`'s account-creation/portfolio-assignment screen; `SCR-SCH-014`/`016`/`017`/`019` updated) all propagated | `docs/architecture/RBAC_MATRIX.md` §2.12; `docs/architecture/DATA_MODEL.md` §6.14, §6.16; `docs/ux/SCREEN_CATALOG.md` `SCR-SCH-021`; `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-013`, `DEC-SCOPE-014`, `DEC-ROLE-007` |
| 77 | Parent Portal items with no confirmed backing module — **Skills**, ~~**Portfolio**~~, ~~**Overseas education progress**~~ (`EVID-014` §23, restated by the user 2026-09-15) | `DEC-SCOPE-015` adopted the §23 list for the Parent Portal surface, but these three read from modules that were never confirmed: §9 Soft Skills / §10 Digital Skills, §14 Digital Portfolio, §15–20 Global Education. **Overseas education progress RESOLVED 2026-09-15 (`DEC-SCOPE-018`, `SCH-010`):** the School→Overseas bridge (`OverseasApplication.school_student_id`, since `SchoolStudent` still has no login/Overseas identity per `DEC-ROLE-004`) plus `SCH-007`'s new `global_education` overview section give this a real data path. **Digital Portfolio RESOLVED 2026-09-23 (`ENH-012`):** built as its own Feature ID — `portfolio_entries`/`portfolio_profiles` (migration `0035`), `GET/POST/PATCH/DELETE /school/students/{id}/portfolio*`. Sections: profile, academic achievements, psychometric report, career guidance, languages, personal statement, and 10 self-entry sections (project/internship/competition/sport/leadership/volunteering/extracurricular/award/certification/skill). Who edits: `school_coordinator`/`school_teacher` (assigned-only)/`academic_team` (own portfolio) enter the self-entry sections and personal statement **on the student's behalf** — not the student, who has no login (`DEC-ROLE-004`). Read access: `school_principal`, `school_parent` (own child(ren)), `career_counselor`, `psychometric_team`, alongside the three writers (7 roles total, spec AC-04). Skills remains unconfirmed | Is Skills (who records them — Career Counselor? a new Skills Trainer role?) in scope? Still needs its own Decision ID and Feature ID; `SCH-007` still renders no section for it | `docs/decisions/PRODUCT_DECISION_REGISTER.md` `DEC-SCOPE-015`, `DEC-SCOPE-018`; `docs/decisions/CLIENT_QUESTIONS.md` Round 5; `docs/superpowers/specs/2026-09-22-enh-012-digital-portfolio-design.md` |
| 78 | Parent notification chain steps with no confirmed trigger — ~~**Test**~~, ~~**Application**~~, **Important deadline** (`EVID-014` §23) | Assessment / Counselling / Workshop are built (`SCH-007`), and result-Published is built as §11's own "results are now available" parent notification. **Test and Application RESOLVED 2026-09-15 (`DEC-SCOPE-018`, `SCH-009`/`SCH-010`):** Test now means IELTS/SAT test-prep start/result (`SCH-009`'s `_notify_student_parents` calls), and Application means the School→Overseas bridge's own application-started notification (`SCH-010`). Important deadline depends on a deadline entity no confirmed module has | What event should "Important deadline" notify on, and from which module? | `DEC-SCOPE-015`, `DEC-SCOPE-018`; `CLIENT_QUESTIONS.md` Round 5 |
| 79 | Parent access to the psychometric **report file** itself | `GET /school/psychometric-records` and the `SCH-007` overview return assessment status only, never `report_url`, for school-side roles — kept as built; `EVID-014` §6 lists "Report" and "Parent discussion" but nothing confirms Parents open the file | Should a Parent be able to open the report? If so, is a Psychometric Team release step (like results' Published gate) wanted first? | `DEC-SCOPE-015` |

**Rule enforced throughout `PRD.md`:** no item above was treated as resolved. Every PRD requirement
that depends on one of these items is explicitly marked OPEN or PROPOSED rather than silently
written as if settled.

# Release Backlog

Generated by `prompts/11_IMPLEMENTATION_PLAN_AND_RELEASE_BACKLOG.md`, NO-ASSUMPTION MODE. Exact
Feature IDs per milestone, sourced from each feature's own `Release` field in
`docs/features/MASTER_FEATURE_CATALOG.md` / `feature_catalog.json` (GATE-05 approved) — not
re-derived or re-sequenced here.

## 0a. Addendum, 2026-09-14 — School Portal (Wave 4, 6 net-new features)

`SCH-001`–`006` (School Portal: role-based access, bulk roster upload, partner onboarding, Career
Guidance & Counselling, Psychometric Assessment, Academic Results) were added to the catalogue this
session per `DEC-SCOPE-011`/`012` and `DEC-ROLE-006`, after this document's 2026-09-01 approval.
`MASTER_FEATURE_CATALOG.md` already carries `"release": "Wave 4"` for all six and explicitly flagged
them "not yet re-baselined in `RELEASE_BACKLOG.md`" — this addendum resolves that flag by adding
**§4A** below, per each feature's own `Release` field (same sourcing rule as §0 below). Sections 1–4
and 6–7's original 2026-09-01 approved content are unchanged; this addendum only adds §4A and the
dated notes in §6/§7 that reference it. Per `CHANGE_CONTROL.md`, this is a propagation of an
already-confirmed decision, not a reopening of the 2026-09-01 approval.

## 0. Source-of-truth note (a correction surfaced in this pass)

`docs/features/DELIVERY_SEQUENCE.md` (generated during `prompts/06`) narratively groups `AUTH-001`/
`AUTH-002` into "Wave 1," but each feature's own `Release` field in the approved catalogue says
**Wave 0**. The per-feature field is what GATE-05 actually approved (it's a column in the approved
table), so it is treated as authoritative here; `DELIVERY_SEQUENCE.md`'s prose narrative has drifted
from its own source data on this one point. **Correction, not a re-decision** — flagged for a
housekeeping fix to `DELIVERY_SEQUENCE.md`, not a scope change.

## 1. Wave 0 — Foundation (6 features)

| Feature | Name |
|---|---|
| `FND-001` | Reference-implementation extension baseline |
| `FND-002` | Division-aware RBAC & identity framework |
| `AUTH-001` | Division-aware authenticated login |
| `AUTH-002` | Role-based access control enforcement (UI) |
| `OPS-001` | DigitalOcean deployment |
| `OPS-002` | Database migration execution and seed-data decision |

**Test gate:** all six features' full AC sets pass (`FEATURE_ACCEPTANCE_CRITERIA.md`); `RBAC_MATRIX.md`
§1's deny-by-default principle verified end-to-end on at least one protected route before Wave 1
starts.

## 2. Wave 1 — MVP / domestic core loop (20 features)

`PUB-001`, `PUB-002`, `PUB-003`, `STU-001`, `STU-002`, `STU-003`, `STU-004`, `STU-009`, `STU-010`,
`TRN-001`, `TRN-002`, `TRN-003`, `TRN-005`, `TRN-007`, `TRN-008`, `ADM-001`, `ADM-002`, `ADM-003`,
`ADM-005`, `PAY-001`

**Milestone M1 = Wave 0 + Wave 1 = 26 features** (the MVP slice per `MVP_FEATURES.md`'s own
recommendation — note this document's count, 26, differs from `MVP_FEATURES.md`'s originally-stated
27 because that document was written before `AUTH-001`/`AUTH-002`'s Wave-0-vs-Wave-1 placement was
reconciled per §0 above; the actual feature set is identical, only the wave label changed for two of
them).

**Delivers:** the full domestic loop — enquiry → enrolment → payment → learning → grading → admin
oversight — as a real, demonstrable, end-to-end business capability.

**Test gate:** `UAT-001` (Student Journey) and `UAT-002` (Trainer Journey), retargeted per `RTM.md`,
pass. `PAY-001`'s webhook signature verification (`SEC-022`, `INT-RAZORPAY-01`)
passes before any real payment is accepted in a shared environment.

## 3. Wave 2 — Domestic depth + Overseas/Visa core (29 features)

**Domestic depth (parallelizable with Overseas below):** `PUB-004`, `STU-005`, `STU-006`, `STU-007`,
`STU-008`, `STU-011`, `TRN-004`, `TRN-006`, `TRN-009`, `ADM-004`, `ADM-006`, `ADM-007`, `ADM-008`,
`NOT-001`, `NOT-002`, `SEC-001`, `SEC-002`

**Employer (parallelizable, fully independent once `FND-002` exists):** `EMP-001`, `EMP-002`,
`EMP-003`, `EMP-004`

**Overseas/Visa core (sequential within this stream):** `OVS-001`, `OVS-002`, `OVS-003`, `OVS-004`,
`OVS-005`, `VISA-001`, `VISA-003`, `CNS-001`

**Test gate:** RBAC resource-scope cases `CNS-SCOPE-01` (Counselor), `EMP-FIELD-01` (Employer
candidate visibility), and `SEC-GDPR-01`/`02` (GDPR self-service) all pass — these are the highest-
risk new-domain security controls (`TEST_REWRITE_PLAN.md` §4–5) and must not be deferred to Wave 3.
New "Overseas Student Journey" UAT case (`COVERAGE_GAPS.md` §4) authored and passing before Wave 2
is considered release-ready.

## 4. Wave 3 — Employer breadth, Agent, remaining depth (13 features)

`PUB-005`, `ADM-014`, `EMP-005`, `OVS-006`, `OVS-007`, `VISA-002`, `AGT-001`, `AGT-002`, `AGT-003`,
`AGT-004`, `UNI-001`, `NOT-003`, `RPT-001`

**Test gate:** `AGT-COMM-01`/`AGT-PAYOUT-01` (`TEST_REWRITE_PLAN.md` §4 — the commission two-gate
fraud control) and `SEC-AUDIT-01` (fail-closed audit) pass before any commission feature is
considered release-ready — this is the domain `SECURITY_CONTROLS.md` §7 flags as the highest fraud
surface in the entire project. New "Agent Journey" UAT case authored and passing.

## 4A. Wave 4 — School Portal (6 features, net-new domain, addendum 2026-09-14)

| Feature | Name |
|---|---|
| `SCH-003` | School partner onboarding (Admin-created, Coordinator-seeded invites) |
| `SCH-001` | School Portal role-based access (Principal/Coordinator/Teacher/Parent) |
| `SCH-002` | School Coordinator bulk student roster upload (template-download-first) |
| `SCH-004` | Career Guidance & Counselling module |
| `SCH-005` | Psychometric Assessment module |
| `SCH-006` | Academic Results module (Draft → Verified → Published) |
| `SCH-007` | Parent Portal: child 360 overview + parent notifications (added 2026-09-15, `DEC-SCOPE-015`; built same day) |
| `SCH-008` | Student Journey Timeline, narrow (added 2026-09-15, `DEC-SCOPE-016`; built same day) |

**Sequencing:** `SCH-003` → `SCH-001` → then `SCH-002`/`SCH-004`/`SCH-005`/`SCH-006` in parallel (no
cross-dependency among the four, per each feature's `deps` field in `feature_catalog.json`).
Independent of Waves 1–3 once `FND-002` exists (`DELIVERY_SEQUENCE.md`'s Wave 4 addendum).

**Milestone M4 = M3 + Wave 4 = 74 features** — the full confirmed scope as of this addendum.

**Test gate:** `SCH-001`–`006`'s full AC sets pass (`FEATURE_ACCEPTANCE_CRITERIA.md`,
`SCH-001-AC01`–`06` through `SCH-006-AC01`–`05`). Own-institution resource-scope cases (same IDOR
class as `UNI-001`/`AGT-002`) for all four School-side roles, plus `SCH-006`'s Draft→Verified→
Published visibility gate (`SCH-006-AC02`/`AC04`), must pass before Wave 4 is release-ready. Three
provisioning/scoping questions remain genuinely open and are **not** covered by any test gate yet —
`PRD_OPEN_ITEMS.md` items 74, 75, 76 (results-gate wording, Academic Team/Career Counselor/
Psychometric Team account provisioning, and the assigned-student scope mechanism) — confirm before
Contracts-phase work on `SCH-004`/`005`/`006` locks in.

## 5. Unscheduled — 10 `BLOCKED` features (not authorized to build)

`AUTH-003`, `PUB-010`, `ADM-009`, `ADM-010`, `ADM-011`, `ADM-012`, `ADM-013`, `EMP-006`, `LMS-001`,
`RPT-002` — each has a specific blocking reason in `MASTER_FEATURE_CATALOG.md` (an unconfirmed PRD
item). Resequencing any of these into a wave requires that PRD item to move from `PROPOSED` to
`CONFIRMED_CURRENT` first — a Decision Register action, not a scheduling one.

## 6. Effort/duration summary (from `IMPLEMENTATION_PLAN.md` §2)

| Milestone | Cumulative features | Cumulative effort | Illustrative duration (4-engineer team) |
|---|---|---|---|
| M0 (Wave 0) | 6 | ≈ 55 eng-days | ≈ 3 weeks |
| M1 (Wave 0+1, MVP) | 26 | ≈ 190 eng-days | ≈ 10 weeks |
| M2 (+ Wave 2) | 55 | ≈ 380 eng-days | ≈ 19 weeks |
| M3 (+ Wave 3, full scope) | 68 | ≈ 471 eng-days | ≈ 24 weeks |
| M4 (+ Wave 4, School) | 74 | ≈ 541 eng-days | ≈ 27 weeks |

None of these durations is a commitment — see `IMPLEMENTATION_PLAN.md` §2.5/§2.5a for the full
illustrative team-size scenario table and its explicit caveats.

**Addendum, 2026-09-14 (resolved same day):** the M4 row above is sourced from
`IMPLEMENTATION_PLAN.md` §2.4a/§2.5a (School raw effort ≈35 eng-days ×2.0 overlay +0 provider
buffer ≈70 eng-days on top of the 471-eng-day base, illustrative duration at a 4-engineer team).

## 7. Traceability check

68 of 68 `CURRENT` Feature IDs appear in exactly one wave above (6+20+29+13=68). All 10 `BLOCKED`
features appear in §5, none scheduled. No Feature ID appears in a wave earlier than its dependencies
resolve, per `FEATURE_DEPENDENCY_MAP.md` (spot-checked: `STU-002` depends on `STU-001`, both Wave 1;
`AGT-003` depends on `AGT-002`+`OVS-004`, `AGT-002`/`AGT-003` both Wave 3, `OVS-004` Wave 2 — resolved
before Wave 3 starts, consistent).

**Addendum, 2026-09-14** — with §4A added, 74 of 74 `CURRENT` Feature IDs (68 above + 6 School) now
appear in exactly one wave (6+20+29+13+6=74); the 68-count check above stands unchanged for Waves
0–3. School's internal ordering is dependency-consistent: `SCH-001` depends on `SCH-003` (Wave 4,
scheduled first); `SCH-002`/`SCH-004`/`SCH-005`/`SCH-006` each depend only on `SCH-001`+`FND-002`
(Wave 0), both resolved before they run.

---

**APPROVED** (user, in-session, 2026-09-01, approved as drafted), alongside `IMPLEMENTATION_PLAN.md`,
`DEPENDENCY_PLAN.md`, `RAID.md`, and `CHANGE_CONTROL.md`. GATE-09 (Coding) is now reachable.

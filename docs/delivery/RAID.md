# RAID Log (Risks, Assumptions, Issues, Dependencies)

Generated by `prompts/11_IMPLEMENTATION_PLAN_AND_RELEASE_BACKLOG.md`, NO-ASSUMPTION MODE. Carries
forward every risk in `docs/product/BRD.md` §16 (still valid — none resolved by this phase), adds
delivery-specific items surfaced while building the effort re-estimate and dependency plan.

## Risks

| ID | Risk | Severity | Source | Status after this phase |
|---|---|---|---|---|
| R-01 | Timeline infeasibility — original `DEC-DATE-001` target (2026-09-05) is not achievable for the confirmed scope under any credible team-size assumption. | Critical | `BRD.md` | **Quantified, not resolved** — `IMPLEMENTATION_PLAN.md` §2 gives the disclosed re-estimate; the user must re-confirm a target date against it. |
| R-02 | (superseded — see `BRD.md` v1.3 correction note) | — | `BRD.md` | Carried as historical only. |
| R-03 | No commercial re-quotation exists for the confirmed scope; the only quotation (`EVID-009`) predates CRM/Employer/Overseas/Agent/LMS/architecture changes. | High | `BRD.md` | Still open — `IMPLEMENTATION_PLAN.md` §2 gives an effort figure that could inform a re-quotation, but re-quoting itself is a business action outside this document's scope. |
| R-04 | (superseded — see `BRD.md` v1.3 correction note) | — | `BRD.md` | Carried as historical only. |
| R-05 | GDPR compliance risk — "standard defaults" is a product-level choice, not legal sign-off. | Medium | `BRD.md` | Still open. `SECURITY_CONTROLS.md` §9 and `SEC-GDPR-01`/`02` cover the mechanism; legal review is not something this project can self-certify. |
| R-06 | UX reference under-inspected — only 1 of 160+ screens in the confirmed design seen. | Medium | `BRD.md` | Still open — `IMPLEMENTATION_PLAN.md` §7 flags this as a UX/content blocker on final polish, not on functional build. |
| R-07 | Overseas/Agent/Visa business rules rest on one informal transcript (`EVID-008`) — no written spec for this entire domain. | Medium | `BRD.md` | Still open — directly the reason Stream C (`DEPENDENCY_PLAN.md`) carries the most named open items of any stream. |
| R-08 | Employer Portal and extended commission logic are net-new build work against the reference implementation, previously unsized. | Medium | `BRD.md` | **Resolved by this phase** — `IMPLEMENTATION_PLAN.md` §2.1's per-feature complexity tags size both explicitly (Employer module: 16 raw eng-days; Agent module: 15 raw eng-days). |
| R-09 *(new)* | The 2.0× overlay multiplier and day-per-complexity convention (`IMPLEMENTATION_PLAN.md` §2.1–2.2) are this document's own disclosed estimation assumption, not derived from any historical velocity data (none exists — from-scratch project). | Medium | This phase | Open by design — re-estimation checkpoint set after Wave 0 ships (`IMPLEMENTATION_PLAN.md` §2.5). |
| R-10 *(new)* | Employer Stream D's exact publish-workflow (`EMP-002`) may need rework if `FEATURE_QUESTIONS.md` #1/#7 are answered after Stream D has already started. | Medium | `DEPENDENCY_PLAN.md` §2 | Open — recommend resolving before Stream D's first sprint. |
| R-11 *(new)* | No team size/composition is specified anywhere in evidence — every calendar-time figure in `IMPLEMENTATION_PLAN.md` §2.5 is illustrative only. | High | This phase | Open — genuinely blocks turning the effort estimate into a real schedule until the user confirms team composition. |

## Assumptions

| ID | Assumption | Where used | Confirmation needed? |
|---|---|---|---|
| A-01 | 1 ideal engineer-day per `XS`, 2.5 per `S`, 5 per `M`, 10 per `L` complexity tag. | `IMPLEMENTATION_PLAN.md` §2.1 | This is a disclosed *convention*, not a fact requiring user confirmation — but the resulting totals should be re-checked once real velocity exists. |
| A-02 | +35% test-authoring, +25% review/overhead, +20% contingency (combined ≈2.0×) overlay on raw feature-build effort. | `IMPLEMENTATION_PLAN.md` §2.2 | Same as A-01. |
| A-03 | ~1.5–2 engineer-days per external provider for account/credential/first-integration setup. | `IMPLEMENTATION_PLAN.md` §2.3 | Same as A-01. |
| A-04 | The MVP slice (`RELEASE_BACKLOG.md` Wave 0+1) is the right first-release cut. | `IMPLEMENTATION_PLAN.md` §4, `RELEASE_BACKLOG.md` §2 | **Yes** — adopted from `MVP_FEATURES.md`'s own flagged-as-unconfirmed recommendation; still open for the user to override. |

## Issues (present-tense problems, not future risks)

| ID | Issue | Impact |
|---|---|---|
| I-01 | `DELIVERY_SEQUENCE.md`'s narrative wave grouping drifted from the approved per-feature `Release` field for `AUTH-001`/`AUTH-002` (Wave 1 in prose, Wave 0 in the approved table). | Corrected in `RELEASE_BACKLOG.md` §0; recommend a housekeeping fix to `DELIVERY_SEQUENCE.md` itself so the two documents don't keep disagreeing. |
| I-02 | `MVP_FEATURES.md` states "27 features" for its proposed MVP; the reconciled count (per I-01's correction) is 26 — same feature set, one feature's wave label changed. | Cosmetic — flagged so the discrepancy isn't mistaken for a scope change. |
| I-03 *(new, `prompts/13`, 2026-09-01)* | `stu-004-submissions.spec.ts` (STU-004) creates a real, uniquely-titled `Assignment` per E2E run against the seeded demo student's batch, and no `DELETE /workflows/it/trainer/assignments/{id}` endpoint exists yet (assignment delete is TRN-005's own future scope) to clean it up afterward. Repeated full-suite reruns without an intervening `python -m app.seed` accumulate enough of these that `STU-002`'s "Upcoming work" dashboard widget (which shows the soonest-due items, unfiltered by past/future) gets crowded out by old overdue ones, intermittently failing `stu-002-dashboard.spec.ts`'s "dashboard shows upcoming assignments" case — a test-data-hygiene gap, not a product defect (manually verified and cleaned up twice this session). | Low, operational only — reseed (`python -m app.seed`) restores a clean state; the CI-safe fix is either a trainer-assignment delete endpoint (naturally lands with TRN-005) or scoping the dashboard widget query to non-overdue items, neither in scope for STU-009. |

| I-04 *(new, `prompts/13`, 2026-09-02)* | `adm-005-enrollment-review.spec.ts`'s approve action intermittently times out (5s assertion window) only when the *full* 67-spec Playwright suite runs in parallel (6 workers) against the single shared dev `docker compose` stack -- confirmed pure request-latency under contention, not a logic bug: the same spec passes cleanly and quickly (~7s total) every time it's run in isolation or with fewer concurrent workers. Not caused by, or related to, any specific feature's code. | Low, test-infrastructure only -- doesn't affect product correctness. If it keeps recurring, raise the assertion timeout for that one action or reduce Playwright's worker count for full-suite runs against this shared dev stack; not worth chasing further as a "bug" in the feature itself. |

| I-05 *(new, `prompts/13`, 2026-09-02, found building `STU-005`; closed 2026-09-09)* | **CLOSED.** A real, reproducible bug: several client form-submission handlers read `event.currentTarget` (to call `.reset()`) *after* an `await fetch(...)` -- the DOM nulls a synthetic event's `currentTarget` once dispatch finishes, which happens as soon as the handler yields at that `await`, so the read can throw `TypeError: Cannot read properties of null (reading 'reset')`. Caught via `stu-005-support-tickets.spec.ts` failing against `WorkflowPanel.tsx`'s shared `ActionForm` (used by the STU-005 "Open support ticket" form, among many others). Fixed in that pass: `ActionForm` and `DocumentUpload` (both in `WorkflowPanel.tsx`, STU-005's own dependency path) and `WebinarRegisterForm.tsx` (that session's own `PUB-004` build). | **Fixed 2026-09-09**: applied the same mechanical fix (capture the form element into a local variable before the first `await`, use that instead of `event.currentTarget` afterward) to the three remaining instances: `EnquiryForm.tsx`, `AdminBatchCreatePanel.tsx`, `TrainerWorkflowForm.tsx`. Re-audited the rest of `apps/web/components` for the same pattern first (a broader multiline scan for `currentTarget` used after `await fetch`) -- no other live instance exists; the few extra grep hits (`AdminCertificatePanel.tsx`, `CounselorDocumentReviewPanel.tsx`) read `currentTarget` *before* their own `await`, already safe. Note: `TrainerWorkflowForm.tsx` is not imported/used anywhere in the app (dead code) -- fixed anyway since it's still a real bug in the source, but no live page or E2E spec exercises it. Verified: type-check clean, `next build`'s lint step clean, the two live components' existing E2E coverage re-run clean (`pub-002-enquiry.spec.ts` 4/4, `adm-003-batch-creation.spec.ts` 3/3) -- confirms the fix didn't change submit/reset behavior, though the original bug was an intermittent race that a synchronous test can't reliably force either way; the fix itself is the same already-proven-correct pattern used elsewhere in this codebase, not a new invented one. |

| I-06 *(new, `prompts/13`, 2026-09-02, found building `ADM-004`)* | The backend pytest suite (`apps/api/tests/conftest.py`) has **no test-database isolation** -- `db_session` opens a plain session against the same `SessionLocal`/`engine` (and thus the same Postgres database) the live `docker compose` stack and `python -m app.seed` use, with no per-test transaction rollback and no separate test database. Every test that creates a `User`/`Batch`/`Assignment`/etc. via `db_session.add(...)` + `commit()` leaves that row permanently in the shared dev database. Confirmed directly while building `ADM-004`: `GET /admin/users` (capped `.order_by(created_at.desc()).limit(500)`) returned 500 rows, none of them the long-lived seeded demo accounts -- hundreds of accounts created by this session's own pytest runs (across many prior features, not just `ADM-004`) had pushed the real seed data out of the window entirely. The same accumulated volume also measurably slowed full-suite Playwright runs late in this session (total runtime grew from ~50-57s to ~70s+), intermittently timing out specs (`trn-006`, `trn-007-grading`, on top of the already-tracked `adm-005` case) that pass instantly in isolation -- confirmed by rerunning each in isolation immediately after a full-suite failure. | Medium -- doesn't affect product correctness (every failure here reproduces as a clean pass in isolation), but actively degrades this project's own test signal and dev-database usability as the suite grows, and will keep making full-suite E2E runs noisier over time. Real fix (not attempted this session -- cross-cutting, touches all 200+ existing backend tests' assumptions): either wrap each test in `conftest.py` in a savepoint/transaction that's rolled back in a fixture teardown, or point `db_session`/the test client at a genuinely separate test database (e.g. a `_test` suffix schema, migrated fresh per test run). Until then, periodically truncate the obviously-synthetic accounts (`WHERE email LIKE '%@example.local'` matches nearly all of them, per this session's own test-factory convention) or fully recreate the `postgres` volume and reseed, if the dev database's size or `GET /admin/users`-style capped-list behavior becomes disruptive. **Addendum (found building `OVS-001`/`OVS-002`):** manual cleanup of synthetic `User` rows this way is itself blocked by FK cascades (`user_role_assignments` and others reference `users.id`, with no `ON DELETE CASCADE`) -- a plain `DELETE FROM users WHERE ...` on a test-created account fails with a `ForeignKeyViolationError` unless every referencing table is cleaned first, in dependency order. Non-`User` synthetic rows (e.g. `Country`/`University`/`Scholarship`/`OverseasApplication`) are more tractable this way since their FK graphs are shallower, but were still order-sensitive (a `Scholarship` referencing a test `Country` blocked that `Country`'s own delete until cleaned first). Full recreate-and-reseed remains the reliably simple option once this gets disruptive. **Addendum (found building `VISA-002`, 2026-09-03):** this predicted degradation has arrived for the Counselor role specifically -- the single seeded `counselor@edusphere.local` account has been assigned as `counselor_id` on a very large number of test-created `OverseasApplication` rows over the session (every spec using the `createApplicationAssignedToCounselor`-style helper pattern assigns this same seeded counselor). `services/portal.py`'s counselor dashboard/applications query (`.limit(500)`) is now large enough that panels which fan out a follow-up fetch per row (`CounselorVisaPanel.tsx`: 2 extra requests per application) can occasionally not finish resolving the newest row within a spec's assertion timeout, intermittently failing `visa-001-checklist.spec.ts`'s counselor-side assertion under contention with other tests -- confirmed passing cleanly every time in isolation, so not a regression in any feature's own logic. Not fixed this session (cross-cutting, same root cause as I-06 itself); mitigation is the same as I-06's -- reseed/recreate periodically, and be aware that any spec creating a new application assigned to the shared demo counselor may need extra time or a narrower counselor-side query going forward. **Addendum (found building `ADM-010`, 2026-09-03):** measured the actual scale directly rather than assuming -- `SELECT count(*) FROM users WHERE role='it_student'` returned **5,573 rows** in the shared dev DB. A capped list query (`.limit(500)`) ordered alphabetically (`User.full_name`) essentially never surfaces a freshly created test row at this volume, since almost the entire alphabet's worth of synthetic names sorts ahead of any one new row -- caught directly: `test_adm_010_consent_oversight.py`'s own new student was missing from a 500-row alphabetical result. Fixed by ordering `.desc()` by `created_at` instead (matching the pre-existing convention already used by this same function's `role_map` section a few lines below, which was already right) -- a newly created row is always the most recent, so it's reliably in the capped window regardless of total volume. **Any new capped admin-list query added to `services/portal.py` should default to `created_at.desc()`, never a name/alphabetical sort, for exactly this reason** -- alphabetical is the one sort order this shared DB's debris volume defeats by construction. Volume this large (5,573 for one role alone) also means a full `postgres` volume recreate + reseed is now a genuinely reasonable thing to suggest to the user if dev-environment slowness becomes noticeable, not just a theoretical mitigation. **A second, more concrete consequence found in the same pass:** `Agreement` is a division-wide singleton read by `created_at.desc()` (`GET /workflows/it/student/agreements/current`, `ADM-010`'s new consent-oversight section) -- but nothing ever deactivates a test-created `Agreement` row, and `test_stu_009_consent.py`/`test_adm_010_consent_oversight.py` (among others) each create a fresh `active=True` one per test run. The practical effect: the real seeded demo account's "current agreement" is now some arbitrary leftover test row (confirmed directly: `student.it@edusphere.local` was served an `Agreement` titled "IT Programme Agreement" / body "Terms." -- a test fixture's default values -- not the real seeded "EduSphere IT Programme Enrolment Agreement"). Harmless to test correctness (each test creates and reads its own agreement within the same test), but a real product-visible side effect of I-06 that would never occur against an isolated test database or in production. Not fixed this session -- same cross-cutting root cause as I-06 itself; an E2E spec touching this flow should assert against whatever the demo account's *actual* current agreement is, not the specific seeded title/body, until I-06's real fix (test-DB isolation) lands. |

| I-07 *(new, `prompts/13`, 2026-09-02, found stabilizing `TRN-006`'s spec while building `ADM-006`)* | A real, reproducible E2E-*test* bug (not a product defect), and a direct consequence of I-06's own accumulated debris: the generic `DataTable` component (`apps/web/components/DataTable.tsx`) paginates client-side at **10 rows per page** by default. A bare `page.locator("table")).toContainText(someUniqueTitle)` assertion only ever sees whatever page is currently rendered -- once a batch's row count for a given list (assessments, questions, whatever `DataTable` is rendering) passes 10, a freshly-created, correctly-saved row can legitimately sit on page 2+ and never appear in that locator's text, no matter how long you wait or retry (confirmed the hard way: added a 30-second `expect.poll` around the assertion and it still never resolved, because the data was never missing -- it was just off-screen). Traced by comparing direct API checks (always correct, immediately) against the same UI assertion (reliably wrong once the row count crossed 10) for `trn-006-assessments.spec.ts`. Fixed there by using the table's own "Search records" box to filter down to the run's unique title before asserting, the same "narrow to what this run actually created" principle already used for shared-seed-data isolation elsewhere this session. | Medium -- this is a latent trap in *any* E2E spec that asserts against a `DataTable`-rendered list with a bare `toContainText`/similar and doesn't first search/filter to its own unique record. It won't fail today for specs whose target list normally stays under 10 rows, but will start failing, confusingly and non-obviously (looking exactly like a timing/consistency bug, as it did here), the moment I-06's accumulated debris (or just organic real data growth in production) pushes any given list past that threshold. Not audited across every existing spec this session -- the fix is mechanical and cheap per-spec (search/filter before asserting) but doing it retroactively everywhere is its own pass, not folded into `ADM-006`. **Addendum (found building `OVS-007`, 2026-09-03):** the same client-side-pagination shape also exists in `CollectionExplorer` (`apps/web/components/CollectionExplorer.tsx`, `initialPageSize=9`), used by every public catalogue-style page (`/overseas/events`, `/overseas/scholarships`, program/university/course listings, etc.), not just the portal-side `DataTable`. Hit directly: `ovs-007-events.spec.ts` initially asserted against a seeded event card without searching first, and intermittently failed once enough other features' E2E/pytest runs had accumulated test-created `Event` rows in the shared dev DB to push the seeded card past page 1 -- same fix, use the page's own "Search ___" box first. |

| I-11 *(new, found building `PAY-001`/`STU-010`, 2026-09-03)* | The inherited Razorpay webhook handler (`api/payments.py`) matched an inbound event to a `Payment` via `entity.notes.reference` or `entity.receipt` (a `"PAY-{id}"` string this app sets as the Razorpay order's `receipt` at checkout time). Verified directly against Razorpay's own API reference while adding real test-mode coverage: the **Payment** entity (as opposed to the Order entity) has no `receipt` field at all -- only Order does -- and whether `notes` set at order-creation time cascade onto the resulting Payment entity is undocumented either way. This meant a real `payment.captured` webhook (the actual event Razorpay sends on a successful card/UPI payment) could very plausibly never match its `Payment` row, silently leaving it `pending` forever despite genuinely completing at the provider. Never caught before because no real webhook had ever been exercised against this code (blocked on missing credentials until this feature). | High for the specific defect (silently un-confirmed real payments), but fixed in the same pass, not left open: replaced the notes/receipt lookup with matching on `checkout_provider_order_id`, a value this app sets and controls itself at checkout time (present on both the Order entity directly, for `order.paid` events, and as `entity.order_id` on the Payment entity, for `payment.captured` events) -- verified against the real Razorpay TEST API rather than assumed. The old notes/receipt lookup is kept only as a fallback for a payment that didn't originate from this app's own checkout endpoint. |
| I-12 *(tester feedback via WhatsApp, 2026-09-04; reproduced and partially fixed `prompts/13`, 2026-09-05)* | University Representative reported offer-letter tracking, admission updates, and personal-details editing "not working" on `UNI-001`. Reproduced directly against the running app: **(1) Offer Letters — confirmed and fixed** — the nav item rendered the exact same unfiltered "Application Tracking" table as Applications/Admission Updates; given its own dedicated filtered view, mirroring the Student role's existing precedent (`services/portal.py`). **(2) Admission Updates — confirmed, not fixed** — same mislabeling, but no data model exists for a queryable update feed (updates are fire-and-forget notifications, not persisted/listable records); building one would be new, unconfirmed scope. **(3) Personal details — confirmed absent, not fixed** — no `profile`/`personal-details` route exists for this role (404) or any staff role codebase-wide; no requirement anywhere calls for one. Logged as `PRD_OPEN_ITEMS.md` item 60 rather than built on assumption. | (1) Closed — 2 new backend pytest cases, 1 new E2E case, all passing (`test_uni_001_university_rep_portal.py` 9/9, `uni-001-university-rep-portal.spec.ts` 5/5), plus the 4 other files sharing this code path re-run clean (26/26). (2)/(3) Remain open, deliberately not silently closed. |
| I-13 *(tester feedback via WhatsApp, 2026-09-04; reproduced and fixed `prompts/13`, 2026-09-05)* | HR Team reported no way to set/see a closing date on a job requirement (`ADM-008`). Confirmed directly: `Job.closes_on` was already collected by the "Create job requirement" form and already enforced server-side (`EMP-002-AC02`), but the Job Requirements table never displayed it back, and no "Update" action existed at all to set/change it on an existing requirement — only "Create". The backend (`PATCH /it/jobs/{id}`) already accepted `closes_on` for this role; this was a pure frontend gap. | Closed — added the `closes_on` column and a real "Update job requirement" form. 2 new backend pytest cases + 23/23 across the 3 other files sharing this code path, 1 new E2E case, confirmed live in a real browser. |
| I-14 *(tester feedback via WhatsApp, 2026-09-04; reproduced and fixed `prompts/13`, 2026-09-05)* | Administrator reported "reports not working," role ambiguous. Resolved by checking every candidate Administrator role directly against the running app rather than waiting on clarification: IT Admin's and Placement Team's own Reports pages (`RPT-001`) both rendered real data correctly. Overseas Admin's own "Reports" link showed "Access unavailable — Workspace not found" — `RPT-002` (the feature that owns this role's own report) was `NOT_STARTED` in the catalog, not a regression, but already an approved Feature ID with confirmed AC/RBAC/dependencies. | Closed — built `RPT-002` (application funnel by stage, visa-status aging, agent commission report) per its own confirmed scope. 6 new backend pytest cases + 35/35 across the 5 other files sharing this code path, 3 new E2E cases, confirmed live in a real browser. |
| I-15 *(tester feedback via WhatsApp, 2026-09-04; reproduced and fixed `prompts/13`, 2026-09-05)* | Trainer reported a table rendering outside its container/alignment on the Trainer dashboard (`TRN-001`). Reproduced directly at a 390px viewport without needing the reporter's own screenshot: not the `DataTable` (correctly self-contained via `.table-wrap{overflow:auto}`) but the shared `PortalShell.tsx` sidebar nav, which becomes a fixed bottom bar below 640px with horizontal scroll and no visible affordance — Trainer's own 9-item nav truncated mid-word ("Mate[rials]") with a hard clipped edge. Violated `RESPONSIVE_RULES.md`'s confirmed baseline ("no horizontal scroll... collapses behind a toggle"). | Closed — replaced with the existing public-site `MobileNavToggle` pattern (toggle+dropdown), generalized via optional props, defaults unchanged so the public site's own usage is unaffected (confirmed live). Shared component — fix applies to every role's portal. 3 new E2E cases; `stu-002-dashboard.spec.ts`'s 375px responsive case and `trn-001-my-batches.spec.ts` re-run clean. Pure frontend/CSS, no backend change. |
| I-17 *(new, found investigating I-15, 2026-09-05; closed 2026-09-09)* | **CLOSED.** The same `PortalShell.tsx` sidebar has a second, distinct responsive gap in the 640–980px (tablet) range: `.portal-nav a{font-size:0;padding:12px}.portal-nav a:before{content:"•"}` replaces every nav item's visible label with an identical, unlabeled bullet point — "Dashboard", "Attendance", "Materials", etc. all render as the same "•", indistinguishable from each other. Confirmed directly in `globals.css`'s `@media(max-width:980px)` block; not fixed at the time (out of `I-15`'s own narrow scope — a different breakpoint, different defect, not what the tester reported). | **Fixed 2026-09-09**: `RESPONSIVE_RULES.md`'s own tablet guidance calls for "icon+label or drawer" — rather than inventing an icon set for a nav that has never had one, extended the already-proven drawer pattern (`MobileNavToggle`, `I-15`'s own fix) up from its 640px breakpoint to the existing 980px one. Moved `.portal{display:block}`/`.sidebar{display:none}`/`.portal-mobile-menu`/`.portal-mobile-nav-panel` into the `@media(max-width:980px)` block (removed the now-dead duplicate declarations from the 640px block, since a max-width:980px rule already applies at any narrower width) and deleted the icon-only-strip rules entirely (`.portal{grid-template-columns:82px 1fr}`, `.sidebar-logo{width:54px...}`, `.role-pill{display:none}`, `.portal-nav a{font-size:0...}`, `.portal-nav a:before{content:"•"...}`, `.sidebar-footer{font-size:0}`) — the sidebar is now fully hidden behind the toggle across the whole 0–980px range rather than shown as an unlabeled icon strip from 641–980px. Verified: `next build`'s lint step clean, existing `trn-001-mobile-nav.spec.ts`/`trn-001-my-batches.spec.ts`/`stu-002-dashboard.spec.ts`'s 375px case all re-run clean (16/17 across a broader sweep — the one failure is the pre-existing, unrelated `I-03` test-data issue), 1 new Playwright E2E case added to `trn-001-mobile-nav.spec.ts` at an 800px viewport (confirms zero body-level overflow, the toggle opens a panel with all 9 real readable labels, not bullets), plus a live browser run confirming the same visually. |
| I-16 *(new, tester feedback via WhatsApp, 2026-09-04; split and triaged 2026-09-09)* | Student reported that after uploading a document, several things aren't visible/working on "the documents page": which documents were uploaded, counselor chat, meeting notification, profile picture, personal information, a hover option, and a download option. This bundled multiple distinct sub-issues into one report, originally logged against `STU-011` (IT Student). | **Split and reproduced directly against the running app, 2026-09-09, before touching any code** (per `CLAUDE.md` NO-ASSUMPTION MODE): most of these named items — "counselor chat," "meeting notification," a "documents" page as its own nav entry — don't exist for the IT Student role at all; they match the **Overseas Student** portal's own nav almost verbatim (`overseas/student`: `documents`, `counselor-chat`, `downloads`, `profile` are separate real pages there). The original `STU-011` attribution was very likely a misread of a report that was actually (at least partly) about the Overseas Student portal — flagged here, not silently corrected without saying so. Per-item findings: **(1) "download option" — CONFIRMED real bug, on `STU-011` specifically** (`ProfileDocumentUpload.tsx` rendered every uploaded document as an inert `<li>{type} -- {filename}</li>`, no link/button at all — the exact class of gap already fixed once for `STU-007`'s certificates and `OVS-005`'s documents, never carried over here). **Fixed 2026-09-09**: added `GET /workflows/it/student/profile/documents/{id}/download` (self-scoped only, per `STU-011-AC03`'s confirmed narrower scope — no admin/staff secondary actor named, unlike its two siblings) and a real "Download" button per row, same "exchange an id for a signed URL, never trust the stored value" pattern as those two. Verified: 4 new backend pytest cases (`test_stu_011_profile_documents.py`, 11/11), full backend regression 476/476 clean, 1 new Playwright E2E case (`stu-011-profile-documents.spec.ts`, 4/4) plus a live browser run confirming a working Download button per row. **(2) "which documents were uploaded" — not a real gap once (1) is fixed**: documents were always listed by name, just with nothing actionable; a real Download button resolves the practical complaint. **(3) "profile picture" — confirmed absent**, and not `STU-011`-specific: no profile-picture/photo upload exists anywhere in this codebase for any role. New, unconfirmed scope — not built (needs a Decision ID, `PRD_OPEN_ITEMS.md`-style). **(4) "personal information" — `STU-011`'s own read-only table already shows Name/Email/Phone/Education/Skills**; not a confirmed gap as literally reported, though the Overseas Student's equivalent table (5 fields: Name/Education/Preferred countries/Preferred intake/English test) notably omits Phone/Email despite the update form collecting Phone — flagged as a smaller, separate, not-yet-fixed inconsistency, not conflated with this item. **(5) "hover option" — still unspecifiable as written**: no reporter clarification on what should appear on hover; not invented. **(6)/(7) "counselor chat"/"meeting notification" — out of `STU-011`'s scope entirely** (Overseas-only concepts); the Overseas Student's own Counselor Chat page loads and shows real message history, but has no visible way to *send* a new message from that page — a real, confirmed finding, spun out as its own item (`I-19`) rather than folded into this fix; "meeting notification" remains unclear (possibly a live-session reminder, unconfirmed) and needs reporter clarification like item 57 in `PRD_OPEN_ITEMS.md`. |
| I-19 *(new, found investigating I-16, 2026-09-09; closed same day)* | **CLOSED.** The Overseas Student's "Counselor Chat" page (`services/portal.py`'s `counselor-chat` section) was read-only — it listed real message history (`Message` rows where the student is sender or recipient) but had no compose form wired to it at all. The Counselor role had no way to see or reply either — `PORTAL_NAV` never listed this section for that role at all, so a one-sided send would have gone into a black hole. `ROLE_ACTION_MATRIX.md`'s own confirmed base action for the Student is "counselor-chat (**direct messaging** with their assigned counselor)" — a one-sided implementation would not have satisfied that already-confirmed action, so both sides were completed as one fix, not scope creep. | **Fixed 2026-09-09.** Student side: added `POST /workflows/overseas/student/counselor-chat`, which resolves the student's assigned counselor server-side from their own `OverseasApplication.counselor_id` (never a client-supplied recipient id) and sends; wired to a plain single-field generic `ActionSpec` form (no picker needed — exactly one implicit recipient). Counselor side: added a `counselor-chat` nav entry (previously absent) and portal section (reuses the exact same generic message query the Student side's own section already used — zero duplication), plus a new `CounselorChatPanel.tsx` with a real assigned-student picker (added `student_id` to the existing `applications` payload row, additive) that fetches the thread and replies — both via the **already-existing, unchanged** generic `GET`/`POST /communications/messages...` endpoints, which needed no backend change at all. Verified: 8 new backend pytest cases (`test_i19_counselor_chat.py`), full backend regression 484/484 clean, 1 new Playwright E2E case (`i19-counselor-chat.spec.ts`, 2/2) plus a live browser run confirming the full round trip — student sends, counselor picks the student and sees/replies, student sees the reply. |
| I-20 *(found verifying Zoho on request, 2026-09-09; closed same day)* | **CLOSED — real bug found and fixed while verifying, not just a status check.** Real Zoho Meeting credentials (`ZOHO_CLIENT_ID`/`SECRET`/`REFRESH_TOKEN`/`ORGANIZATION_ID`/`PRESENTER_ID`) had landed in `.env` at some point (`.env.example` is still blank) — the same way real Razorpay credentials unblocked `STU-010`/`PAY-001` — but nobody had ever verified they actually worked: `test_provider_services.py` tests Google Meet's and manual-link's error paths but had zero Zoho coverage at all, not even a negative test. Verified directly against the real API before assuming anything worked: `_zoho_access_token()` OAuth refresh succeeded (a real token from `accounts.zoho.in`), but the actual session-creation call to `ZOHO_MEETING_BASE_URL` (`.env`'s value: `https://meeting.zoho.com`) failed with a real `401 INVALID_OAUTHTOKEN` — confirmed via the raw response body, not assumed from the wrapped exception. Tested the hypothesis directly: the same token against `https://meeting.zoho.in` returned `200` with a genuine created session (`"webServer":"meeting.zoho.in"`) — a data-center region mismatch, exactly the risk `PENDING_ZOHO_LIVE_CLASSES.md` §3.A had already flagged as unconfirmed ("a wrong region silently fails auth"). | **Fixed 2026-09-09**: corrected `ZOHO_MEETING_BASE_URL` in `.env` to `https://meeting.zoho.in`, matching this account's actual OAuth-issuing data center. Re-verified through the real application code path (`create_provider_meeting("zoho_meeting", ...)`, the exact function `POST /communications/it/live-sessions` calls) — real join/host URLs and a real `provider_meeting_id` returned. Verified: 3 new backend pytest cases against the real live API (`test_zoho_meeting_integration.py`, no mocking, same treatment `STU-010` gave real Razorpay), full backend regression 487/487 clean, plus a live browser run scheduling a real Zoho session through the actual "Schedule live session" UI. **Scope note**: this verifies only the base one-off Zoho Meeting provider (already-built live-class scheduling, `DEC-LIVE-001`) — the separate, much larger recurring/auto-invite/auto-attendance initiative in `PENDING_ZOHO_LIVE_CLASSES.md` remains fully unbuilt and still blocked on its own 11 unanswered capability questions; landing real credentials doesn't resolve those. |
| I-21 *(user request, screenshot-driven, 2026-09-09; a/b/c/e closed same day)* | The raw "Join" URL on the trainer's own generic "Live Sessions" table (`DataTable`, `PENDING_ZOHO_LIVE_CLASSES.md` §6's own already-identified gap) rendered as inert text, not a button — no time-window gating, no identity confirmation, matching the screenshot the user shared. `LiveClassesPanel.tsx`'s own "Join class" was already a clickable `<a>`, but also had no gating of any kind. Bundled user request, split per `§1b`: (a) button not link, (b) "too early" pop-up >15 min before start, (c) "completed" pop-up after end time, (d) per-session Zoho account override, (e) confirm joining identity, (f) mute-all/no-speak defaults. | **(a)/(b)/(c)/(e) fixed 2026-09-09.** New shared `JoinSessionButton.tsx` (15-minute early window sourced from `EVID-008`, not a new number) used by both `LiveClassesPanel.tsx` and, per `§6`'s own plan, a new server-declared `type: "join"` `DataTable` column kind (`_payload()` now accepts an optional 3rd column-tuple element, a plain string, never a function). Identity confirmation ("Join as {name} ({email})?") applies only to a joining participant, never the host — a trainer setting up early isn't asked to confirm their own identity. Also fixed one adjacent inconsistency found while touching the trainer's row: the "meeting" cell now prefers `host_url` over the plain `meeting_url`, matching `LiveClassesPanel`'s own precedent. Honest limitation, unchanged from `§2` item 11: this only gates EduSphere's own button, not the raw provider URL. Verified: 4 new backend pytest cases (`test_join_session_button.py`), full backend regression 491/491 clean, 3 new Playwright E2E cases (`join-session-button.spec.ts`) covering all three pop-up/confirm paths against controlled fixture sessions, plus a live browser run matching the user's own screenshot. Along the way, fixed `trn-003-upcoming-sessions.spec.ts` to create its own throwaway session instead of depending on the shared seeded one, which had by now actually drifted into the past exactly as `RAID.md` I-10 predicted. **(f) closed 2026-09-09 — cannot be built.** Checked directly against Zoho's own public API docs (`PENDING_ZOHO_LIVE_CLASSES.md` §2 item 12, sources cited there): the Meeting API has no mute/audio/participant-permission field at session-creation time anywhere in its documented parameters or attribute schema — "mute all"/"control who can unmute" exist only as a live, in-meeting moderator action inside Zoho's own client, never a schedulable property. Not a temporary blocker; this is Zoho's real feature boundary. **(d) deliberately moved to pending, at the user's own request 2026-09-09** — real new OAuth-connect infrastructure (new table, authorize/callback endpoints, encrypted token storage), explicitly deferred rather than picked up next; see `PENDING_ZOHO_LIVE_CLASSES.md` §1b and `pending.md` for tracking. |
| I-22 *(research pass on request, 2026-09-09)* | User asked to continue resolving `PENDING_ZOHO_LIVE_CLASSES.md` §2's 11 open Zoho capability questions (beyond item 12, already closed via `I-21`). Research task, not a bug — logged here to keep the full Zoho investigation trail in one place rather than splitting it across files. | **6 of 11 answered 2026-09-09** (items 1, 2, 6, 7, 10 — plus item 12 from `I-21` — see `PENDING_ZOHO_LIVE_CLASSES.md` §2 for full detail and sources on each): (1) no native recurrence support, confirming `DEC-LIVE-002`'s own assumption that EduSphere must expand recurrence itself; (2) **Zoho's Participant Report API does expose real per-participant join/leave/duration data** — `DEC-ATT-001`'s auto-attendance rule is technically feasible; (3) still unconfirmed, but the *absence* of any public webhook documentation (unlike Zoho CRM/Desk, which do have some) plus (2)'s pull-style report shape both point toward **polling, not webhooks**, matching `§7`'s own already-proposed design; (6) Zoho auto-emails invitees at session creation (with a documented toggle to suppress it) — EduSphere's own existing `_notify_user` call on the same event is a second, separate notification on top of that, not a replacement for it; (7) the codebase's hardcoded `"Asia/Calcutta"` timezone value matches Zoho's own documented example exactly; (10) checked directly against the live token endpoint (not docs) — the OAuth response has no presenter/user-ID field, confirming a per-trainer connect flow (`§1b` item d) will need a separate, not-yet-identified call to resolve that. **4 of 11 remain genuinely unanswerable from public docs** (items 4, 5, 8, 9 — concurrent-session limits, shared-vs-per-trainer seats, rate limits, and the account's actual plan/tier) and need either a real support conversation or someone checking the account's own subscription page directly. |
| I-23 *(user-spotted, screenshot-driven, 2026-09-09; closed same day)* | **CLOSED.** User asked "when I click Pay Now it should go to Razorpay right?" with a screenshot showing the static message *"Checkout session created. Continue with the configured payment provider."* after clicking. Confirmed directly (no changes made for the question itself, per the user's own instruction, before fixing): `FeePaymentPanel.tsx`'s `payNow()` called the real checkout endpoint — which genuinely creates a real Razorpay order (`key_id`, `provider_order_id`, `amount`, `currency`, confirmed already-working per `PAY-001`/`STU-010`) — and then **discarded that response entirely**, showing only the static message. Nothing ever opened Razorpay's actual checkout; a student had no way to complete a payment through this UI at all. | **Fixed 2026-09-09**: added Razorpay's official Checkout.js widget (`next/script`, matching this codebase's own existing third-party-script convention from `Analytics.tsx`, plus an on-demand fallback load in case the tag hasn't finished by click time) and wired the already-returned `key_id`/`provider_order_id`/`amount`/`currency` into it — clicking "Pay Now" for a `razorpay` checkout now opens the real widget (confirmed live: real "Test Mode" ribbon, real UPI/Cards/EMI options, real ₹ amount). The static message is kept only for the `manual`/non-Razorpay path, where there's genuinely no client-side widget to open. Verified: `next build`/type-check clean, the existing `pay-001-stu-010-fee-payments.spec.ts` E2E spec updated to assert the real Razorpay iframe actually opens (checked via `iframe[src*='razorpay.com']`, the strongest available signal short of driving Razorpay's own hosted UI, which would be testing their frontend, not ours) — 3/3 passing, plus the two other tests in that file re-run clean. No backend change was needed; the real order/key were already being returned, just never used. |
| I-24 *(user-spotted, screenshot-driven, 2026-09-09; closed same day)* | **CLOSED.** User reported "not able to click on sub menu" on the public site's "Programs" header dropdown, with a screenshot of the closed nav. A synthetic Playwright `.click()` on the trigger and then the child link did *not* reproduce anything wrong (`data.status === 200`, navigation succeeded) — only a real, gradual mouse movement from the trigger down toward a submenu link reproduced it: the panel closed before the cursor arrived. Root cause confirmed directly: `.nav-dropdown-panel` had `margin-top:16px` below the trigger button. A CSS margin sits outside an element's own box, so moving the real cursor through that empty 16px strip left `.nav-dropdown`'s hoverable area entirely (mid-gap, over page background, not over any element), firing the wrapper's `onMouseLeave` and calling `setOpenIndex(null)` before the mouse ever reached a link inside the panel. | **Fixed 2026-09-09**: moved the 16px spacing from `margin-top` (outside the box, not hoverable) to `padding-top` on `.nav-dropdown-panel` itself (now the actual hover-detected element, extending flush from the trigger through the old gap), with the visible white card (background/border/shadow) moved to a new inner `.nav-dropdown-panel-inner` div so the appearance is unchanged. Verified: type-check/build clean, a real gradual-mouse-movement reproduction (`page.mouse.move` in 20 small steps, not Playwright's own `.click()` which teleports and never caught this) confirmed the panel now stays open and a real click-through navigates correctly; visually confirmed identical spacing via screenshot. 2 new Playwright E2E cases (`desktop-nav-dropdown.spec.ts`) lock in both the real-mouse-trajectory case and the existing click-to-open behavior; `pub-001-content.spec.ts`'s own dropdown-touching test re-run clean (one unrelated test in that file hit the already-documented Next.js fetch-cache warm-up flake on the first post-rebuild run, cleared immediately on rerun). |
| I-18 *(from `pending.md`'s "built, then reverted" note, 2026-09-03; all three sub-items fixed 2026-09-09)* | **CLOSED.** Trainer assessment grading (`/it/trainer/assessments`) had three confirmed bugs, previously built and then explicitly reverted at the user's own request alongside an unrelated design discussion: **(1) blind grading** — the "Grade written attempt" card showed only a bare total-score input, never what the student actually wrote/uploaded; **(2) file-response questions unanswerable** — no real file `<input>` exists for `question_type: "file"` on the student attempt form (`AssessmentForm` in `WorkflowPanel.tsx` falls back to a plain `<textarea>` for any question with no `options`); **(3) no lock against adding a question after a student has already started an attempt**. | **(1) Fixed 2026-09-09**: added `GET /workflows/it/trainer/assessment-attempts/{id}` (reuses the `AssessmentAnswer` rows and `answer_scores` grading path that already existed server-side) and rebuilt "Grade written attempt" as a per-question review pane (`.qa-item`/`.answer-text`, the same pattern `TRN-007`'s "Grade submission" card already used) with per-question scoring; activated the previously-unused `.status`/`.status.pending` badge classes already sitting dormant in `globals.css` instead of inventing new colors. Verified: 5 new backend pytest cases (`test_grade_assessment_attempts.py`, 10/10 with `test_trn_006_assessments.py`), full backend regression 467/467 clean, 1 new Playwright E2E case (`grade-assessment-attempts.spec.ts`, 2/2) plus a live browser run confirming the review pane renders the student's real submitted answer before scoring. **(2) Fixed 2026-09-09**: `AssessmentForm` now renders a real `<input type="file">` for `question_type: "file"` (was falling through to the same plain `<textarea>` as a `text` question); on submit, the file uploads through the existing `/files/local-upload`→`/files/presign` fallback flow (a new shared `uploadFile()` helper mirroring `DocumentUpload`'s own inline copy) and the returned URL becomes the answer value — no backend change needed, `AssessmentAnswerIn.value` already accepted a plain string. The (1)-built review pane's existing file-link branch (`question.question_type === "file"`) now has real data to render against for the first time. Verified: 1 new backend pytest case (`test_file_response_question_answer_is_a_real_uploaded_file_url`, 6/6 in `test_grade_assessment_attempts.py`), full backend regression 468/468 clean, 1 new Playwright E2E case (`file-response-assessment-question.spec.ts`, 1/1) plus a live browser run confirming a real file input renders, a real upload succeeds, and the trainer's grading pane shows a working "Open submitted file" link to the actually-uploaded object. **(3) Fixed 2026-09-09**: added `_assert_no_attempts_yet()`, called from both `POST .../questions` and `POST .../questions/bulk` — once any `AssessmentAttempt` row exists for an assessment (in_progress, submitted, or graded), further question creation is rejected `409`. `GET /workflows/it/trainer/assessments` now also reports a real `attempt_count` per assessment so the "Add question" picker can disable a locked assessment's `<option>` client-side (defense in depth — the server-side check is authoritative regardless). Verified: 4 new backend pytest cases (`test_assessment_question_lock.py`, 15/15 with the other two assessment test files), full backend regression 472/472 clean, 1 new Playwright E2E case (`assessment-question-lock.spec.ts`, 1/1) plus a live browser run confirming the option disables and an inline note appears once a student has started. |
| I-25 *(user-spotted, screenshot-driven, 2026-09-09; closed same day)* | **CLOSED.** User shared a screenshot of the student "Book a slot" picker showing many near-identical duplicate batch cards, and asked why, plus whether one booking should cover the whole recurring batch (already true — `create_enrollment` already 409s on a repeat `student_id`+`batch_id`, confirmed by reading the code, no fix needed) and whether multi-course registration is allowed (already true, no restriction beyond the same-batch check). The duplicate cards themselves were real: `test_rpt_001_reporting.py` and `test_trn_002_batch_roster.py` each uuid-suffixed only `slug` (the machine key), not `title`/`name` (the human-readable fields actually rendered on this page) — the exact same gap already documented once for `AGT-002`'s fixtures (`I-09`) and `ADM-004`'s general test-DB-isolation issue (`I-06`), just never carried over to these two files. Every run left a real, permanently-visible, near-identical "RPT-001 Test Program / Batch A"-style batch in the shared dev DB. Separately, `GET /workflows/it/batches/available` (`services/workflows.py`) never excluded a batch whose own `end_date` had already passed, so old test batches kept showing as "available" long after they could mean anything. Measuring the actual scale directly (not assuming) found **5,313 duplicate/debris batch rows** in the shared dev DB — the accumulated result of `I-06`'s already-known lack of test-DB isolation, now visible on a page a real student uses, not just an admin list. | **Fixed 2026-09-09.** Uniquified every human-readable fixture field (`title`, `name`), not just `slug`, in both files — same fix pattern as `I-09`. Added an `end_date >= date.today()` filter to `available_batches` (`Batch.end_date` is non-nullable, so this is always safe) — 2 new backend pytest cases (`test_available_batches_end_date_filter.py`). These two fixes only stop *future* growth; they don't retroactively clean up existing debris, since almost all of it uses a computed `end_date = today + 90 days` that won't naturally expire for months. Given the scale (5,313 rows) and that this was now product-visible to a real student, the user chose to recreate the `postgres_data` volume and reseed fresh (`I-06`/`I-10`'s own already-suggested mitigation) rather than write a narrow one-off cleanup script — the user ran the volume removal directly; the stack was then brought back up (`docker compose up -d`) and reseeded (`python -m app.seed`). Verified directly against the fresh database: `SELECT count(*) FROM batches` returned **1** (the single real seeded batch, zero duplicates by name), and a live browser login as the seeded student confirmed the "Choose a trainer/time-slot" panel now shows exactly one clean "Python Full Stack" card. **Addendum, same day — the broader sweep closed too, at the user's own explicit request.** The same "only `slug`/a machine key was uuid-suffixed, not every human-readable identifying field" gap was found in 26 other test files (not the ~24 first estimated) — every `Program.title`, `Batch.name`, `Agreement.title`, `Job.title`, `LiveSession.title`, `Assessment.title`, and `Assignment.title` literal that was still a bare hardcoded string across the suite. Fixed all 26: each now appends its own `uuid.uuid4().hex[:6]` suffix, the same mechanical pattern as the two files fixed above. Three files needed more than a one-line swap because an assertion checked the literal string by exact equality rather than by id: `test_emp_003_candidate_search.py` (assertion changed to `.startswith(...)`), and `test_trn_006_assessments.py` (draft/scheduled assessment titles captured into local variables, reused in both the fixture and the assertion, rather than repeating the literal). Verified: all 26 files parse cleanly (`ast.parse`), full backend regression re-run **493/493 clean** (same count as before the sweep — confirms nothing was broken by the change). |
| I-26 *(user-requested, screenshot/question-driven, 2026-09-09; closed same day)* | Following on from `I-25`: the user asked (1) "what are the different ways to solve" the scrolling problem, then approved implementing "1+2" (exclude a batch the caller already booked, from `available_batches`; swap the card grid for a compact searchable table), then separately asked to "proceed with 3 and 4" (group by program, collapsed by default; add a program filter). None of these four existed before. | **All four fixed 2026-09-09.** (1) `available_batches` (`workflows.py`) now also excludes any batch the caller already has an `Enrollment` row for (any status) — matches the same rule `create_enrollment`'s existing 409 already enforces, so "Book this slot" is never shown, let alone clickable, for a batch already booked; 2 new backend pytest cases (`test_stu_001_enrollment.py`, one excluding the booker's own view, one confirming a different student still sees the same batch). (2) `BatchSlotPicker.tsx` now renders a compact `.table`/`.table-wrap` (the same visual language as the "Course Enrolled" table directly above it on the same page) instead of a card grid, with a "Search records" box; the booking-error message was also moved to render directly below the intro text instead of after the whole list, so a 409 is visible without scrolling regardless of list length — the original complaint ("no error/pop-up on a duplicate click") was this placement, not missing error-handling. (3) Batches are grouped into a native `<details>`/`<summary>` per program, collapsed by default, auto-expanded only when exactly one group remains (nothing left to collapse into). (4) A "Program" `<select>` filter narrows the whole view to one program. Verified: `docker compose build web` (type-check/lint) clean, targeted backend tests 15/15, a live browser run confirming the seeded student's real batch renders correctly, and — separately, while doing that live check — confirmed the shared dev DB had regrown **239 batches/279 programs/159 enrollments** of fresh test debris (same `I-06` root cause, regenerated by this session's own full-regression reruns) — user informed, cleanup offered but not yet actioned (their call). |
| I-27 *(user-requested, 2026-09-09; closed same day)* | User asked for "a fixed template for certificate, generate certificate dynamically after course completion, and a button," inviting `/frontend-design` to "choose best template." Investigated before assuming any of this was missing: **all three already existed** — `services/certificates.py`'s `generate_certificate_pdf()` already draws a real PDF dynamically at issuance time (`POST /it/certificates/{enrollment_id}/issue`, gated on completion criteria), and `CertificateDownloadPanel.tsx` already has a real "Download certificate" button wired to a signed short-lived URL (`STU-007`). The one real, confirmed gap found while reviewing the template: `Certificate.verification_code` has always had a real, working public lookup (`GET /public/certificates/{verification_code}`) but was never printed anywhere on the certificate PDF itself — nothing for a human holding a printed/PDF copy to actually type in to verify it. | **Fixed 2026-09-09** — redesigned the template (previous version: plain blue-bordered rectangle, generic corporate-blue Helvetica headline, no distinctive signature element) grounded in an IT-training academy's own vernacular rather than a generic laurel-wreath diploma look: an ink-navy left gutter with the wordmark set vertically (an editor's line-number-gutter reference), Times-Bold for the headline/name (with a signal-green underline rule under the name), and a Courier-set "terminal" verification block in the footer — `verify {certificate_no}` / `code: {verification_code}` inside a bordered box with a small green status dot — which is also the fix for the printed-but-unusable verification code. Iterated once on the layout after rendering a real preview: the first draft left a large unexplained gap between the program title and the footer; added a thin section rule + one caption line ("Authenticity can be confirmed with the verification code below.") to give that space a purpose rather than trimming it away. `generate_certificate_pdf()`'s signature gained a new required `verification_code` param; its one call site (`issue_certificate`) updated to pass the value it already had. Verified: 19 targeted backend pytest cases clean (`test_adm_006_certificates.py`, `test_stu_007_certificate_download.py`, `test_stu_001_enrollment.py`), a real certificate generated and rendered to PNG (via PyMuPDF) for direct visual review before and after the layout fix, preview files deleted from storage afterward. |
| I-28 *(user question + request + screenshot, 2026-09-09; closed same day)* | User asked "where does the fee get updated" after enrolling. Traced directly before assuming: it didn't -- `create_enrollment` never read `Program.fees`, and the "Fees" page (`GET /portal/it/student/fees`) is a plain read of the `Payment` table, so nothing appeared there until an Admin separately remembered to call `POST /admin/payments` by hand. User then asked to auto-create it, plus let an Admin apply a discount. Separately, a screenshot showed a real, reproducible bug: after a real Razorpay test payment succeeded in the browser, "Pay Now" stayed visible with no receipt option and only the generic "Payment submitted. It will reflect here once confirmed." message, forever. Root cause: `FeePaymentPanel.tsx`'s Razorpay `handler` callback only called `load()` (a plain refetch) -- the only thing that ever marks a `Payment` "paid" was the webhook, and Razorpay's servers can never reach a local dev `localhost` at all, so the success path could never actually complete in this environment. | **All three fixed 2026-09-09.** (1) `create_enrollment` now auto-creates a `pending` `Payment` for the program's own listed `fees` (skipped only if the program is genuinely free) and its invoice, the moment a seat is booked -- verified live: enrolling a fresh student in "Python Full Stack" (₹65,000) produced a real, immediately-visible "Pay Now" row on the Fees page. (2) Added `POST /admin/payments/{id}/discount` (amount + a required written reason, same "never a silent discretionary change" pattern as `ADM-006`'s certificate override and `AGT-003`'s commission adjustment) -- only lowers a still-*unpaid* amount, never raises it, and regenerates the payment's own invoice (never touched once `status` is paid/succeeded) since the original one would otherwise show a stale figure. Added a matching "Apply discount" admin form; verified live through the real Super Admin console (₹65,000 → ₹55,000). (3) Added `POST /payments/{id}/verify` -- Razorpay's own documented client-side confirmation step (HMAC of `order_id\|payment_id` under the account's key secret, distinct from the webhook's own separate signature), wired into `FeePaymentPanel.tsx`'s `handler` callback so a successful payment marks itself paid and generates a receipt immediately, independent of whether the webhook (still the durable backstop in a real deployment) ever arrives. Verified: `docker compose build api`/`web` (type-check/lint) clean, 46 targeted backend pytest cases (enrollment-fee auto-creation x2, discount x7, payment-verify x5, plus the full existing `test_pay_001_stu_010_payment_gateway.py`/`test_stu_009_consent.py` suites re-run clean), a live browser run confirming (1) and (2) end to end. (3)'s own crypto/logic is fully covered by real backend tests; a full click-through of Razorpay's own hosted checkout UI was deliberately not attempted, matching this session's own established boundary (`I-23`) that testing their frontend is out of scope. |
| I-29 *(found from the app's own request logs, 2026-09-10; closed same day)* | User asked to check the latest certificate-issue errors from the API's own logs — pulled the 5 most recent, all 409/422 across 4 different enrollments. Every 422 traced to the same cause: the Trainer's own "Issue certificate" card (`TeacherWorkspaceActions.tsx`, under `student-progress` — a separate, dedicated component from `AdminCertificatePanel.tsx`) sent `{override: true}` with no `override_reason` field at all when the checkbox was ticked. The backend always requires a non-blank `override_reason` once `override` is true and criteria aren't met, so ticking this box could never actually succeed against a genuinely ineligible learner — confirmed directly against real request logs, not assumed. | **Fixed 2026-09-10.** Added a controlled `certificateOverride` state to the checkbox and a conditional "Override reason" textarea (shown only once ticked, `required`), matching `AdminCertificatePanel.tsx`'s own existing pattern; the form now sends `override_reason` in the request body. Verified live through the real trainer UI: checked the box, the reason field appeared, filled it, submitted — a real certificate (`EDU-CERT-2026-EBD233D5`) was issued with the exact reason text stored in `criteria_snapshot.override_reason`, confirmed directly in the database. Added 1 new Playwright E2E case to `adm-006-certificates.spec.ts` covering this exact path (checkbox unticked → no reason field; ticked → field appears; submit → success) — caught and fixed two of my own test-assertion mistakes along the way (this page's success message is a shared banner above both cards, not scoped inside the triggering card; and it's one fixed literal string, not interpolated with the certificate number, unlike the Admin panel's own message) before landing on a real pass. `docker compose build web` clean. |
| I-30 *(user-requested check, 2026-09-10; closed same day)* | User asked to check the Assignments and Assessments pages too. Verified both end to end with real actions, not just a read: submitted+graded a real assignment (assignments: clean, no bugs). Assessments also worked functionally end to end (real attempt, auto-grade, manual grade, final percentage all correct), but the student's "Take assessment" action (`AssessmentForm` in `WorkflowPanel.tsx`) required manually typing the assessment's raw UUID into a bare text input -- the exact "raw ID typed by hand" gap already fixed for nearly every other action in this app, just never carried over here, even though every assessment's id was already visible in the "Examinations" table directly above this same form. | **Fixed 2026-09-10.** `AssessmentForm` now fetches the student's own `GET /portal/it/student/examinations` on mount and renders a real `<select>` of startable assessments (`title · scheduled date`), filtered to the same statuses `start_assessment` itself accepts (scheduled/open/published) -- a helpful narrowing only, the backend's own check remains authoritative. Verified live: a real attempt correctly reused the picker's value and hit the backend's genuine attempt-limit enforcement ("No assessment attempts remain") rather than any client-side fabrication. Updated `file-response-assessment-question.spec.ts` (the one existing E2E spec exercising this form) to select by the new picker instead of typing the id -- doing so surfaced a second, pre-existing, unrelated test fragility in the same spec (it grabbed the trainer's `batches[0]` blindly; the trainer now has two batches and index 0 isn't reliably the seeded demo student's own) -- fixed by explicitly matching the batch by name. Both affected E2E specs (`file-response-assessment-question.spec.ts`, `adm-006-certificates.spec.ts`) re-run clean. `docker compose build web` clean. |
| I-31 *(user-requested audit, 2026-09-11; closed same day)* | **CLOSED.** User asked to strictly check `ADM-001`'s user-creation cross-role/cross-division limitations (e.g. a counsellor creating an overseas admin, an IT admin creating a counsellor) after an earlier session added a role dropdown to `POST /admin/users`' "Create user" form. Audited every path that can create a `User` row (`POST /auth/register`, `POST /employer/register`, `POST /admin/users`) plus the edit path (`PATCH /admin/users/{id}`) before touching any code, per `CLAUDE.md` NO-ASSUMPTION MODE. Confirmed `ADM-001-AC03`'s RBAC requirement already holds correctly at the API layer: `ensure_admin` (`admin.py`) rejects every non-admin role outright (a counsellor creating anything 403s before division/role logic runs); a division-lock gate (`division != user.division` for any non-`super_admin`) blocks cross-division creation (an IT admin creating a counsellor 403s here); a per-division role allow-list (`allowed_by_division`) is the second, independent gate. `PATCH /admin/users/{id}` doesn't expose `role`/`division` in its field allowlist at all, so no privilege-escalation path exists via edit either. **Three real UI-completeness gaps found** (not security holes — the server-side gates above are authoritative regardless of what the UI offers): (1) the Create User "Division" `<select>` offered `it`/`overseas` to every admin role, including `it_admin`/`overseas_admin`, even though Gate 1 means only their own division's value can ever succeed — the other option just produced a 403 after submit; (2) the same select never offered `"global"`, so a `super_admin` had no way to create another `super_admin` through the UI at all (division `global` -> role `super_admin` is backend-valid but unreachable from the form); (3) the Role dropdown's `super_admin` option list (added this session) combined only the IT + overseas role sets, omitting `super_admin` itself, consistent with gap (2). | **Fixed 2026-09-11.** `WorkflowPanel.tsx`'s `adminSpecs` "Create user" spec: Division field now shows only the caller's own division as a fixed single-option select for `it_admin`/`overseas_admin` (closes gap 1 — no more dead-end option), and adds `"global"` for `super_admin` (closes gap 2). `createUserRoleOptions` now includes `"super_admin"` in the `super_admin` caller's combined role list (closes gap 3), and `ROLES_BY_DIVISION`'s comment now documents the `global -> {super_admin}` leg of the backend's own `allowed_by_division` map alongside the two it already mirrored. Division and Role remain two independent, non-reactive `<select>`s (matching this file's existing convention — no field elsewhere in this generic `ActionForm` reacts to another field's value); a `super_admin` picking a mismatched combination (e.g. `global` + `overseas_student`) still gets the same real backend 422 message this form has always surfaced for any invalid submission, not a new failure mode. No RBAC/backend change was needed or made — `admin.py`'s gates were already correct; this audit only confirmed that and closed the frontend gaps it surfaced. Verified: `npx tsc --noEmit` clean, live browser checks as `it_admin`/`overseas_admin` (Division field now shows a single fixed value, e.g. "overseas", no dead-end second option) and as `super_admin` (Division offers `it`/`overseas`/`global`, Role offers all ten roles including `super_admin`) against the running dev stack after a `docker compose build web`. Targeted backend regression clean (38/38 across the 5 files sharing the `/admin/users` code path: `test_adm_001_admin_crud.py`, `test_adm_014_super_admin_console.py`, `test_sec_001_audit_trail.py`, `test_agt_004_commission_payout.py`, `test_adm_004_directory.py`) — expected, since this fix is frontend-only and none of `admin.py`'s gates changed. **3 new Playwright E2E cases added to `adm-001-admin-crud.spec.ts`** (this feature's own spec file) asserting the exact Division/Role option lists for `it_admin` and `overseas_admin`, plus a real end-to-end run of the previously-unreachable path: `super_admin` submitting `division=global`/`role=super_admin` through the actual form and getting a real created user back. All 6 cases in the file (3 pre-existing + 3 new) pass, single-worker. |
| I-32 *(user-requested audit, 2026-09-11; Tier 0 closed same day)* | **CLOSED (Tier 0 of 3).** User asked to strictly check "country slugs, university slugs and any references" across the app -- generalizing from `POST /admin/universities`' "Create university" form, where `country_slug` was a bare typed text field with no hint of what a valid value looks like. Audited every remaining raw-typed ID/slug/reference field still reachable through `WorkflowPanel.tsx`'s generic `ActionForm` (~30 across 9 role-specific spec functions) against the API endpoints already available to back a real picker, before touching any code, per `CLAUDE.md` NO-ASSUMPTION MODE. Found the established, already-proven fix pattern for this exact class of gap (`OverseasApplyPanel.tsx`/OVS-002, `AdminBatchCreatePanel.tsx`/ADM-003, `CounselorEvaluationPanel`/`CounselorDocumentReviewPanel`/`CounselorVisaPanel`/OVS-003/005/VISA-001): a small dedicated component fetches a real list into a controlled `<select>`, cascading via a second field-keyed `useEffect` where needed -- not a generic reactive-field mechanism (the shared `ActionForm` is deliberately static/uncontrolled and used by dozens of other forms). Several remaining gaps are already flagged *in the code's own comments* ("Admin keeps this broader tool", three times) as known, deliberate, not-yet-closed. Produced a 3-tier backlog (Tier 0: the two fields the user named directly; Tier 1: the same "Admin keeps this broader tool" gap on the Overseas side; Tier 2: IT/Placement/Trainer-side raw references, several needing a new backend list endpoint or a product decision on search-vs-list first) and proceeded with Tier 0 on request. | **Tier 0 fixed 2026-09-11.** (1) `country_slug` on "Create university" (Overseas Admin/Super Admin): new `AdminUniversityCreatePanel.tsx`, `GET /public/countries` (10 rows, no new endpoint needed) into a real `<select>` -- `slug`/`name`/etc. stay free text since those define the *new* university, not a reference. Wired in as its own `showUniversityCreate` flag gated by the exact role set `admin.py`'s own `create_university` enforces (`super_admin`/`overseas_admin` -- deliberately *not* the existing narrower `isAdmin` constant, which excludes `overseas_admin` and would have hidden the form from its actual primary user). (2) `university_id`/`course_id` on Agent's "Create application": new `AgentApplicationCreatePanel.tsx`, reusing `OverseasApplyPanel`'s exact cascading University→Course fetch logic (`GET /public/universities`, then `GET /public/universities/{slug}` for `.courses`) rather than a second implementation. **Found and fixed a third field in the same form that the Tier 0 plan had under-scoped**: `student_id` ("Linked student reference") turned out to already have a real bounded source -- `create_overseas_application` (`workflows.py`) requires the student to be one of the agent's own *active* `AgentStudent` links, exactly what `GET /overseas/agent/students` (already existing) returns -- so it became a real `<select>` too, not left as typed text. (Agent's *separate* "Link student" action, which adds a brand-new, not-yet-linked student, genuinely has no bounded list to source from -- correctly left as Tier 1's own open item, a product decision on search-by-email, not silently built here.) `WorkflowPanel.tsx`'s generic specs for both sections were removed (`agentSpecs`'/`adminSpecs`' old raw-field branches replaced with "handled by X" comments, the same convention `AdminBatchCreatePanel`/`AdminLeadManagementPanel` already established). No backend/RBAC change was needed or made -- both endpoints' own validation was already correct; this only replaced how the request body gets built. Verified: `npx tsc --noEmit` clean, live browser runs as `overseas_admin` (Country dropdown lists real countries, a real university created end to end) and as `agent` (Student/University/Course dropdowns all populate from real data, a real second application created end to end for the seeded agent's one active linked student) against the running dev stack after `docker compose build web`; both ad hoc test records deleted afterward. Targeted backend regression clean (38/38 across `test_ovs_001_discovery.py`, `test_ovs_002_application.py`, `test_agt_001_registration_approval.py`, `test_agt_002_referrals.py`, `test_visa_002_interview_prep.py` -- expected, no backend code changed). Targeted E2E: 16/17 clean, single worker; the one failure (`ovs-001-discovery.spec.ts`'s country-list case) reproduced this project's own already-documented Next.js fetch-cache warm-up flake immediately after a `web` rebuild (`pending.md`'s "Startup" section) -- passed clean on an isolated rerun, confirmed unrelated to this change. **Tier 1/Tier 2 remain open**, not started this session. |
| I-10 *(new, found running a full-suite regression after `VISA-002`, 2026-09-03)* | `app/seed.py`'s seeded `LiveSession` ("FastAPI APIs - Live Session") is created with `starts_at=datetime.now(UTC) + timedelta(days=1)` **only if one doesn't already exist for that batch** (`if b and not await db.scalar(select(LiveSession)...)`) -- so its timestamp is fixed relative to whenever this row was first seeded (at the very start of this long session) and is never refreshed by a later `python -m app.seed` rerun. Confirmed directly: querying it now returns `starts_at: 2026-09-02T15:21:...`, a day in the past relative to the current session date -- it has silently become a *past* session, and `trn-003-upcoming-sessions.spec.ts`'s "trainer dashboard lists an upcoming session" assertion now fails deterministically (reproduces in isolation, single worker, not a contention flake) because the "Upcoming sessions" view correctly excludes it. Same root cause likely affects any other seed row whose date/time field is computed relative to seed time under an `if not exists` guard, if a session runs long enough. | Low-to-medium -- product behavior is correct (the session genuinely is in the past); this is purely a stale test fixture. Not fixed this session (out of `VISA-002`'s scope, and the fix touches `TRN-003`'s own seed/spec, not this feature's files) -- real fix is either dropping the `if not exists` guard for date-bearing seed rows so a reseed refreshes the timestamp, or having the spec create its own throwaway upcoming session instead of relying on this named seeded one (the same "own throwaway record" principle used elsewhere this session). Recreating the `postgres` volume and reseeding fresh immediately before running this spec is the interim workaround. |

## Dependencies (external to engineering effort)

| ID | Dependency | Blocks | Owner (unconfirmed) |
|---|---|---|---|
| D-01 | Zoho account + auth-mechanism decision | `PUB-002`, `ADM-002` integration testing | Not assigned — carried from `INTEGRATION_CONTRACTS.md` §7 |
| D-02 | ~~Razorpay live+test credentials~~ — **resolved 2026-09-03**: real Razorpay TEST-mode credentials (`rzp_test_...`) landed in `.env`, unblocking `PAY-001`/`STU-010`. Live-mode credentials for production go-live remain a separate, still-open item (`DEC-ENV-001`'s test/live separation). | ~~`PAY-001`, `STU-010`~~ (unblocked) | Not assigned |
| D-03 | Twilio account (WhatsApp confirmed; SMS provider identity still open) | `NOT-002`, `NOT-003` | Not assigned |
| D-04 | Email provider selection | `NOT-001` | Not assigned — open item |
| D-05 | DigitalOcean account, region/data-residency decision | `OPS-001`, `OVS-005`, `VISA-001` | Not assigned |
| D-06 | Full UX reference screen export (`DAHRCNYnu6g`) | Final UI polish across all streams | Not assigned |
| D-07 | Actual document checklists per country/university/course | `OVS-005` upload UI finalization | Not assigned — `DEC-DATA-001` |
| D-08 | Team composition/size confirmation | Turning `IMPLEMENTATION_PLAN.md` §2.5's illustrative scenarios into a real schedule | User |

---

**APPROVED** (user, in-session, 2026-09-01, approved as drafted), alongside `IMPLEMENTATION_PLAN.md`,
`RELEASE_BACKLOG.md`, `DEPENDENCY_PLAN.md`, and `CHANGE_CONTROL.md`. GATE-09 (Coding) is now
reachable.

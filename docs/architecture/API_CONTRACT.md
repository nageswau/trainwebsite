# API Contract

Generated by `prompts/09_DATA_API_RBAC_INTEGRATION_CONTRACTS.md`, NO-ASSUMPTION MODE. NO CODE — this
defines endpoint surface, request/response shape at a field level (not full JSON Schema/OpenAPI YAML),
auth type, permission scope, validation/error behavior, pagination, and idempotency. Full OpenAPI
generation is implementation-phase work (`prompts/12`+), owned by Engineering, generated from the
FastAPI route definitions themselves (base codebase already auto-generates OpenAPI — carried over,
not re-designed here).

## 0. Document control

| Field | Value |
|---|---|
| Version | 1.1 (APPROVED — GATE-08, user, in-session, 2026-09-01, no changes requested) |
| Date | 2026-09-01 |
| Scope | 68 `CURRENT` Feature IDs only. Endpoints are listed **only where a feature needs one** per the prompt's own instruction — several `CURRENT` features are UI-only aggregations of already-listed endpoints (e.g. `STU-002` dashboard composes `STU-001`/`STU-010`/`STU-004` reads, no new endpoint). |
| Addendum, 2026-09-14 | §12A (School, `SCH-001`–`006`) added after this document's 2026-09-01 GATE-08 approval, per `DEC-SCOPE-011`/`012` and `DEC-ROLE-006`, propagating `RBAC_MATRIX.md` §2.12 and `DATA_MODEL.md` §6.11–6.18. Sections 1–13's original approved content is unchanged; §13's traceability check has a dated note appended. |
| Conventions | REST over HTTPS. Base path `/api/v1`. All authenticated routes require a valid access JWT (`AUTH-001`) unless marked **Public**. All list endpoints are paginated (§0.1) unless marked **Not paginated** with a reason. |
| OpenAPI ownership | Engineering, auto-generated from FastAPI route/Pydantic-model definitions at implementation time — this document is the source contract it must match, not a substitute for it. |

### 0.1 Pagination convention
Cursor-based (`?cursor=...&limit=...`, default `limit=25`, max `limit=100`) for collections that can
grow unbounded (enquiries, applications, students, jobs). Fixed small collections (a Trainer's own
batch list, an Agent's own referral roster — bounded by real-world scale, not by policy) are **not
paginated**, marked explicitly per endpoint.

### 0.2 Idempotency convention
Idempotency keys (`Idempotency-Key` header) are required only on financial/state-creating endpoints
where a client retry could double-create a record with real-world consequence (payment checkout
session creation, commission claim, GDPR request submission). Read endpoints and simple CRUD updates
do not require one — natural idempotency (PUT/PATCH semantics) suffices.

### 0.3 Standard error shape
`{ "error_code": string, "message": string, "field_errors": [{field, message}]? }`. 401 = not
authenticated. 403 = authenticated but not permitted (never a 404 that leaks resource existence to an
unauthorized actor, except where the resource-scope rule in `RBAC_MATRIX.md` explicitly says to mask
existence). 422 = validation failure, with `field_errors`. 409 = conflict (e.g. slot capacity race,
duplicate application).

---

## 1. Auth (`FND-002`, `AUTH-001`, `AUTH-002`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /auth/login` | Public | — | Single endpoint; `division` (`it`/`overseas`/`global`) is a body field, not a separate path, matching the base codebase's implementation — corrected here during `AUTH-001`'s build (`prompts/13`) from this document's original two-path draft, which never matched working code. Generic "invalid credentials" error — no user enumeration (`AUTH-001-AC03`, verified by Playwright E2E: identical error text for a wrong password vs. a nonexistent account). A division mismatch (e.g. an `it` account posting `division=overseas`) is 403, not 401 — verified. |
| `POST /auth/register` | Public | — | Creates `User` + syncs `UserRoleAssignment` (`FND-002`). |
| `POST /auth/refresh` | Cookie (refresh JWT) | Self | Rotates access JWT from the `HttpOnly` refresh cookie. |
| `POST /auth/logout` | Authenticated | Self | Invalidates refresh cookie server-side. |
| `POST /auth/forgot-password` | Public | — | Generic `{"accepted": true}` response regardless of whether the email exists (no enumeration) — verified by Playwright E2E. |
| `POST /auth/reset-password` | Public (token) | — | Token single-use and hashed at rest, consumed atomically (two simultaneous submissions yield exactly one success). Forgot-password links expire in 30 minutes; admin-provisioned **welcome** links (`DEC-SCOPE-019`, `ENH-003`) in 72 hours, and using one also sets `email_verified`. `new_password` is 10–128 characters (`422` otherwise, without consuming the link). Unknown, used, expired, revoked/superseded and inactive-account welcome links all return the same `400 "Reset token is invalid or expired"`. |
| `GET /auth/me` | Authenticated | Self | Returns identity + active `UserRoleAssignment` list (supports the multi-role-per-User model, `DATA_MODEL.md` §1.1). |
| `PATCH /auth/me` | Authenticated | Self | Profile field updates. **Addendum, 2026-09-20 (ENH-004 security review):** `profile.school_id` and `profile.university_id` (the two profile keys that scope access: the School portal, and a University Representative's applications) are server-owned. A request whose `profile.school_id` or `profile.university_id` differs from the caller's current value (including a user who has none) is **403** (`<key> cannot be changed here`), applies no other change from that request, and is audited as `profile.update_denied` (`outcome=denied`); echoing the unchanged value is accepted (the web "Update profile" form sends the whole profile). Only an admin route (`PATCH /admin/users/{id}`) sets them. `university_id` was added after the independent code review of ENH-004 (2026-09-20); other profile keys are unchanged (see `DEC-SCOPE-020`). |
| `POST /auth/change-password` | Authenticated | Self | `ENH-006` / `DEC-SCOPE-021`. Body `{current_password, new_password}`; `new_password` 10–128 characters and not only whitespace. `200 {"ok": true}`. `400 "Incorrect current password"` is the only message for a wrong current password. `422` for a malformed body, a length violation, a new password made only of whitespace (`"Password must not consist only of spaces"`), or a new password identical to the current one — none of these consumes an attempt. `429` + integer `Retry-After` (seconds) after 5 wrong current passwords in 15 minutes for that user, including for a correct password while blocked. `401` without a session (checked before body validation). Failures are audited as `auth.change_password_failed` (`outcome=denied`), a change as `auth.change_password`. A successful change also revokes the user's unused password-reset links. Other sessions are **not** invalidated (access tokens live 60 minutes, refresh tokens 14 days — accepted risk, `DEC-SCOPE-021`). **Not safe to retry blindly:** if a response is lost after the change was committed, a retry (which still carries the old current password) returns `400` and counts as a failed attempt. No `Idempotency-Key` (§0.2). Errors use FastAPI's `{"detail": …}` body, as every route does. |

**Auth type for every other route below:** Bearer access JWT unless marked **Public**. Every
protected route validates JWT signature, expiry, division, and role server-side before executing —
never a frontend-only gate (`FND-002`, `NFR-SEC-001`).

---

## 2. Public Website (`PUB-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /public/pages/{slug}` | Public | — | CMS page. 404 (not empty-state) for a truly nonexistent slug; empty-state body for a published-but-contentless page. |
| `GET /public/courses` | Public | — | Paginated; filter/search params (`PUB-003`). |
| `GET /public/courses/{id}` | Public | — | Course detail. |
| `GET /public/career-paths`, `GET /public/career-paths/{slug}` | Public | — | List/detail, `PUB-001`/`PRD-PUB-004`. Added during `prompts/13`'s `PUB-001` build — no equivalent existed anywhere in this document's original draft. |
| `GET /public/real-projects`, `GET /public/real-projects/{slug}` | Public | — | List/detail, `PUB-001`/`PRD-PUB-005`. Added during `prompts/13`. |
| `GET /public/testimonials`, `GET /public/testimonials/{id}` | Public | — | List/detail (Success Stories), `PUB-001`/`PRD-PUB-006`. Added during `prompts/13`; detail is by `id` (the base `Testimonial` model has no `slug`). |
| `GET /cms/pages/{division}/{slug}` | Public | — | Generic content page, reused for Business Services (`PUB-001`/`PRD-PUB-007`, slug=`business-services`) rather than a dedicated endpoint. |
| `POST /public/enquiries` | Public | — | Creates `Enquiry`; fires CRM webhook async (never blocks the response on webhook success — `INTEGRATION_CONTRACTS.md` §2). Rate-limited (`THREAT_MODEL.md` — open item, see `SECURITY_CONTROLS.md`). |
| `GET /public/webinars`, `GET /public/webinars/{id}` | Public | — | List/detail, `PUB-004`. Built during `prompts/13` — reuses `Event` (division-aware, no dedicated `Webinar` table; see `DATA_MODEL.md` §2.4 correction), `?division=` filter, response includes a computed `is_past`. |
| `POST /public/webinars/{id}/register` | Public | — | Creates `WebinarRegistration` (net-new, `PUB-004`). 404 unknown id; 409 if the webinar's `starts_at` has already passed (`PUB-004-AC02`/`SCR-PUB-016` — enforced server-side, not just hidden in the UI). Capacity/waitlist behavior open (`PUB-004-AC02`) — currently unconditional accept. |
| `GET /public/news`, `GET /public/gallery` | Public | — | Paginated. |
| `GET /public/jobs`, `GET /public/jobs/{id}` | Public | — | Existing public apply/track surface (base codebase), retained alongside `EMP-002`. |
| `POST /public/jobs/{id}/apply` | Public | — | Creates `JobApplication` with a tracking code + email, no account. |
| `GET /public/jobs/track` | Public | — | Query by tracking code + email. |

---

## 3. Student (`STU-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /student/batches/available` | Authenticated | Self | Slot list with live capacity counts, for enrolment (`STU-001`). |
| `POST /student/enrollments` | Authenticated | Self | Books a slot. **409** if capacity exceeded or a locked enrolment already exists for that program (`STU-001-AC02`). Idempotency key required (double-submit protection on a financial-adjacent action). |
| `GET /student/dashboard` | Authenticated | Self | Aggregation read (`STU-002`). Partial-failure tolerant: one failed sub-query (e.g. payments) returns `null`/omitted for that section, not a 500 for the whole response (`STU-002-AC02`). |
| `GET /student/live-sessions/upcoming` | Authenticated | Self | (`STU-003`) |
| `GET /student/live-sessions/{id}/join` | Authenticated | Self | Returns the provider join URL (Zoho/Google Meet) only when joinable; otherwise a clear not-yet-available state, not an error (`STU-003-AC02`). |
| `GET /student/assignments`, `GET /student/assignments/{id}` | Authenticated | Self | |
| `POST /student/assignments/{id}/submissions` | Authenticated | Self | File upload. Rejects unsupported type/size with a clear `field_errors` message, never silent drop (`STU-011-AC02` pattern applied here too). |
| `POST /workflows/support` | Authenticated | Self | Creates a `SupportTicket` (`STU-005`). Actual path differs from this document's original `/student/support-tickets` draft -- corrected `prompts/13`, 2026-09-02, to match the inherited generic `/workflows/support` implementation rather than inventing a parallel route. |
| `GET /workflows/support` | Authenticated | Self / Trainer+Admin (division-scoped) | Student sees only their own tickets; Trainer/IT Admin/Overseas Admin see every ticket in their division, including unassigned ones (`STU-005-AC02` -- never hidden); Super Admin sees all. No separate `{id}` detail route -- the list response already carries `description`/`resolution_note`. |
| `PATCH /workflows/support/{id}` | Authenticated | Trainer/Admin (own division) | Staff-only (`STU-005`). Auto-claims an unassigned ticket to the acting staff member on first action -- `assigned_to_user_id` is never client-supplied. 403 outside the acting staff member's division (except Super Admin). Ticket creator is notified when `status` becomes `resolved`. |
| `GET /student/attendance`, `GET /student/progress` | Authenticated | Self | Read-only (`STU-006`). |
| `GET /student/certificates` | Authenticated | Self | (`STU-007`) |
| `GET /student/certificates/{id}/download` | Authenticated | Self | Signed, short-lived URL — never a public object link. |
| `POST /student/feedback` | Authenticated | Self | **422** if no matching `Enrollment` exists for the target course/trainer (`STU-008-AC03`). |
| `GET /student/agreements/current`, `POST /student/agreements/{id}/accept` | Authenticated | Self | (`STU-009`) |
| `GET /payments/mine`, `GET /payments/emi-schedule` | Authenticated | Self | (`STU-010`). Actual paths differ from this document's original `/student/payments*` draft — corrected `prompts/13`, 2026-09-03, to match the inherited dedicated `/payments` router rather than inventing a parallel `/student/*` route family (same correction class as `STU-005`'s `/workflows/support`). |
| `POST /payments/{payment_id}/checkout` | Authenticated | Self | Creates a Razorpay hosted checkout session. `Idempotency-Key` header required (§0.2) — a repeat with the same key replays the original result rather than creating a second provider order. Self only, even for Admin roles (`STU-010-AC04` — Admin's payments scope is view, not act). |
| `GET /payments/{payment_id}/invoice`, `GET /payments/{payment_id}/receipt` | Authenticated | Self / Admin (own division, view) | Signed download URL (STU-007's certificate-download pattern). 404 if not yet generated — a receipt only exists once the payment has completed. |
| `GET /student/profile`, `PATCH /student/profile` | Authenticated | Self | (`STU-011`) |
| `POST /student/profile/documents` | Authenticated | Self | Same upload-validation contract as assignment submission. |

---

## 4. Trainer (`TRN-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /trainer/batches` | Authenticated | Self (own batches) | **Not paginated** — bounded by one Trainer's real assignment count (`TRN-001`). |
| `GET /trainer/batches/{id}` | Authenticated | Self, own batch only | 403 (not 404) if the batch exists but isn't the Trainer's — resource existence isn't sensitive here, ownership is (`TRN-002-AC02`). |
| `GET /trainer/sessions/upcoming` | Authenticated | Self | (`TRN-003`) |
| `GET /trainer/batches/{id}/recordings` | Authenticated | Self, own batch | (`TRN-004`) |
| `POST /trainer/batches/{id}/resources` | Authenticated | Self, own batch | File upload. |
| `POST /trainer/batches/{id}/assignments`, `PATCH /trainer/assignments/{id}` | Authenticated | Self, own batch | (`TRN-005`) |
| `POST /trainer/batches/{id}/assessments`, `PATCH /trainer/assessments/{id}`, `POST /trainer/assessments/{id}/publish` | Authenticated | Self, own batch | Draft never returned by any Student-facing read (`TRN-006-AC02`) — enforced by the `Assessment.status` filter in every Student query, not by omitting a field. |
| `GET /trainer/submissions?assignment_id=` | Authenticated | Self, own batch | |
| `POST /trainer/submissions/{id}/grade` | Authenticated | Self, own batch | Writes a `SubmissionGradeHistory` row on re-grade (`TRN-007-AC02`), never overwrites silently. |
| `POST /trainer/batches/{id}/attendance` | Authenticated | Self, own batch | Bulk per-session marking. Empty roster (no scheduled students) returns `[]`, not an error (`TRN-008-AC02`). |
| `GET /trainer/questions`, `POST /trainer/questions/{id}/replies` | Authenticated | Self, own batch | Only the assigned Trainer may reply (`TRN-009-AC02`) — checked server-side against `Batch.trainer_id`. |

---

## 5. IT Admin (`ADM-001`–`008`, `014`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET/POST/PATCH /admin/users`, `/admin/students`, `/admin/trainers`, `/admin/courses`, `/admin/batches` | Authenticated | IT Admin | Paginated lists. `DELETE`/deactivate on an entity with active dependents (e.g. a course with active batches) returns 409 requiring explicit cascade confirmation (`ADM-001-AC02`) — never a silent cascade. |
| `POST /admin/users` (`ENH-003`) | Authenticated | Admin, own division (Super Admin any) | **No password is accepted or generated**: a `password` field returns `422`; a malformed `email` returns `422`; a duplicate returns `409` (also under a race). The account is created with an unusable credential and a 72-hour set-password link is emailed after commit. Response adds `email_status` (`sent` \| `failed` \| `not_configured`) and `expires_at` (and `development_welcome_token` in development/test only); a failed or unconfigured send never rolls back or blocks creation. |
| `GET /admin/users` (`ENH-003`) | Authenticated | Admin, own division | Each row gains `provisioning_status` (`active` \| `pending_setup` \| `link_expired`). Optional `?provisioning_status=pending_setup\|link_expired` (other values `422`) filters to the exact set in scope, not limited by the 500-row cap. `GET /admin/dashboard` gains integer `expired_welcome_links` (same scope). The `it/admin` and `overseas/admin` `GET /portal/{division}/admin/dashboard` payloads lead with the same scoped count as an "Expired welcome links" metric, so it is visible without scrolling to the panel. The role-scoped user tables (`GET /portal/{division}/admin/{users\|students\|trainers\|counselors}`) gain a `setup` column ("Password set" \| "Awaiting setup" \| "Link expired"), so they agree with the Manage users panel. |
| `PATCH /admin/users/{id}` (`ENH-003`) | Authenticated | Admin, own division | Any real change of `active` revokes the account's open welcome links (a link mailed to a wrong recipient must not revive on reactivation); the account then reads `link_expired` and needs a Re-send. An unchanged `active` revokes nothing. |
| `POST /admin/users/{id}/welcome-links` (`ENH-003`) | Authenticated | Admin, own division (Super Admin any) | Re-send. `201` with `{id, email_status, expires_at}` (+ dev token in development/test). Only for an active account that never set a password; `404` unknown, `403` other division, `409` password already set or account deactivated, `429` + `Retry-After` when a link was issued for that account in the last 60 s (the first Re-send after creation is always allowed). **Not idempotent**: each call supersedes the previous link. Concurrent calls serialize on the user row. |
| `GET /admin/enquiries`, `PATCH /admin/enquiries/{id}` | Authenticated | IT Admin | Visible/actionable regardless of `crm_sync_status` (`ADM-002-AC02`). |
| `POST /admin/batches`, `PATCH /admin/batches/{id}` | Authenticated | IT Admin | 422 if `capacity > 20` (`ADM-003-AC02`). |
| `GET/PATCH /admin/directory/students`, `/trainers`, `/employers` | Authenticated | IT Admin | Field-level permission check — a PATCH can never write a field the acting Admin's role isn't permitted to change (`ADM-004-AC02`; see `RBAC_MATRIX.md` §6 for field-level grants). |
| `POST /admin/enrollments/{id}/approve`, `POST /admin/enrollments/{id}/reject` | Authenticated | IT Admin | Rejection is terminal for that enrolment attempt — does not silently proceed to active (`ADM-005-AC02`). |
| `POST /admin/certificates` | Authenticated | IT Admin | Requires `override_reason` in the body if completion criteria aren't met (`ADM-006-AC02`) — 422 without it. |
| `GET/PATCH /admin/placement/candidates`, `/companies`, `/interviews`, `/offers` | Authenticated | Placement Team | (`ADM-007`) — withdrawn candidates excluded from active matching queries but retained in history reads. |
| `GET/PATCH /admin/hr/requirements`, `/shortlists` | Authenticated | HR Team | (`ADM-008`) — empty state for a requirement with no matches, not an error. |
| `GET /admin/super/dashboard`, `/security-logs`, `/backups`, `/roles`, `/settings` | Authenticated | Super Admin | (`ADM-014`). Every privileged action here (e.g. security-log export) writes an `AuditLog` row in the same transaction (`ADM-014-AC02`). |

---

## 6. Employer (`EMP-001`–`005`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /employer/register` | Public | — | Creates `User` + `EmployerProfile` + `Company(owner_type='employer_self_service')`. `registration_status` gating is conditional on `FEATURE_QUESTIONS.md` #7 being confirmed — currently unenforced (`EMP-001-AC02`). |
| `GET/PATCH /employer/profile` | Authenticated | Employer, self | |
| `POST /employer/jobs`, `GET /employer/jobs`, `PATCH /employer/jobs/{id}` | Authenticated | Employer, own postings only | `Job.status` starts `draft`; whether it needs `pending_review` before `published` is the open mediation question (`FEATURE_QUESTIONS.md` #1) — the field exists, the workflow gate is not yet enforced either way pending that answer. |
| `GET /employer/candidates` | Authenticated | Employer | Reads `PlacementProfile`; response fields filtered to the GDPR-approved visible set (`EMP-003-AC02`) — **exact field list is an open item**, currently a conservative allowlist (name, course, skills, availability — no raw contact info) pending confirmation. |
| `POST /employer/shortlist` | Authenticated | Employer, own | |
| `POST /employer/interviews` | Authenticated | Employer, own candidates | 409 on a detected scheduling overlap for the same candidate (`EMP-004-AC02`), not a silent double-book. |
| `GET /employer/interviews` | Authenticated | Employer | Cancelled interviews included by default, `status` field distinguishes them (`EMP-005-AC02`). |

---

## 7. Overseas (`OVS-*`, `VISA-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /overseas/countries`, `/universities`, `/courses` | Public | — | Incomplete admin-entered fields simply omitted from the response, never fabricated (`OVS-001-AC02`). |
| `POST /overseas/applications` | Authenticated | Self (Student) | 409 on a duplicate (student, university, course) with a non-withdrawn existing application (`OVS-002-AC02`). |
| `GET /overseas/applications`, `GET /overseas/applications/{id}` | Authenticated | Self / Counselor (assigned) | |
| `POST /overseas/applications/{id}/advance` | Authenticated | Counselor, assigned students only | Writes `ApplicationStatusHistory`; only advances along the confirmed enum (`DATA_MODEL.md` §6.2) — an attempt to set a rejection/waitlist/deferral outcome is rejected with a clear "not yet supported" error rather than silently accepted (`OVS-003-AC02`). |
| `GET /overseas/applications/{id}/status` | Authenticated | Self / Counselor | Notification dispatch on change is async and does not block this write from succeeding even if it fails (`OVS-004-AC02`). |
| `POST /overseas/applications/{id}/documents` | Authenticated | Self (Student) | Signed upload; least-privilege object storage (§ `SECURITY_CONTROLS.md`). |
| `POST /overseas/documents/{id}/verify` | Authenticated | Counselor, assigned | Sets `verification_status`. |
| `GET /overseas/scholarships`, `POST /overseas/scholarships/{id}/apply` | Public (list) / Authenticated (apply) | Self (Student) | |
| `GET /overseas/events`, `POST /overseas/events/{id}/register` | Public | — | Capacity/waitlist open, as §2 (`OVS-007-AC02`). |
| `GET /overseas/applications/{id}/visa-checklist` | Authenticated | Self / Counselor | Viewable even with unverified documents; only *advancing* past a gated stage is blocked (`VISA-001-AC02`). |
| `GET /overseas/visa/interview-prep` | Authenticated | Self | Missing country-specific material returns an explicit fallback payload, not a 500 (`VISA-002-AC02`). |
| `GET /overseas/applications/{id}/visa-status` | Authenticated | Self / Counselor | Response copy never states EduSphere as the visa decision-maker (`VISA-003-AC02`, a content constraint, not a schema one). |

---

## 8. Agent (`AGT-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /agent/register` | Public | — | Creates `User` + `UserRoleAssignment(role='agent', approval_status='pending')`. |
| `POST /overseas-admin/agents/{id}/approve`, `POST /overseas-admin/agents/{id}/reject` | Authenticated | Overseas Admin | Not the stray `ADM-015` reference in `MASTER_FEATURE_CATALOG.md`'s `AGT-001` workflow text — no such feature ID exists in the catalogue (`SCR-ADM-015` in `SCREEN_CATALOG.md` is an unrelated Super Admin console screen). This endpoint is the actual approval action, owned by `AGT-001`/`ADM` module boundary, not a numbered Admin feature. Writes `AuditLog` (`SEC-001`). |
| `GET /agent/referrals` | Authenticated | Self (Agent, own referrals only) | **Not paginated** unless referral count is later shown to warrant it — bounded per agent in practice. A `Pending`/`Rejected` agent gets 403 on this route, not an empty list (`AGT-001-AC02`). |
| `GET /agent/commissions` | Authenticated | Self (Agent) | |
| `POST /agent/commissions/{id}/claim` | Authenticated | Self (Agent) | Only from `eligible`/`estimated` status (`AGT-004-AC01`). Idempotency key required. |
| `POST /overseas-admin/commissions` | Authenticated | Overseas Admin | Manual creation path, retained (`DATA_MODEL.md` §6.7 `created_by='admin_manual'`). |
| `POST /overseas-admin/commissions/{id}/approve-payout` | Authenticated | Overseas Admin, distinct actor from creation where system-triggered | **Net-new** — no equivalent existed in the base codebase (`REFERENCE_IMPLEMENTATION_FINDINGS.md` §5.4). 403 if the acting Admin is the same identity that created a manually-entered commission, where that separation is enforceable (`NFR-SEC-002`, `AGT-004-AC02`). |

---

## 9. Overseas Staff (`CNS-001`, `UNI-001`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /counselor/caseload` | Authenticated | Counselor, own assigned students only | Direct-ID access to a non-assigned student's record is 403, even via a valid record ID (`CNS-001-AC02`). |
| `GET/PATCH /counselor/students/{id}`, `/applications/{id}`, `/visa-cases/{id}`, `/appointments` | Authenticated | Counselor, assigned only | |
| `GET /university-rep/applications` | Authenticated | University Representative, own institution only | Filtered server-side by `university_id` scope (`DATA_MODEL.md` §6.10), never a client-supplied filter (`UNI-001-AC02`). |
| `PATCH /university-rep/applications/{id}/offer-status` | Authenticated | University Representative, own institution | |
| `POST /university-rep/applications/{id}/updates` | Authenticated | University Representative, own institution | Posts an admission update, visible to the assigned Counselor and Student. |

---

## 10. Notifications, Payments (system-facing, `NOT-*`, `PAY-001`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| *(internal)* notification dispatch | System (Celery task, no external route) | — | Triggered by domain events (status changes, approvals). Not a client-callable endpoint — listed for traceability only (`NOT-001`–`003`). |
| `POST /payments/webhooks/razorpay` | Provider signature (Razorpay), no session/cookie — a system endpoint | System | Actual path corrected `prompts/13`, 2026-09-03, to match the inherited `/payments` router (this document's original draft omitted the router prefix). Idempotent on the `x-razorpay-event-id` header (Razorpay's own dedup key — confirmed against Razorpay's docs, it is not a JSON body field). Never marks a `Payment` `succeeded`/generates a `Receipt` without signature verification (`PAY-001-AC03`); an invalid-signature or duplicate delivery is logged to `PaymentWebhookEvent` and discarded, never reprocessed. See `INTEGRATION_CONTRACTS.md` §2. |
| `POST /admin/payments` | Authenticated | Admin (own division; Super Admin any) | Manual/offline payment record (`STU-010-AC02`) — an Admin can record a `Payment` directly as `paid`, generating both `Invoice` and `Receipt` in the same step. |
| `POST /admin/payments/emi-schedule` | Authenticated | Admin (own division; Super Admin any) | Admin-discretionary EMI plan (`STU-010`) — each installment's amount/due date is supplied explicitly (no fixed amortization policy is confirmed anywhere), creating one linked `Payment` row per installment. |
| `POST /webhooks/twilio/status` | Provider signature (Twilio) | System | Delivery-status callback for WhatsApp/SMS — updates `NotificationLog`, does not affect the underlying business event. |
| `POST /webhooks/crm/zoho` | Shared-secret / provider signature (config-dependent, see `INTEGRATION_CONTRACTS.md` §1) | System | Inbound sync-status acknowledgement from Zoho, if the configured integration supports it — outbound direction is primary (§ Integration Contracts). |

---

## 11. GDPR (`SEC-002`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /account/data-requests` | Authenticated | Self | `type=export\|delete`. Idempotency key required (avoid duplicate request records on retry). |
| `GET /account/data-requests/{id}` | Authenticated | Self | Status polling. |
| `GET /account/data-requests/{id}/export` | Authenticated | Self | Signed download once `status=fulfilled`. |
| `POST /admin/data-requests/{id}/fulfil`, `POST /admin/data-requests/{id}/reject` | Authenticated | IT Admin | `reject` requires a `rejection_reason` referencing a retention hold (422 without one) — no silent rejection (`SEC-002-AC02`). |

---

## 12. Reporting (`RPT-001`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /reports/enrolment-funnel`, `/course-performance`, `/student-progress` | Authenticated | IT Admin, Placement Team | Empty state for a period with no underlying data — never a zero-filled fabrication (`RPT-001-AC02`). |
| `GET /reports/employer-job-activity`, `/placement-outcomes` | Authenticated | IT Admin, Placement Team (Employer sees own subset via `/employer/*` reads, not this admin surface) | |

---

## 12A. School (`SCH-001`–`006`) — addendum, 2026-09-14

Per `RBAC_MATRIX.md` §2.12 and `DATA_MODEL.md` §6.11–6.18. School role division assignment
(`overseas`, proposed) is **not confirmed** (`RBAC_MATRIX.md` §5) — routes below are grouped under
`/school/*` and `/overseas-admin/*` regardless, since URL structure in this document is domain-based,
not division-based; the open division question does not block listing the surface itself.

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /overseas-admin/schools` | Authenticated | Overseas Admin | Creates `School` + seed `school_coordinator` account, both active immediately, no approval gate (`SCH-003-AC02`/`AC04`). `ENH-003`: `coordinator_password` is no longer accepted (`422`); the Coordinator receives a 72-hour set-password link; response adds `email_status`, `expires_at` (and `development_welcome_token` in development/test only). A malformed `coordinator_email` is `422`, a duplicate `409` (a lost race also rolls the school back). **Addendum, 2026-09-22 (`ENH-009`, `DEC-SCOPE-025`, design doc `docs/superpowers/specs/2026-09-22-enh-009-school-profile-design.md`):** the body additively accepts the full School Profile field set — `branch`, `address`, `contact_number`, `email`, `website`, `grades_available`, `board` (`CBSE`/`ICSE`/`State`/`IB`/`Other`, `422` otherwise), `partnership_date`, `mou_reference`, `edusphere_bdm`, `monthly_visit_schedule`, `vice_principal_name`, plus the still-accepted `tier`/`tier_valid_until`. Every field is optional and every `max_length` matches its column exactly (a longer value is `422`, never a database error — the same QA-001 rule as the name limits above). An unknown field is `422` (`extra: forbid`). The server generates a unique 8-character uppercase-hex `school_code` (the business-facing School ID, same convention as `student_code`) and returns it, along with the computed `student_count`, `teacher_count`, `principal_name`, `school_coordinator_name` and `career_counsellor_names` — those five are derived from role assignments at read time, never stored on `School`. |
| `POST /overseas-admin/school-staff` | Authenticated | Overseas Admin / Super Admin | Creates an `academic_team` / `career_counselor` / `psychometric_team` account (`DEC-SCOPE-014`). `ENH-003`: same provisioning contract as above — `password` is `422`, malformed `email` `422`, a name longer than its column is `422` naming the field (`full_name` / `coordinator_full_name` ≤ 160, school `name` ≤ 200, `city` / `state` ≤ 120 — applies equally to `POST /admin/users` and `POST /overseas-admin/schools`; a value at exactly the limit is accepted), duplicate `409`, a 72-hour set-password link is emailed after commit, response adds `email_status` and `expires_at`. |
| `GET /overseas-admin/schools` | Authenticated | Overseas Admin | List of all partner schools (not paginated — this endpoint has never taken paging parameters; earlier wording here was wrong). **Addendum, 2026-09-22 (`ENH-009`):** each row carries the same full profile shape as `POST /overseas-admin/schools` returns, including `school_code` and the five computed fields. Those computed fields are batched across the whole list, so the query count is a small constant rather than five per school. |
| `PATCH /overseas-admin/schools/{school_id}` | Authenticated | Overseas Admin / Super Admin | Updates a `School`'s partnership `tier`/`tier_valid_until` (`DEC-SCOPE-017`). **Addendum, 2026-09-22 (`ENH-009`, `DEC-SCOPE-025`):** additively accepts every School Profile field listed under `POST /overseas-admin/schools` above, with the same validation. Only the fields actually present in the body are written (`exclude_unset`), so an explicit `null` clears a field while an omitted field is left untouched. `name`/`city`/`state`/`coordinator_*` stay out of scope — they were never editable here. Writes a `school.tier_update` audit row when `tier`/`tier_valid_until` change and a separate `school.profile_update` row listing the changed **field names only** (never their values) when any profile field changes; a request touching both writes both rows. 404 for an unknown `school_id`. |
| `GET /overseas-admin/schools/lookup?code=` | Authenticated | Overseas Admin / Super Admin only | **`ENH-009`.** Resolves a business-facing School ID (`school_code`) to the full school profile, mirroring the `GET /overseas-admin/school-students/lookup?code=` convention — deliberately **narrower** than that sibling, which also allows `counselor`: a Career Counselor has no reason to read a partner school's commercial profile (MoU reference, BDM, tier). 403 for any other role, 404 for an unknown code. |
| `POST /school/team/invites` | Authenticated | School Coordinator, own institution | Body includes target `role` (`school_principal`/`school_teacher`/`school_parent`). 422 for any other role value (`SCH-003-AC03`). Creates a single-use `SchoolAccountInvite` token; invite delivery is email only (`NOT-001`). **Addendum, 2026-09-14:** actually sent via real SMTP (`app/services/mailer.py`, `SMTP_*` env vars) in addition to the existing webhook-forwarding `send_notification` call — HTML template with the EduSphere logo, single accept link, Reply-To set to the inviting Coordinator (their name/email, not the recipient's) so replies reach them directly, since the technical SMTP sender must stay a single verified account. `email_status` in the response is `"sent"`/`"failed"`/`"not_configured"`; `development_invite_token` is still returned in a development environment regardless of email outcome. |
| `GET /school/team` | Authenticated | School Coordinator, own institution | Lists the institution's Principal/Teacher/Parent accounts and pending invites. |
| `PATCH /school/team/accounts/{user_id}` | Authenticated | School Coordinator, own institution | **Addendum, 2026-09-22 (`ENH-010`, `School CRM.md` Part B §2, `DEC-SCOPE-025` drafted).** Activates/deactivates a Principal/Teacher/Parent account. Body: `{"active": bool}` (raw `dict`, not a typed schema — extra fields are silently ignored and cannot mutate anything else, pinned by `test_toggle_ignores_extra_fields_and_changes_only_active`). `422` if `active` is missing or non-boolean. `404` if the target account doesn't exist. `403` if it exists but is outside the coordinator's own institution, is not a Principal/Teacher/Parent (blocks targeting a Coordinator, including the caller's own account, or a peer Coordinator), or the caller isn't a Coordinator. A deactivated account is rejected on its very next request anywhere in the app — `User.active` is re-checked on every call via `get_current_user`, not just at login. Writes one `AuditLog` row (`school.team_account_update`) per call with actor (`user_id`), target (`entity_id`), and the new `active` value in `metadata_json`. Returns `{"id": uuid, "active": bool}`. |
| `POST /school/invites/{token}/accept` | Public (token) | — | Single-use — an already-`accepted`/`expired`/`revoked` token returns 409, never silently re-processed (`SCH-003-AC06`). The created account's `school_id` is always the invite's own, never a value supplied in this request body — closes the same IDOR class as `AGT-002`/`UNI-001`. **Exception, `ENH-008` (2026-09-22), `school_parent` invites only:** a newly-accepted Parent account gets `profile={}` (no `school_id` at all), not the invite's `school_id` — a Parent's institution(s) are derived entirely from their `SchoolParentLink` rows, since `ENH-008` allows one Parent to be linked across more than one school. Every other invited role (`school_principal`/`school_teacher`) is unaffected: `profile.school_id` is still always the invite's own. **Addendum, 2026-09-14:** for a `school_parent` invite, acceptance also links every `SchoolStudent` at this school whose `pending_parent_email` matches the invite's own email (not just the one that triggered the invite) — covers a second child added to the roster while the first invite was still pending, `DATA_MODEL.md` §6.11 addendum. |
| `GET /school/dashboard` | Authenticated | School Coordinator / Principal (read-only), own institution | Aggregation read — full `School CRM.md` point-1 dashboard (`SCH-001` addendum, 2026-09-16): all named KPI cards, grade-wise breakdown, completion rings, global-education pipeline, visa status, upcoming activities, and explicit `tracked=false` / `value=null` markers for point-1 modules that still have no confirmed data model (digital portfolio, internship, soft-skills training, program participation). |
| `GET /school/reports` | Authenticated | School Coordinator / Principal (read-only), own institution | **Addendum, 2026-09-14 (Reports module), extended 2026-09-16 to share the dashboard aggregate.** Real, computed-from-live-data figures only, never a fabricated placeholder (`DATA_MODEL.md` §8's rule, applied identically to `RPT-001`). Returns roster/team counts, a grade-wise breakdown, career-guidance/psychometric/counselling/test-prep/language/results completion, global-education pipeline, visa status, and activity/attendance totals. `results_published` counts only `status=published` rows — a Draft/Verified result's existence stays sensitive to these two roles even in aggregate (`SCH-006-AC02`), never surfaced here in any form. Teacher/Parent get 403. |
| `GET /school/students`, `GET /school/students/{id}` | Authenticated | School Coordinator (own institution) / Principal (own institution, read-only) / Teacher (own institution, assigned students only) / Parent (own institution, own child(ren) only) | 403 (not 404) on a student that exists but is outside the acting role's scope — ownership isn't sensitive here, existence within the caller's own institution generally is not either (`SCH-001-AC02`/`AC03`, same masking convention as `TRN-002-AC02`). **Addendum, 2026-09-18 (ENH-001):** response now additively includes `academic_year_id` (string\|null, system-assigned at student creation to the then-active academic year) and `grade_level` (integer 1-12\|null, nullable). |
| `POST /school/students` | Authenticated | School Coordinator, own institution | Creates one `SchoolStudent`. `school_id` always server-derived from the acting Coordinator, never client-supplied (`SCH-001-AC04`). **Addendum, 2026-09-14:** accepts optional `parent_name`/`parent_email`. An email matching an existing `school_parent` account links immediately (`parent_status: "linked"`); a genuinely new email creates and sends a `SchoolAccountInvite` (`"invited"`); an email already pending from another roster entry is reused, not re-sent (`"invite_reused"`); an email belonging to a non-Parent account is rejected (422). **Updated, `ENH-008` (2026-09-22):** an email matching an existing Parent at a *different* school is no longer rejected — cross-school linking is the AC1 success case, and links immediately exactly like a same-school Parent (`"linked"`). The rejection error text also dropped its former "at this school"/"at your school" qualifier (`_parent_email_conflict`, `link_parent`) — it now just says the email belongs to an existing account that is not a Parent, with no school qualifier, since the school is no longer part of the check. **Addendum, 2026-09-18 (ENH-001):** returns the created student with `academic_year_id` and `grade_level` as per the list endpoint above. Accepts optional `grade_level` in the request body (1-12, nullable); 422 if provided but invalid. |
| `PATCH /school/students/{id}` | Authenticated | School Coordinator, own institution only | Same `parent_name`/`parent_email` addendum as `POST /school/students` above. **Addendum, 2026-09-18 (ENH-001):** accepts optional `grade_level` in the request body; 422 if provided but invalid. Returns the updated student with `academic_year_id` and `grade_level` as per the list endpoint above. |
| `GET /school/students/roster-template` | Authenticated | School Coordinator | Downloads the fixed-format upload template — template-download-first (`SCH-002`). Columns (2026-09-14, extended 2026-09-18 for `ENH-001`): `full_name`, `date_of_birth`, `grade_or_class`, `assigned_teacher_email`, `parent_name`, `parent_email`, `grade_level` — the middle two follow the same linked/invited/invite_reused/rejected behavior as `POST /school/students` above, per row. |
| `POST /school/students/bulk-upload` | Authenticated | School Coordinator, own institution | Idempotency key required (batch-creating action). Server-side validation before any row commits (§ `SECURITY_CONTROLS.md` §5 pattern). Creates a `SchoolRosterUploadBatch`; a row that fails validation does not block the rest of the batch — each row succeeds or fails independently (`SCH-002-AC04`), including an invalid `parent_email`. **Addendum, 2026-09-18 (ENH-001):** also accepts an optional `grade_level` column (1-12); an invalid value rejects only that row, same per-row-independent behavior as an invalid `parent_email`. |
| `GET /school/roster-uploads/{batch_id}` | Authenticated | School Coordinator, own institution | Row-level accept/reject report; `accepted_count` + `rejected_count` always equals `total_rows` once `status=completed` (`DATA_MODEL.md` §6.13). |
| `POST`/`PATCH /school/students`, `GET /school/students[/{id}]`, `…/overview` — **Addendum, 2026-09-23 (`ENH-025`, `DEC-SCOPE-027`)** | as the rows above | as the rows above | **Additive only.** Ten optional Student Master fields: `section` (≤20), `roll_number` (≤20), `gender` (`female`\|`male`\|`other`\|`prefer_not_to_say`, case-insensitive in, lower-case out), `student_mobile` (7–20 of digits/space/`+-()`, ≥7 digits), `city` (≤120), `subjects`/`career_interests`/`preferred_countries`/`preferred_courses` (string lists, ≤20 items × ≤80 chars, trimmed, case-insensitive de-dup), `global_education_interest` (JSON boolean or `null`). Control and bidi-override characters are refused. PATCH: an absent key is unchanged, `null` clears, `""`/`[]` are stored as `null`. Responses add those ten keys plus `has_photo` (bool); the storage key is never returned. Other unknown top-level keys keep today's behaviour (ignored, never applied). **Errors:** 422 `"<field> <reason>"` (one string, never echoing the value); **409** `roll_number '<r>' is already used in this grade and section for this academic year` — uniqueness is per school + academic year + `grade_level` + `lower(section)`, a blank value forming its own group, decided by the `uq_school_students_roll` index (no read-then-check). Audit metadata gains `changed_fields` (names only). |
| `GET /school/students/roster-template`, `POST /school/students/bulk-upload` — **Addendum, 2026-09-23 (`ENH-025`)** | as the rows above | School Coordinator | The ten columns above are **appended after** the original seven (`full_name, date_of_birth, grade_or_class, assigned_teacher_email, parent_name, parent_email, grade_level, section, roll_number, gender, student_mobile, city, subjects, career_interests, global_education_interest, preferred_countries, preferred_courses`), so existing positions never move and a seven-column file uploads exactly as before. A missing column, short row or blank cell means "not set". List cells split on `;`; `global_education_interest` accepts `yes/no/true/false/1/0` (else the row is rejected with `global_education_interest must be yes or no`). Invalid values and roll-number clashes (including two rows of the same file) reject **that row only** (`SCH-002-AC04`); a rejected row never links or invites its parent. **Photo cannot be set by CSV** (single-edit only, `DEC-SCOPE-027` item 3). |
| `PUT`/`GET`/`DELETE /school/students/{id}/photo` — **`ENH-025`** | Authenticated | PUT/DELETE: School Coordinator, own institution. GET: the same scope as `GET /school/students/{id}` (Coordinator/Principal own institution, Teacher assigned, Parent linked) | **PUT** (multipart `file`) replaces the photo: type detected from the file's bytes (JPEG/PNG only, else **415**), > 2 MB **413**, empty or unreadable **422**; EXIF/text metadata is stripped before storage; returns `{"has_photo": true}`. **GET** streams the image with `Cache-Control: private, no-store`, `X-Content-Type-Options: nosniff`, `Content-Security-Policy: default-src 'none'; sandbox`; **404** when none. **DELETE** is idempotent (**204**). Stored under a random key that is never serialized or logged. Audit: `school.student_photo_set` / `school.student_photo_remove`. |
| `GET`/`PATCH /school/students/{id}/career-preferences` — **`ENH-025`** | Authenticated | Career Counselor, students in their own portfolio (`_student_in_portfolio`); every other role 403 | Reads/writes only `career_interests`, `global_education_interest`, `preferred_countries`, `preferred_courses` (same rules as above); any other body key is **422** `<key> is not an accepted field`. Returns `{student_id, …the four fields}`. Audit: `school.student_career_preferences_update` with `changed_fields` (names only). |
| `POST /school/students/{id}/attendance`, `POST /school/activities` | Authenticated | School Coordinator, own institution | Activity scheduling / attendance tracking (`SCH-001`'s main workflow). |
| `POST /school/academic-team/results` | Authenticated | Academic Team | Creates a result in `draft` status (`SCH-006`). Scope is the caller's own school portfolio (`SchoolStaffAssignment`, `DEC-SCOPE-013`); a student at a school outside it is 403 even by direct ID. **Addendum, 2026-09-18 (ENH-002):** also accepts an optional `teacher_remarks` — a string (a non-string value is 422), max 2000 chars (422 over limit), `null`/blank stored as no remarks; omitting it is unchanged, backward-compatible behavior. |
| `PATCH /school/academic-team/results/{id}` | Authenticated | Academic Team — **the uploader only**, within their own portfolio | Edits a `draft` result. 403 if the caller is not the member who uploaded it: a peer who could rewrite a draft would then be a legitimate (non-uploader) verifier of their own rewritten content, defeating `DEC-ROLE-007` (`ENH-002` review, 2026-09-18 — tightens earlier behavior, where any portfolio member could edit). 409 if the result has already advanced past `draft` (`SCH-006-AC04`'s forward-only constraint). `teacher_remarks` follows the same draft-only rule as every other field here (`ENH-002`). The row is locked (`SELECT … FOR UPDATE`) for the duration of the request, so a concurrent verify cannot interleave between the draft check and the write. |
| `POST /school/academic-team/results/{id}/verify`, `POST /school/academic-team/results/{id}/publish` | Authenticated | Academic Team | `draft → verified → published` only, one stage at a time — a request to publish a still-`draft` result is 409, not silently accepted (`SCH-006-AC04`). Writes `SchoolResultStatusHistory`. **Resolved, `DEC-ROLE-007`:** verify/publish must be performed by a *different* `academic_team` member than the uploader (403 otherwise), enforced server-side. Each transition locks the row (`SELECT … FOR UPDATE`), so two reviewers racing on the same result produce one transition and one 409, never two history rows. |
| `GET /school/academic-team/progress` | Authenticated | Academic Team | **Addendum, 2026-09-18 (ENH-002)**, closing `EVID-014` §13's "view academic progress" gap: one row per portfolio student (`school_student_id`, `full_name`, `school_name`, `result_count`, `average_percentage`) scoped via the same `SchoolStaffAssignment` portfolio as every other Academic Team endpoint. Average is the mean of each result's own percentage (not `sum(marks)/sum(max_marks)`, which would be wrong across subjects with different `max_marks`), computed over **all** statuses (draft/verified/published) — the Academic Team member's own exposure level, not the published-only rule that applies to Coordinator/Teacher/Parent. A student with zero results is still listed, with `average_percentage: null`. |
| `GET /school/results` | Authenticated | Coordinator/Principal (read-only, own institution) / Teacher (assigned) / Parent (own child(ren)) | Filtered server-side to `status=published` only for every role here — a `draft`/`verified` result is never returned, **including on a direct-ID lookup, which returns 404** (existence of an unpublished result is treated as sensitive, not just its content — this document's own disclosed choice, not dictated by `DATA_MODEL.md` §6.16) (`SCH-006-AC02`). |
| `POST /school/career-counselor/records`, `PATCH /school/career-counselor/records/{id}` | Authenticated | Career Counselor | Creates/edits a `guidance_session`/`counselling_note`/`recommendation` row (`SCH-004`). Same undesigned assignment-scope caveat as the Academic Team endpoint above applies here too. |
| `GET /school/career-counselor/records` | Authenticated | Career Counselor | Own-assignment list. |
| `POST /school/career-counselor/skill-batches`; `GET /school/career-counselor/skill-batches?module_type=&status=&limit=25&offset=0`; `GET`/`PATCH /school/career-counselor/skill-batches/{id}` | Authenticated | Career Counselor (own school portfolio) | **ENH-011 / `DEC-SCOPE-026`.** Soft Skills / Digital Skills batches for one portfolio school. List is `{items, total, limit, offset}` (ENH-005's D6 precedent, `limit` 1–100). Detail (batch + enrolments with attendance summary, scores and `frozen`, sessions, assessments) is **not paginated**: at most 200 enrolments per batch. `PATCH` never changes `school_id`/`module_type` (422). A batch outside the portfolio is a masking 404; a school outside it on create is 403; a wrong role is 403 before any lookup. |
| `POST /school/career-counselor/skill-batches/{id}/enrollments` `{school_student_ids[1..100]}`; `PATCH /school/career-counselor/skill-enrollments/{id}` `{status}` | Authenticated | Career Counselor | **ENH-011.** Enrol is all-or-nothing: 403 student outside portfolio, 422 student at another school, 409 already enrolled / batch closed / cap. Status transitions `enrolled→completed/certified/withdrawn`, `completed→certified/enrolled`, `withdrawn→enrolled`; `certified` terminal; same status is a 200 no-op; a transferred-out (frozen) enrolment is 409. Parents are notified after commit on enrolment and on completed/certified. |
| `POST /school/career-counselor/skill-batches/{id}/sessions`; `PUT /school/career-counselor/skill-sessions/{id}/attendance` `{records[1..200]}`; `POST /school/career-counselor/skill-batches/{id}/assessments`; `PUT /school/career-counselor/skill-assessments/{id}/scores` `{scores[1..200]}` | Authenticated | Career Counselor | **ENH-011.** One session per batch per day (409); session date within the batch dates (422). The two `PUT`s upsert only the listed rows (retry-safe) and return the full set. 409 on a closed batch or a withdrawn/certified/frozen enrolment; 422 for an enrolment of another batch or a score above `max_score`. No `Idempotency-Key` (§0.2). Errors use FastAPI's `{"detail": ...}`, like every implemented endpoint (§0.3's `error_code` shape is not implemented anywhere). |
| `GET /school/career-records` | Authenticated | Coordinator (read-only, own institution) / Parent/Teacher (own scope) | No `Draft`/`Published` gate — visible immediately on creation, unlike results (`DATA_MODEL.md` §6.17). |
| `POST /school/psychometric-team/records`, `PATCH /school/psychometric-team/records/{id}` | Authenticated | Psychometric Team | Assigns an assessment / uploads a report (`SCH-005`). Same undesigned assignment-scope caveat applies. |
| `GET /school/psychometric-team/records` | Authenticated | Psychometric Team | Own-assignment list. |
| `GET /school/psychometric-records` | Authenticated | Coordinator (read-only, own institution) / Parent/Teacher (own scope) | No `Draft`/`Published` gate, same as career records. |
| `GET /school/students/{id}/overview` | Authenticated | Parent (own child(ren)) / Teacher (assigned) / Coordinator, Principal (own institution) | `SCH-007` (added 2026-09-15): one child's full picture — `student` (+ `school_name`, `assigned_teacher_name`), `career_guidance{status,sessions}`, `counselling{status,notes}`, `recommended_careers[]`, `psychometric{status,assessments}`, `results[]` (Published only, `SCH-006-AC02`), `activities{attended[],upcoming[]}`. Same scope loader as `GET /school/students/{id}`; a student outside the caller's scope is 403 even by direct ID. No skills/portfolio/overseas fields — none confirmed (`DEC-SCOPE-015`). **ENH-011 addendum (2026-09-22, `DEC-SCOPE-026`):** additive `skills: {soft_skills, digital_skills}` key, each `{status, enrollments[]}` (status `certified` > `completed` > `in_progress` > `not_started`). |
| *(notification side-effects)* `POST /school/psychometric-team/records`, `PATCH .../records/{id}` (report attached), `POST /school/career-counselor/records`, `POST /school/activities`, `POST /school/academic-team/results/{id}/publish` | — | — | `SCH-007`: each write also creates one `Notification` per linked Parent (school-wide, once per Parent, for `/activities`) and one `NotificationDelivery` (`channel=email`) with the real send outcome; the Parent reads the feed via the existing `GET /workflows/notifications`. Verify and Draft steps never notify. |
| `GET /school/students/{id}/timeline` | Authenticated | Parent (own child(ren)) / Teacher (assigned) / Coordinator, Principal (own institution) | `SCH-008` (added 2026-09-15): a chronologically-sorted `events[]` list (`date`, `category` — profile/career/psychometric/academic/activity, `type`, `title`, `detail`) derived purely from existing `SCH-001`/`004`/`005`/`006` rows — no new tables. Same scope loader as `GET /school/students/{id}/overview`; a student outside the caller's scope is 403 even by direct ID. A Draft/Verified result is never included (`SCH-006-AC02`). **ENH-011 addendum (2026-09-22, `DEC-SCOPE-026`):** adds categories `soft_skills`/`digital_skills` (`skill_enrolled`, `skill_completed`, `skill_certified`). |
| `POST /overseas-admin/academic-years` | Authenticated | Overseas Admin / Super Admin | Creates a global academic year (ENH-001). Body: `{label, start_date, end_date}` (all required; dates in YYYY-MM-DD format). Returns created year with `{id, label, start_date, end_date, status}` where `status` is `"draft"`. **409** if an academic year with the same `label` already exists. **422** if dates are invalid or `end_date ≤ start_date`. |
| `PATCH /overseas-admin/academic-years/{year_id}` | Authenticated | Overseas Admin / Super Admin | Updates an academic year's status (ENH-001). Body: `{status}` (one of `"draft"`, `"active"`, `"closed"`). Status transitions are forward-only (`draft` → `active` → `closed`); **409** if attempting a backward transition. **422** if status value is unrecognized. Returns updated year with `{id, label, start_date, end_date, status}`. Nothing prevents more than one row from being `status="active"` simultaneously; callers resolve "the" active year by most recent `start_date` (a documented best-effort convention, not a uniqueness guarantee). |
| `GET /overseas-admin/academic-years` | Authenticated | Overseas Admin / Super Admin | Lists all academic years (ENH-001, not paginated). Returns array of `{id, label, start_date, end_date, status}` ordered by `start_date` descending. |
| `GET /school/academic-years/active` | Authenticated | Any School-domain role (School Coordinator, Principal, Teacher, Parent, Academic Team, Career Counselor, Psychometric Team, School Partnership Manager, EduSphere School Manager, Overseas Admin, Super Admin) | Returns the currently active academic year (ENH-001), or `null` if none is active. Never returns **404**. Response shape (when active exists): `{id, label, start_date, end_date, status}`. |
| `POST /school/students/promotions` | Authenticated | School Coordinator, own institution only | **ENH-004 / `DEC-SCOPE-020`.** Body `{items: [{student_id, action: "promote"\|"hold_back", grade_or_class?}]}`, 1–500 items; `grade_or_class` (≤60 chars) only with `promote`. Moves each student into the **active** academic year: `promote` adds one to `grade_level` and advances the number inside `grade_or_class` (an unswappable label needs the override); `hold_back` keeps grade and label. Always **200** with `{academic_year, counts, results[{student_id, status: promoted\|held_back\|failed\|skipped, reason, message, grade_level, grade_or_class}]}` (200 even when nothing changed, unlike `bulk-upload`'s 201, because a request can create nothing). Row `reason` codes (stable): `already_in_active_year`, `grade_level_not_set`, `terminal_grade`, `label_unparseable`. Valid rows commit; failed/skipped rows change nothing. **Errors:** 401; 403 if the caller is not a coordinator (checked before body validation) or if any listed student is unknown **or** at another institution (one generic message, nothing written); 409 if there is no active academic year, on a concurrent-promotion conflict, or if row locks cannot be taken within 5 s; 422 (FastAPI list-shaped `detail`) for an empty/oversized/duplicated/malformed list, an unknown action, a label with `hold_back` or containing control characters, or **any unknown body field** (a client-supplied `school_id` or `academic_year_id` is refused, never ignored). A 403 for a student outside the caller's institution is recorded as `school.student_promotion_denied` (`outcome=denied`, counts only). **Retry:** no `Idempotency-Key`; the natural key is (student, target year), enforced by a row lock plus `UNIQUE (school_student_id, to_academic_year_id)`. A repeat reports `skipped: already_in_active_year` instead of replaying `promoted`, so confirm via `results[].grade_level` or `GET …/grade-history`. |
| `GET /school/students/{id}/grade-history` | Authenticated | Coordinator, Principal (own institution) / Teacher (assigned) / Parent (own child(ren)) | **ENH-004.** `{student: {id, full_name}, history: [{id, action: promoted\|held_back, from: {academic_year_id, academic_year_label, grade_level, grade_or_class}, to: {…}, created_at}]}`, newest first (**ENH-025 addendum:** `from`/`to` also carry `section`, and `from.roll_number` — a year move clears the student's roll number, so the ledger keeps the previous one; `to.roll_number` is always `null`; both `null` on rows recorded before ENH-025); `[]` if never promoted. Same scope loader as `GET /school/students/{id}` (403 outside scope, 404 for an unknown ID). Not paginated (at most one row per academic year per student). The performer is stored but not returned. |
| `GET /school/transfer-destinations` | Authenticated | School Coordinator | **ENH-005 / `DEC-SCOPE-022`.** Every other school as `[{id, name}]`, nothing more. Not paginated: bounded by the number of partner schools (§0.1 marks it explicitly). |
| `POST /school/students/{id}/transfer-requests` | Authenticated | School Coordinator, own institution only | **ENH-005.** Body `{to_school_id, reason?}` (`extra="forbid"`; `reason` ≤ 500 chars, no control or bidirectional-override characters). Files an outgoing request; `201` with the request. An unknown student ID and another school's student give the **identical `403`** ("This student is not at your institution", audited). Same or unknown destination `422`; a second pending request for the student `409`; over 50 open requests `409`; the 31st filing attempt in an hour `429` with `Retry-After`. |
| `POST /school/transfer-requests/incoming` | Authenticated | School Coordinator | **ENH-005.** Body `{student_code, reason?}`; the code is normalised to 8 hex characters (`422` otherwise). **Always `202 {"accepted": true}`** for a well-formed code (the same shape as `POST /auth/forgot-password`) whether it names a student at another school, nobody, the caller's own student, or a duplicate: a row is created only for a real student at a different school. Every attempt is audited with a reason token, never the code. Same cap (`409`) and throttle (`429`), both checked before any lookup. |
| `GET /school/transfer-requests?status=pending&limit=25&offset=0` | Authenticated | School Coordinator | **ENH-005.** The requests **this school filed**, newest first, `{items, total, limit, offset}` (`limit` 1-100; `status` in `pending`/`approved`/`rejected`/`cancelled`/`all`, else `422`). One response schema always: a not-yet-approved *incoming* row has `student_id`, `student_name` and `from_school` set to `null` (the gaining coordinator knows only the code they typed). The other school is not told of a request until an admin decides (`DEC-SCOPE-022` D5). |
| `POST /school/transfer-requests/{id}/cancel` | Authenticated | School Coordinator of the filing school | **ENH-005.** Withdraws a pending request. An unknown request and another school's request give the identical `403`; a decided request is `409`. Serialises with a concurrent admin decision. |
| `GET /school/students/{id}/transfer-history` | Authenticated | Coordinator, Principal (own institution) / Teacher (assigned) / Parent (own child(ren)) | **ENH-005.** `{student: {id, full_name}, history: [{id, decided_at, from_school, to_school}]}`, approved transfers only. **No `reason` and no staff IDs**, so a Parent never receives either. Not paginated (bounded per student). After a transfer the losing school can no longer read the student, so this is the gaining school's and the parent's view. |
| `GET /overseas-admin/school-transfer-requests?status=pending&limit=25&offset=0` | Authenticated | Overseas Admin / Super Admin | **ENH-005.** The admin's queue, unredacted, with requester, deciding admin and, for pending rows, a preview `{linked_parents, in_flight_results, to_school_has_portfolio_staff, pending_parent_invite}` computed with one grouped query per count over the page. `pending_parent_invite` (added 2026-09-21, additive, default `false`) is a boolean read from the student row already loaded: `true` means approval will clear a parent invite that has not been accepted; the invited address is never returned. |
| `POST /overseas-admin/school-transfer-requests/{id}/approve` | Authenticated | Overseas Admin / Super Admin | **ENH-005.** The one action that moves the student, in one transaction with a bounded lock wait: sets `school_id`, clears the assigned teacher and pending parent email, withdraws Draft/Verified results, re-scopes a parent account only when no other child remains at the losing school (links are never deleted), and writes audit rows in the same transaction. Returns the request with its `outcome` counts. Decided request `409`; the student moved since filing `409` (request stays pending); lock wait exceeded `409`. Notifications go out after the commit and can never undo it. |
| `POST /overseas-admin/school-transfer-requests/{id}/reject` | Authenticated | Overseas Admin / Super Admin | **ENH-005.** Optional body `{note?}`. Changes only the request; the note is shown to the filing coordinator and is never copied into audit or logs. A notice to the coordinator who filed an *incoming* request carries the Student ID only. |
| `GET /overseas-admin/school-students/{id}/transfer-history` | Authenticated | Overseas Admin / Super Admin | **ENH-005.** Every request for one student, all statuses, with staff names (admin-only). Not paginated (bounded per student). |
| `GET /school/students/{id}/portfolio` | Authenticated | Coordinator/Teacher (assigned)/Principal (read-only), own institution; Parent (own child(ren)); `academic_team`/`career_counselor`/`psychometric_team` (own school portfolio) | **ENH-012.** No `response_model` (matches `student_timeline()`'s own computed-aggregate convention). Returns `{student, completion_percentage, can_edit, profile_complete, academic_achievements[], psychometric_report[], career_guidance[], languages[], entries: {<10 sections>: [...]}, personal_statement}`. `completion_percentage` is over 16 sections (profile + 4 auto-populated + 10 self-entry + personal statement). `can_edit` is `true` only for the 3 writer roles (and, for `school_teacher`, only when assigned to this student). Same 403-not-404 scope masking as `GET /school/students/{id}`. |
| `POST /school/students/{id}/portfolio/entries` | Authenticated | `school_coordinator` (own institution) / `school_teacher` (own institution, assigned only) / `academic_team` (own school portfolio) | **ENH-012.** Body `{section, title, description?, organization?, date_from?, date_to?}` (`extra="forbid"`; `section` one of the 10 fixed values, 422 otherwise; `title` 1–200 chars, no control characters; `description` ≤2000 chars, permits `\n`/`\t`, blocks bidi-override characters (`clean_free_text` house precedent, not the single-line rule); `organization` ≤200 chars, no control characters; `date_to` must not precede `date_from`). `201` with the created entry. Writes an `AuditLog` row with section + student id only — never title/description/organization (free text is never logged). |
| `PATCH /school/students/{id}/portfolio/entries/{entry_id}` | Authenticated | Same as POST above, and only the entry's own student | **ENH-012.** Partial update; unknown entry or an entry belonging to a different student both give `404`. Uses `payload.model_fields_set` to distinguish "field explicitly sent" (applied, including an explicit `null`, which clears it) from "field omitted" (left untouched) — an explicit `{"organization": null}` actually clears a previously-set value. Post-merge date-range validation re-checked (`422` if the merged `date_to` precedes the merged `date_from`; the entry is left unchanged on that rejection). |
| `DELETE /school/students/{id}/portfolio/entries/{entry_id}` | Authenticated | Same as POST above, and only the entry's own student | **ENH-012.** `204` on success. Unknown entry, an already-deleted entry, and an entry belonging to a different student all give `404` (no ownership leak). Writes an `AuditLog` row (section + student id, no free text) before commit. |
| `PATCH /school/students/{id}/portfolio/personal-statement` | Authenticated | Same as POST above | **ENH-012.** Body `{personal_statement?}` (≤4000 chars, permits `\n`/`\t`, blocks bidi-override characters, blank/`null` clears it). Race-safe upsert: `begin_nested()` insert, and on the expected unique-constraint `IntegrityError` (a concurrent first `PATCH`), re-queries and updates the same row instead of a `409` — a second "set the statement" is not a duplicate-intent conflict like a transfer filing. Returns `{personal_statement, updated_at}`. A second `PATCH` always updates the same `PortfolioProfile` row, never creates a second one. |

**Addendum, 2026-09-21 (`ENH-005` / `DEC-SCOPE-022`) — conventions for the ten rows above (the ten `/school/students/…/transfer-*`/`/overseas-admin/…` rows preceding the five `ENH-012` portfolio rows).** Errors are FastAPI `{"detail": "..."}` (this section's rows never emitted §0.3's `{error_code, message}` shape, as for `ENH-004`); the `409`/`403`/`429` `detail` strings are stable and listed in the design spec §5.3a. Two lists (`GET /school/transfer-requests`, `GET /overseas-admin/school-transfer-requests`) use `limit`/`offset` with a `{items, total, limit, offset}` envelope, a deliberate deviation from §0.1's cursor convention, which no endpoint implements. No `Idempotency-Key` (§0.2: not a financial action): every write is naturally safe to retry (a duplicate filing, approve, reject or cancel answers `409` with a message saying the state has already changed). The existing parent read scope changes: a `school_parent` is scoped by their `SchoolParentLink`s, not by the school on their account, and keeps the same `403` messages. `GET /portal/overseas/admin/school-transfers` serves the admin section's read table.

**Addendum, 2026-09-23 (`ENH-012`) — conventions for the five portfolio rows above.** Errors are FastAPI `{"detail": "..."}`, same convention as `ENH-004`/`ENH-005`. No `Idempotency-Key` (§0.2: not a financial action) — every write here is naturally safe to retry except the two-step DELETE-confirm UI pattern, which is a frontend-only concern (`PortfolioPanel.tsx`'s own busy/inFlight guard prevents a double-click from sending two DELETEs). No pagination: `entries` is bounded per student per section, and every list here is small by construction. `GET /school/students/{id}/portfolio` is read by `apps/web/components/PortfolioPanel.tsx` (writer UI: add/edit/delete entries, edit personal statement) via `apps/web/lib/portfolio.ts`'s `loadPortfolio()`.

**Not specified — genuinely open, not invented:** exact request/response field shapes for the three
service-delivery modules (`DATA_MODEL.md` §6.16–6.18 each flag their own field list as open), the
`academic_team`/`career_counselor`/`psychometric_team` assignment-scope mechanism, and how those three
roles' own accounts are provisioned (`SCH-003`'s invite flow covers only the four school-side roles).
Do not build against an assumed answer to any of these — confirm first.

---

## 13. Traceability check

Every `CURRENT` Feature ID with `API required: Y` in `MASTER_FEATURE_CATALOG.md` has at least one
endpoint above, or is explicitly noted as a UI-only aggregation of already-listed endpoints
(`STU-002`, `VISA-002` per its own `API required: N`). Every endpoint's error/edge behavior column
cites the specific `-AC0#` it satisfies. `BLOCKED` features (`AUTH-003`, `PUB-010`, `ADM-009`–`013`,
`EMP-006`, `LMS-001`, `RPT-002`) have **no** endpoint listed — consistent with `FEATURE_ACCEPTANCE_
CRITERIA.md`'s "NOT APPLICABLE YET" markers for those IDs.

**Addendum, 2026-09-14** — `SCH-001`–`006` (all `API required: Y` in `MASTER_FEATURE_CATALOG.md`) are
now covered by §12A. Three sub-surfaces (Academic Team/Career Counselor/Psychometric Team's own
assignment scoping and account provisioning) are listed for completeness but explicitly flagged
unspecified pending open decisions — not a gap in this traceability check, a disclosed limit of it.

---

**GATE-08 APPROVED** (user, in-session, 2026-09-01 — approved as drafted, no changes requested)
alongside the other four contract documents. `prompts/10_TEST_CATALOG_AUDIT_AND_REBUILD.md` may now
proceed.

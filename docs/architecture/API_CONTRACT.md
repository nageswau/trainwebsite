# API Contract

Generated by `prompts/09_DATA_API_RBAC_INTEGRATION_CONTRACTS.md`, NO-ASSUMPTION MODE. NO CODE — this
defines endpoint surface, request/response shape at a field level (not full JSON Schema/OpenAPI YAML),
auth type, permission scope, validation/error behavior, pagination, and idempotency. Full OpenAPI
generation is implementation-phase work (`prompts/12`+), owned by Engineering, generated from the
FastAPI route definitions themselves (base codebase already auto-generates OpenAPI — carried over,
not re-designed here).

## 0. Document control

| Field | Value |
|---|---|
| Version | 1.1 (APPROVED — GATE-08, user, in-session, 2026-09-01, no changes requested) |
| Date | 2026-09-01 |
| Scope | 68 `CURRENT` Feature IDs only. Endpoints are listed **only where a feature needs one** per the prompt's own instruction — several `CURRENT` features are UI-only aggregations of already-listed endpoints (e.g. `STU-002` dashboard composes `STU-001`/`STU-010`/`STU-004` reads, no new endpoint). |
| Addendum, 2026-09-14 | §12A (School, `SCH-001`–`006`) added after this document's 2026-09-01 GATE-08 approval, per `DEC-SCOPE-011`/`012` and `DEC-ROLE-006`, propagating `RBAC_MATRIX.md` §2.12 and `DATA_MODEL.md` §6.11–6.18. Sections 1–13's original approved content is unchanged; §13's traceability check has a dated note appended. |
| Conventions | REST over HTTPS. Base path `/api/v1`. All authenticated routes require a valid access JWT (`AUTH-001`) unless marked **Public**. All list endpoints are paginated (§0.1) unless marked **Not paginated** with a reason. |
| OpenAPI ownership | Engineering, auto-generated from FastAPI route/Pydantic-model definitions at implementation time — this document is the source contract it must match, not a substitute for it. |

### 0.1 Pagination convention
Cursor-based (`?cursor=...&limit=...`, default `limit=25`, max `limit=100`) for collections that can
grow unbounded (enquiries, applications, students, jobs). Fixed small collections (a Trainer's own
batch list, an Agent's own referral roster — bounded by real-world scale, not by policy) are **not
paginated**, marked explicitly per endpoint.

### 0.2 Idempotency convention
Idempotency keys (`Idempotency-Key` header) are required only on financial/state-creating endpoints
where a client retry could double-create a record with real-world consequence (payment checkout
session creation, commission claim, GDPR request submission). Read endpoints and simple CRUD updates
do not require one — natural idempotency (PUT/PATCH semantics) suffices.

### 0.3 Standard error shape
`{ "error_code": string, "message": string, "field_errors": [{field, message}]? }`. 401 = not
authenticated. 403 = authenticated but not permitted (never a 404 that leaks resource existence to an
unauthorized actor, except where the resource-scope rule in `RBAC_MATRIX.md` explicitly says to mask
existence). 422 = validation failure, with `field_errors`. 409 = conflict (e.g. slot capacity race,
duplicate application).

---

## 1. Auth (`FND-002`, `AUTH-001`, `AUTH-002`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /auth/login` | Public | — | Single endpoint; `division` (`it`/`overseas`/`global`) is a body field, not a separate path, matching the base codebase's implementation — corrected here during `AUTH-001`'s build (`prompts/13`) from this document's original two-path draft, which never matched working code. Generic "invalid credentials" error — no user enumeration (`AUTH-001-AC03`, verified by Playwright E2E: identical error text for a wrong password vs. a nonexistent account). A division mismatch (e.g. an `it` account posting `division=overseas`) is 403, not 401 — verified. |
| `POST /auth/register` | Public | — | Creates `User` + syncs `UserRoleAssignment` (`FND-002`). |
| `POST /auth/refresh` | Cookie (refresh JWT) | Self | Rotates access JWT from the `HttpOnly` refresh cookie. |
| `POST /auth/logout` | Authenticated | Self | Invalidates refresh cookie server-side. |
| `POST /auth/forgot-password` | Public | — | Generic `{"accepted": true}` response regardless of whether the email exists (no enumeration) — verified by Playwright E2E. |
| `POST /auth/reset-password` | Public (token) | — | Token single-use, 30-minute expiry. |
| `GET /auth/me` | Authenticated | Self | Returns identity + active `UserRoleAssignment` list (supports the multi-role-per-User model, `DATA_MODEL.md` §1.1). |
| `PATCH /auth/me` | Authenticated | Self | Profile field updates. |

**Auth type for every other route below:** Bearer access JWT unless marked **Public**. Every
protected route validates JWT signature, expiry, division, and role server-side before executing —
never a frontend-only gate (`FND-002`, `NFR-SEC-001`).

---

## 2. Public Website (`PUB-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /public/pages/{slug}` | Public | — | CMS page. 404 (not empty-state) for a truly nonexistent slug; empty-state body for a published-but-contentless page. |
| `GET /public/courses` | Public | — | Paginated; filter/search params (`PUB-003`). |
| `GET /public/courses/{id}` | Public | — | Course detail. |
| `GET /public/career-paths`, `GET /public/career-paths/{slug}` | Public | — | List/detail, `PUB-001`/`PRD-PUB-004`. Added during `prompts/13`'s `PUB-001` build — no equivalent existed anywhere in this document's original draft. |
| `GET /public/real-projects`, `GET /public/real-projects/{slug}` | Public | — | List/detail, `PUB-001`/`PRD-PUB-005`. Added during `prompts/13`. |
| `GET /public/testimonials`, `GET /public/testimonials/{id}` | Public | — | List/detail (Success Stories), `PUB-001`/`PRD-PUB-006`. Added during `prompts/13`; detail is by `id` (the base `Testimonial` model has no `slug`). |
| `GET /cms/pages/{division}/{slug}` | Public | — | Generic content page, reused for Business Services (`PUB-001`/`PRD-PUB-007`, slug=`business-services`) rather than a dedicated endpoint. |
| `POST /public/enquiries` | Public | — | Creates `Enquiry`; fires CRM webhook async (never blocks the response on webhook success — `INTEGRATION_CONTRACTS.md` §2). Rate-limited (`THREAT_MODEL.md` — open item, see `SECURITY_CONTROLS.md`). |
| `GET /public/webinars`, `GET /public/webinars/{id}` | Public | — | List/detail, `PUB-004`. Built during `prompts/13` — reuses `Event` (division-aware, no dedicated `Webinar` table; see `DATA_MODEL.md` §2.4 correction), `?division=` filter, response includes a computed `is_past`. |
| `POST /public/webinars/{id}/register` | Public | — | Creates `WebinarRegistration` (net-new, `PUB-004`). 404 unknown id; 409 if the webinar's `starts_at` has already passed (`PUB-004-AC02`/`SCR-PUB-016` — enforced server-side, not just hidden in the UI). Capacity/waitlist behavior open (`PUB-004-AC02`) — currently unconditional accept. |
| `GET /public/news`, `GET /public/gallery` | Public | — | Paginated. |
| `GET /public/jobs`, `GET /public/jobs/{id}` | Public | — | Existing public apply/track surface (base codebase), retained alongside `EMP-002`. |
| `POST /public/jobs/{id}/apply` | Public | — | Creates `JobApplication` with a tracking code + email, no account. |
| `GET /public/jobs/track` | Public | — | Query by tracking code + email. |

---

## 3. Student (`STU-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /student/batches/available` | Authenticated | Self | Slot list with live capacity counts, for enrolment (`STU-001`). |
| `POST /student/enrollments` | Authenticated | Self | Books a slot. **409** if capacity exceeded or a locked enrolment already exists for that program (`STU-001-AC02`). Idempotency key required (double-submit protection on a financial-adjacent action). |
| `GET /student/dashboard` | Authenticated | Self | Aggregation read (`STU-002`). Partial-failure tolerant: one failed sub-query (e.g. payments) returns `null`/omitted for that section, not a 500 for the whole response (`STU-002-AC02`). |
| `GET /student/live-sessions/upcoming` | Authenticated | Self | (`STU-003`) |
| `GET /student/live-sessions/{id}/join` | Authenticated | Self | Returns the provider join URL (Zoho/Google Meet) only when joinable; otherwise a clear not-yet-available state, not an error (`STU-003-AC02`). |
| `GET /student/assignments`, `GET /student/assignments/{id}` | Authenticated | Self | |
| `POST /student/assignments/{id}/submissions` | Authenticated | Self | File upload. Rejects unsupported type/size with a clear `field_errors` message, never silent drop (`STU-011-AC02` pattern applied here too). |
| `POST /workflows/support` | Authenticated | Self | Creates a `SupportTicket` (`STU-005`). Actual path differs from this document's original `/student/support-tickets` draft -- corrected `prompts/13`, 2026-09-02, to match the inherited generic `/workflows/support` implementation rather than inventing a parallel route. |
| `GET /workflows/support` | Authenticated | Self / Trainer+Admin (division-scoped) | Student sees only their own tickets; Trainer/IT Admin/Overseas Admin see every ticket in their division, including unassigned ones (`STU-005-AC02` -- never hidden); Super Admin sees all. No separate `{id}` detail route -- the list response already carries `description`/`resolution_note`. |
| `PATCH /workflows/support/{id}` | Authenticated | Trainer/Admin (own division) | Staff-only (`STU-005`). Auto-claims an unassigned ticket to the acting staff member on first action -- `assigned_to_user_id` is never client-supplied. 403 outside the acting staff member's division (except Super Admin). Ticket creator is notified when `status` becomes `resolved`. |
| `GET /student/attendance`, `GET /student/progress` | Authenticated | Self | Read-only (`STU-006`). |
| `GET /student/certificates` | Authenticated | Self | (`STU-007`) |
| `GET /student/certificates/{id}/download` | Authenticated | Self | Signed, short-lived URL — never a public object link. |
| `POST /student/feedback` | Authenticated | Self | **422** if no matching `Enrollment` exists for the target course/trainer (`STU-008-AC03`). |
| `GET /student/agreements/current`, `POST /student/agreements/{id}/accept` | Authenticated | Self | (`STU-009`) |
| `GET /payments/mine`, `GET /payments/emi-schedule` | Authenticated | Self | (`STU-010`). Actual paths differ from this document's original `/student/payments*` draft — corrected `prompts/13`, 2026-09-03, to match the inherited dedicated `/payments` router rather than inventing a parallel `/student/*` route family (same correction class as `STU-005`'s `/workflows/support`). |
| `POST /payments/{payment_id}/checkout` | Authenticated | Self | Creates a Razorpay hosted checkout session. `Idempotency-Key` header required (§0.2) — a repeat with the same key replays the original result rather than creating a second provider order. Self only, even for Admin roles (`STU-010-AC04` — Admin's payments scope is view, not act). |
| `GET /payments/{payment_id}/invoice`, `GET /payments/{payment_id}/receipt` | Authenticated | Self / Admin (own division, view) | Signed download URL (STU-007's certificate-download pattern). 404 if not yet generated — a receipt only exists once the payment has completed. |
| `GET /student/profile`, `PATCH /student/profile` | Authenticated | Self | (`STU-011`) |
| `POST /student/profile/documents` | Authenticated | Self | Same upload-validation contract as assignment submission. |

---

## 4. Trainer (`TRN-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /trainer/batches` | Authenticated | Self (own batches) | **Not paginated** — bounded by one Trainer's real assignment count (`TRN-001`). |
| `GET /trainer/batches/{id}` | Authenticated | Self, own batch only | 403 (not 404) if the batch exists but isn't the Trainer's — resource existence isn't sensitive here, ownership is (`TRN-002-AC02`). |
| `GET /trainer/sessions/upcoming` | Authenticated | Self | (`TRN-003`) |
| `GET /trainer/batches/{id}/recordings` | Authenticated | Self, own batch | (`TRN-004`) |
| `POST /trainer/batches/{id}/resources` | Authenticated | Self, own batch | File upload. |
| `POST /trainer/batches/{id}/assignments`, `PATCH /trainer/assignments/{id}` | Authenticated | Self, own batch | (`TRN-005`) |
| `POST /trainer/batches/{id}/assessments`, `PATCH /trainer/assessments/{id}`, `POST /trainer/assessments/{id}/publish` | Authenticated | Self, own batch | Draft never returned by any Student-facing read (`TRN-006-AC02`) — enforced by the `Assessment.status` filter in every Student query, not by omitting a field. |
| `GET /trainer/submissions?assignment_id=` | Authenticated | Self, own batch | |
| `POST /trainer/submissions/{id}/grade` | Authenticated | Self, own batch | Writes a `SubmissionGradeHistory` row on re-grade (`TRN-007-AC02`), never overwrites silently. |
| `POST /trainer/batches/{id}/attendance` | Authenticated | Self, own batch | Bulk per-session marking. Empty roster (no scheduled students) returns `[]`, not an error (`TRN-008-AC02`). |
| `GET /trainer/questions`, `POST /trainer/questions/{id}/replies` | Authenticated | Self, own batch | Only the assigned Trainer may reply (`TRN-009-AC02`) — checked server-side against `Batch.trainer_id`. |

---

## 5. IT Admin (`ADM-001`–`008`, `014`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET/POST/PATCH /admin/users`, `/admin/students`, `/admin/trainers`, `/admin/courses`, `/admin/batches` | Authenticated | IT Admin | Paginated lists. `DELETE`/deactivate on an entity with active dependents (e.g. a course with active batches) returns 409 requiring explicit cascade confirmation (`ADM-001-AC02`) — never a silent cascade. |
| `GET /admin/enquiries`, `PATCH /admin/enquiries/{id}` | Authenticated | IT Admin | Visible/actionable regardless of `crm_sync_status` (`ADM-002-AC02`). |
| `POST /admin/batches`, `PATCH /admin/batches/{id}` | Authenticated | IT Admin | 422 if `capacity > 20` (`ADM-003-AC02`). |
| `GET/PATCH /admin/directory/students`, `/trainers`, `/employers` | Authenticated | IT Admin | Field-level permission check — a PATCH can never write a field the acting Admin's role isn't permitted to change (`ADM-004-AC02`; see `RBAC_MATRIX.md` §6 for field-level grants). |
| `POST /admin/enrollments/{id}/approve`, `POST /admin/enrollments/{id}/reject` | Authenticated | IT Admin | Rejection is terminal for that enrolment attempt — does not silently proceed to active (`ADM-005-AC02`). |
| `POST /admin/certificates` | Authenticated | IT Admin | Requires `override_reason` in the body if completion criteria aren't met (`ADM-006-AC02`) — 422 without it. |
| `GET/PATCH /admin/placement/candidates`, `/companies`, `/interviews`, `/offers` | Authenticated | Placement Team | (`ADM-007`) — withdrawn candidates excluded from active matching queries but retained in history reads. |
| `GET/PATCH /admin/hr/requirements`, `/shortlists` | Authenticated | HR Team | (`ADM-008`) — empty state for a requirement with no matches, not an error. |
| `GET /admin/super/dashboard`, `/security-logs`, `/backups`, `/roles`, `/settings` | Authenticated | Super Admin | (`ADM-014`). Every privileged action here (e.g. security-log export) writes an `AuditLog` row in the same transaction (`ADM-014-AC02`). |

---

## 6. Employer (`EMP-001`–`005`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /employer/register` | Public | — | Creates `User` + `EmployerProfile` + `Company(owner_type='employer_self_service')`. `registration_status` gating is conditional on `FEATURE_QUESTIONS.md` #7 being confirmed — currently unenforced (`EMP-001-AC02`). |
| `GET/PATCH /employer/profile` | Authenticated | Employer, self | |
| `POST /employer/jobs`, `GET /employer/jobs`, `PATCH /employer/jobs/{id}` | Authenticated | Employer, own postings only | `Job.status` starts `draft`; whether it needs `pending_review` before `published` is the open mediation question (`FEATURE_QUESTIONS.md` #1) — the field exists, the workflow gate is not yet enforced either way pending that answer. |
| `GET /employer/candidates` | Authenticated | Employer | Reads `PlacementProfile`; response fields filtered to the GDPR-approved visible set (`EMP-003-AC02`) — **exact field list is an open item**, currently a conservative allowlist (name, course, skills, availability — no raw contact info) pending confirmation. |
| `POST /employer/shortlist` | Authenticated | Employer, own | |
| `POST /employer/interviews` | Authenticated | Employer, own candidates | 409 on a detected scheduling overlap for the same candidate (`EMP-004-AC02`), not a silent double-book. |
| `GET /employer/interviews` | Authenticated | Employer | Cancelled interviews included by default, `status` field distinguishes them (`EMP-005-AC02`). |

---

## 7. Overseas (`OVS-*`, `VISA-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /overseas/countries`, `/universities`, `/courses` | Public | — | Incomplete admin-entered fields simply omitted from the response, never fabricated (`OVS-001-AC02`). |
| `POST /overseas/applications` | Authenticated | Self (Student) | 409 on a duplicate (student, university, course) with a non-withdrawn existing application (`OVS-002-AC02`). |
| `GET /overseas/applications`, `GET /overseas/applications/{id}` | Authenticated | Self / Counselor (assigned) | |
| `POST /overseas/applications/{id}/advance` | Authenticated | Counselor, assigned students only | Writes `ApplicationStatusHistory`; only advances along the confirmed enum (`DATA_MODEL.md` §6.2) — an attempt to set a rejection/waitlist/deferral outcome is rejected with a clear "not yet supported" error rather than silently accepted (`OVS-003-AC02`). |
| `GET /overseas/applications/{id}/status` | Authenticated | Self / Counselor | Notification dispatch on change is async and does not block this write from succeeding even if it fails (`OVS-004-AC02`). |
| `POST /overseas/applications/{id}/documents` | Authenticated | Self (Student) | Signed upload; least-privilege object storage (§ `SECURITY_CONTROLS.md`). |
| `POST /overseas/documents/{id}/verify` | Authenticated | Counselor, assigned | Sets `verification_status`. |
| `GET /overseas/scholarships`, `POST /overseas/scholarships/{id}/apply` | Public (list) / Authenticated (apply) | Self (Student) | |
| `GET /overseas/events`, `POST /overseas/events/{id}/register` | Public | — | Capacity/waitlist open, as §2 (`OVS-007-AC02`). |
| `GET /overseas/applications/{id}/visa-checklist` | Authenticated | Self / Counselor | Viewable even with unverified documents; only *advancing* past a gated stage is blocked (`VISA-001-AC02`). |
| `GET /overseas/visa/interview-prep` | Authenticated | Self | Missing country-specific material returns an explicit fallback payload, not a 500 (`VISA-002-AC02`). |
| `GET /overseas/applications/{id}/visa-status` | Authenticated | Self / Counselor | Response copy never states EduSphere as the visa decision-maker (`VISA-003-AC02`, a content constraint, not a schema one). |

---

## 8. Agent (`AGT-*`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /agent/register` | Public | — | Creates `User` + `UserRoleAssignment(role='agent', approval_status='pending')`. |
| `POST /overseas-admin/agents/{id}/approve`, `POST /overseas-admin/agents/{id}/reject` | Authenticated | Overseas Admin | Not the stray `ADM-015` reference in `MASTER_FEATURE_CATALOG.md`'s `AGT-001` workflow text — no such feature ID exists in the catalogue (`SCR-ADM-015` in `SCREEN_CATALOG.md` is an unrelated Super Admin console screen). This endpoint is the actual approval action, owned by `AGT-001`/`ADM` module boundary, not a numbered Admin feature. Writes `AuditLog` (`SEC-001`). |
| `GET /agent/referrals` | Authenticated | Self (Agent, own referrals only) | **Not paginated** unless referral count is later shown to warrant it — bounded per agent in practice. A `Pending`/`Rejected` agent gets 403 on this route, not an empty list (`AGT-001-AC02`). |
| `GET /agent/commissions` | Authenticated | Self (Agent) | |
| `POST /agent/commissions/{id}/claim` | Authenticated | Self (Agent) | Only from `eligible`/`estimated` status (`AGT-004-AC01`). Idempotency key required. |
| `POST /overseas-admin/commissions` | Authenticated | Overseas Admin | Manual creation path, retained (`DATA_MODEL.md` §6.7 `created_by='admin_manual'`). |
| `POST /overseas-admin/commissions/{id}/approve-payout` | Authenticated | Overseas Admin, distinct actor from creation where system-triggered | **Net-new** — no equivalent existed in the base codebase (`REFERENCE_IMPLEMENTATION_FINDINGS.md` §5.4). 403 if the acting Admin is the same identity that created a manually-entered commission, where that separation is enforceable (`NFR-SEC-002`, `AGT-004-AC02`). |

---

## 9. Overseas Staff (`CNS-001`, `UNI-001`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /counselor/caseload` | Authenticated | Counselor, own assigned students only | Direct-ID access to a non-assigned student's record is 403, even via a valid record ID (`CNS-001-AC02`). |
| `GET/PATCH /counselor/students/{id}`, `/applications/{id}`, `/visa-cases/{id}`, `/appointments` | Authenticated | Counselor, assigned only | |
| `GET /university-rep/applications` | Authenticated | University Representative, own institution only | Filtered server-side by `university_id` scope (`DATA_MODEL.md` §6.10), never a client-supplied filter (`UNI-001-AC02`). |
| `PATCH /university-rep/applications/{id}/offer-status` | Authenticated | University Representative, own institution | |
| `POST /university-rep/applications/{id}/updates` | Authenticated | University Representative, own institution | Posts an admission update, visible to the assigned Counselor and Student. |

---

## 10. Notifications, Payments (system-facing, `NOT-*`, `PAY-001`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| *(internal)* notification dispatch | System (Celery task, no external route) | — | Triggered by domain events (status changes, approvals). Not a client-callable endpoint — listed for traceability only (`NOT-001`–`003`). |
| `POST /payments/webhooks/razorpay` | Provider signature (Razorpay), no session/cookie — a system endpoint | System | Actual path corrected `prompts/13`, 2026-09-03, to match the inherited `/payments` router (this document's original draft omitted the router prefix). Idempotent on the `x-razorpay-event-id` header (Razorpay's own dedup key — confirmed against Razorpay's docs, it is not a JSON body field). Never marks a `Payment` `succeeded`/generates a `Receipt` without signature verification (`PAY-001-AC03`); an invalid-signature or duplicate delivery is logged to `PaymentWebhookEvent` and discarded, never reprocessed. See `INTEGRATION_CONTRACTS.md` §2. |
| `POST /admin/payments` | Authenticated | Admin (own division; Super Admin any) | Manual/offline payment record (`STU-010-AC02`) — an Admin can record a `Payment` directly as `paid`, generating both `Invoice` and `Receipt` in the same step. |
| `POST /admin/payments/emi-schedule` | Authenticated | Admin (own division; Super Admin any) | Admin-discretionary EMI plan (`STU-010`) — each installment's amount/due date is supplied explicitly (no fixed amortization policy is confirmed anywhere), creating one linked `Payment` row per installment. |
| `POST /webhooks/twilio/status` | Provider signature (Twilio) | System | Delivery-status callback for WhatsApp/SMS — updates `NotificationLog`, does not affect the underlying business event. |
| `POST /webhooks/crm/zoho` | Shared-secret / provider signature (config-dependent, see `INTEGRATION_CONTRACTS.md` §1) | System | Inbound sync-status acknowledgement from Zoho, if the configured integration supports it — outbound direction is primary (§ Integration Contracts). |

---

## 11. GDPR (`SEC-002`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /account/data-requests` | Authenticated | Self | `type=export\|delete`. Idempotency key required (avoid duplicate request records on retry). |
| `GET /account/data-requests/{id}` | Authenticated | Self | Status polling. |
| `GET /account/data-requests/{id}/export` | Authenticated | Self | Signed download once `status=fulfilled`. |
| `POST /admin/data-requests/{id}/fulfil`, `POST /admin/data-requests/{id}/reject` | Authenticated | IT Admin | `reject` requires a `rejection_reason` referencing a retention hold (422 without one) — no silent rejection (`SEC-002-AC02`). |

---

## 12. Reporting (`RPT-001`)

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `GET /reports/enrolment-funnel`, `/course-performance`, `/student-progress` | Authenticated | IT Admin, Placement Team | Empty state for a period with no underlying data — never a zero-filled fabrication (`RPT-001-AC02`). |
| `GET /reports/employer-job-activity`, `/placement-outcomes` | Authenticated | IT Admin, Placement Team (Employer sees own subset via `/employer/*` reads, not this admin surface) | |

---

## 12A. School (`SCH-001`–`006`) — addendum, 2026-09-14

Per `RBAC_MATRIX.md` §2.12 and `DATA_MODEL.md` §6.11–6.18. School role division assignment
(`overseas`, proposed) is **not confirmed** (`RBAC_MATRIX.md` §5) — routes below are grouped under
`/school/*` and `/overseas-admin/*` regardless, since URL structure in this document is domain-based,
not division-based; the open division question does not block listing the surface itself.

| Method/Path | Auth | Scope | Notes |
|---|---|---|---|
| `POST /overseas-admin/schools` | Authenticated | Overseas Admin | Creates `School` + seed `school_coordinator` account, both active immediately, no approval gate (`SCH-003-AC02`/`AC04`). |
| `GET /overseas-admin/schools` | Authenticated | Overseas Admin | Paginated list of all partner schools. |
| `POST /school/team/invites` | Authenticated | School Coordinator, own institution | Body includes target `role` (`school_principal`/`school_teacher`/`school_parent`). 422 for any other role value (`SCH-003-AC03`). Creates a single-use `SchoolAccountInvite` token; invite delivery is email only (`NOT-001`), per `DATA_MODEL.md` §6.15's Contracts-phase note. |
| `GET /school/team` | Authenticated | School Coordinator, own institution | Lists the institution's Principal/Teacher/Parent accounts and pending invites. |
| `POST /school/invites/{token}/accept` | Public (token) | — | Single-use — an already-`accepted`/`expired`/`revoked` token returns 409, never silently re-processed (`SCH-003-AC06`). The created account's `school_id` is always the invite's own, never a value supplied in this request body — closes the same IDOR class as `AGT-002`/`UNI-001`. |
| `GET /school/dashboard` | Authenticated | School Coordinator / Principal (read-only), own institution | Aggregation read — roster counts, recent activity (`SCH-001`). |
| `GET /school/students`, `GET /school/students/{id}` | Authenticated | School Coordinator (own institution) / Principal (own institution, read-only) / Teacher (own institution, assigned students only) / Parent (own institution, own child(ren) only) | 403 (not 404) on a student that exists but is outside the acting role's scope — ownership isn't sensitive here, existence within the caller's own institution generally is not either (`SCH-001-AC02`/`AC03`, same masking convention as `TRN-002-AC02`). |
| `POST /school/students` | Authenticated | School Coordinator, own institution | Creates one `SchoolStudent`. `school_id` always server-derived from the acting Coordinator, never client-supplied (`SCH-001-AC04`). |
| `PATCH /school/students/{id}` | Authenticated | School Coordinator, own institution only | |
| `GET /school/students/roster-template` | Authenticated | School Coordinator | Downloads the fixed-format upload template — template-download-first (`SCH-002`). Exact column schema is Contracts-phase detail, not fixed by `DATA_MODEL.md` §6.13. |
| `POST /school/students/bulk-upload` | Authenticated | School Coordinator, own institution | Idempotency key required (batch-creating action). Server-side validation before any row commits (§ `SECURITY_CONTROLS.md` §5 pattern). Creates a `SchoolRosterUploadBatch`; a row that fails validation does not block the rest of the batch — each row succeeds or fails independently (`SCH-002-AC04`). |
| `GET /school/roster-uploads/{batch_id}` | Authenticated | School Coordinator, own institution | Row-level accept/reject report; `accepted_count` + `rejected_count` always equals `total_rows` once `status=completed` (`DATA_MODEL.md` §6.13). |
| `POST /school/students/{id}/attendance`, `POST /school/activities` | Authenticated | School Coordinator, own institution | Activity scheduling / attendance tracking (`SCH-001`'s main workflow). |
| `POST /school/academic-team/results` | Authenticated | Academic Team | Creates a result in `draft` status (`SCH-006`). Scope is "assigned school-affiliated students only" — the assignment mechanism itself is **not designed** (`RBAC_MATRIX.md` §5, `PRD_OPEN_ITEMS.md` item 76); this endpoint cannot yet be fully specified pending that decision, listed here for surface completeness only. |
| `PATCH /school/academic-team/results/{id}` | Authenticated | Academic Team, own assignment only | Edits a `draft` result. 409 if the result has already advanced past `draft` (`SCH-006-AC04`'s forward-only constraint). |
| `POST /school/academic-team/results/{id}/verify`, `POST /school/academic-team/results/{id}/publish` | Authenticated | Academic Team | `draft → verified → published` only, one stage at a time — a request to publish a still-`draft` result is 409, not silently accepted (`SCH-006-AC04`). Writes `SchoolResultStatusHistory`. **Open:** whether verify/publish must be a *different* `academic_team` member than the uploader, or a different role entirely, is unresolved (`PRD_OPEN_ITEMS.md` item 74) — currently unenforced pending that answer. |
| `GET /school/results` | Authenticated | Coordinator/Principal (read-only, own institution) / Teacher (assigned) / Parent (own child(ren)) | Filtered server-side to `status=published` only for every role here — a `draft`/`verified` result is never returned, **including on a direct-ID lookup, which returns 404** (existence of an unpublished result is treated as sensitive, not just its content — this document's own disclosed choice, not dictated by `DATA_MODEL.md` §6.16) (`SCH-006-AC02`). |
| `POST /school/career-counselor/records`, `PATCH /school/career-counselor/records/{id}` | Authenticated | Career Counselor | Creates/edits a `guidance_session`/`counselling_note`/`recommendation` row (`SCH-004`). Same undesigned assignment-scope caveat as the Academic Team endpoint above applies here too. |
| `GET /school/career-counselor/records` | Authenticated | Career Counselor | Own-assignment list. |
| `GET /school/career-records` | Authenticated | Coordinator (read-only, own institution) / Parent/Teacher (own scope) | No `Draft`/`Published` gate — visible immediately on creation, unlike results (`DATA_MODEL.md` §6.17). |
| `POST /school/psychometric-team/records`, `PATCH /school/psychometric-team/records/{id}` | Authenticated | Psychometric Team | Assigns an assessment / uploads a report (`SCH-005`). Same undesigned assignment-scope caveat applies. |
| `GET /school/psychometric-team/records` | Authenticated | Psychometric Team | Own-assignment list. |
| `GET /school/psychometric-records` | Authenticated | Coordinator (read-only, own institution) / Parent/Teacher (own scope) | No `Draft`/`Published` gate, same as career records. |

**Not specified — genuinely open, not invented:** exact request/response field shapes for the three
service-delivery modules (`DATA_MODEL.md` §6.16–6.18 each flag their own field list as open), the
`academic_team`/`career_counselor`/`psychometric_team` assignment-scope mechanism, and how those three
roles' own accounts are provisioned (`SCH-003`'s invite flow covers only the four school-side roles).
Do not build against an assumed answer to any of these — confirm first.

---

## 13. Traceability check

Every `CURRENT` Feature ID with `API required: Y` in `MASTER_FEATURE_CATALOG.md` has at least one
endpoint above, or is explicitly noted as a UI-only aggregation of already-listed endpoints
(`STU-002`, `VISA-002` per its own `API required: N`). Every endpoint's error/edge behavior column
cites the specific `-AC0#` it satisfies. `BLOCKED` features (`AUTH-003`, `PUB-010`, `ADM-009`–`013`,
`EMP-006`, `LMS-001`, `RPT-002`) have **no** endpoint listed — consistent with `FEATURE_ACCEPTANCE_
CRITERIA.md`'s "NOT APPLICABLE YET" markers for those IDs.

**Addendum, 2026-09-14** — `SCH-001`–`006` (all `API required: Y` in `MASTER_FEATURE_CATALOG.md`) are
now covered by §12A. Three sub-surfaces (Academic Team/Career Counselor/Psychometric Team's own
assignment scoping and account provisioning) are listed for completeness but explicitly flagged
unspecified pending open decisions — not a gap in this traceability check, a disclosed limit of it.

---

**GATE-08 APPROVED** (user, in-session, 2026-09-01 — approved as drafted, no changes requested)
alongside the other four contract documents. `prompts/10_TEST_CATALOG_AUDIT_AND_REBUILD.md` may now
proceed.

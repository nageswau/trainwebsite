# Master Feature Catalogue

## 0. Document control

| Field | Value |
|---|---|
| Version | 1.0 (**APPROVED**) |
| Date | 2026-09-01 |
| Status | **APPROVED by user (in-session), 2026-09-01** — approval given in the same instruction that requested this catalogue. GATE-05 satisfied — `prompts/07_UX_SCREEN_AND_FLOW_CATALOG.md` may proceed. Approval covers the 68 `CURRENT` features as written; it does not authorize the 10 `BLOCKED` features, and does not resolve any item in `FEATURE_QUESTIONS.md` or `docs/product/PRD_OPEN_ITEMS.md`. |
| Prerequisite satisfied | `docs/product/PRD.md` v1.0 **APPROVED** 2026-09-01 |
| Traces to | `docs/product/BRD.md` (v1.3), `docs/product/PRD.md`, `docs/decisions/PRODUCT_DECISION_REGISTER.md`, `docs/evidence/REFERENCE_IMPLEMENTATION_FINDINGS.md` |
| Companion files | `FEATURE_ACCEPTANCE_CRITERIA.md`, `USER_STORIES.md`, `FEATURE_DEPENDENCY_MAP.md`, `MVP_FEATURES.md`, `DELIVERY_SEQUENCE.md`, `FEATURE_TRACEABILITY.md`, `BACKLOG_GAP_ANALYSIS.md`, `FEATURE_QUESTIONS.md`, `feature_catalog.json` |

Generated by `prompts/06_MASTER_FEATURE_CATALOG.md`, NO-ASSUMPTION MODE. Every feature below traces to an approved BRD/PRD requirement or an explicit Decision ID (for base-codebase-inherited role actions extended per `DEC-ARCH-001`). No old blueprint/workbook backlog item was carried forward without that trace — see `BACKLOG_GAP_ANALYSIS.md` for what was explicitly excluded.

**84 features total (updated 2026-09-14, five times: `SCH-001` first added propagating
`DEC-SCOPE-009`, then rewritten and `SCH-002` added per `DEC-SCOPE-011`/`DEC-SCOPE-010`/
`DEC-ROLE-005`, then `SCH-003` added per `DEC-SCOPE-012`, then `SCH-004`/`005`/`006` added per
`DEC-ROLE-006`, then `SCH-004`/`005`/`006` refined (no new Feature ID) per `DEC-SCOPE-013`/`014`/
`DEC-ROLE-007` — see `docs/product/BRD_CHANGE_LOG.md` v1.6/v1.7/v1.8/v1.9 and `docs/product/
PRD_CHANGE_LOG.md` v1.3/v1.5/v1.6/v1.7): 74 CURRENT (buildable), 10 BLOCKED (traced to a PRD item
still marked PROPOSED — not authorized to build until confirmed). 9 are `[base]`, inherited from
the extended reference implementation rather than independently PRD-specified — flagged for PRD
backfill in `FEATURE_QUESTIONS.md`.** The original 2026-09-01 approval below covers the 68 features
as they stood that day; `SCH-001`–`006` are dated addenda, not a reopening of that approval.

Field legend: **Scope** CURRENT/BLOCKED · **Pri** P0(critical)–P3(low) · **MoSCoW** · **Cx** complexity XS–L · **UX/API/DB** required Y/N · **Status** implementation/test, both start NOT_STARTED.


## Foundation

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `FND-001` | Reference-implementation extension baseline | CURRENT | P0 | Must | Wave 0 | M | N | N | N |
| `FND-002` | Division-aware RBAC & identity framework | CURRENT | P0 | Must | Wave 0 | M | N | Y | Y |

### `FND-001` — Reference-implementation extension baseline
- **Module:** Foundation  
- **Description:** Fork/adopt the extended reference implementation (Next.js+FastAPI+PostgreSQL+Redis/Celery, modular monolith) as the working codebase.  
- **Actors:** primary — Engineering; secondary — -  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001, DEC-TECH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 0  
- **Preconditions:** Reference implementation accessible at the given path.  
- **Main workflow:** Codebase imported/forked; local dev environment runs via docker-compose.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Missing dependency/tooling halts setup; documented in LOCAL_DEVELOPMENT.md (base repo).  
- **Dependencies/blockers:** —  
- **UX required:** N · **API required:** N · **DB impact:** N · **RBAC/resource-scope:** N/A  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Secrets never committed; .env excluded.  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** Build/CI smoke test  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/12`) — codebase adopted at `apps/api`/`apps/web`; `docker compose up` (postgres, redis, api) verified healthy end-to-end · **Test status:** PASS — `docker compose build` (api+web), `npm run build`/`lint`/`tsc --noEmit` (web) all clean  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#fnd-001`  

### `FND-002` — Division-aware RBAC & identity framework
- **Module:** Foundation  
- **Description:** Deny-by-default backend RBAC enforcing division (it/overseas/global) and role on every protected route, per the base codebase's own security baseline.  
- **Actors:** primary — Engineering; secondary — All roles  
- **Traces to:** BR — · PRD PRD-AUTH-002, NFR-SEC-001 · Decisions DEC-ARCH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 0  
- **Preconditions:** FND-001 complete.  
- **Main workflow:** Every protected API route validates JWT, division, and role before executing.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Unauthorized/wrong-division request returns 401/403, never partial data.  
- **Dependencies/blockers:** FND-001  
- **UX required:** N · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** All roles  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Backend-enforced, never trust frontend role checks (base ARCHITECTURE.md).  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** Unit, API, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/12`) — `UserRoleAssignment` model/migration (`apps/api/app/models.py`, `alembic/versions/0005_user_role_assignments.py`), deny-by-default dependencies (`app/api/deps.py`: `require_division`/`require_role`/`require_permission`), resolution helpers (`app/core/rbac.py`). Not yet wired into any business-module router — that is per-feature work in `prompts/13`, not FND-002's scope · **Test status:** PASS — `apps/api/tests/test_role_assignments.py` (9 new tests) + full existing suite, 25/25 passing against a migrated Postgres  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#fnd-002`  


## Auth

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `AUTH-001` | Division-aware authenticated login | CURRENT | P0 | Must | Wave 0 | S | Y | Y | N |
| `AUTH-002` | Role-based access control enforcement (UI) | CURRENT | P0 | Must | Wave 0 | S | Y | N | N |
| `AUTH-003` | Google OAuth login | BLOCKED | P2 | Could | Unscheduled | S | Y | Y | N |

### `AUTH-001` — Division-aware authenticated login
- **Module:** Auth  
- **Description:** Separate IT and Overseas login entry points; JWT access + refresh token in HttpOnly cookies.  
- **Actors:** primary — All roles; secondary — -  
- **Traces to:** BR — · PRD PRD-AUTH-001 · Decisions DEC-ROLE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 0  
- **Preconditions:** Account exists (self-registered or admin-created).  
- **Main workflow:** User submits credentials on the correct division's login page; session established.  
- **Alternate workflow:** Password reset flow.  
- **Error/edge behavior:** Invalid credentials rejected with generic error (no user enumeration).  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** bcrypt password hashing; short-lived access JWT; Secure cookie flag in prod.  
- **Accessibility/responsive:** Responsive login form. · **Performance:** -  
- **Required test types:** Unit, API, UI, Security  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — login/register/refresh/logout/me already present from `FND-001`/`FND-002`; added forgot/reset-password UI (`ForgotPasswordForm`, `ResetPasswordForm`, 4 pages) closing the previously-missing alternate workflow · **Test status:** PASS — backend `test_role_assignments.py` (25/25 full suite) + Playwright E2E (9/9): success redirect, no-enumeration (wrong password vs. unknown email, and forgot-password), wrong-division rejection, unauthenticated redirect, label association, mobile viewport  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#auth-001`  

### `AUTH-002` — Role-based access control enforcement (UI)
- **Module:** Auth  
- **Description:** Frontend hides navigation/actions outside the user's role, mirroring backend enforcement (FND-002).  
- **Actors:** primary — All roles; secondary — -  
- **Traces to:** BR — · PRD PRD-AUTH-002 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 0  
- **Preconditions:** AUTH-001, FND-002.  
- **Main workflow:** Only role-permitted navigation/actions render.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Role-restricted UI accessed via direct URL/API is still blocked server-side (FND-002) even if briefly rendered client-side before redirect.  
- **Dependencies/blockers:** AUTH-001, FND-002  
- **UX required:** Y · **API required:** N · **DB impact:** N · **RBAC/resource-scope:** All roles  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Frontend visibility is UX only, never the security boundary.  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — dashboard-level enforcement already existed structurally (Server Component data fetch blocks before any HTML is sent, per `portal.py`'s role/division check); added `HeaderAuthActions.tsx` to make the public-site header session-aware (was previously showing "Login" unconditionally even when authenticated) · **Test status:** PASS — Playwright E2E 4/4 (`tests/e2e/auth-002-rbac-ui.spec.ts`), including a direct check that the raw HTTP response body for a cross-role dashboard request never contains the other role's content  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#auth-002`  

### `AUTH-003` — Google OAuth login
- **Module:** Auth  
- **Description:** Optional Google sign-in alongside credential login.  
- **Actors:** primary — Student, Trainer, Admin, Employer, Agent; secondary — -  
- **Traces to:** BR — · PRD PRD-AUTH-003 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** BLOCKED / P2 / Could / Unscheduled  
- **⚠ Blocked because:** PRD-AUTH-003 is PROPOSED (unconfirmed) — sourced only from the original tech-stack document, never independently re-confirmed via the Decision Register.  
- **Preconditions:** AUTH-001.  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** -  
- **Dependencies/blockers:** AUTH-001  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** Google OAuth  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** Unit, API  
- **Complexity:** S  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#auth-003`  


## Public Website

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `PUB-001` | Corporate & IT marketing content | CURRENT | P1 | Must | Wave 1 | M | Y | Y | Y |
| `PUB-002` | Enquiry submission synced to CRM | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `PUB-003` | Course catalogue and detail | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `PUB-004` | Webinar listing and registration | CURRENT | P2 | Should | Wave 2 | S | Y | Y | Y |
| `PUB-005` | News and gallery (CMS-managed) `[base]` | CURRENT | P3 | Could | Wave 3 | S | Y | Y | Y |
| `PUB-010` | Site search, FAQ, and legal pages | CURRENT | P2 | Could | Wave 4 | S | Y | Y | N |

### `PUB-001` — Corporate & IT marketing content
- **Module:** Public Website  
- **Description:** Home, About, Career Paths (+detail), Real Projects (+detail), Success Stories (+detail), Business Services, Blog — public, unauthenticated content browsing.  
- **Actors:** primary — Visitor; secondary — -  
- **Traces to:** BR BR-PUB-001 · PRD PRD-PUB-001, PRD-PUB-004, PRD-PUB-005, PRD-PUB-006, PRD-PUB-007, PRD-PUB-008, NFR-RESP-001 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Must / Wave 1  
- **Preconditions:** None.  
- **Main workflow:** Visitor browses list/detail pages.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Unpublished/missing content shows empty state, not an error.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public  
- **Integration/job/file/audit impact:** CMS (base)  
- **Security/privacy:** No PII collected.  
- **Accessibility/responsive:** Responsive; SEO metadata per page. · **Performance:** Static/CDN-cacheable.  
- **Required test types:** UI, Accessibility, Responsive  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — Home/About/Programs/Blog already adopted from `FND-001`; built the genuinely missing pieces: `CareerPath`/`RealProject` models+migration+list/detail pages, Success Stories pages (reusing `Testimonial`), Business Services page (reusing `ContentPage`), nav entries in `IT_PUBLIC` · **Test status:** PASS — 8 new backend pytest cases (33/33 suite), 6 new Playwright E2E cases (18/18 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#pub-001`  

### `PUB-002` — Enquiry submission synced to CRM
- **⚠ Direction superseded 2026-09-12 (`DEC-SCOPE-001` re-resolved, asked directly and confirmed
  in-session):** the description below is accurate to what is actually shipped today (Zoho-webhook
  sync), and is left as-is per this catalogue's "as-built" convention. The confirmed *direction* is
  now a custom-built Internal CRM, not Zoho-only — this entry has **not** been redesigned, since the
  required CRM workflow document is still pending from the client. See `docs/product/
  PRD_OPEN_ITEMS.md` item 61 before touching this feature.
- **Module:** Public Website  
- **Description:** Contact/request-callback form; enquiry reaches Admin and syncs via the CRM webhook (configured for Zoho).  
- **Actors:** primary — Visitor; secondary — Admin  
- **Traces to:** BR BR-PUB-001 · PRD PRD-PUB-002 · Decisions DEC-SCOPE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** None (public form).  
- **Main workflow:** Submission creates an enquiry record; webhook fires to Zoho.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Webhook failure must not lose the locally-captured enquiry (retry/queue).  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public submit / Admin view  
- **Integration/job/file/audit impact:** Zoho CRM webhook  
- **Security/privacy:** Enquiry data is personal data — GDPR-scoped.  
- **Accessibility/responsive:** Responsive form; accessible labels. · **Performance:** -  
- **Required test types:** API, Integration  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — found the inherited endpoint awaited the CRM webhook inline (no retry, blocking); added `sync_enquiry_to_crm_task` (Celery, exponential backoff, 5 retries) and switched to dispatch-and-return, matching `INTEGRATION_CONTRACTS.md` §1's outbox pattern. Also fixed `EnquiryForm.tsx`'s label association (no `id`/`htmlFor` existed despite this feature's own "accessible labels" claim) · **Test status:** PASS — 5 new backend pytest cases (38/38 suite), 4 new Playwright E2E cases (22/22 suite: response-latency, cross-division, admin division-scoped visibility, label association)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#pub-002`  

### `PUB-003` — Course catalogue and detail
- **Module:** Public Website  
- **Description:** Search/filter course catalogue; course detail (curriculum, fee, duration, certification, placement-support flags).  
- **Actors:** primary — Visitor; secondary — -  
- **Traces to:** BR BR-PUB-001 · PRD PRD-PUB-003 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** None.  
- **Main workflow:** Visitor searches/filters/opens a course.  
- **Alternate workflow:** -  
- **Error/edge behavior:** No results → empty state with guidance.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive; SEO. · **Performance:** Pagination for large catalogues.  
- **Required test types:** UI, API  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`; catalogue UI refreshed 2026-09-10) — the dedicated `ProgramCatalogue` now provides responsive category tabs/counts, search, sort, pagination, URL-addressable state, distinct unavailable/empty/no-match states, and data-backed programme cards; detail pages retain programme-specific `generateMetadata`. No enrolment counts, discounts, or public schedules are fabricated because the public programme contract does not expose governed values for them · **Test status:** PASS — historic backend evidence retained (7 cases, 45/45 suite); current focused checks: production web build, TypeScript, ESLint, and 6/6 Playwright E2E cases (search/empty recovery, category URL state, detail fields, per-page SEO, graceful 404, 375px responsive/no overflow)
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#pub-003`  

### `PUB-004` — Webinar listing and registration
- **Module:** Public Website  
- **Description:** Upcoming/past webinars listing; registration capture.  
- **Actors:** primary — Visitor; secondary — -  
- **Traces to:** BR BR-PUB-001 · PRD PRD-PUB-009 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 2  
- **Preconditions:** None.  
- **Main workflow:** Visitor registers for a webinar.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Capacity/waitlist rule unspecified — open item.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Registration data is personal data — GDPR-scoped.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI, API  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`) — reused the existing `Event` model (division-aware, already seeded with an IT webinar) for listing rather than the `Webinar` table `DATA_MODEL.md` §2.4 originally assumed carried over (same discovery pattern as `PUB-001`'s `CareerPath`/`RealProject` correction — see §2.4's own correction note); added the genuinely-missing piece, in-app registration capture (`webinar_registrations`, alembic `0009`), since `Event.registration_url` only ever pointed at an external link. Detail route is `/it/webinars/[id]` (UUID), not `[slug]`, as `Event` has no slug column and none is required by confirmed evidence. Past-webinar registration is rejected server-side (409), not just hidden in the UI (`PUB-004-AC02`); capacity/waitlist stays unenforced per the documented open item · **Test status:** PASS — 7 new backend pytest cases (143/143 suite), 5 new Playwright E2E cases (72/72 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#pub-004`  

### `PUB-005` — News and gallery (CMS-managed)
- **Module:** Public Website  
- **Description:** Published news articles and gallery items, managed via CMS/Admin panel.  
- **Actors:** primary — Visitor; secondary — Admin  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 3  
- **Preconditions:** CMS content published.  
- **Main workflow:** Visitor browses; Admin publishes via CMS.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Empty state when nothing published.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public view / Admin publish  
- **Integration/job/file/audit impact:** S3 (media)  
- **Security/privacy:** Upload malware scanning recommended (base ARCHITECTURE.md).  
- **Accessibility/responsive:** Responsive. · **Performance:** CDN for media.  
- **Required test types:** UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 381/381 backend full regression, 9 new backend cases, 3 new E2E cases  
  - News (`BlogPost`) already had full admin CRUD (`GET/POST /cms/posts`, `GET /cms/manage/posts`, `PATCH /cms/posts/{id}`) and a working public `/news` page with an honest empty state ("No published news yet. Administrators can publish posts through the CMS.") -- confirmed directly, no gap. The real, confirmed gap: `GalleryItem` had zero write/manage endpoints anywhere in the codebase -- only the public read (`GET /public/gallery`) and seed-time direct DB inserts existed, so an Admin genuinely could not publish a gallery item at all through the application, violating `PUB-005-AC01`'s "Admin publishes via CMS" half. Added `GET /cms/manage/gallery`, `POST /cms/gallery`, `PATCH /cms/gallery/{id}`, mirroring the exact `posts` pattern already established in `cms.py` (same `require_editor` division-scoping -- IT/Overseas Admin within their own division, Super Admin cross-division -- and the same audit-log shape) -- no schema change needed, every field the endpoints use (`division`, `title`, `image_url`, `alt_text`, `category`, `published`) already existed on the model. Added the missing "Gallery Management" Admin console section from scratch -- nav entry, table (`admin/[module]/page.tsx`), and create/update action forms (`WorkflowPanel.tsx`) -- none of it existed before, matching the pattern "content"/"blogs"/"events" already used. Given the shared `cms.py` change, ran the full backend regression rather than a module-scoped one. Backend: 9 new pytest cases -- Admin creates a gallery item in their own division, a cross-division Admin is rejected, Super Admin creates in any division, update toggles `published`, an unknown item 404s, a non-admin role is rejected, auth required, the manage list correctly shows unpublished items the public endpoint hides, the manage-list endpoint itself requires an admin role (`test_pub_005_gallery_management.py`). E2E: 3 new Playwright cases -- a Super Admin publishes a gallery item through the real "Publish gallery item" UI action, a non-admin is 403'd at the API layer, unauthenticated access to the console redirects to `/admin/login`. Deliberately did not also assert the freshly-published item appears on the real public `/gallery` page within the same test run -- `apps/web/lib/api.ts`'s `publicApi()` caches with `next: { revalidate: 60 }`, making that non-deterministic inside one run (the same known Next.js fetch-cache transient-staleness pattern already documented for other public-content pages, reconfirmed directly this session when `pub-001-content.spec.ts`'s unrelated specs failed then passed cleanly on an immediate rerun); the public read path itself is pre-existing, unchanged code, not this feature's own gap.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#pub-005`  

### `PUB-010` — Site search, FAQ, and legal pages
- **Module:** Public Website  
- **Description:** Cross-site search, FAQ, Privacy Policy, Terms & Conditions, Cookie Preferences.  
- **Actors:** primary — Visitor; secondary — -  
- **Traces to:** BR — · PRD PRD-PUB-010 · Decisions DEC-SCOPE-008  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Could / Wave 4  
- **Preconditions:** -  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** Legal pages (Privacy/Terms/Cookie) ship as explicitly-labeled non-final placeholder content only — real legal sign-off is `DEC-PRIV-001`, still open. Site search and FAQ are real, full scope.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Public  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Privacy/Terms content must reflect actual GDPR posture, not boilerplate — placeholder content must be clearly labeled non-final, never presented as final legal text (`DEC-SCOPE-008`).  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** S  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#pub-010`  


## Student

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `STU-001` | Trainer/batch slot enrolment | CURRENT | P0 | Must | Wave 1 | M | Y | Y | Y |
| `STU-002` | Student dashboard | CURRENT | P0 | Must | Wave 1 | M | Y | Y | N |
| `STU-003` | Live class access | CURRENT | P0 | Must | Wave 1 | S | Y | Y | N |
| `STU-004` | Assignment submission | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `STU-005` | Support ticket | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `STU-006` | Attendance and progress view | CURRENT | P1 | Should | Wave 2 | XS | Y | Y | N |
| `STU-007` | Certificate download | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `STU-008` | Feedback submission | CURRENT | P2 | Could | Wave 2 | XS | Y | Y | Y |
| `STU-009` | Digital agreement / consent | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `STU-010` | Fee payment, EMI, invoices, receipts | CURRENT | P0 | Must | Wave 1 | M | Y | Y | Y |
| `STU-011` | Profile and document management | CURRENT | P1 | Should | Wave 2 | XS | Y | Y | Y |

### `STU-001` — Trainer/batch slot enrolment
- **Module:** Student  
- **Description:** Student selects an available trainer/time-slot at enrolment; 20/slot capacity; locked once booked (~3 months).  
- **Actors:** primary — Student; secondary — IT Admin  
- **Traces to:** BR BR-STU-001 · PRD PRD-STU-001 · Decisions DEC-WF-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** Student has selected a course/track; payment initiated or completed per business rule.  
- **Main workflow:** Student browses capacity-aware slot list and books one.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Booking a full slot is rejected; changing a locked slot is rejected.  
- **Dependencies/blockers:** FND-002, PUB-003  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Student)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive slot picker. · **Performance:** -  
- **Required test types:** Unit, API, UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — backend capacity-aware listing/booking already existed (row-locked capacity check); fixed `slot_locked` being client-controlled (now always server-set). Built the actual "slot picker" this feature's own catalogue entry named — the inherited frontend only had a bare batch-UUID text field, no real browsing UI · **Test status:** PASS — 6 new backend pytest cases (51/51 suite), 4 new Playwright E2E cases (31/31 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-001`  

### `STU-002` — Student dashboard
- **Module:** Student  
- **Description:** Course progress, attendance, pending fees, applied jobs, upcoming assignments in one view.  
- **Actors:** primary — Student; secondary — -  
- **Traces to:** BR — · PRD PRD-STU-002 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** STU-001.  
- **Main workflow:** Dashboard aggregates data from enrolment, attendance, payments, assignments.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A failed data source for one dashboard widget (e.g. payments) does not block the rest of the dashboard from rendering.  
- **Dependencies/blockers:** STU-001  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** Aggregation query performance for scale.  
- **Required test types:** UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — added the missing "Applied jobs" metric; fixed `STU-002-AC02`'s real violation (one failed query poisoned the whole aggregation) via SAVEPOINT-isolated `_safe()` helper in `app/services/portal.py` · **Test status:** PASS — 5 new backend pytest cases including a forced-failure isolation test (56/56 suite), 4 new Playwright E2E cases (35/35 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-002`  

### `STU-003` — Live class access
- **Module:** Student  
- **Description:** Join scheduled live class via Zoho (default) or Google Meet (retained).  
- **Actors:** primary — Student; secondary — Trainer  
- **Traces to:** BR BR-LIVE-001 · PRD PRD-STU-003 · Decisions DEC-LIVE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** Session scheduled by Trainer (TRN-003).  
- **Main workflow:** Student joins from dashboard at session time.  
- **Alternate workflow:** -  
- **Error/edge behavior:** No recording available for a session hit by the concurrency limit — accepted limitation, not a fault.  
- **Dependencies/blockers:** TRN-003  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** Zoho Meeting, Google Meet  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, Integration  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — backend scoping/host_url-hiding already correct; built `LiveClassesPanel.tsx`, the actual "join" UI (previously an unclickable text dump), and cleaned up the now-duplicated crude panels this and `STU-001` had left in the course-section payload · **Test status:** PASS — 6 new backend pytest cases (62/62 suite), 4 new Playwright E2E cases (39/39 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-003`  

### `STU-004` — Assignment submission
- **Module:** Student  
- **Description:** View assigned work, submit before due date, see trainer score/feedback.  
- **Actors:** primary — Student; secondary — Trainer  
- **Traces to:** BR — · PRD PRD-STU-004 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** TRN-005 assignment created. *(corrected `prompts/13`, 2026-09-01 — the approved catalogue said "TRN-006 assignment created," but TRN-006 is Assessment create/edit; TRN-005 is Assignment create/edit, matching this precondition's own wording.)*  
- **Main workflow:** Student submits; Trainer grades (TRN-007).  
- **Alternate workflow:** Late submission per trainer rule.  
- **Error/edge behavior:** Submission after due date flagged, not silently accepted unless allowed.  
- **Dependencies/blockers:** TRN-005  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** File storage  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive; file upload accessible. · **Performance:** -  
- **Required test types:** Unit, API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — backend already scoped submission to the student's own enrolled batch (403 otherwise) and required an answer or file; the real gap (`STU-004-AC02`) was that a late submission was silently accepted as on-time. Added `Submission.is_late` (migration `0007`), computed server-side on submit/resubmit, surfaced in the API response, the trainer-facing grid, and the student's own confirmation message. Replaced the old crude "type an assignment UUID" generic form with `AssignmentSubmissionPanel.tsx` (per-assignment inline form, correct ID wired automatically) · **Test status:** PASS — 7 new backend pytest cases (69/69 suite), 3 new Playwright E2E cases (42/42 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-004`  

### `STU-005` — Support ticket
- **Module:** Student  
- **Description:** Raise and track a support ticket; Trainer/Admin resolves.  
- **Actors:** primary — Student; secondary — Trainer, Admin  
- **Traces to:** BR — · PRD PRD-STU-005 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** Student is authenticated.  
- **Main workflow:** Student raises ticket; staff responds/resolves.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A ticket with no assigned staff member remains visibly unassigned, not silently lost.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self / assigned staff  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — the student side (raise + self-scoped list) already existed and worked; staff resolution genuinely didn't: `DATA_MODEL.md` §4.5's own stated `assigned_to_user_id` field was missing entirely (added via alembic `0010`, along with a net-new `resolution_note`), no `PATCH /workflows/support/{id}` existed, and neither Trainer nor IT Admin had a "support" workspace at all (`PORTAL_NAV` lacked the entry, so the route 404'd). Server auto-claims an unassigned ticket to whichever staff member first acts on it — never a client-supplied assignee. Ticket creator is notified on resolution (matching this codebase's existing `_notify_user` convention for every other staff-resolves-something-for-a-student action). Also found and fixed a real, reproducible bug while testing this feature: `WorkflowPanel.tsx`'s shared `ActionForm` (used by this ticket's own creation form, among many others) read `event.currentTarget` *after* an `await fetch`, which the DOM nulls once dispatch finishes — intermittently threw on submit; fixed in `ActionForm`, `DocumentUpload` (same file), and `PUB-004`'s own `WebinarRegisterForm.tsx`; the same pattern remains in three untouched files, tracked as `RAID.md` I-05 · **Test status:** PASS — 8 new backend pytest cases (151/151 suite), 3 new Playwright E2E cases (75/75 suite; `adm-005` remains the pre-existing, already-tracked I-04 parallel-load flake, confirmed unrelated)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-005`  

### `STU-006` — Attendance and progress view
- **Module:** Student  
- **Description:** Student views own attendance record and course/module completion percentage with outstanding blockers.  
- **Actors:** primary — Student; secondary — -  
- **Traces to:** BR — · PRD PRD-STU-006, PRD-STU-007 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** TRN-008 attendance marked.  
- **Main workflow:** Student views read-only attendance/progress.  
- **Alternate workflow:** -  
- **Error/edge behavior:** No attendance recorded yet for a session shows as not-yet-marked, never defaulted to absent.  
- **Dependencies/blockers:** TRN-008  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — the read-only session list and its self-scoped RBAC (`GET /workflows/it/student/attendance`) already existed and already had test coverage, added incidentally by `TRN-008`'s own test file. The actual gap was PRD-STU-007, folded into this feature: "module/course completion percentage and outstanding blockers (e.g. unfinished assignments)" was specified but never shown anywhere on the student's attendance view. Added `Course progress` and `Attendance` % metrics and an `Outstanding blockers` panel (published assignments in the student's batch with no submission yet) to `GET /portal/it/student/attendance`'s payload, alongside the pre-existing session table and correction-request panel. STU-006-AC02 holds by construction, not by extra code: `Attendance` rows are only ever created when a trainer actually marks a session, so an unmarked session is simply absent from the response — never synthesized as "absent" · **Test status:** PASS — 5 new backend pytest cases (156/156 suite: progress/blockers metrics, no-sessions-yet never defaults to absent, self-scope isolation, non-student role rejected, auth required), 1 new Playwright E2E case (76 total; `adm-005` is the pre-existing, already-tracked `RAID.md` I-04 parallel-load flake, confirmed unrelated by running it in isolation)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-006`  

### `STU-007` — Certificate download
- **Module:** Student  
- **Description:** Download earned certificates once eligibility criteria are met.  
- **Actors:** primary — Student; secondary — Admin  
- **Traces to:** BR — · PRD PRD-STU-008 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** Course completion criteria met.  
- **Main workflow:** Student downloads certificate (auto-generated or admin-uploaded — mechanism open).  
- **Alternate workflow:** -  
- **Error/edge behavior:** Certificate unavailable before eligibility met.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive; accessible PDF. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — issuance (`ADM-006`) and the read-only certificate listing (`GET /portal/it/student/certificates`) already existed. The real gap: that listing's "file" column exposed the raw, permanently-public `Certificate.file_url` through the generic table (which only ever renders plain text, never a working link) — not the "signed, short-lived URL... never a public object link" `API_CONTRACT.md` specifies for this feature. Added `GET /workflows/it/certificates/{id}/download` (self-scoped, admin-accessible, derives the storage key from `certificate_no` rather than trusting the stored `file_url`) and a real `CertificateDownloadPanel.tsx` action, replacing the inert table cell — same pattern as `STU-001`/`STU-003`/`STU-004`'s dedicated panels. Also found and fixed a real, adjacent bug introduced by this feature's own design before it shipped: local-storage mode's presigned URL (`/local-files/...`) is relative to the API container, not the web app, and nothing proxied that path, so a real click would have 404'd — added `apps/web/app/local-files/[...path]/route.ts`, mirroring the existing `/api/*` proxy route (S3 mode, already an absolute URL, is unaffected). STU-007-AC02 (certificate unavailable before eligibility) holds by construction — no `Certificate` row exists until issued · **Test status:** PASS — 6 new backend pytest cases (162/162 suite: list+download, cross-student 403, admin override-access, 404 on unknown id, empty-list-not-error, auth required), 2 new Playwright E2E cases (78 total; `adm-005`'s pre-existing, already-tracked `RAID.md` I-04 contention flake is the only non-pass)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-007`  

### `STU-008` — Feedback submission
- **Module:** Student  
- **Description:** Submit feedback on a course/trainer.  
- **Actors:** primary — Student; secondary — -  
- **Traces to:** BR — · PRD PRD-STU-009 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Could / Wave 2  
- **Preconditions:** Student is enrolled in the course/trainer being reviewed.  
- **Main workflow:** Student submits feedback form.  
- **Alternate workflow:** Anonymous option (open item).  
- **Error/edge behavior:** A student cannot submit feedback for a course/trainer they are not enrolled with.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — `DATA_MODEL.md` §4.6 described `CourseFeedback` as "carries over" from the reference implementation, but no such table (or any other feedback model) existed anywhere in the codebase — the same "doc says X carries over but never existed" pattern already found once for `STU-005`'s `assigned_to_user_id`. Net-new: `CourseFeedback` model (alembic `0011_course_feedback`), `POST /workflows/it/student/feedback` (422, not a silent drop, when no matching `Enrollment` exists for the target batch — `STU-008-AC03`), a `feedback` portal section listing the student's own enrolled batches with per-batch submission status, and `FeedbackSubmissionPanel.tsx`. The PRD's own anonymous-submission alternate path (`STU-008-AC02`) is an explicitly unconfirmed, PROPOSED-only open item (`PRD-STU-009`) — deliberately not built; every feedback row is identified · **Test status:** PASS — 6 new backend pytest cases (168/168 suite: submit, reject-if-not-enrolled, rating out of 1–5 range rejected, non-student role rejected, listing reflects submission status, auth required), 3 new Playwright E2E cases (81 total; `adm-005`'s pre-existing, already-tracked `RAID.md` I-04 contention flake is the only non-pass)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-008`  

### `STU-009` — Digital agreement / consent
- **Module:** Student  
- **Description:** Review and accept a digital agreement as part of enrolment.  
- **Actors:** primary — Student; secondary — -  
- **Traces to:** BR — · PRD PRD-STU-010 · Decisions DEC-PRIV-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** STU-001 enrolment initiated.  
- **Main workflow:** Student reviews and accepts agreement; acceptance timestamped.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Enrolment cannot complete without acceptance.  
- **Dependencies/blockers:** STU-001  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Consent capture point for GDPR.  
- **Accessibility/responsive:** Responsive; accessible document viewer. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — `DATA_MODEL.md` §3.4's own approved constraint ("`Enrollment.status` cannot reach `active` without a matching `ConsentRecord`") was entirely unimplemented; every booking went straight to `active`. Added `Agreement`/`ConsentRecord` models (migration `0008`), a `pending_consent` enrolment status for new bookings, `GET/POST /workflows/it/student/agreements/current`/`{id}/accept`, and `AgreementConsentPanel.tsx` (review + accept UI). Capacity accounting (`DEC-WF-002`) preserved across all 5 call sites that count enrolled seats, so a not-yet-consented booking still locks its slot · **Test status:** PASS — 6 new backend pytest cases (75/75 suite), 3 new Playwright E2E cases (45/45 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-009`  

### `STU-010` — Fee payment, EMI, invoices, receipts
- **Module:** Student  
- **Description:** Payment dashboard (total/paid/pending/next due), EMI/instalment timeline, invoice/receipt access — Razorpay only (Stripe removed, `DEC-PAY-001` re-resolved 2026-09-03).  
- **Actors:** primary — Student; secondary — IT Admin  
- **Traces to:** BR BR-PAY-001 · PRD PRD-STU-011, PRD-PAY-001, PRD-PAY-002, PRD-PAY-003 · Decisions DEC-PAY-001, DEC-PAY-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** STU-001 enrolment.  
- **Main workflow:** Student pays via Razorpay; invoice/receipt generated; EMI schedule tracked.  
- **Alternate workflow:** Manual/offline payment record (base codebase).  
- **Error/edge behavior:** Failed payment does not mark fee as paid; webhook signature must verify.  
- **Dependencies/blockers:** STU-001  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Student) / Admin (view)  
- **Integration/job/file/audit impact:** Razorpay  
- **Security/privacy:** PCI scope minimized via hosted checkout; webhook signature verification required before accepting.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** Unit, API, Integration, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-03, `prompts/13`) — unblocked by real Razorpay TEST-mode credentials landing in `.env`. The base checkout/webhook mechanics already existed (`api/payments.py`); the real gaps were the four models `DATA_MODEL.md` §7.2 names (`Invoice`/`Receipt`/`EMISchedule`/`PaymentWebhookEvent` — mislabeled "carries over" there, none existed in code) plus `API_CONTRACT.md` §0.2's `Idempotency-Key` gate. Added all four (migration `0022`), `_ensure_invoice`/`_ensure_receipt` (PRD-PAY-003, reusing STU-007's PDF+signed-download pattern), an admin-discretionary `POST /admin/payments/emi-schedule` (no fixed amortization policy is confirmed anywhere, so installment amounts/dates are Admin-supplied, not computed), and `FeePaymentPanel.tsx` (replacing the raw-UUID "Start checkout" generic form — same UX gap class as `ADM-001`/`003`/`005`/`007`). Found+fixed a real `STU-010-AC04` gap: any admin role could trigger checkout on another user's payment (RBAC_MATRIX.md's admin scope is "view", not "act"). Also found+corrected a stale `.env` leftover from the superseded 2026-09-01 Stripe decision (comment + two empty `STRIPE_*` vars) that `DEC-PAY-001`'s 2026-09-03 re-resolution had already named for removal but never actually got removed · **Test status:** PASS — 17 new backend pytest cases in `test_pay_001_stu_010_payment_gateway.py`, including real calls against the live Razorpay TEST-mode API (order creation) and real HMAC verification against the real `RAZORPAY_WEBHOOK_SECRET` — closing the "needs real webhook signature verification" blocker directly rather than mocking it  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-010`  

### `STU-011` — Profile and document management
- **Module:** Student  
- **Description:** Maintain own profile (contact info; CV/portfolio fields open).  
- **Actors:** primary — Student; secondary — -  
- **Traces to:** BR — · PRD PRD-STU-012 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** Student is authenticated.  
- **Main workflow:** Student edits profile fields.  
- **Alternate workflow:** -  
- **Error/edge behavior:** An invalid file type/size on document upload is rejected with a clear message, not silently dropped.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — profile field editing already existed and worked (`PATCH /auth/me`, inherently self-scoped since there is no id parameter to target another user with) but had zero test evidence anywhere in the suite. Document management genuinely did not exist: `StudentDocument` is Overseas-division-specific (FK to `overseas_applications`, gated to overseas roles) — not reusable for an IT student's own CV/portfolio uploads without conflating two different domains, and `DATA_MODEL.md` names no table for this feature at all. Net-new: `ProfileDocument` model (alembic `0012_profile_documents`), `GET/POST /workflows/it/student/profile/documents` (415/413, not a silent drop, on a disallowed file type or oversized file — `STU-011-AC02`, reusing the exact same `_allowed()`/`max_upload_bytes` check `/files/presign` already applies), and `ProfileDocumentUpload.tsx`, reusing the same presign-then-upload flow as the Overseas `DocumentUpload` panel · **Test status:** PASS — 7 new backend pytest cases (175/175 suite: profile edit, upload+list, disallowed type rejected, oversized file rejected, self-scope isolation, non-student role rejected, auth required), 3 new Playwright E2E cases (84 total, all green)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#stu-011`  


## Trainer

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `TRN-001` | My Batches | CURRENT | P0 | Must | Wave 1 | XS | Y | Y | N |
| `TRN-002` | Batch detail (roster and schedule) | CURRENT | P0 | Must | Wave 1 | S | Y | Y | N |
| `TRN-003` | Upcoming sessions and live-class join | CURRENT | P0 | Must | Wave 1 | S | Y | Y | N |
| `TRN-004` | Recording list and resource upload | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `TRN-005` | Assignment create and edit | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `TRN-006` | Assessment create and edit | CURRENT | P1 | Should | Wave 2 | M | Y | Y | Y |
| `TRN-007` | Submission review and grading | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `TRN-008` | Attendance marking | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `TRN-009` | Q&A response | CURRENT | P1 | Should | Wave 2 | XS | Y | Y | Y |

### `TRN-001` — My Batches
- **Module:** Trainer  
- **Description:** List of batches assigned to the authenticated Trainer only.  
- **Actors:** primary — Trainer; secondary — -  
- **Traces to:** BR — · PRD PRD-TRN-001 · Decisions DEC-ROLE-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** Trainer has at least one assigned batch (ADM-003).  
- **Main workflow:** Trainer views own batch list.  
- **Alternate workflow:** -  
- **Error/edge behavior:** No batches → empty state.  
- **Dependencies/blockers:** ADM-003  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self (Trainer)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — no code change needed: `GET /portal/it/trainer/dashboard` already correctly lists the authenticated trainer's own batches (`Batch.trainer_id == user.id`) with proper empty state and deny-by-default RBAC (`portal.py` division/role route match, 403 otherwise) already in place. The actual gap was test evidence, not implementation · **Test status:** PASS — 4 new backend pytest cases (79/79 suite), 2 new Playwright E2E cases (47/47 suite)
  **Follow-up (2026-09-05, tester feedback via WhatsApp 2026-09-04, `RAID.md` I-15):** "one table was coming outside from alignment" -- confirmed at 390px this wasn't the `DataTable` (its own scroll wrapper correctly contains it) but the shared `PortalShell.tsx` sidebar nav, a fixed bottom bar below 640px with unindicated horizontal scroll -- Trainer's own 9-item nav truncated mid-word with a hard edge, violating `RESPONSIVE_RULES.md`'s confirmed "no horizontal scroll... collapses behind a toggle" baseline. Fixed by generalizing the existing public-site `MobileNavToggle` pattern rather than inventing a new one; applies to every role's portal (shared component). 3 new E2E cases, confirmed live in a real browser.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-001`  

### `TRN-002` — Batch detail (roster and schedule)
- **Module:** Trainer  
- **Description:** Open a batch: student roster (capped at 20 per DEC-WF-002) and schedule/status.  
- **Actors:** primary — Trainer; secondary — -  
- **Traces to:** BR — · PRD PRD-TRN-002 · Decisions DEC-WF-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** TRN-001.  
- **Main workflow:** Trainer opens batch, sees roster+schedule.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Roster scoped strictly to that batch.  
- **Dependencies/blockers:** TRN-001, STU-001  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self (Trainer, own batch only)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — `GET /workflows/it/trainer/context` already returned the trainer's own batches and active enrolments correctly scoped; extended it with the missing schedule/status detail (start/end date, status, mode, timezone, enrolled_count) and built `BatchRosterPanel.tsx` -- each assigned batch renders as its own card with its own roster, never mixed across batches (`DEC-WF-002`) · **Test status:** PASS — 5 new backend pytest cases (84/84 suite), 2 new Playwright E2E cases (49/49 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-002`  

### `TRN-003` — Upcoming sessions and live-class join
- **Module:** Trainer  
- **Description:** Dashboard lists upcoming sessions across assigned batches with a join link to the live class.  
- **Actors:** primary — Trainer; secondary — Student  
- **Traces to:** BR — · PRD PRD-TRN-003 · Decisions DEC-LIVE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** TRN-001; batch has a scheduled session.  
- **Main workflow:** Trainer views/joins upcoming session.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Not-yet-joinable session shown as such, not an error.  
- **Dependencies/blockers:** TRN-001  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self (Trainer)  
- **Integration/job/file/audit impact:** Zoho Meeting, Google Meet  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, Integration  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — `GET /communications/it/live-sessions` already scoped the trainer to their own batches and returned `host_url` for trainer/admin roles; the frontend `LiveClassesPanel` (built for `STU-003`) always used `meeting_url`, never rendering `host_url` at all. Generalized it to prefer `host_url` when present and added an `upcomingOnly` dashboard mode (sessions not yet concluded, soonest-first) · **Test status:** PASS — 5 new backend pytest cases (89/89 suite), 2 new Playwright E2E cases (51/51 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-003`  

### `TRN-004` — Recording list and resource upload
- **Module:** Trainer  
- **Description:** Past session recordings and course resource/notes upload, visible to enrolled Students.  
- **Actors:** primary — Trainer; secondary — Student  
- **Traces to:** BR — · PRD PRD-TRN-004, PRD-TRN-005 · Decisions DEC-LIVE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** TRN-002.  
- **Main workflow:** Trainer uploads resource / recording appears post-session.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Missing recording (concurrency limit) shown as unavailable, not an error.  
- **Dependencies/blockers:** TRN-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Trainer) / enrolled Students (view)  
- **Integration/job/file/audit impact:** S3 storage  
- **Security/privacy:** Restricted upload types/sizes.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — needed no code change at all: `PATCH /communications/it/live-sessions/{id}/recording` (own-batch RBAC) already fed the `GET .../live-sessions` list `TRN-003`/`STU-003` cover, with `LiveClassesPanel.tsx` already rendering "Watch recording" or a clean "not available" message for both roles, and `POST /workflows/it/trainer/materials` (own-batch RBAC) already fed the student "downloads" portal section — both built earlier without this Feature ID ever being cited, and neither had any test evidence under it. Added evidence for both, including the trainer's own recording-attach action, which nothing previously exercised (only the student-facing read side was proven). TRN-004-AC02 (missing recording shown as unavailable, not an error) holds by construction, the same mechanism already proven for `STU-003` · **Test status:** PASS — 7 new backend pytest cases (182/182 suite: recording attach + visible to enrolled student, missing-recording clean state, cross-trainer 403 on both recording-attach and resource-upload, resource visible only to the uploading trainer's own enrolled students, auth required on both endpoints), 3 new Playwright E2E cases (87 total; `adm-005`'s pre-existing, already-tracked `RAID.md` I-04 contention flake is the only non-pass)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-004`  

### `TRN-005` — Assignment create and edit
- **Module:** Trainer  
- **Description:** Create/edit assignments for a batch with due date and rubric.  
- **Actors:** primary — Trainer; secondary — Student  
- **Traces to:** BR — · PRD PRD-TRN-006 · Decisions DEC-ROLE-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** TRN-002.  
- **Main workflow:** Trainer authors assignment; visible to batch students by due date.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Editing an assignment after student submissions exist does not retroactively invalidate work already submitted.  
- **Dependencies/blockers:** TRN-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Trainer, own batch)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-01, `prompts/13`) — create already existed (`POST /workflows/it/trainer/assignments`, already wired to the "Create assignment" form); added the entirely-missing edit half: `PATCH /workflows/it/trainer/assignments/{id}` (partial update, own-batch RBAC), `GET /workflows/it/trainer/assignments`, and an "Edit assignment" form. `AC02` holds by construction since `Submission.is_late` (`STU-004`) is computed once at submission time, never recomputed from the assignment's live `due_date` — verified directly. This description's "rubric" isn't specified as a structured field/entity anywhere in `DATA_MODEL.md`/`API_CONTRACT.md`; treated as covered by the existing free-text `description` field rather than invented without evidence · **Test status:** PASS — 6 new backend pytest cases (95/95 suite), 2 new Playwright E2E cases (53/53 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-005`  

### `TRN-006` — Assessment create and edit
- **Module:** Trainer  
- **Description:** Create/edit assessments — distinct content type from assignments, with draft/scheduled states.  
- **Actors:** primary — Trainer; secondary — Student  
- **Traces to:** BR — · PRD PRD-TRN-007 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** TRN-002.  
- **Main workflow:** Trainer authors assessment in Draft; moves to Scheduled to publish.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Draft assessment never visible to Students.  
- **Dependencies/blockers:** TRN-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Trainer, own batch)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — create already existed; the entire edit half was missing (no `PATCH`, no edit UI), same gap shape as `TRN-005` before its own fix. Added `PATCH /workflows/it/trainer/assessments/{id}` (own-batch RBAC), carrying the "Draft → Scheduled" publish transition as one more field (no separate publish endpoint, same precedent as `TRN-005`'s plain `published` boolean); extended the trainer's `GET` list with `description`/`instructions`/`publish_results` to feed a real "Edit assessment" form, and gave "Create assessment" an explicit Status field defaulting to Draft (it previously always created as "scheduled" by omission, contradicting the feature's own stated Draft-first workflow). Also found and fixed a real, confirmed `TRN-006-AC02` violation: the student-facing "examinations" portal section had no status filter at all — a Draft assessment was fully visible there, even though `POST /it/assessments/{id}/attempts` already correctly blocked attempting one · **Test status:** PASS — 5 new backend pytest cases (187/187 suite: create+edit, cross-trainer 403, 404 on unknown id, Draft never visible to an enrolled student while Scheduled is, auth required), 2 new Playwright E2E cases (89 total; `adm-005`'s pre-existing, already-tracked `RAID.md` I-04 contention flake is the only non-pass)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-006`  

### `TRN-007` — Submission review and grading
- **Module:** Trainer  
- **Description:** Review assignment/assessment submissions, record score/grade and feedback.  
- **Actors:** primary — Trainer; secondary — Student  
- **Traces to:** BR — · PRD PRD-TRN-008 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** STU-004 submission exists.  
- **Main workflow:** Trainer grades; Student sees result.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Re-grading a submission overwrites the prior grade with an audit trail, not a duplicate record.  
- **Dependencies/blockers:** STU-004  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Trainer, own batch)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`) — `PATCH /workflows/it/trainer/submissions/{id}` and the "Grade submission" UI already existed and already worked: own-batch RBAC, an audit-log entry per call, and the student portal already surfaced the resulting score/feedback. `AC02` holds by construction since grading always updates the same row in place — verified with two grading calls confirming one row and two distinct audit entries · **Test status:** PASS — 6 new backend pytest cases (101/101 suite), 2 new Playwright E2E cases (55/55 suite, confirmed stable across repeated runs)
- **Follow-up (2026-09-03, reported directly by the user against the live site):** the "Grade submission" card already had the student's real `answer`/`file_url` in its own already-fetched data but never rendered either — a trainer picked a submission by name and entered a score having never seen what the student actually wrote or attached. Added a preview (the answer text, and a real "Open submitted file" link) above the grading form itself. No API change — same endpoint, same data, just displayed. (A parallel gap on the Assessments quiz-grading flow was reported and fixed the same day, then reverted at the user's request pending a considered redesign — see `pending.md`.)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-007`  

### `TRN-008` — Attendance marking
- **Module:** Trainer  
- **Description:** Mark per-session attendance (present/absent) for an assigned batch.  
- **Actors:** primary — Trainer; secondary — -  
- **Traces to:** BR — · PRD PRD-TRN-009 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** TRN-002; session occurred.  
- **Main workflow:** Trainer marks attendance per student per session.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Marking attendance for a session with no scheduled students returns an empty roster, not an error.  
- **Dependencies/blockers:** TRN-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Trainer, own batch)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`) — `POST /workflows/it/trainer/attendance` already existed with own-batch RBAC, upsert-by-session semantics (re-marking updates, never duplicates), and rejection of duplicate/non-enrolled student ids. `AC02`'s empty-roster case is already handled on the frontend (disabled Save + a clear message) and backed by the schema's own `min_length=1` correctly rejecting an empty submission · **Test status:** PASS — 6 new backend pytest cases (107/107 suite), 2 new Playwright E2E cases (57/57 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-008`  

### `TRN-009` — Q&A response
- **Module:** Trainer  
- **Description:** View and respond to student questions raised against the Trainer's batch.  
- **Actors:** primary — Trainer; secondary — Student  
- **Traces to:** BR — · PRD PRD-TRN-010 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** TRN-002.  
- **Main workflow:** Trainer answers a question; Student sees the reply.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Only the Trainer assigned to that batch can reply; multiple replies on one question show in order.  
- **Dependencies/blockers:** TRN-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Trainer, own batch)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — `DATA_MODEL.md` §4.8 described `QuestionThread`/`QuestionReply` as "carries over," but neither table (nor any Q&A model) existed anywhere in the codebase — same "carries over but never existed" pattern already found for `STU-005`'s `assigned_to_user_id` and `STU-008`'s `CourseFeedback`. Fully net-new: models (alembic `0013_question_threads`), `POST`/`GET /workflows/it/student/questions` (raise, 422 if not enrolled — same pattern as `STU-008`), `GET /workflows/it/trainer/questions` and `POST .../{id}/replies` (own-batch RBAC checked against `Batch.trainer_id` — `TRN-009-AC02`), a `questions` portal section on both sides, `QuestionAskPanel.tsx`, and a reply queue added to `TeacherWorkspaceActions.tsx`. Replies return in `created_at` order, verified directly with two replies on one thread · **Test status:** PASS — 6 new backend pytest cases (193/193 suite: raise+reply+student sees it in order, reject-if-not-enrolled, cross-trainer 403, non-student role rejected, 404 on unknown thread, auth required on all three endpoints), 2 new Playwright E2E cases (91 total; `adm-005`'s pre-existing, already-tracked `RAID.md` I-04 contention flake is the only non-pass)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#trn-009`  


## Admin (IT)

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `ADM-001` | User/course/batch administration | CURRENT | P0 | Must | Wave 1 | M | Y | Y | Y |
| `ADM-002` | CRM-linked enquiry/lead management | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `ADM-003` | Batch creation and trainer assignment | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `ADM-004` | Directory management: Students, Trainers, Employers | CURRENT | P1 | Should | Wave 2 | S | Y | Y | N |
| `ADM-005` | Enrolment review and approval | CURRENT | P0 | Must | Wave 1 | S | Y | Y | Y |
| `ADM-006` | Certificate administration | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `ADM-007` | Placement Team workspace `[base]` | CURRENT | P1 | Should | Wave 2 | M | Y | Y | Y |
| `ADM-008` | HR Team workspace `[base]` | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `ADM-009` | Resources/recordings oversight | CURRENT | P3 | Could | Wave 4 | S | Y | Y | N |
| `ADM-010` | Agreement/consent oversight | CURRENT | P3 | Could | Wave 4 | XS | Y | Y | N |
| `ADM-011` | Notification template management | CURRENT | P3 | Could | Wave 4 | M | Y | Y | Y |
| `ADM-012` | Roles/permission administration | CURRENT | P3 | Could | Wave 4 | M | Y | Y | Y |
| `ADM-013` | Operational-tooling cluster | CURRENT | P3 | Could | Wave 4 | L | Y | Y | Y |
| `ADM-014` | Super Admin cross-division console `[base]` | CURRENT | P2 | Should | Wave 3 | L | Y | Y | Y |

### `ADM-001` — User/course/batch administration
- **Module:** Admin (IT)  
- **Description:** Admin manages users, students, trainers, courses, batches.  
- **Actors:** primary — IT Admin; secondary — -  
- **Traces to:** BR BR-ADM-001 · PRD PRD-ADM-001 · Decisions DEC-SCOPE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** FND-002.  
- **Main workflow:** Admin performs CRUD on core entities.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Deleting/deactivating an entity with active dependents (e.g. a course with active batches) is blocked or requires explicit cascade confirmation, never a silent cascade delete.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** Pagination for admin lists.  
- **Required test types:** API, UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`) — create+update already existed for users/programs but `active` could flip to `False` with zero dependent check (`AC02` violation); added a block-then-confirm (`confirm_cascade`) guard to both. Built `AdminUserManagementPanel.tsx`/`AdminProgramManagementPanel.tsx` -- there was no UI path at all to deactivate/reactivate either (create-only forms). Batch creation's existing raw program/trainer-reference text fields are left as-is this pass -- deeper batch-administration UX is `ADM-005`'s own scope, not duplicated here · **Test status:** PASS — 8 new backend pytest cases (115/115 suite), 3 new Playwright E2E cases against throwaway API-created records (60/60 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-001`  

### `ADM-002` — CRM-linked enquiry/lead management
- **⚠ Direction superseded 2026-09-12 (`DEC-SCOPE-001` re-resolved):** same note as `PUB-002` above
  — description left as-built; redesign blocked on the client's pending Internal CRM workflow
  document (`docs/product/PRD_OPEN_ITEMS.md` item 61).
- **Module:** Admin (IT)  
- **Description:** View and route enquiries/leads synced via the CRM webhook (configured for Zoho).  
- **Actors:** primary — IT Admin; secondary — -  
- **Traces to:** BR BR-ADM-001 · PRD PRD-ADM-002 · Decisions DEC-SCOPE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** PUB-002 enquiry submitted.  
- **Main workflow:** Admin views/routes enquiry.  
- **Alternate workflow:** -  
- **Error/edge behavior:** An enquiry that fails to sync to Zoho remains visible and actionable in Admin regardless of sync status.  
- **Dependencies/blockers:** PUB-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** Zoho CRM webhook  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Integration  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`) — `GET`/`PATCH /admin/leads` already existed and already got `AC02` right (list and routing never gate on `crm_sync_status`, so a failed Zoho sync stays visible and actionable). Replaced the generic "type the lead's raw UUID" form with `AdminLeadManagementPanel.tsx` (search, a visible CRM-sync column, per-row status picker) · **Test status:** PASS — 7 new backend pytest cases (122/122 suite), 2 new Playwright E2E cases (62/62 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-002`  

### `ADM-003` — Batch creation and trainer assignment
- **Module:** Admin (IT)  
- **Description:** Create a batch, assign a trainer, set capacity (≤20/slot).  
- **Actors:** primary — IT Admin; secondary — Trainer  
- **Traces to:** BR — · PRD PRD-ADM-003 · Decisions DEC-WF-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** ADM-001.  
- **Main workflow:** Admin creates batch and assigns trainer; batch becomes student-selectable.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Capacity exceeding 20 rejected.  
- **Dependencies/blockers:** ADM-001  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`) — a real `AC02` violation: the approved contract states `capacity <= 20` explicitly, but `BatchCreate` allowed up to 500 with zero enforcement, and the `PATCH` path had no upper bound at all. Fixed both. Replaced the generic "type the program/trainer's raw UUID" form with `AdminBatchCreatePanel.tsx` (real pickers, capacity capped at 20) · **Test status:** PASS — 7 new backend pytest cases (129/129 suite), 3 new Playwright E2E cases (65/65 suite, confirmed stable across repeated runs)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-003`  

### `ADM-004` — Directory management: Students, Trainers, Employers
- **Module:** Admin (IT)  
- **Description:** Ongoing directory/detail management beyond initial creation.  
- **Actors:** primary — IT Admin; secondary — -  
- **Traces to:** BR — · PRD PRD-ADM-006 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** ADM-001.  
- **Main workflow:** Admin views/edits directory detail records.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Editing a Student/Trainer/Employer record never overwrites a field the acting Admin lacks permission to change.  
- **Dependencies/blockers:** ADM-001  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — `GET`/`PATCH /admin/users` already existed (`ADM-001`) but only offered a flat, unfiltered list with an activate/deactivate toggle, identical on `/it/admin/students` and `/it/admin/trainers` alike, and no way to view or edit a directory record's detail fields at all — despite that being this feature's own main workflow. The field-level grant this feature's own `ADM-004-AC02` calls for already existed on `PATCH /admin/users/{id}` (an explicit allowlist that never included `role`/`division`/`email`/`password_hash`), confirmed directly rather than assumed. Extended the `GET` response with `phone`/`profile`, added `section`-based role filtering plus an "Edit details" action to `AdminUserManagementPanel.tsx`, and added an `employers` nav entry/portal section that honestly reports an empty directory — no `employer` role exists anywhere in this codebase yet (`EMP-001` unbuilt) · **Test status:** PASS — 6 new backend pytest cases (199/199 suite: listing includes detail fields, edit persists, role/division/email/password_hash confirmed non-editable, Employers directory is an honest empty 200 not a 404, non-admin rejected, auth required), 3 new Playwright E2E cases (94 total; see `RAID.md` I-06 — a real finding surfaced while building this feature: the backend pytest suite has no test-database isolation, and its accumulated rows in the shared dev database are now measurably slowing full-suite E2E runs and pushing `GET /admin/users`'s capped window past the seeded demo accounts)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-004`  

### `ADM-005` — Enrolment review and approval
- **Module:** Admin (IT)  
- **Description:** Review a student's enrolment/enquiry (post-CRM-sync) and approve into active enrolment.  
- **Actors:** primary — IT Admin; secondary — Student  
- **Traces to:** BR — · PRD PRD-ADM-007 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** ADM-002.  
- **Main workflow:** Admin approves; student enrolment becomes active.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Rejected enrolment does not silently proceed.  
- **Dependencies/blockers:** ADM-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`) — `API_CONTRACT.md` explicitly specifies dedicated `POST /admin/enrollments/{id}/approve`/`.../reject`, with rejection terminal for that enrolment attempt; neither existed, only a generic unguarded `PATCH`. Added both, and closed a back door in the generic `PATCH` that could otherwise reactivate a rejected enrolment directly. Added the "Enrollments" admin nav section end to end (no such UI surface existed at all) — `AdminEnrollmentReviewPanel.tsx`. Note: `DATA_MODEL.md` §3.3's own status list (`pending_payment`/`active`/`completed`/`withdrawn`) predates `STU-009`'s `pending_consent` addition and this feature's own `rejected` addition — flagged here, not silently reconciled, since payment-gated activation (`pending_payment`) depends on `PAY-001`/`STU-010`, both blocked on real Stripe/Razorpay credentials · **Test status:** PASS — 7 new backend pytest cases (136/136 suite), 2 new Playwright E2E cases (67/67 suite)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-005`  

### `ADM-006` — Certificate administration
- **Module:** Admin (IT)  
- **Description:** Admin issues/uploads certificates as an alternative to auto-generation.  
- **Actors:** primary — IT Admin; secondary — Student  
- **Traces to:** BR — · PRD PRD-ADM-009 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** Student course-completion criteria met.  
- **Main workflow:** Admin uploads/issues certificate.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Issuing a certificate for a student who hasn't met completion criteria requires an explicit override, not silent allowance.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** S3 storage  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — issuance already existed and worked (eligibility check, criteria snapshot, own-batch RBAC for a Trainer), but `ADM-006-AC02`'s own contract ("Requires override_reason... 422 without it") wasn't enforced — an override only ever needed a bare boolean. No Admin-facing UI to issue a certificate existed at all either. Added the `override_reason` requirement (durable on the certificate's own `criteria_snapshot` and the `AuditLog` entry) and a new Admin-wide "certificates" portal section + `AdminCertificatePanel.tsx`. Trainer access to the same shared issuance endpoint predates this feature and would break `STU-007`'s already-shipped test coverage if removed — left as-is, an honest interpretive call on `ADM-006-AC03`'s "IT Admin" framing. Also found and fixed a real, pre-existing bug: the plain 409 (no-override) response crashed with a 500 instead, because `HTTPException.detail` doesn't run through `jsonable_encoder` and `criteria["batch_id"]` is a raw `UUID` — never caught before because every prior caller always passed `override=True` · **Test status:** PASS — 5 new backend pytest cases (204/204 suite: override requires a reason, reason recorded durably, Admin directory lists certificates, non-admin/non-trainer rejected, auth required), 2 new Playwright E2E cases (96 total; only non-passes are `RAID.md` I-04/I-06 contention flakes, a different spec each full-suite run, each confirmed to pass in isolation). Also fixed a real E2E-test bug (not a product defect) found while stabilizing `TRN-006`'s own spec in this same pass — see `RAID.md`  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-006`  

### `ADM-007` — Placement Team workspace
- **Module:** Admin (IT)  
- **Description:** [Base role] Candidate pool, company hiring requirements, interviews, offers, placement reports.  
- **Actors:** primary — Placement Team; secondary — -  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** FND-002.  
- **Main workflow:** Placement Team manages the domestic placement pipeline.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A candidate withdrawn from the pool no longer appears in active matching, but historical placement records are retained.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Placement Team  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — most of this `[base]` feature already existed and worked (candidate profiles, job requirements, interviews, offers, all with real RBAC), but `ADM-007-AC02` ("a withdrawn candidate no longer appears in active matching, but historical placement records are retained") had no supporting concept — `PlacementProfile.available` only ever meant "temporarily unavailable." Added `PlacementProfile.withdrawn` (alembic `0014_placement_profile_withdrawn`), excluded by default from both the profiles endpoint (with an `include_withdrawn=true` escape hatch) and the "candidates" portal section — neither of which touches `JobApplication`/`Interview`/`JobOffer`, so historical records are retained by construction. Also replaced the only write path — a generic form requiring the student's raw database reference typed by hand, same gap class already fixed for `ADM-001`/`002`/`003`/`004`/`006` — with a real picker + withdraw/reinstate control (`PlacementCandidatePanel.tsx`) · **Test status:** PASS — 6 new backend pytest cases (210/210 suite: withdrawn excluded by default and visible with `include_withdrawn`, withdrawal doesn't touch `JobApplication` history, reinstating restores visibility, portal section also excludes withdrawn, `hr_team` cannot write profiles, auth required), 2 new Playwright E2E cases (98 total; only non-passes are `RAID.md` I-04/I-06 contention flakes, a different spec each full-suite run, each confirmed to pass in isolation)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-007`  

### `ADM-008` — HR Team workspace
- **Module:** Admin (IT)  
- **Description:** [Base role] Hiring/job requirements, candidate shortlist, interview coordination — internal staff mediating corporate hiring, distinct from the new Employer role.  
- **Actors:** primary — HR Team; secondary — -  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** FND-002.  
- **Main workflow:** HR Team manages hiring requirements and shortlists.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A hiring requirement with no matching candidates shows an empty state, not an error.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** HR Team  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (2026-09-02, `prompts/13`, Wave 2) — most of the underlying job/candidate/interview/offer machinery already existed and was already correctly shared with `ADM-007`'s own RBAC. The real gap: HR Team's own main workflow ("manages hiring requirements and shortlists") had no way to view a specific requirement's shortlist at all — only a flat, job-agnostic candidate pool and a flat requirements list, no bridge between the two. Added `GET /workflows/it/jobs` (any status, for the requirement picker) and `GET /workflows/it/jobs/{id}/shortlist` (the actual per-requirement shortlist), plus `HrShortlistPanel.tsx` and a new `shortlists` nav entry. `ADM-008-AC02` (empty state, not an error) holds by construction — the query never raises just because zero applicants exist · **Test status:** PASS — 6 new backend pytest cases (216/216 suite: lists requirements regardless of status, empty shortlist returns `[]` not an error, shortlist lists real applicants, 404 on unknown job, non-staff role rejected, auth required), 2 new Playwright E2E cases (100 total; only non-passes are `RAID.md` I-04/I-06 contention flakes, a different spec each full-suite run — this feature's own spec passed cleanly every run)
  **Follow-up (2026-09-05, tester feedback via WhatsApp 2026-09-04, `RAID.md` I-13):** "add the close date on job requirements" -- confirmed directly against the running app: `Job.closes_on` was already collected on create and already enforced server-side, but never shown back on the Job Requirements table, and no "Update" action existed to set/change it afterward, even though the backend (`PATCH /it/jobs/{id}`) already accepted it for this role. Added the column and a real "Update job requirement" form. Backend: 2 new pytest cases (8/8 this file; 23/23 across files sharing this code path). E2E: 1 new Playwright case (3/3), confirmed live in a real browser.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-008`  

### `ADM-009` — Resources/recordings oversight
- **Module:** Admin (IT)  
- **Description:** Admin-level view over Trainer-uploaded resources/recordings across batches.  
- **Actors:** primary — IT Admin; secondary — -  
- **Traces to:** BR — · PRD PRD-ADM-008 · Decisions DEC-SCOPE-008  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 4  
- **Preconditions:** TRN-004.  
- **Main workflow:** IT Admin views Trainer-uploaded resources/recordings across all batches, not just their own.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A batch with no uploaded resources shows an honest empty state, not an error.  
- **Dependencies/blockers:** TRN-004  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 4) · **Test status:** COMPLETE — 5 new pytest cases, 2 new E2E cases  
  - `TRN-004`'s own `LearningResource` model and Trainer-scoped "materials" portal section already existed and worked; no cross-trainer Admin oversight existed anywhere. Added a `resources` section to the existing `it_admin`/`super_admin` portal dispatcher (`services/portal.py`), reusing the exact same `LearningResource`+`Batch` join as the Trainer's own section but unscoped across every batch/trainer, with a trainer-name column added since cross-trainer oversight is the entire point of this feature. No migration — `Batch`/`Program` have no `division` field (Overseas has no batches at all), so an `overseas_admin` correctly gets a 404 requesting this section, verified directly rather than assumed. No new frontend component — the existing generic `PortalSection`/`DataTable` renders whatever the backend returns; only a new `resources` nav entry was needed. Backend: 5 new pytest cases — cross-trainer visibility, honest empty state, non-admin role rejected, `overseas_admin` 404s (wrong domain, not silently scoped), auth required (`test_adm_009_resources_oversight.py`). E2E: 2 new Playwright cases — real cross-trainer data renders (searched to first, per `RAID.md` I-07's `DataTable` pagination trap), auth required (`adm-009-resources-oversight.spec.ts`).  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-009`  

### `ADM-010` — Agreement/consent oversight
- **Module:** Admin (IT)  
- **Description:** Admin view of student agreement/consent status.  
- **Actors:** primary — IT Admin; secondary — -  
- **Traces to:** BR — · PRD PRD-ADM-010 · Decisions DEC-SCOPE-008  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 4  
- **Preconditions:** STU-009.  
- **Main workflow:** IT Admin views student agreement/consent status across all students.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A student with no recorded consent shows an honest "not yet consented" state, not an error.  
- **Dependencies/blockers:** STU-009  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 4) · **Test status:** COMPLETE — 5 new pytest cases, 2 new E2E cases  
  - `STU-009`'s self-scoped agreement/consent flow already existed and worked; no cross-student Admin oversight existed anywhere. Added a `consent` section to the existing `it_admin`/`super_admin` portal dispatcher (`services/portal.py`), reusing `Agreement`/`ConsentRecord` directly — no migration. Found and fixed two real gaps while building this feature's own tests: (1) the shared dev DB has accumulated 5,573 synthetic `it_student` rows (measured directly) — an alphabetical sort essentially never surfaces a freshly created row at that volume; switched to `created_at.desc()`, matching the sibling `role_map` section's own existing convention (`RAID.md` I-06 addendum). (2) `Agreement.version` already stores a `"v"` prefix, so the subtitle template produced a literal "vv1" — fixed to interpolate the stored value directly. Also found (not fixed, out of scope) that the same debris has made the real seeded demo account's "current agreement" an arbitrary leftover test row rather than the real seeded one — worked around in this feature's own E2E spec by registering a fresh throwaway student instead. Backend: 5 new pytest cases — both consented and not-yet-consented students visible, pending-enrolment count surfaced, non-admin role rejected, auth required, and a defensive case proving the endpoint never 500s regardless of agreement state (`test_adm_010_consent_oversight.py`). E2E: 2 new Playwright cases — a freshly registered student's consent visibly flips from "Not yet" to "Yes" after accepting, auth required (`adm-010-consent-oversight.spec.ts`).  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-010`  

### `ADM-011` — Notification template management
- **Module:** Admin (IT)  
- **Description:** Manage notification templates and monitor delivery status.  
- **Actors:** primary — IT Admin; secondary — -  
- **Traces to:** BR — · PRD PRD-ADM-011 · Decisions DEC-NOT-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 4  
- **Preconditions:** -  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** Email channel only this round (`DEC-NOT-001`'s 2026-09-03 extension) — WhatsApp/SMS template management stays out of scope until `NOT-002`/`NOT-003` unblock; build extensibly (e.g. a `channel` field), not email-hardcoded.  
- **Dependencies/blockers:** NOT-001  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** Email (Twilio/SMS deferred with NOT-002/NOT-003)  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** M  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-011`  

### `ADM-012` — Roles/permission administration
- **Module:** Admin (IT)  
- **Description:** Admin-configurable role/permission matrix.  
- **Actors:** primary — IT Admin; secondary — -  
- **Traces to:** BR — · PRD PRD-ADM-013 · Decisions DEC-SCOPE-008  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 4  
- **Preconditions:** -  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** -  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** M  
- **Implementation status:** PARTIAL (2026-09-03, `prompts/13`) — `AC01` calls for "views and adjusts", but this feature's actual confirmed requirement (`PRD-ADM-013`, not `PRD.md`'s differently-numbered `PRD-ADM-012` heading) is explicitly view-only: "whether permissions need to be admin-configurable at runtime... was never asked" (`PRD_OPEN_ITEMS.md` item 42). `core/rbac.py`'s `PERMISSIONS` is a static code table, not a DB-backed model — building live editing would mean inventing a security-critical runtime engine on an unconfirmed requirement, not extending a confirmed one. Built the confirmed view half: a `roles` section on the existing `it_admin`/`super_admin` portal dispatcher, rendering the real, live per-role permission bundle straight from `core/rbac.py` plus a real active-user count per role. `AC02` (reject a bad permission removal) is vacuously satisfied — no write endpoint exists at all, confirmed directly. Runtime editing remains open, not silently closed · **Test status:** PASS — 7 new backend pytest cases (`test_adm_012_roles_permissions.py`), 2 new Playwright E2E cases  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-012`  

### `ADM-013` — Operational-tooling cluster
- **Module:** Admin (IT)  
- **Description:** System settings, audit logs, data import/export, backup/restore, system health, background-job monitoring.  
- **Actors:** primary — IT Admin, Super Admin; secondary — -  
- **Traces to:** BR — · PRD PRD-ADM-012 · Decisions DEC-SCOPE-008  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 4  
- **Preconditions:** -  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** -  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Super Admin (`DEC-SCOPE-008` resolves the prior IT Admin/Super Admin ownership ambiguity)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI, API  
- **Complexity:** L  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-013`  

### `ADM-014` — Super Admin cross-division console
- **Module:** Admin (IT)  
- **Description:** [Base role] Central dashboard across both divisions: users/students/staff, programs/batches/universities/recruiters, CMS/blogs/events, leads/applications/payments, reports/notifications, roles/settings/security logs/backups.  
- **Actors:** primary — Super Admin; secondary — -  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 3  
- **Preconditions:** FND-002.  
- **Main workflow:** Super Admin operates cross-division console.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A cross-division privileged action (e.g. security-log export) is itself audit-logged, per the base codebase's privileged-write audit requirement.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Super Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Privileged-write audit records (base ARCHITECTURE.md).  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** L  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 372/372 backend full regression, 7 new backend cases, 3 new E2E cases  
  - Most of this feature already existed: `apps/web/app/admin/[module]/page.tsx` + `SUPER_ADMIN_NAV` already cover the full described console (users/students/staff, programs/batches/universities/recruiters, CMS/blogs/events, leads/applications/payments, reports/notifications, roles/settings/security-logs/backups) as one cross-division view, and `/admin/users`'s existing division-scoping (division admins forced to their own `User.division` via `ensure_admin`'s query filter, only `super_admin` unrestricted) already satisfies `ADM-014-AC01`/`AC03` -- confirmed directly with a dedicated test per role, not assumed the way a `[base]`-tagged feature's pre-existing implementation sometimes turns out incomplete. The real, confirmed gap, named explicitly in this feature's own `AC02` ("a cross-division privileged action (e.g. security-log export) is itself audit-logged"): no distinct export action existed anywhere in the codebase -- only the pre-existing read-only `GET /admin/audit` view (confirmed while building `SEC-001`, deliberately left out of that feature's own narrower scope at the time). Added `POST /admin/audit/export` (Super-Admin-only -- stricter than the broader `ensure_admin` gate the read-only view uses, since division admins reaching a filtered *view* of their own data is legitimate but a cross-division *export* is not), which builds a downloadable JSON export of the audit log via the same `storage` service pattern `SEC-002`'s GDPR export already established (local-storage or S3 depending on environment, a signed/proxied download URL either way) and self-audit-logs the export action itself (`action="admin.audit_export"`, `outcome="exported"`, row count in `metadata_json`) in the same transaction as the write -- literally the "cross-division privileged action... is itself audit-logged" requirement. No schema change needed beyond `SEC-001`'s own `AuditLog.outcome` column. Added `AuditExportPanel.tsx` (mirrors `DataPrivacyPanel.tsx`'s established "click button, get a download link" shape) wired into the Super Admin's existing "Security & Audit Logs" page -- no new page needed. Verified rendering live against the running stack before relying on the Playwright suite alone. Given the shared `admin.py` change, ran the full backend regression rather than a module-scoped one. Backend: 7 new pytest cases -- Super Admin exports successfully and the export is self-audit-logged with a matching row count, IT Admin/Overseas Admin/a non-admin role are all 403'd, auth required, a division admin's `/admin/users` never includes the other division's users, Super Admin's does include both (`test_adm_014_super_admin_console.py`). E2E: 3 new Playwright cases -- the Super Admin clicks the real "Export audit log" button and gets a working download link, a non-Super-Admin is 403'd at the API layer, an unauthenticated request to the console redirects to `/admin/login` (via the existing `middleware.ts`, not the page's own "Administrator access required" fallback -- confirmed by first getting this test's expectation wrong and fixing it against the real behavior).
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#adm-014`  


## Employer

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `EMP-001` | Employer registration `[new]` | CURRENT | P1 | Should | Wave 2 | M | Y | Y | Y |
| `EMP-002` | Job posting `[new]` | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `EMP-003` | Candidate profile search `[new]` | CURRENT | P1 | Should | Wave 2 | M | Y | Y | N |
| `EMP-004` | Interview scheduling and shortlist `[new]` | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `EMP-005` | Interview list and status `[new]` | CURRENT | P2 | Could | Wave 3 | XS | Y | Y | N |
| `EMP-006` | Placement status tracking `[new]` | CURRENT | P3 | Could | Wave 4 | S | Y | Y | Y |

### `EMP-001` — Employer registration
- **Module:** Employer  
- **Description:** [Net-new] Company registers an Employer account — no equivalent exists in the base codebase.  
- **Actors:** primary — Employer; secondary — IT Admin  
- **Traces to:** BR BR-EMP-001 · PRD PRD-EMP-001 · Decisions DEC-SCOPE-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** FND-002 (new role/route to add).  
- **Main workflow:** Company self-registers.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Approval-before-activation workflow open — see FEATURE_QUESTIONS.md.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Employer (new)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** Unit, API, UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 239/239 backend, 108/108 E2E  
  - Fully net-new: no Employer role/login existed anywhere in the base codebase — corporate hiring was previously mediated entirely by internal `placement_team`/`hr_team` staff plus a public apply-and-track flow (`REFERENCE_IMPLEMENTATION_FINDINGS.md`).
  - `Company` (`apps/api/app/models.py`) extended with `owner_type` (`internal`/`employer_self_service`) and `employer_user_id`, per `DATA_MODEL.md` §5.1's explicit resolution — extend the existing table rather than fork a parallel one, since the Placement-Team-mediation question (`FEATURE_QUESTIONS.md` #1) is still open and a parallel table would force a premature answer.
  - Net-new `EmployerProfile` (§5.2): `registration_status` exists on the schema but is deliberately unenforced — whether registration requires Admin approval before activation is an explicitly open item (`FEATURE_QUESTIONS.md` #7). The account is active and usable immediately after registering; `EMP-001-AC02`'s "no silent failure, no partial state" is read as "don't invent an approval gate that was never confirmed," not as license to leave registration half-done.
  - Alembic `0017_employer_registration`.
  - `POST /employer/register` (`apps/api/app/api/employer.py`) creates `User` (role `employer`, division `it`) + `Company` + `EmployerProfile` in one transaction, then auto-logs in (mirrors `auth.register`'s own `_set_auth_cookies`/`_sync_role_assignment` pattern) — exactly matching `API_CONTRACT.md`'s employer table. Duplicate email or duplicate company name is rejected (409) before any row is written, so a conflicting retry never leaves an orphaned `Company`. `GET`/`PATCH /employer/profile` are self-scoped to the employer's own record (403 for any other role, 401 unauthenticated).
  - Frontend: `EmployerRegisterForm.tsx` + `/it/employer-register`, linked from the existing "Corporate Hiring" marketing page (`/it/corporate-hiring`, which previously only offered a "Company / HR Login" link into the internal-staff-mediated flow — kept, not replaced). Post-registration lands on a standalone `/it/employer/dashboard` (no `PORTAL_NAV` entry yet, since job posting/candidate search is `EMP-002`/`EMP-003` — there is nothing else to navigate to until those exist). `ROLE_DASHBOARD_PATH.employer` added so login redirects work the same way as every other role.
  - Backend: 8 new pytest cases (`apps/api/tests/test_emp_001_employer_registration.py`) — atomic creation (`User`+`Company`+`EmployerProfile` all present, `registration_status` null), immediately usable via a normal login (no approval gate), duplicate email rejected, duplicate company name rejected, a failed registration leaves no orphaned company row, self profile read/update, non-employer role rejected, auth required. Full regression: 239/239 passed.
  - Frontend: 4 new Playwright cases (`apps/web/tests/e2e/emp-001-employer-registration.spec.ts`) — full registration flow from the marketing page through to the dashboard, duplicate-email shows a real visible error, dashboard requires auth, a non-employer role's API call is rejected. Full regression: 108/108 (8 unrelated specs failed under known full-suite worker contention — `adm-005`, `adm-007`, `pub-001` ×3, `pub-004`, `stu-009`, `trn-006` — all 8 confirmed passing cleanly in isolation, `RAID.md` I-04/I-06, not a regression).
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#emp-001`  

### `EMP-002` — Job posting
- **Module:** Employer  
- **Description:** [Net-new] Employer posts open roles.  
- **Actors:** primary — Employer; secondary — Student  
- **Traces to:** BR BR-EMP-001 · PRD PRD-EMP-002 · Decisions DEC-SCOPE-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** EMP-001.  
- **Main workflow:** Employer posts a job; visible to eligible Students.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A job posting past its closing date is not shown to Students as open, even if the Employer never explicitly closed it.  
- **Dependencies/blockers:** EMP-001  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Employer (own postings)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 300/300 backend module baseline (8 new), 135/135 E2E baseline (3 new)  
  - Fully net-new: no endpoint let the Employer role (`EMP-001`) post a job at all -- job creation remained Placement Team/HR Team/Admin-only (`POST /it/jobs`). Added a self-scoped `POST`/`GET`/`PATCH /employer/jobs` family reusing the existing `Job`/`Company` tables (`Company.employer_user_id`, from `EMP-001`, ties a posting to its owning Employer) -- no new tables. Cross-employer IDOR on `PATCH` is rejected even via a direct job id, verified directly. `API_CONTRACT.md` §6 fixes a posting's starting state to `"draft"` -- whether it then needs staff `pending_review` before publishing is an open mediation question, so no such gate is invented; the Employer publishes their own draft by setting `status: "open"` themselves, the same "no invented approval gate" precedent `EMP-001`'s own `registration_status` already established (self-caught and corrected mid-session after an initial pass shipped `status="open"` immediately on create, missing this contract line on first read). Also found and fixed a real, confirmed `EMP-002-AC02` gap that predates this feature and affected every existing job-listing/apply path: `GET /it/jobs/open`, `GET /public/jobs`, and both job-application endpoints (`POST /it/jobs/{id}/apply`, `POST /public/jobs/{id}/apply`) only ever checked `Job.status == "open"` -- a posting whose `closes_on` date had already passed, but whose status the Employer never explicitly flipped, still showed as open (and was still applicable to) everywhere. Fixed all four call sites. `status` is constrained to `draft`/`open`/`closed` only (the only values ever evidenced anywhere in this codebase; not inventing a richer enum). Frontend: `EmployerJobsPanel.tsx` on the existing `/it/employer/dashboard` page (previously said "job posting... coming soon") -- real create form + per-posting Publish/Close actions, each showing a real `visible_to_students` flag computed from `status` and `closes_on`. Backend: 8 new pytest cases -- new posting starts draft and is invisible everywhere, publish makes it visible to students (both listings), employer sees only their own postings, published-but-expired job hidden from both listings and both apply endpoints, cross-employer update rejected, employer can close their own published job, unsupported status value rejected, non-employer role rejected, auth required (`test_emp_002_job_posting.py`). E2E: 3 new Playwright cases -- draft-then-publish flow, close action works, dashboard requires an employer session.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#emp-002`  

### `EMP-003` — Candidate profile search
- **Module:** Employer  
- **Description:** [Net-new] Employer searches/browses Student candidate profiles within GDPR-approved visibility limits.  
- **Actors:** primary — Employer; secondary — Student  
- **Traces to:** BR BR-EMP-001 · PRD PRD-EMP-003 · Decisions DEC-SCOPE-002, DEC-PRIV-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** EMP-001; STU-011 profile exists.  
- **Main workflow:** Employer searches; sees permission-limited profile fields.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Fields outside approved visibility never returned to Employer.  
- **Dependencies/blockers:** EMP-001, STU-011  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Employer  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** GDPR-scoped — exact visible field set is an open item.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 307/307 backend module baseline (7 new), 138/138 E2E baseline (3 new)  
  - Fully net-new: no endpoint or UI let an Employer search Student candidate profiles at all. Added `GET /employer/candidates`, reading `PlacementProfile` per `API_CONTRACT.md` §6 and returning only the contract's own named conservative allowlist -- name, course (derived from the student's most recent `Enrollment`→`Batch`→`Program`), skills (`User.profile`), availability -- never raw contact info (email/phone), pending confirmation of the exact GDPR-approved field set (`EMP-003-AC02`). Scoped to the same "active pool" rule `ADM-007-AC02` already established: withdrawn candidates excluded by default, but merely-unavailable ones still shown (with `availability: false`) -- a student with no `PlacementProfile` at all (never entered the placement pipeline) is not a searchable candidate. `q` filters by name/course/skill. Frontend: `EmployerCandidateSearchPanel.tsx` on the existing `/it/employer/dashboard` page. Backend: 7 new pytest cases -- allowlisted fields present and no raw contact info anywhere in the response, withdrawn candidate excluded, unavailable-but-not-withdrawn still shown, no-profile student excluded, search filters by skill, non-employer role rejected, auth required (`test_emp_003_candidate_search.py`). E2E: 3 new Playwright cases -- real search finds an available candidate, response never contains raw contact info, dashboard requires an employer session.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#emp-003`  

### `EMP-004` — Interview scheduling and shortlist
- **Module:** Employer  
- **Description:** [Net-new] Employer maintains a shortlist and schedules interviews with candidates.  
- **Actors:** primary — Employer; secondary — Student  
- **Traces to:** BR BR-EMP-001 · PRD PRD-EMP-004, PRD-EMP-005 · Decisions DEC-SCOPE-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** EMP-003.  
- **Main workflow:** Employer shortlists, then schedules an interview.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Scheduling conflicts (same candidate, overlapping time) are flagged, not silently double-booked.  
- **Dependencies/blockers:** EMP-003  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Employer (own candidates)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 316/316 backend module baseline (9 new), 140/140 E2E baseline (2 new)  
  - Fully net-new: no endpoint or UI let an Employer shortlist a candidate or schedule an interview. `POST /employer/shortlist` creates a `JobApplication` (status "shortlisted") against the Employer's own posting -- reusing the existing table rather than a parallel one, the same "extend, don't fork" precedent as `EMP-001`'s `Company` columns. `POST /employer/interviews` creates an `Interview` against that application; both scoped to the Employer's own postings even via a direct id (`EMP-004-AC03`). No `duration` field exists anywhere on `Interview` to compute a true overlap window, so the only honestly detectable scheduling conflict (`EMP-004-AC02`), without inventing an assumed duration, is another interview for the same candidate at the exact same instant -- flagged with a 409, not silently double-booked. Added `GET /employer/shortlist`/`GET /employer/interviews` (own-scoped listings) to make the feature usable end-to-end; the fuller "cancelled interviews stay visible" nuance is `EMP-005`'s own Wave 3 scope, not touched here. Frontend: `EmployerInterviewsPanel.tsx` on the existing dashboard -- real job/candidate pickers, no raw IDs. Backend: 9 new pytest cases -- shortlist succeeds, duplicate rejected, cross-employer shortlist/interview rejected, interview succeeds, same-instant conflict flagged, own-scoped listing, non-employer role rejected, auth required (`test_emp_004_interview_scheduling.py`). E2E: 2 new Playwright cases -- real shortlist-then-schedule flow, dashboard requires an employer session.  

### `EMP-005` — Interview list and status
- **Module:** Employer  
- **Description:** [Net-new] Employer views scheduled/past interviews in one list.  
- **Actors:** primary — Employer; secondary — -  
- **Traces to:** BR — · PRD PRD-EMP-006 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Could / Wave 3  
- **Preconditions:** EMP-004.  
- **Main workflow:** Employer reviews interview list.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Cancelled interviews remain visible in history, not deleted.  
- **Dependencies/blockers:** EMP-004  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Employer  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 400/400 backend full regression, 4 new backend cases, 2 new E2E cases  
  - `GET /employer/interviews` and `EmployerInterviewsPanel.tsx` already existed, built as part of `EMP-004`'s own scheduling flow -- the backend already returns every interview for the Employer's own company unconditionally, with no `result`-based filter anywhere, and is already correctly scoped via `company.id` (`EMP-005-AC03`, confirmed directly by an existing `EMP-004` test rather than assumed). The real, confirmed gap was entirely in the frontend: the "Scheduled interviews" card rendered only `candidate`/`job_title`/`scheduled_at` -- `result` (and `mode`) were already returned by the API but never displayed anywhere, so an Employer genuinely had no way to see an interview's outcome, including whether one had been cancelled by Placement Team via `PATCH /workflows/it/interviews/{id}` -- the data was never filtered out or deleted (`EMP-005-AC02` already held structurally, at the data layer), it was simply invisible in the UI, which is exactly what `EMP-005-AC01`'s "reviews interview list" requires actually showing. Added a status badge to each card showing the real `result` value, or an honest "Awaiting outcome" label when one hasn't been recorded yet -- never a fabricated or guessed status. While regression-testing this change, found and fixed a real, reproducible (not flaky) test-hygiene bug in `EMP-004`'s own existing E2E spec: it scheduled every run's interview at the exact same hardcoded instant, which on a second/later run collided with the same seeded candidate's interview left over from an earlier run against the shared, non-test-isolated dev DB (`RAID.md` I-06), tripping the backend's own legitimate same-instant conflict check (`EMP-004-AC02`) and failing the test for reasons unrelated to this feature's own changes -- fixed by varying the minute per run instead of a fixed value, and applied the identical fix to this feature's own new spec pre-emptively. Backend: 4 new pytest cases -- a cancelled/resulted interview remains visible in the Employer's list with its real result value intact, an interview with no result yet is also returned (not hidden), a non-employer role is rejected, auth required (`test_emp_005_interview_list.py`). E2E: 2 new Playwright cases -- the interview card shows an honest "Awaiting outcome" status immediately after scheduling and the real recorded result after Placement Team acts on it, unauthenticated access to the list is rejected.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#emp-005`  

### `EMP-006` — Placement status tracking
- **Module:** Employer  
- **Description:** [Net-new] Placement status (offer/accepted/joined) tracked per interviewed candidate.  
- **Actors:** primary — Employer; secondary — IT Admin  
- **Traces to:** BR — · PRD PRD-EMP-007 · Decisions DEC-SCOPE-008  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 4  
- **Preconditions:** -  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** -  
- **Dependencies/blockers:** EMP-004  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Employer, IT Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** S  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#emp-006`  


## Overseas

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `OVS-001` | Destination/university/course discovery | CURRENT | P1 | Should | Wave 2 | M | Y | Y | Y |
| `OVS-002` | Overseas application submission | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `OVS-003` | Eligibility evaluation `[base]` | CURRENT | P1 | Should | Wave 2 | M | Y | Y | Y |
| `OVS-004` | Application status tracking and notifications | CURRENT | P1 | Should | Wave 2 | S | Y | Y | N |
| `OVS-005` | Document upload against checklist | CURRENT | P1 | Should | Wave 2 | M | Y | Y | Y |
| `OVS-006` | Scholarship listing and application | CURRENT | P2 | Could | Wave 3 | S | Y | Y | Y |
| `OVS-007` | Events and workshops | CURRENT | P2 | Could | Wave 3 | XS | Y | Y | Y |

### `OVS-001` — Destination/university/course discovery
- **Module:** Overseas  
- **Description:** Browse countries/destinations, universities (admin-prioritized), courses, with tuition ranges.  
- **Actors:** primary — Visitor, Student; secondary — -  
- **Traces to:** BR BR-OVS-001 · PRD PRD-OVS-001 · Decisions DEC-SCOPE-003, DEC-DATA-002  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** None.  
- **Main workflow:** User browses country/university/course listings and detail.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A university/course with incomplete admin-entered data (per DEC-DATA-002) shows only the fields that exist -- never a fabricated placeholder value.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI, API  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 248/248 backend module baseline (9 new), 108/108 E2E baseline (7 new)  
  - `Country`/`University`/`OverseasCourse`/`Scholarship` and the `/public/countries`, `/universities`, `/overseas-courses` list+detail endpoints all "carried over" (`DATA_MODEL.md` §6.1) and needed no schema change. Found and fixed a real, confirmed defect while verifying this feature's own main workflow: `/public/overseas-courses` 500'd unconditionally (an ambiguous multi-table SQLAlchemy join with no explicit `ON` clause) -- confirmed directly with a raw request against the running stack, not assumed from the test alone. Fixed by making both join conditions explicit in `apps/api/app/api/public.py`. OVS-001-AC02 (no fabricated placeholder for incomplete data) holds by construction -- every field is a required, non-nullable string column round-tripped verbatim, with no substitution logic anywhere in the response path; verified directly with an empty-string field. Frontend list/detail pages (`/overseas/{countries,universities,courses}`) already existed and were already wired to real data. Backend: 9 new pytest cases -- list+detail rendering for all three resources, 404 on unknown slug, country/search filters, no-fabrication check, no-auth-required (`test_ovs_001_discovery.py`). E2E: 7 new Playwright cases -- list+detail rendering, 404 handling, nav discoverability, 375px responsive (`ovs-001-discovery.spec.ts`).  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ovs-001`  

### `OVS-002` — Overseas application submission
- **Module:** Overseas  
- **Description:** Student (existing identity) submits interest in a country/university/course.  
- **Actors:** primary — Student; secondary — Counselor  
- **Traces to:** BR BR-OVS-001 · PRD PRD-OVS-002 · Decisions DEC-SCOPE-003, DEC-ROLE-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** OVS-001; Student authenticated.  
- **Main workflow:** Student submits interest; application enters the state sequence (OVS-003/004).  
- **Alternate workflow:** -  
- **Error/edge behavior:** Submitting interest in the same university/course twice does not create a duplicate application record.  
- **Dependencies/blockers:** OVS-001, AUTH-001  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Student) — **known-incomplete since `DEC-ROLE-004` (2026-09-14):** covers only a self-authenticating applicant; an Agent-referred applicant has no login and needs an Agent-performed equivalent, not yet designed (`AGT-002` reconciliation note, `PRD_OPEN_ITEMS.md` item 68)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 255/255 backend module baseline (7 new), 118/118 E2E baseline (3 new)  
  - `POST /overseas/applications` already existed and already used the real university/course catalogue, but had two real, confirmed gaps against the approved contract. First, `OVS-002-AC02`'s duplicate-prevention (`DATA_MODEL.md` §6.2's unique-constraint requirement) did not exist at all -- any student could submit unlimited applications for the same (university, course). Added an explicit check (409, excludes `status='withdrawn'` per the contract, though nothing sets that value yet since no feature owns withdrawal). Second, the initial `status` was the base codebase's own free-text `"profile_evaluation"`, not the contract-fixed enum's first value (`DATA_MODEL.md` §6.2: `enquiry` → ... → `enrolled`) -- fixed to `"enquiry"` at both the create endpoint and the model's column default; the full enum-transition enforcement itself belongs to `OVS-003`'s own `advance` endpoint, not touched here. Self-scope already held by construction (a Student's own `student_id` is always taken from their session, payload override ignored) -- verified directly rather than assumed. Frontend gap: the "applications" section only offered a raw university/course UUID typed by hand (same class of gap already fixed for `ADM-001`/`002`/`003`/`004`/`006`/`007`) -- replaced with a real picker, `OverseasApplyPanel.tsx` (select university → its real courses → submit, plus the student's own tracking list inline). Backend: 7 new pytest cases -- enters `enquiry`, duplicate rejected, different course not a duplicate, self-scope holds against a spoofed `student_id`, unknown university 404s, wrong-role rejected, auth required (`test_ovs_002_application.py`). E2E: 3 new Playwright cases -- real picker submission, duplicate rejected with a clear message, auth required.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ovs-002`  

### `OVS-003` — Eligibility evaluation
- **Module:** Overseas  
- **Description:** Counselor evaluates eligibility, determining viable university/course options.  
- **Actors:** primary — Counselor; secondary — Student  
- **Traces to:** BR BR-OVS-001 · PRD PRD-OVS-003 · Decisions DEC-WF-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** OVS-002.  
- **Main workflow:** Counselor reviews and advances/declines the application stage.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Rejection/waitlist/deferral outcomes are an open item — do not invent.  
- **Dependencies/blockers:** OVS-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Counselor (assigned students)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 262/262 backend module baseline (7 new), 121/121 E2E baseline (3 new)  
  - `PATCH /overseas/applications/{id}` already let a Counselor change `status` to any free-text value at all -- no dedicated endpoint existed, and nothing enforced `DATA_MODEL.md` §6.2's contract-fixed enum sequence (`OVS-003-AC02`: an attempt to set a rejection/waitlist/deferral outcome must be rejected with a clear "not yet supported" error, never silently accepted -- confirmed as a real, exploitable gap: the old generic form's own dropdown literally offered "rejected" as a selectable value). Added `POST /overseas/applications/{id}/advance` (Counselor-only, forward-only along the confirmed sequence, enum-validated) and closed the identical gap in the generic PATCH for every caller (University Rep/Admin keep that broader tool for non-status corrections). `_assigned_application` already correctly scoped a Counselor to their own assigned applications, including against a direct application ID (BOLA/IDOR-safe, `OVS-003-AC03`) -- verified directly, not assumed. Frontend: the Counselor's "applications"/"students" sections only offered a raw application UUID typed by hand with that same broken status dropdown (same gap class as `ADM-001`/`002`/`003`/`004`/`006`/`007`) -- replaced with a real per-application picker limited to only the stages still ahead, `CounselorEvaluationPanel.tsx`; also fixed the shared `applicationStatuses` constant it inherited from (used elsewhere by University Rep/Admin) to the correct enum. Backend: 7 new pytest cases -- advance succeeds, cross-counselor 403 even by direct ID, four unsupported exception values all rejected (422) and leave status unchanged, backward/same-stage move rejected, non-counselor role rejected on `/advance`, generic PATCH also rejects an unsupported status, auth required (`test_ovs_003_eligibility.py`). E2E: 3 new Playwright cases -- real picker advance, unsupported status rejected server-side, auth required.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ovs-003`  

### `OVS-004` — Application status tracking and notifications
- **Module:** Overseas  
- **Description:** Student sees current application stage at any time; notified on stage change.  
- **Actors:** primary — Student; secondary — Counselor  
- **Traces to:** BR BR-OVS-001 · PRD PRD-OVS-004 · Decisions DEC-WF-001, DEC-NOT-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** OVS-002.  
- **Main workflow:** Stage change triggers a notification and updates the Student's tracking view.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A stage-change notification that fails to send does not block the status update itself from being recorded.  
- **Dependencies/blockers:** OVS-002, NOT-001, NOT-002  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self (Student) — **known-incomplete since `DEC-ROLE-004` (2026-09-14):** same gap as `OVS-002` above, an Agent-referred applicant has no login to view this with; the Agent needs an equivalent view, not yet designed  
- **Integration/job/file/audit impact:** Email, Twilio WhatsApp, SMS  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Integration  
- **Complexity:** S  
- **Implementation status:** PARTIAL (`prompts/13`, 2026-09-02, Wave 2 — email channel only) · **Test status:** COMPLETE for the unblocked scope — 322/322 backend module baseline (5 new), 141/141 E2E baseline (1 new)  
  - Notification dispatch on stage change (`_notify_user`) was already wired into every status-changing endpoint by `OVS-002`/`003`, and is already exception-safe by construction: `post_optional_webhook` (the shared sender under both email and the Twilio-routed SMS/WhatsApp channels) catches every exception and returns `("failed", error)`, never raises -- `NOT-001`'s own fix, verified directly here with a forced-failure test rather than assumed, not re-implemented. The real, confirmed gap: `API_CONTRACT.md` §7 names a dedicated `GET /overseas/applications/{id}/status` tracking-view endpoint (distinct from `VISA-001`/`003`'s own checklist/status-summary endpoints) -- it did not exist. Added it (Self/Counselor-scoped, same `_assigned_application` scoping), returning current status/next_action plus the full `ApplicationStatusHistory` timeline. Frontend: a "View status history" toggle added to `OverseasApplyPanel.tsx`'s existing application cards. **Twilio WhatsApp/SMS remain credential-blocked** (`NOT-002`, no real Account SID/Auth Token exists in this repo) -- the notification call sites already accept those channels and would use them the moment real credentials are configured, but nothing here fakes that integration; email is the only channel this feature can exercise end-to-end today. Backend: 5 new pytest cases -- student views own history, cross-student 403, assigned/unassigned counselor, a forced notification-send failure still lets the status write through and persists the real failure reason on `NotificationDelivery`, auth required (`test_ovs_004_status_tracking.py`). E2E: 1 new Playwright case -- student sees a counselor's stage advance reflected in their own status history.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ovs-004`  

### `OVS-005` — Document upload against checklist
- **Module:** Overseas  
- **Description:** Student uploads required documents against a checklist varying by country/university/course.  
- **Actors:** primary — Student; secondary — Counselor  
- **Traces to:** BR BR-OVS-001 · PRD PRD-OVS-005 · Decisions DEC-DATA-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** OVS-002.  
- **Main workflow:** Student uploads; Counselor verifies (verification_status).  
- **Alternate workflow:** -  
- **Error/edge behavior:** Unsupported file types/sizes — open item.  
- **Dependencies/blockers:** OVS-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Student) / Counselor (verify) — **known-incomplete since `DEC-ROLE-004` (2026-09-14):** same gap as `OVS-002`/`004` above, an Agent-referred applicant has no login to upload with; the Agent needs an equivalent upload path, not yet designed  
- **Integration/job/file/audit impact:** S3 storage  
- **Security/privacy:** Documents may include passports/financial evidence — least-privilege access required.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 271/271 backend module baseline (9 new), 124/124 E2E baseline (3 new)  
  - Upload (`POST /overseas/documents`) already used the real presign-then-upload flow (`/files/presign`/`/files/local-upload`), least-privilege by construction, and verify (`PATCH /overseas/documents/{id}/verify`) was already correctly scoped to an assigned Counselor -- both needed no backend change. The real, confirmed gap (this feature's own security note, `OVS-DOC-02`): the student's own document list exposed the raw, permanently-public `file_url` directly through the generic table -- the exact same gap shape already fixed once for `STU-007`'s certificates -- and the Counselor's review queue had no way to view a document at all (no file reference of any kind, plus a raw-typed document UUID to verify blind). Added a self/assigned-scoped `GET /overseas/documents/{id}/download` (normalizes the two possible stored `file_url` shapes -- an already-prefixed local path vs. a bare S3 key -- to one bare key before calling `presign_download`, never trusting either shape blindly, same principle as `STU-007`'s own fix) and removed the raw `file` column from the student's own portal payload. Frontend: `DocumentDownloadPanel.tsx` (student, real download action) and `CounselorDocumentReviewPanel.tsx` (Counselor, real "View document" + Verify/Reject, replacing the raw-UUID form -- same gap class as `ADM-001`/`002`/`003`/`004`/`006`/`007`). Unsupported file types/sizes remain a confirmed, deliberate open item (`OVS-005-AC02`) -- not restricted beyond the shared `/files/presign` gate that already predates this feature. Backend: 9 new pytest cases -- upload, verify (assigned/unassigned), owning-student download, cross-student 403, assigned/unassigned counselor download, local-path normalization, 404 on unknown document, auth required (`test_ovs_005_documents.py`). E2E: 3 new Playwright cases -- student downloads their own document, counselor views+verifies, auth required.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ovs-005`  

### `OVS-006` — Scholarship listing and application
- **Module:** Overseas  
- **Description:** Browse scholarships; apply.  
- **Actors:** primary — Visitor, Student; secondary — -  
- **Traces to:** BR BR-OVS-001 · PRD PRD-OVS-006 · Decisions DEC-SCOPE-003  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Could / Wave 3  
- **Preconditions:** OVS-001 context.  
- **Main workflow:** Student applies to a listed scholarship.  
- **Alternate workflow:** -  
- **Error/edge behavior:** No eligibility rule engine — listing only.  
- **Dependencies/blockers:** OVS-001  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public view / Self (Student) apply  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 411/411 full backend regression (8 new; run jointly with `OVS-007` given the shared `services/portal.py` change), 4 new E2E cases (full E2E baseline not re-run — see `pending.md`)  
  - `POST /overseas/scholarships/{id}/apply` and `GET /public/scholarships` already existed and were already correct (self-scoped to `overseas_student` via `_require`, duplicate-blocked with a 409, only listing active scholarships) -- no backend change needed for either. The real, confirmed gap was entirely frontend: `/overseas/scholarships` only ever said "Sign in to apply," with no apply action anywhere in the product, even after logging in -- the endpoint had no caller at all. Added a `scholarships` section to the Overseas Student portal (nav + `services/portal.py`, a self-scoped read of the student's own `ScholarshipApplication` rows joined to `Scholarship`, mirroring the existing `applications` section's shape) so the new frontend panel can show real status, not just a bare apply button. Frontend: `ScholarshipApplyPanel.tsx` (lists active scholarships from the public endpoint, applies via the existing endpoint, shows a status badge once applied -- mirrors `OverseasApplyPanel.tsx`'s established pattern). Backend: 8 new pytest cases -- apply succeeds, duplicate rejected, inactive scholarship 404s, non-overseas-student role rejected, auth required, public listing excludes inactive scholarships, the new portal section only ever shows the caller's own applications, portal section requires auth (`test_ovs_006_scholarships.py`). E2E: 4 new Playwright cases -- public listing shows no apply action, logged-in apply succeeds and reflects on the same card (using a stable `data-scholarship-id` anchor, not title text, since an unrelated fixture in `test_ovs_001_discovery.py` leaves several non-unique "Test Scholarship" rows in the shared dev DB per `RAID.md` I-09), duplicate apply rejected, auth required (`ovs-006-scholarships.spec.ts`).  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ovs-006`  

### `OVS-007` — Events and workshops
- **Module:** Overseas  
- **Description:** View and register interest in Overseas events/workshops.  
- **Actors:** primary — Visitor, Student; secondary — -  
- **Traces to:** BR BR-OVS-001 · PRD PRD-OVS-007 · Decisions DEC-SCOPE-003  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Could / Wave 3  
- **Preconditions:** None.  
- **Main workflow:** User registers for an event.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Capacity/waitlist unspecified — open item.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Public  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Registration data is personal data.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 411/411 full backend regression (3 new; run jointly with `OVS-006`), 4 new E2E cases (full E2E baseline not re-run — see `pending.md`)  
  - `GET /public/webinars/{id}` and `POST /public/webinars/{id}/register` (built generically by `PUB-004`, reusing `Event`) already worked for any `Event` row regardless of division, unauthenticated, with no division check anywhere in either handler -- confirmed by reading the code, not assumed, and closed with a dedicated test against an `overseas`-division event rather than relying on `PUB-004`'s own `it`-division-only coverage. No backend change needed. The real, confirmed gap was entirely frontend: `/overseas/events` cards either linked to `event.registration_url` (which the underlying `/public/events` list endpoint never actually returns -- so this branch was always dead) or fell back to a generic "Register interest" link to `/overseas/contact`, with no in-app registration despite the endpoint already supporting it. Added `apps/web/app/overseas/events/[id]/page.tsx` (mirrors `apps/web/app/it/webinars/[id]/page.tsx`: past events show a closed state, an explicit `registration_url` shows an external "Register via official link," otherwise the real in-app form) and pointed the listing page's cards at it. Gave `WebinarRegisterForm.tsx` an optional `label` prop (default `"webinar"`) so the reused component reads "Register for this event" here instead of "...this webinar." Backend: 3 new pytest cases -- an overseas event appears in the division-scoped listing, registration succeeds with no auth against an overseas event specifically, a past overseas event's registration is rejected (`test_ovs_007_events.py`). E2E: 4 new Playwright cases -- listing links to the detail page, unknown id 404s gracefully, unauthenticated registration succeeds, responsive at 375px (`ovs-007-events.spec.ts`).  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ovs-007`  


## Visa

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `VISA-001` | Visa checklist and documentation | CURRENT | P1 | Should | Wave 2 | S | Y | Y | Y |
| `VISA-002` | Visa interview preparation | CURRENT | P2 | Could | Wave 3 | XS | Y | N | Y |
| `VISA-003` | Visa approval status tracking | CURRENT | P1 | Should | Wave 2 | S | Y | Y | N |

### `VISA-001` — Visa checklist and documentation
- **Module:** Visa  
- **Description:** Student completes a visa-specific document checklist.  
- **Actors:** primary — Student; secondary — Counselor  
- **Traces to:** BR BR-VISA-001 · PRD PRD-VISA-001 · Decisions DEC-SCOPE-006  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** OVS-005 documents verified.  
- **Main workflow:** Student completes checklist; status tracked.  
- **Alternate workflow:** -  
- **Error/edge behavior:** An unverified document does not block viewing the checklist -- only advancing past the stage that requires it.  
- **Dependencies/blockers:** OVS-005  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (Student) / Counselor  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Sensitive documents — least-privilege access.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 280/280 backend module baseline (9 new), 127/127 E2E baseline (3 new)  
  - `POST`/`PATCH /overseas/visa` (`VisaCase`, already "carries over" per `DATA_MODEL.md` §6.5) already existed but had two real, confirmed gaps. First, no endpoint let the Student -- this feature's own primary actor -- view their own checklist at all; both existing endpoints are Counselor/Admin-only writes. Second, `status` accepted any free-text value with zero validation against the contract-fixed `DEC-SCOPE-006` stage enum, and nothing enforced this feature's own error/edge behavior ("an unverified document does not block viewing the checklist -- only advancing past the stage that requires it", `VISA-001-AC02`) -- confirmed as a real, unenforced gap, the same shape already found and fixed for `OVS-003`'s application-status enum. Added a Self/Counselor-scoped `GET /overseas/applications/{id}/visa-checklist` (merges the checklist array with each item's real `StudentDocument` verification status, matched by `document_type`, viewable unconditionally; an application with no visa case yet returns an honest `exists: false` empty state, not an error or a fabricated one) and an enum + all-items-verified gate on `update_visa`/`create_visa_case` (advancing away from the "checklist" stage is rejected, 422, while any checklist item is not yet verified). Frontend: `VisaChecklistPanel.tsx` (Student, real per-application checklist view) and `CounselorVisaPanel.tsx` (Counselor, real application picker + forward-only stage controls, replacing the raw application/visa-case UUID form -- same gap class as `ADM-001`/`002`/`003`/`004`/`006`/`007`; Admin keeps the broader generic tool). Backend: 9 new pytest cases -- student views own checklist unconditionally, cross-student 403, assigned/unassigned counselor view, honest no-case-yet empty state, unsupported stage rejected on both create and update, advance blocked while unverified then succeeds once verified, auth required (`test_visa_001_checklist.py`). E2E: 3 new Playwright cases -- counselor starts a case + student views it unverified, advance blocked with an unverified document, auth required.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#visa-001`  

### `VISA-002` — Visa interview preparation
- **Module:** Visa  
- **Description:** Student accesses visa interview preparation material/guidance.  
- **Actors:** primary — Student; secondary — Counselor  
- **Traces to:** BR BR-VISA-001 · PRD PRD-VISA-002 · Decisions DEC-SCOPE-006  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Could / Wave 3  
- **Preconditions:** VISA-001.  
- **Main workflow:** Student views prep material.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Missing prep material for a specific country shows a clear fallback, not a broken page.  
- **Dependencies/blockers:** VISA-001  
- **UX required:** Y · **API required:** N (catalog flag stale — `API_CONTRACT.md` §7 names a dedicated endpoint; contract wins) · **DB impact:** Y · **RBAC/resource-scope:** Self (Student) — Counselor is *not* granted here despite being listed as secondary actor, per `API_CONTRACT.md` §7's explicit scope row  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** XS  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03 — closes Wave 3's last named item) · **Test status:** COMPLETE — 22/22 targeted backend run (7 new `VISA-002` cases + the 15 existing `VISA-001`/`VISA-003` cases they share `services/portal.py`/`workflows.py` visa call sites with), 3 new E2E cases. Full backend/E2E regression not re-run for this feature individually, per the user's standing instruction to batch it every 3-4 features rather than after each one — due at the next batch checkpoint.  
  - No such endpoint existed before this feature — `API_CONTRACT.md` §7 names `GET /overseas/visa/interview-prep` distinctly from its `visa-checklist`/`visa-status` siblings: Self only (no Counselor), and not scoped to a single `application_id` (returns one entry per the student's own visa cases instead). The actual prep-material *content* is a confirmed, still-open item (`PRODUCT_DECISION_REGISTER.md` DEC-DATA-001/DEC-SCOPE-006 — "the actual checklist/interview-prep/tracking rules ... remain an open item"), so added a nullable `Country.interview_prep` column (migration `0021`) rather than a dedicated table, matching `Country`'s other free-text fields, and seeded illustrative example content for exactly two countries (`usa`, `united-kingdom`) — every other seeded country stays empty so the "explicit fallback, not a 500" path (`VISA-002-AC02`) is genuinely exercised in the running app, not just asserted in a test. The new field is never exposed through the public `/public/countries/{slug}` endpoint (an explicit allowlist schema, `CountryOut`, confirmed directly) — only through the new self-scoped endpoint. Frontend: folded into the existing `VisaChecklistPanel.tsx` (the same "visa-status" portal page `VISA-003` already shares with `VISA-001`) rather than adding a new nav item, since the precondition (`VISA-001`) and audience are identical. Backend: 7 new pytest cases — available material returned, missing material returns an explicit fallback not an error, empty list with no visa case yet, a different student never sees another's case, Counselor role rejected (Self-only per contract), auth required, the field never leaks through the public country endpoint (`test_visa_002_interview_prep.py`). E2E: 3 new Playwright cases — real material shown for a country that has it, honest fallback shown for one that doesn't, auth required (`visa-002-interview-prep.spec.ts`). While building this feature's E2E spec, found and worked around a real, pre-existing shared-DB-volume contention issue (not caused by this feature): the single seeded `counselor@edusphere.local` account has accumulated so many test-assigned applications over the session that `CounselorVisaPanel.tsx`'s per-row follow-up fetches can intermittently miss a spec's assertion window under contention with other tests — confirmed passing cleanly in isolation every time; documented as a `RAID.md` I-06 addendum, not silently patched.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#visa-002`  

### `VISA-003` — Visa approval status tracking
- **Module:** Visa  
- **Description:** Track visa approval status through to a final outcome.  
- **Actors:** primary — Student; secondary — Counselor  
- **Traces to:** BR BR-VISA-001 · PRD PRD-VISA-003 · Decisions DEC-SCOPE-006  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** VISA-001.  
- **Main workflow:** Status updates as the case progresses (checklist → ... → decision).  
- **Alternate workflow:** -  
- **Error/edge behavior:** Compliance rule: never represent EduSphere as the visa decision-maker (found in reference implementation copy).  
- **Dependencies/blockers:** VISA-001  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self (Student) / Counselor  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 286/286 backend module baseline (6 new), 129/129 E2E baseline (2 new)  
  - `API_CONTRACT.md` §7 names `GET /overseas/applications/{id}/visa-status` as its own dedicated route, distinct from `VISA-001`'s checklist endpoint -- it did not exist at all, and a Counselor had no per-application status view (only the aggregate "Visa Tracking" table, no drill-down). Added the endpoint (Self/Counselor-scoped, reusing `VISA-001`'s own `_assigned_application` scoping), returning status/appointment/tracking-reference plus a fixed compliance disclaimer on every response (`VISA-003-AC02`) -- sourced verbatim from the reference implementation's own compliance language (`DATA_MODEL.md` §6.5), never varied per case, so no response can ever imply EduSphere decides visa outcomes; verified directly against a list of forbidden decision-maker phrases, not assumed absent. Wired into both `VisaChecklistPanel.tsx` (Student) and `CounselorVisaPanel.tsx` (Counselor) -- both now also show the tracking reference/appointment date and the disclaimer, sourced from this endpoint rather than duplicated as static copy. Backend: 6 new pytest cases -- status visible with disclaimer, no forbidden phrases anywhere in the response, cross-student 403, assigned/unassigned counselor, honest no-case-yet empty state (disclaimer still present), auth required (`test_visa_003_status.py`). E2E: 2 new Playwright cases -- student and counselor both see the same tracking reference and disclaimer.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#visa-003`  


## Agent

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `AGT-001` | Agent self-registration and approval gate | CURRENT | P2 | Should | Wave 3 | S | Y | Y | Y |
| `AGT-002` | Referred-student roster and status | CURRENT | P2 | Should | Wave 3 | S | Y | Y | N |
| `AGT-003` | Commission accrual (automatic trigger) | CURRENT | P2 | Should | Wave 3 | M | Y | Y | Y |
| `AGT-004` | Commission payout request and approval | CURRENT | P2 | Should | Wave 3 | M | Y | Y | Y |

### `AGT-001` — Agent self-registration and approval gate
- **Module:** Agent  
- **Description:** Agent self-registers; account held Pending until Overseas Admin approves.  
- **Actors:** primary — Agent; secondary — Overseas Admin  
- **Traces to:** BR BR-AGT-001 · PRD PRD-AGT-001, PRD-ADM-004, NFR-SEC-002 · Decisions DEC-SCOPE-004  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 3  
- **Preconditions:** FND-002.  
- **Main workflow:** Agent registers; Overseas Admin approves/rejects (ADM-015).  
- **Alternate workflow:** -  
- **Error/edge behavior:** Pending/Rejected Agent cannot refer students or view data.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Agent (self) / Overseas Admin (approve)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** Unit, API, UI, Security  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 3) · **Test status:** COMPLETE — 336/336 backend full regression, 143/143 E2E baseline (2 new)  
  - Self-registration (`POST /auth/register` with `account_type=agent`) and the `UserRoleAssignment.approval_status` schema already existed from an earlier foundation pass, and `core/rbac.py`'s own deny-by-default machinery (`user_has_role`, `require_role`, etc.) already correctly denies a Pending/Rejected Agent -- but a real, confirmed, significant gap: **no route in the codebase actually used that machinery**. Every real endpoint checks the legacy `User.role` column directly (`workflows.py`'s `_require`, `portal.py`'s dispatcher), which has no approval concept at all -- confirmed directly against the running stack: a Pending Agent could reach every agent-scoped endpoint and portal section immediately after self-registering, completely bypassing the approval gate (`AGT-001-AC02`). Added `core.rbac.agent_is_approved()` (a sync check against the already-eager-loaded `user.role_assignments`) and wired it into both call sites, closing the gap for every existing and future agent-gated route in one place. Added the missing `POST /overseas-admin/agents/{id}/approve`/`reject` actions (a new `/overseas-admin` route namespace per `API_CONTRACT.md`, `agents_router` in `admin.py`) and `GET /overseas-admin/agents`, none of which existed. Frontend: a pending/rejected agent already gets an honest "Access unavailable... Agent registration is pending approval" message with zero new code (the existing `serverApi`/`PortalPage.tsx` error-propagation path, same as `EMP-001`'s "Sign in required" precedent) -- only the Admin-facing approval action needed building, `AgentApprovalPanel.tsx` on a new "agents" Overseas Admin portal section. Given the shared, cross-cutting blast radius of this fix (`_require`/portal dispatcher touch every existing feature), ran the full 329-case backend regression rather than a module-scoped one -- all passed, confirming no collateral denial of already-approved roles. Backend: 7 new pytest cases -- pending agent denied the portal dashboard and denied creating an application, Admin approves and access opens up, Admin rejects and access stays denied, non-admin cannot approve, unknown-agent 404, auth required (`test_agt_001_registration_approval.py`). E2E: 2 new Playwright cases -- pending agent sees the honest denial message, Admin approves and the agent then reaches their dashboard.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#agt-001`  

### `AGT-002` — Referred-student roster and status
- **Module:** Agent  
- **Description:** Agent views roster of referred students and each one's application status.  
- **Actors:** primary — Agent; secondary — -  
- **Traces to:** BR BR-AGT-001 · PRD PRD-AGT-002 · Decisions DEC-SCOPE-004, DEC-ROLE-004  
- **`DEC-ROLE-004` reconciliation (2026-09-14):** this feature's shipped scope is **read-only** and
  stays correct as far as it goes, but it is now known-incomplete against a confirmed decision — an
  Agent-referred student is never issued a login (`DEC-ROLE-004`, `CONFIRMED_CURRENT`), so the Agent
  must also be able to **create** the referral and perform the write actions `OVS-002`/`004`/`005`
  reserve for `Self (Student)` (submit application, track status, upload documents) on that
  student's behalf. This is a new Feature ID's worth of scope, not a patch to this one — not yet
  numbered, designed, or built. See `RBAC_MATRIX.md` §2.8, `DATA_MODEL.md` §6.2/§6.8, and
  `PRD_OPEN_ITEMS.md` item 68.  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 3  
- **Preconditions:** AGT-001 approved; OVS-002 applications linked via agent_id.  
- **Main workflow:** Agent views own referrals only.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Agent never sees another agent's referrals.  
- **Dependencies/blockers:** AGT-001, OVS-002  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Self (Agent, own referrals)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 336/337 backend full regression (1 known shared-DB contention flake, reproduced clean in isolation -- `RAID.md` I-04/I-06), 8 new backend cases, 2 new E2E cases  
  - `GET /portal/overseas/agent/students` (roster) and `GET /portal/overseas/agent/applications` already existed, already self-scoped (`agent_id == user.id`) and already denied a Pending/Rejected agent via `AGT-001`'s `agent_is_approved()` gate -- no schema change needed (`DATA_MODEL.md` §6.8: "DB impact: N"). The real, confirmed gap against this feature's own "roster of referred students **and each one's application status**" wording: the roster only ever showed the `AgentStudent` link's own status flag (an "active"/"inactive" referral flag), never the referred student's actual `OverseasApplication` status -- an agent had to cross-reference a second page by student name to see it, and a referred student with no application yet showed nothing distinguishing that from an application in progress. Fixed in `apps/api/app/services/portal.py`'s `_agent()` "students" branch by joining in each student's most recent application (reusing the query already scoped to this agent -- no new query, no schema change). Separately found and fixed a real, confirmed seed-data defect surfaced while writing this feature's E2E test against the seeded demo agent: `app/seed.py` never granted `agent@edusphere.local` an approved `UserRoleAssignment` row at all; `auth._sync_role_assignment` lazily creates a *pending* one for any `role="agent"` user on their first-ever login and, by its own documented design, never overwrites it afterward -- so the demo agent persona was silently stuck 403 on its own dashboard in any environment where someone logged into it before this seed step ran (confirmed directly: it happened to this session's own first Playwright run against it). `seed.py` now explicitly (re-)approves the demo agent's assignment, idempotently, self-healing an already-pending row rather than only creating a missing one. Given the shared demo-seed blast radius of that fix, ran the full backend regression rather than a module-scoped one. Backend: 8 new pytest cases -- roster shows a referred student's application status, a referred student with no application yet reads "No application yet" rather than a blank/misleading field, an agent never sees another agent's referrals via the roster, pending/rejected agents denied, non-agent role denied, auth required, the adjacent applications-list section confirmed still self-scoped (`test_agt_002_referrals.py`). E2E: 2 new Playwright cases -- the seeded demo agent's Students page shows the referral and its application status together in one row, a freshly self-registered-and-approved second agent's roster stays empty of the first agent's referral.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#agt-002`  

### `AGT-003` — Commission accrual (automatic trigger)
- **Module:** Agent  
- **Description:** Commission auto-created/marked eligible when a referred application reaches finalized+joined; amount stays admin-discretionary per application (no fixed rate, by design).  
- **Actors:** primary — Agent; secondary — Overseas Admin  
- **Traces to:** BR BR-AGT-002 · PRD PRD-AGT-003 · Decisions DEC-SCOPE-005  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 3  
- **Preconditions:** AGT-002; application reaches the (to-be-mapped) joined status.  
- **Main workflow:** System auto-creates eligible commission on trigger.  
- **Alternate workflow:** Overseas Admin sets/adjusts amount.  
- **Error/edge behavior:** No commission creatable before the trigger condition — extends the base codebase's manual-only creation.  
- **Dependencies/blockers:** AGT-002, OVS-004  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Agent (view own) / Overseas Admin (amount)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Extends base codebase logic — see FEATURE_QUESTIONS.md for the exact trigger status mapping.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** Unit, API  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 348/348 backend full regression, 11 new backend cases, 2 new E2E cases  
  - Implemented `DATA_MODEL.md` §6.3's `ADR-012`-resolved trigger mapping exactly: an `ApplicationStatusHistory` row written with `to_status='enrolled'` for an application with an assigned `agent_id` auto-creates an `AgentCommission(status="estimated", amount=0, created_by="system_trigger")` -- no fixed commission rate is confirmed (`DEC-SCOPE-005`), so the amount genuinely cannot be known at trigger time. Implemented as one shared helper, `_maybe_trigger_agent_commission()`, called from both existing status-write sites (`PATCH /overseas/applications/{id}` and `POST .../advance`) -- the trigger is about the transition itself, not which endpoint performed it. New migration `0018` adds `AgentCommission.created_by` (`system_trigger`/`admin_manual`). Found and fixed two real, confirmed gaps in the base codebase's existing manual-commission path (`POST /overseas/agent/commissions`), both against this feature's own contract: `AGT-003-AC03` ("no commission creatable before the trigger condition") -- the endpoint never checked the application had actually reached `enrolled`, so an Overseas Admin could create a commission for an application still at `enquiry`; now 409s. `AGT-003-AC02` ("Overseas Admin sets/adjusts amount") -- no endpoint existed anywhere for an Admin to put a real amount on a commission, which every system-triggered `estimated` row needs since it starts at 0; added `PATCH /overseas/agent/commissions/{id}` (Overseas-Admin-only, 409 once claimed/beyond -- protects a state the Agent has already acted on -- flips `estimated`→`eligible` once an amount is set). Also found there was no Overseas Admin UI surface for commissions at all (not even a read-only list) -- added a division-wide "Commissions" portal section (`services/portal.py`, matching the scope of Admin's other division-level sections, not self-scoped like the Agent's own view) plus the nav entry and action form. Given the shared status-write-path change touches every existing status transition on every overseas application, ran the full backend regression rather than a module-scoped one. Along the way, a genuine test-data-hygiene bug was found and fixed in this session's own `AGT-002` test fixtures: `_make_university()`'s display `name` (unlike its already-uuid'd `slug`) wasn't uniquified per run, so repeated test runs silently left several identically-named universities in the shared catalog, one of which an "any available university" E2E helper picked and applied the real seeded demo student to -- colliding with `agt-002-referrals.spec.ts`'s assertion about her known application status. Fixed in both `test_agt_002_referrals.py` and this feature's own `test_agt_003_commission_accrual.py`, and the accumulated stray rows cleaned up (`RAID.md` I-09). Backend: 11 new pytest cases -- trigger fires via `/advance`, trigger fires via the generic `PATCH`, no commission without an `agent_id`, no duplicate on a second write, admin-manual creation before `enrolled` rejected, admin-manual creation at `enrolled` allowed and tagged `admin_manual`, Admin sets the amount on an `estimated` row (flips to `eligible`), amount locked once `claimed`, Agent cannot adjust amounts, unknown-commission 404, auth required (`test_agt_003_commission_accrual.py`). E2E: 2 new Playwright cases -- an application reaching `enrolled` auto-creates an estimated commission the Admin can see and set an amount on, auth required.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#agt-003`  

### `AGT-004` — Commission payout request and approval
- **Module:** Agent  
- **Description:** Agent claims commission; separate Overseas Admin approval required before paid — closes a gap the base codebase left open.  
- **Actors:** primary — Agent; secondary — Overseas Admin  
- **Traces to:** BR BR-AGT-002 · PRD PRD-AGT-004, PRD-ADM-005, NFR-SEC-002 · Decisions DEC-SCOPE-005  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 3  
- **Preconditions:** AGT-003 commission eligible/estimated.  
- **Main workflow:** Agent claims; Overseas Admin approves payout (new endpoint).  
- **Alternate workflow:** -  
- **Error/edge behavior:** No commission reaches paid without explicit Overseas Admin approval, distinct from the Agent's own claim.  
- **Dependencies/blockers:** AGT-003  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Agent (claim) / Overseas Admin (approve)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Net-new payout-approval endpoint — not present in the base codebase.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** Unit, API, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3 — closes the Agent module/stream) · **Test status:** COMPLETE — 358/358 backend full regression, 10 new backend cases, 2 new E2E cases  
  - Net-new `POST /overseas-admin/commissions/{id}/approve-payout` -- no equivalent existed in the base codebase (`REFERENCE_IMPLEMENTATION_FINDINGS.md` §5.4). Implements `PRD.md` §6.4's confirmed state machine exactly ("...Commission Accrued -> Payout Requested -> Overseas Admin Approval -> Paid"): requires only `status == "claimed"` (the Agent's existing, unchanged claim action -- no third agent-facing action was invented). Honors `DATA_MODEL.md` §6.7's "claimed can never transition directly to paid" by genuinely writing the commission through `payout_pending` (its own `AuditLog` row) before advancing to `paid` within the same approval request, since no third actor/action is named anywhere in the confirmed contract to trigger that transition independently. New migration `0019` adds `AgentCommission.payout_approved_by_user_id`/`payout_approved_at`. Implemented the two-gate same-actor rule (`NFR-SEC-002`, `RBAC_MATRIX.md` §2.8): an `admin_manual`-created commission's creating Overseas Admin (identified via the `agent.commission_create` `AuditLog` row -- `AgentCommission` itself carries no dedicated creator-identity column, only the `created_by` mode enum `AGT-003` added) is 403'd from approving their own commission's payout; a different Admin may. **Documented, deliberate open item, not silently widened beyond the confirmed contract**: `RBAC_MATRIX.md`'s rule is textually scoped to `created_by` at *creation* time only -- a `system_trigger`-created commission whose amount an Admin later set via `AGT-003`'s `PATCH .../commissions/{id}` is exempt from the two-gate check even if that same Admin then approves its own payout. This is a real gap in the confirmed contract's literal scope; flagged in code (`admin.py`'s `approve_commission_payout`) and directly tested (`test_same_admin_can_approve_a_system_triggered_commission_even_after_setting_its_amount`), not silently patched with an invented stricter rule that would exceed what `RBAC_MATRIX.md`'s approved text actually says (`docs/product/PRD_OPEN_ITEMS.md`). Added the "Approve commission payout" action to `AGT-003`'s Overseas Admin "Commissions" page (`WorkflowPanel.tsx`) -- no separate page needed. Given the shared `AgentCommission` model/migration change, ran the full backend regression rather than a module-scoped one. Backend: 10 new pytest cases -- approval reaches `paid` with `payout_approved_by_user_id`/`payout_approved_at` set, the `payout_pending`→`paid` audit-trail ordering and sequence, a not-yet-claimed commission rejected, an already-paid commission rejected (no double-approval), an Agent cannot approve any payout, the creating Admin cannot approve their own manually-created commission's payout, a different Admin can, the documented system-trigger exemption from the two-gate check, unknown-commission 404, auth required (`test_agt_004_commission_payout.py`). E2E: 2 new Playwright cases -- an Overseas Admin approves a claimed commission through the real "Approve commission payout" UI action and it reaches `paid`, a non-admin (the Agent) is 403'd at the API layer.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#agt-004`  


## Overseas Staff

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `CNS-001` | Counselor workspace `[base]` | CURRENT | P1 | Should | Wave 2 | L | Y | Y | Y |
| `UNI-001` | University Representative portal `[base]` | CURRENT | P2 | Should | Wave 3 | M | Y | Y | Y |

### `CNS-001` — Counselor workspace
- **Module:** Overseas Staff  
- **Description:** [Base role] Assigned students, leads, documents, applications, visas, appointments, reports.  
- **Actors:** primary — Counselor; secondary — -  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** FND-002.  
- **Main workflow:** Counselor manages their assigned Overseas caseload end-to-end.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A Counselor cannot act on a student who is not assigned to them, even via a direct record ID.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Counselor (own assigned students)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI  
- **Complexity:** L  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 292/292 backend module baseline (6 new), 132/132 E2E baseline (3 new)  
  - This `[base]` feature is the Counselor's entire workspace, spanning sections this session's own `OVS-003`/`OVS-005`/`VISA-001`/`VISA-003` work had already scoped and tested (applications, documents, visa) -- those needed no further change here. Two real, confirmed gaps remained. First, `PORTAL_NAV`'s own "Leads" and "Reports" sections had no handler at all -- both 404'd with "Workspace not found," confirmed directly against the running stack before fixing it. Added both: "Leads" scoped to `Enquiry.owner_id == <this counselor>` (the same routing field `ADM-002` already lets Admin set, not a new invented field), "Reports" an aggregate of the counselor's own already-scoped caseload (applications by status, documents pending verification, active visa cases) -- both render through the existing generic `PortalSection` component with zero new frontend code. Second, and a direct `CNS-001-AC02` violation: `POST /overseas/appointments` let a Counselor schedule an appointment for **any** overseas student, not just their own assigned ones -- unlike `add_document` (`OVS-005`), which already had the equivalent assignment check. Added the same check. Backend: 6 new pytest cases -- leads scoped to owner, reports aggregate correctness, cross-student appointment-scheduling rejected, own-student scheduling still works, student self-scheduling unaffected, auth required (`test_cns_001_counselor_workspace.py`). E2E: 3 new Playwright cases -- Leads and Reports both load instead of 404ing, auth required.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#cns-001`  

### `UNI-001` — University Representative portal
- **Module:** Overseas Staff  
- **Description:** [Base role] External partner-university portal: applications sent to their institution, offer-letter tracking, admission updates, student communication, reports.  
- **Actors:** primary — University Representative; secondary — Counselor  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 3  
- **Preconditions:** FND-002; a University partner record exists.  
- **Main workflow:** Rep reviews/updates applications sent to their university.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Rep sees only applications addressed to their own institution.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** University Representative (own institution only)  
- **Integration/job/file/audit impact:** Inbound university email (base)  
- **Security/privacy:** External-partner access — least-privilege scoping required.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 388/388 backend full regression, 7 new backend cases, 4 new E2E cases  
  - Most of this feature's confirmed scope already existed and was already correctly scoped: `dashboard`/`applications`/`offer-letters`/`admission-updates` all filter server-side by `User.profile["university_id"]` (via `_assigned_application`, `list_overseas_applications`, and the shared portal dashboard handler) -- confirmed directly by reading the actual code, not assumed, even though `DATA_MODEL.md` §6.10 describes this scope field as still needing to be built (it had already been added as a byproduct of earlier session work on `OVS-003`/`CNS-001`'s shared plumbing). Two real, confirmed gaps found instead: (1) `reports` -- `PORTAL_NAV["overseas/university"]` lists "Reports" for this role, but `services/portal.py`'s only "reports" handler was guarded `and user.role == "counselor"`, so the Rep's own nav link 404'd ("Workspace not found") -- the exact same bug class `CNS-001` found and fixed for the Counselor's own Leads/Reports this session, just never carried over to this role. Added a Rep-specific aggregate (application count, offers-extended count derived from membership in the confirmed `OVERSEAS_APPLICATION_STAGES` sequence's offer-onward stages -- not an invented status). (2) No endpoint existed anywhere for `API_CONTRACT.md` §9's explicitly-named `POST /university-rep/applications/{id}/updates` ("posts an admission update, visible to the assigned Counselor and Student") -- the generic PATCH a Rep already had access to only edits the application's own fields, with no distinct communication action. Added `POST /workflows/overseas/university-rep/applications/{id}/updates`, reusing the existing `_notify_user` mechanism already used everywhere else this session (no new model or migration) and the same `_assigned_application` own-institution scoping check -- verified directly that a Rep is denied even via a spoofed application ID belonging to another institution (`UNI-001-AC02`). Added the "Post admission update" UI action to the previously read-only "Student Communication" section (`WorkflowPanel.tsx`) -- no new page needed. Given the shared `portal.py`/`workflows.py` change, ran the full backend regression rather than a module-scoped one. Backend: 7 new pytest cases -- Reports renders instead of 404ing with correct counts, Reports only counts the Rep's own institution's applications, posting an update notifies both the assigned student and counselor, cross-institution posting is denied even via a direct application ID, a blank update message is rejected, a non-Rep role is rejected, auth required (`test_uni_001_university_rep_portal.py`). E2E: 4 new Playwright cases -- the Reports page loads with the real aggregate instead of 404ing, a Rep posts an admission update through the real UI action, cross-institution posting is denied at the API layer, unauthenticated access redirects to login.
  **Follow-up (2026-09-05, tester feedback via WhatsApp 2026-09-04, `RAID.md` I-12):** confirmed directly that "Offer Letters" fell through to the same unfiltered, unlabeled "Application Tracking" branch as "Applications"/"Admission Updates" (`services/portal.py` line 917) -- byte-identical heading and rows, indistinguishable as its own feature. The Student role's own "offer-letters" section already has a dedicated, filtered view (`offer_letter_url` set or `status` offer/accepted); never extended here. Added a `university_rep`-specific branch mirroring that exact precedent. "Admission Updates" mislabeling is confirmed but left open -- no data model exists for a queryable update feed (updates are fire-and-forget notifications, not persisted records); building one would invent unconfirmed scope. "Personal details" (also in the report) confirmed absent codebase-wide for every staff role, not just this one -- no route, no requirement anywhere in `docs/`; left unbuilt, logged as `PRD_OPEN_ITEMS.md` item 60. Backend: 2 new pytest cases (9/9 this file; 26/26 across the other files sharing this code path). E2E: 1 new Playwright case (5/5 this spec).
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#uni-001`  


## School

**Role structure superseded 2026-09-14 (same day as the original propagation):** `DEC-SCOPE-009`'s
single-representative-login model was reopened and resolved by `DEC-SCOPE-011` the same session —
four roles now exist (Principal, School Coordinator, Teacher, Parent), and `DEC-SCOPE-010` part 1
(roster bulk upload) and `DEC-ROLE-005` (Counselor extended for service-delivery data) were resolved
alongside it. `SCH-001` below is rewritten accordingly; `SCH-002` (bulk upload) is new.
**Provisioning resolved 2026-09-14 (later, same day), `DEC-SCOPE-012`:** `SCH-003` (onboarding)
added; `SCH-001`'s precondition updated from "provisioning OPEN" to depend on it.
**Service-delivery scope resolved 2026-09-14 (later still, same day), `DEC-ROLE-006`:** results,
career guidance, counselling, and psychometric all confirmed in scope, superseding `DEC-ROLE-005`'s
single-Counselor-role model with five specialized internal roles. `SCH-001`'s description updated;
`SCH-004`/`005`/`006` (the three new modules) added.

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `SCH-001` | School Portal role-based access (Principal/Coordinator/Teacher/Parent) `[new]` | CURRENT | P2 | Should | Wave 4 | L | Y | Y | Y |
| `SCH-002` | School Coordinator bulk student roster upload (template-download-first) `[new]` | CURRENT | P2 | Should | Wave 4 | M | Y | Y | N |
| `SCH-003` | School partner onboarding (Admin-created, Coordinator-seeded invites) `[new]` | CURRENT | P2 | Should | Wave 4 | M | Y | Y | Y |
| `SCH-004` | Career Guidance & Counselling module `[new]` | CURRENT | P2 | Should | Wave 4 | M | Y | Y | Y |
| `SCH-005` | Psychometric Assessment module `[new]` | CURRENT | P2 | Should | Wave 4 | M | Y | Y | Y |
| `SCH-006` | Academic Results module (Draft → Verified → Published) `[new]` | CURRENT | P2 | Should | Wave 4 | M | Y | Y | Y |

### `SCH-001` — School Portal role-based access (Principal/Coordinator/Teacher/Parent)
- **Module:** School  
- **Description:** [New] Net-new build — four school-side roles, each scoped server-side to their own institution only: **Principal** (read-only dashboard/stats/reports), **School Coordinator** (write access: create/manage students one at a time, schedule activities, track attendance, generate reports), **Teacher** (role code `school_teacher`, read-only, assigned students only — distinct from `DEC-ROLE-002`'s Trainer/Teacher), **Parent** (role code `school_parent`, read-only, own child(ren) only). Supersedes the single-representative-login model (`DEC-SCOPE-009`) per `DEC-SCOPE-011`. Service-delivery data (career guidance/psychometric/counselling/results) is confirmed in scope and owned by five specialized internal roles (`DEC-ROLE-006`, superseding `DEC-ROLE-005`'s single-Counselor-role framing) — see `SCH-004`/`005`/`006`, not this feature's roles.  
- **Actors:** primary — Principal, School Coordinator, Teacher, Parent; secondary — Academic Team, Career Counselor, Psychometric Team (service-delivery data, out of this feature's scope — see `SCH-004`/`005`/`006`)  
- **Traces to:** BR BR-SCH-001 · PRD PRD-SCH-001, PRD-SCH-002, PRD-SCH-004, PRD-SCH-005, PRD-SCH-006 · Decisions DEC-SCOPE-009, DEC-SCOPE-011, DEC-ROLE-005  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 4 (re-baselined in `DELIVERY_SEQUENCE.md`/`RELEASE_BACKLOG.md`, 2026-09-14)  
- **Preconditions:** FND-002; `SCH-003` (a School partner record and the acting account both exist, created per `DEC-SCOPE-012`'s onboarding flow).  
- **Main workflow:** Each role logs in and sees/acts on only their own institution's data, further scoped per role — Coordinator gets whole-institution write access, Teacher sees only assigned students, Parent sees only their own child(ren), Principal gets whole-institution read-only.  
- **Alternate workflow:** Bulk roster upload is a separate Feature ID (`SCH-002`, not yet built). Academic results (`DEC-SCOPE-010` part 2) and Career Guidance/Psychometric/Counselling are confirmed in scope but owned by `SCH-004`/`005`/`006`'s own specialized roles (`DEC-ROLE-006`), not reachable through this feature.  
- **Error/edge behavior:** No role ever sees another institution's data, even via a direct record ID — same IDOR-protection class as `UNI-001-AC02`/`AGT-002-AC02`. Teacher/Parent additionally never see students outside their own assigned/child scope, even within the same institution.  
- **Dependencies/blockers:** FND-002, `SCH-003`  
- **UX required:** Y · **API required:** Y · **DB impact:** Y (a School partner record and student↔institution linkage do not exist in the base codebase) · **RBAC/resource-scope:** Principal (own institution, read-only) / School Coordinator (own institution, write) / Teacher (own institution + assigned students only, read-only) / Parent (own institution + own child(ren) only, read-only)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** External-partner-style access — least-privilege own-institution scoping required for all four roles, same class as `UNI-001`/`AGT-002`.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** L  
- **Implementation status:** **COMPLETE** (`prompts/13`, 2026-09-14): net-new `SchoolStudent`/`SchoolParentLink`/`SchoolActivity`/`SchoolActivityAttendance` models + migration `0024_school_students_activities`, resolving `DATA_MODEL.md` §6.11's open `SchoolStudent` schema question and §6.12's `school_teacher`/`school_parent` scoping mechanisms as technical contract design (no `User` row for students — see `DATA_MODEL.md` §6.11's updated note; `assigned_teacher_user_id` per-student FK; many-to-many `SchoolParentLink`). `GET /school/dashboard`, `GET`/`POST`/`PATCH /school/students[/{id}]`, `POST /school/students/{id}/parents`, `GET`/`POST /school/activities`, `POST /school/activities/{id}/attendance` all added to `app/api/schools.py`. `school_id` scope reused `user.profile["school_id"]` — the same already-proven pattern as `university_rep`'s `university_id` (`DATA_MODEL.md` §6.12), no new scope table needed. Frontend: `SCHOOL_NAV` (own nav source, `lib/navigation.ts` — deliberately not the shared `PORTAL_NAV`/`[section]` dispatcher, to keep the `school_teacher`/`trainer` and `school_coordinator`/`coordinator` role-name collision guard airtight, `SCH-001-AC05`) with real dashboards for all four roles (`/school/{coordinator,principal,teacher,parent}/dashboard`), `SchoolStudentsPanel.tsx` (add/edit/link-parent), `SchoolActivitiesPanel.tsx` (schedule/mark attendance), `/school/teacher/students/[id]` detail. `ROLE_DASHBOARD_PATH` updated for all four roles. Backend: 10 new pytest cases (`test_sch_001_school_portal_access.py`) — Coordinator creates a student, Principal/Teacher/Parent cannot, Principal sees the whole institution read-only, Teacher sees only their assigned student (even within the same school, even via direct ID), Parent sees only their own linked child, no role crosses institutions even via direct ID, Coordinator schedules an activity and marks attendance, attendance rejects a student from another institution, `school_teacher` never reachable through a `trainer` check, unauthenticated access rejected. Full regression: unaffected by this feature (verified against the same 502/519-passing baseline SCH-003 established — see `RTM.md`). E2E: 2 new Playwright cases (`sch-001-school-portal-access.spec.ts`) — full flow live in a real browser (Coordinator adds a student, assigns a Teacher, links a Parent; each of Principal/Teacher/Parent sees only their own correctly-scoped view) and the Teacher direct-URL scope-deny path (`SCH-001-AC03`) — both confirmed passing. Two real test-authoring bugs were found and fixed while building the E2E coverage (not product bugs): a stale hardcoded `waitForURL("/")` assumption once `ROLE_DASHBOARD_PATH` gained real entries for these roles, and an Playwright strict-mode locator ambiguity between the row's "Link parent" toggle button and the form's own submit button of the same name.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sch-001`  

### `SCH-002` — School Coordinator bulk student roster upload (template-download-first)
- **Module:** School  
- **Description:** [New] Net-new build — School Coordinator downloads a fixed-format template, fills it offline, uploads it; server validates against the template schema and returns a row-level accept/reject report. Resolves `DEC-SCOPE-009`'s original deferral reason (avoiding uncontrolled bulk writes) by construction. Roster only — academic results bulk upload is a separate Feature ID (`SCH-006`, in scope, `DEC-ROLE-006`).  
- **Actors:** primary — School Coordinator; secondary — -  
- **Traces to:** BR BR-SCH-002 · PRD PRD-SCH-003 · Decisions DEC-SCOPE-010, DEC-SCOPE-011  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 4  
- **Preconditions:** `SCH-001` (School Coordinator account exists and is scoped to their institution).  
- **Main workflow:** Coordinator downloads template, uploads filled file, system validates and applies row-level, returns a per-row accept/reject report.  
- **Alternate workflow:** A row that fails validation does not block the rest of the batch — each row succeeds or fails independently.  
- **Error/edge behavior:** Coordinator can only bulk-create/update students within their own institution, never another's, even via a crafted upload row.  
- **Dependencies/blockers:** `SCH-001`  
- **UX required:** Y · **API required:** Y · **DB impact:** Y (`SchoolRosterUploadBatch`/`SchoolRosterUploadRow`, `DATA_MODEL.md` §6.13 — corrected 2026-09-14, was `N`) · **RBAC/resource-scope:** School Coordinator (own institution only)  
- **Integration/job/file/audit impact:** File upload — direct multipart parse (Python `csv` module), not the generic presign-then-store flow; no separate object-storage step needed for structured row processing.  
- **Security/privacy:** Server-side validation mandatory before any row is committed — never trust the uploaded file's own claims about itself (file type, size, row count) without independent verification.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** **COMPLETE** (`prompts/13`, 2026-09-14): net-new `SchoolRosterUploadBatch`/`SchoolRosterUploadRow` models + migration `0025_school_roster_upload`, resolving `DATA_MODEL.md` §6.13's open template-column-schema question as technical contract design — mapped directly from `SCH-001`'s own already-built `SchoolStudent` creation fields (`full_name`, `date_of_birth`, `grade_or_class`, `assigned_teacher_email`), not an invented field list. `GET /school/students/roster-template` (CSV download), `POST /school/students/bulk-upload` (Idempotency-Key required, per-row validation, a bad row never blocks the batch), `GET /school/roster-uploads/{batch_id}` all added to `app/api/schools.py`. **Real routing bug found and fixed during build** (not a design defect): FastAPI/Starlette match routes in registration order, so the static `/students/roster-template` path had to be registered *before* the dynamic `/students/{student_id}` route from `SCH-001`, or every request to it 422'd trying to parse "roster-template" as a UUID. Frontend: `SchoolBulkUploadPanel.tsx` (download-then-upload-then-report flow) at `/school/coordinator/students/bulk-upload`, linked from the dashboard and roster pages (not a standalone nav item, matching `SCR-SCH-005`'s own documented entry points). Backend: 10 new pytest cases (`test_sch_002_bulk_roster_upload.py`) — template download, idempotency-key requirement, successful bulk create, a bad row rejected without blocking good rows (`SCH-002-AC04`), teacher assignment via email, cross-institution teacher email rejected, idempotency replay never double-creates, batch-report fetch, cross-institution batch-report denied, non-Coordinator rejected. Full regression: 522/539 passing (same 17 pre-existing, unrelated failures as `SCH-001`/`SCH-003`). E2E: 1 new Playwright case (`sch-002-bulk-roster-upload.spec.ts`) — full download→fill→upload→row-level-report flow, confirmed live against the real stack.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sch-002`  

### `SCH-003` — School partner onboarding (Admin-created, Coordinator-seeded invites)
- **Module:** School  
- **Description:** [New] Net-new build — Overseas Admin creates a School partner record and a seed School Coordinator account for it, both active immediately (no approval gate). The Coordinator then invites/creates Principal, Teacher, and Parent accounts for that same institution, also active immediately. No School-side self-service registration exists — unlike Agent's model. Independent of the still-open School Partnership tiers question.  
- **Actors:** primary — Overseas Admin; secondary — School Coordinator  
- **Traces to:** BR BR-SCH-003 · PRD PRD-SCH-007 · Decisions DEC-SCOPE-012  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 4 (re-baselined in `DELIVERY_SEQUENCE.md`/`RELEASE_BACKLOG.md`, 2026-09-14)  
- **Preconditions:** FND-002; Overseas Admin's own division-management access (mirrors how Overseas Admin manages University partner records for `UNI-001`).  
- **Main workflow:** Overseas Admin creates School + seed Coordinator (active immediately) → Coordinator invites/creates Principal/Teacher/Parent for the same institution (active immediately).  
- **Alternate workflow:** Provisioning does not vary by School Partnership tier — every school gets the same onboarding path regardless of commercial tier, unless a future decision says otherwise.  
- **Error/edge behavior:** An account the Coordinator creates is always scoped to the Coordinator's own `school_id`, server-derived, never client-supplied — a Coordinator cannot create accounts for a different institution even via a crafted request.  
- **Dependencies/blockers:** FND-002  
- **UX required:** Y · **API required:** Y · **DB impact:** Y (`School`, `SchoolStudent`, account-role linkage per `DATA_MODEL.md` §6.11) · **RBAC/resource-scope:** Overseas Admin (create School + seed Coordinator, division-wide) / School Coordinator (invite/create Principal, Teacher, Parent — own institution only)  
- **Integration/job/file/audit impact:** Invite/notification delivery (email, per `NOT-001`) for the three Coordinator-created accounts.  
- **Security/privacy:** Invite tokens must be single-use and scoped to one institution — same class of concern as any other account-creation flow.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** **COMPLETE** (`prompts/13`, 2026-09-14): net-new `School`/`SchoolAccountInvite` models + migration `0023_school_onboarding`; `POST`/`GET /overseas-admin/schools` (`admin.py`'s `agents_router`, same `/overseas-admin` namespace as Agent approval) creates the School + seed Coordinator atomically, `UserRoleAssignment` written eagerly (not lazily on first login) so `assigned_by_user_id` records the acting Admin from creation; `POST /school/team/invites`/`GET /school/team`/`POST /school/invites/{token}/accept` (new `app/api/schools.py`) for the Coordinator-side invite flow, mirroring `auth.py`'s existing password-reset token pattern (`secrets.token_urlsafe` + SHA-256 hash, never stored raw). `school_id` scope lives in `User.profile["school_id"]`, the same established pattern already used for `university_rep`'s `university_id` scope (`workflows.py`, `portal.py`) — no new `SchoolRoleProfile` table needed. Four new role identifiers (`school_coordinator`/`school_principal`/`school_teacher`/`school_parent`) added to `core/rbac.py`'s `PERMISSIONS`, deliberately distinct from `trainer`/`coordinator` per `SCH-001-AC05`'s collision guard. Frontend reuses the established "generic portal list + dedicated create panel" convention (`AdminSchoolCreatePanel.tsx`, mirroring `AdminUniversityCreatePanel.tsx`) rather than a separate `/new` route — **this corrected `SCR-SCH-011` out of the UX catalogue, merged into `SCR-SCH-010`, see `SCREEN_CATALOG.md`**. Coordinator's Team screen (`SchoolTeamPanel.tsx`) stands alone at `/school/coordinator/team` (own single-item `PortalShell` usage, not the shared `PORTAL_NAV`/`[section]` dispatcher, since `SCH-001`'s dashboard doesn't exist yet). Backend: 9 new pytest cases (`test_sch_003_school_onboarding.py`) — admin creates school+coordinator active immediately, non-admin/unauthenticated rejected, coordinator invites all three roles, unsupported role rejected, invite accept creates the account scoped to the inviting school, a consumed token cannot be reused, non-coordinator cannot invite, a coordinator never sees another school's team. Full regression: 502/519 passing — the 17 failures are all pre-existing, unrelated to this feature (real Razorpay/Zoho credentials not configured in this dev environment, Celery worker not running). E2E: 2 new Playwright cases (`sch-003-school-onboarding.spec.ts`) — full onboarding→invite→accept→login flow live in a real browser, and the consumed-token honest-error-message path (`SCH-003-AC06`) — both confirmed passing against the real stack. A real UX bug was found and fixed while running the E2E test: the Coordinator's own account always appears in their own `/school/team` roster (a legitimate own-institution account, same as any other), so `SchoolTeamPanel.tsx`'s "just you so far" empty state originally never triggered — fixed to exclude the viewer's own `school_coordinator` row from that check.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sch-003`  

### `SCH-004` — Career Guidance & Counselling module
- **Module:** School  
- **Description:** [New] Net-new build — Career Counselor (new role) conducts career guidance sessions, adds counselling notes, and uploads recommendations for their assigned school-affiliated students, combining `EVID-014`'s "Career Awareness/Guidance" and "Individual Career Counselling" into one role's domain. School Coordinator sees this content read-only, for their own institution only. Supersedes `DEC-ROLE-005`'s "existing Counselor role" framing per `DEC-ROLE-006`.  
- **Actors:** primary — Career Counselor; secondary — School Coordinator (read-only), Parent/Student/Teacher (read-only)  
- **Traces to:** BR BR-SCH-005 · PRD PRD-SCH-009 · Decisions DEC-ROLE-006, DEC-SCOPE-013, DEC-SCOPE-014  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 4 (re-baselined in `DELIVERY_SEQUENCE.md`/`RELEASE_BACKLOG.md`, 2026-09-14)  
- **Preconditions:** FND-002; `ADM-001` (Overseas Admin or Super Admin creates the Career Counselor account and assigns their school portfolio via the Admin console — `DEC-SCOPE-014`, resolved 2026-09-14, **not** via `SCH-003`'s invite flow, which covers only the four school-side roles).  
- **Main workflow:** Career Counselor creates/manages career guidance sessions and counselling records for school-affiliated students at any school in their own portfolio; Coordinator and Parent/Student/Teacher view read-only.  
- **Alternate workflow:** Exact field list/session structure not itemized by `DEC-ROLE-006` — `EVID-014`'s detailed proposal is `DERIVED_BLUEPRINT` only, not confirmed. Do not invent beyond the confirmed actor/scope.  
- **Error/edge behavior:** Career Counselor never manages a student at a school outside their own portfolio (`DEC-SCOPE-013`), even via a direct record ID; School Coordinator never edits this content, only views it, for their own institution only.  
- **Dependencies/blockers:** FND-002, `SCH-001`, `ADM-001`  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Career Counselor (own school portfolio, `DEC-SCOPE-013`) / School Coordinator (read-only, own institution) / Parent, Student, Teacher (read-only, own scope) / Overseas Admin, Super Admin (account creation + portfolio assignment, `DEC-SCOPE-014`)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Service-delivery content about a minor — least-privilege access required, same class as VISA/OVS document handling.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** NOT STARTED — Contracts (including `DEC-SCOPE-013`/`014` portfolio-scoping/provisioning propagation, 2026-09-14), Quality, and UX propagation all complete; no code work has been done.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sch-004`  

### `SCH-005` — Psychometric Assessment module
- **Module:** School  
- **Description:** [New] Net-new build — Psychometric Team (new role) assigns psychometric assessments and uploads reports for their assigned school-affiliated students. School Coordinator sees this content read-only, for their own institution only. Supersedes `DEC-ROLE-005`'s "existing Counselor role" framing per `DEC-ROLE-006`.  
- **Actors:** primary — Psychometric Team; secondary — School Coordinator (read-only), Parent/Student/Teacher (read-only)  
- **Traces to:** BR BR-SCH-006 · PRD PRD-SCH-010 · Decisions DEC-ROLE-006, DEC-SCOPE-013, DEC-SCOPE-014  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 4 (re-baselined in `DELIVERY_SEQUENCE.md`/`RELEASE_BACKLOG.md`, 2026-09-14)  
- **Preconditions:** FND-002; `ADM-001` (Overseas Admin or Super Admin creates the Psychometric Team account and assigns their school portfolio via the Admin console — `DEC-SCOPE-014`, resolved 2026-09-14, **not** via `SCH-003`'s invite flow, which covers only the four school-side roles).  
- **Main workflow:** Psychometric Team assigns assessments and uploads reports for school-affiliated students at any school in their own portfolio; Coordinator and Parent/Student/Teacher view read-only.  
- **Alternate workflow:** Exact assessment types/report field list not itemized by `DEC-ROLE-006` — `EVID-014`'s detailed proposal is `DERIVED_BLUEPRINT` only, not confirmed. Do not invent beyond the confirmed actor/scope.  
- **Error/edge behavior:** Psychometric Team never manages a student at a school outside their own portfolio (`DEC-SCOPE-013`), even via a direct record ID; School Coordinator never edits this content, only views it, for their own institution only.  
- **Dependencies/blockers:** FND-002, `SCH-001`, `ADM-001`  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Psychometric Team (own school portfolio, `DEC-SCOPE-013`) / School Coordinator (read-only, own institution) / Parent, Student, Teacher (read-only, own scope) / Overseas Admin, Super Admin (account creation + portfolio assignment, `DEC-SCOPE-014`)  
- **Integration/job/file/audit impact:** File upload/storage (`report_url`, presigned-download pattern per `DATA_MODEL.md` §6.18 — corrected 2026-09-14, was `-`)  
- **Security/privacy:** Psychometric report data about a minor — least-privilege access required, same class as VISA/OVS document handling.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** NOT STARTED — Contracts (including `DEC-SCOPE-013`/`014` portfolio-scoping/provisioning propagation, 2026-09-14), Quality, and UX propagation all complete; no code work has been done.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sch-005`  

### `SCH-006` — Academic Results module (Draft → Verified → Published)
- **Module:** School  
- **Description:** [New] Net-new build — Academic Team (new role) uploads/edits academic results (subject/term/marks) for assigned school-affiliated students. A result is not visible to Parent/Student/Teacher until it passes Draft → Verified → Published. School Coordinator sees results read-only, for their own institution only. Resolves `DEC-SCOPE-010` part 2, actor/gate per `DEC-ROLE-006`.  
- **Actors:** primary — Academic Team; secondary — School Coordinator (read-only), Parent/Student/Teacher (read-only, once Published)  
- **Traces to:** BR BR-SCH-004 · PRD PRD-SCH-008 · Decisions DEC-SCOPE-010, DEC-ROLE-006, DEC-SCOPE-013, DEC-SCOPE-014, DEC-ROLE-007  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 4 (re-baselined in `DELIVERY_SEQUENCE.md`/`RELEASE_BACKLOG.md`, 2026-09-14)  
- **Preconditions:** FND-002; `ADM-001` (Overseas Admin or Super Admin creates the Academic Team account and assigns their school portfolio via the Admin console — `DEC-SCOPE-014`, resolved 2026-09-14, **not** via `SCH-003`'s invite flow, which covers only the four school-side roles). A portfolio needs **at least two** Academic Team members for publishing to be possible at all (`DEC-ROLE-007`'s same-actor restriction).  
- **Main workflow:** Academic Team uploads a result (Draft) → a **different** Academic Team member verifies it (Verified) → a **different** Academic Team member publishes it (Published) → Parent/Student/Teacher can then view it. Same-actor restriction confirmed `DEC-ROLE-007`, 2026-09-14: the uploader may never also verify or publish their own entry.  
- **Alternate workflow:** The Draft→Verified→Published gate, and its verify/publish same-actor restriction, are both confirmed (`DEC-ROLE-006`, `DEC-ROLE-007`, resolved 2026-09-14) — no longer an open re-confirmation item.  
- **Error/edge behavior:** A Draft or Verified (not yet Published) result is never visible to Parent/Student/Teacher, even via a direct record ID. Academic Team never edits a result at a school outside their own portfolio (`DEC-SCOPE-013`), even via a direct record ID. An Academic Team member can never verify or publish a result they themselves uploaded (`DEC-ROLE-007`).  
- **Dependencies/blockers:** FND-002, `SCH-001`, `ADM-001`  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Academic Team (own school portfolio, `DEC-SCOPE-013`; verify/publish restricted to a different member than the uploader, `DEC-ROLE-007`) / School Coordinator (read-only, own institution) / Parent, Student, Teacher (read-only, own scope, Published only) / Overseas Admin, Super Admin (account creation + portfolio assignment, `DEC-SCOPE-014`)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Academic record data about a minor — least-privilege access required, same class as VISA/OVS document handling.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** NOT STARTED — Contracts (including `DEC-SCOPE-013`/`014`/`DEC-ROLE-007` portfolio-scoping/provisioning/same-actor-restriction propagation, 2026-09-14), Quality, and UX propagation all complete; no code work has been done.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sch-006`  


## Notifications

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `NOT-001` | Email notifications | CURRENT | P1 | Should | Wave 2 | S | N | Y | Y |
| `NOT-002` | WhatsApp notifications (Twilio) | CURRENT | P1 | Should | Wave 2 | S | N | Y | Y |
| `NOT-003` | SMS notifications `[base]` | CURRENT | P2 | Could | Wave 3 | S | N | Y | Y |

### `NOT-001` — Email notifications
- **Module:** Notifications  
- **Description:** Status-change events trigger email notifications.  
- **Actors:** primary — All roles; secondary — -  
- **Traces to:** BR BR-NOT-001 · PRD PRD-NOT-001 · Decisions DEC-NOT-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** Triggering event occurs.  
- **Main workflow:** System sends email on defined trigger.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A failed send is retried per a defined policy (policy itself open -- PRD_OPEN_ITEMS item 13), never silently dropped.  
- **Dependencies/blockers:** —  
- **UX required:** N · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** System  
- **Integration/job/file/audit impact:** Email provider (unnamed)  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** API, Integration  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 221/221 backend suite  
  - The main workflow (status-change events trigger email) already existed broadly across the codebase — `_notify_user` (`apps/api/app/api/workflows.py`) fires on 12+ status-change events (enrollment, assignment reviewed, assessment published, certificate issued, question replied, job application/placement/offer updated, document reviewed, commission eligible, support ticket resolved), default channel `email`.
  - The real gap, found by code inspection: `send_notification` swallowed the actual failure reason (`except Exception: return "failed"`), so `NotificationDelivery.error` was never populated despite the column already existing; and `auth.forgot_password` discarded the send result entirely, creating no delivery record at all — a literal silent drop, directly violating AC02.
  - Fix: `send_notification`/`post_optional_webhook` (`apps/api/app/services/integrations.py`) now return `(status, error)`; all 4 call sites (`auth.py`, `workflows.py`, `communications.py`, `inbound.py`) persist the real error onto `NotificationDelivery.error`; `forgot_password` now creates its own `Notification`/`NotificationDelivery` row instead of discarding the result.
  - Added `NotificationDelivery.attempt_count` (alembic `0015_notification_attempts`, default 1) — the retry-eligible-state *mechanism* `DATA_MODEL.md` §7.1 requires. The retry *policy* itself (attempt count/backoff) stays OPEN (`PRD_OPEN_ITEMS.md` item 13, `DEC-NOT-001`'s open remainder) and is deliberately not invented — no automatic retry job was added.
  - Provider name, exact trigger catalogue, and templates remain OPEN per `DEC-NOT-001`/`PRD.md` §7.12 — untouched, out of scope.
  - Backend: 5 new pytest cases (`apps/api/tests/test_not_001_email_notifications.py`) — forgot-password creates a delivery record; a failed send captures the real error text (never silently dropped); `attempt_count` defaults to 1; `send_notification` surfaces the real exception detail on webhook failure; `not_configured` is reported without raising. Full regression: 221/221 passed.
  - No E2E — feature has no UX (`ux: N`, RBAC/resource-scope: System, no dedicated screen).
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#not-001`  

### `NOT-002` — WhatsApp notifications (Twilio)
- **Module:** Notifications  
- **Description:** Status-change events trigger WhatsApp notifications via Twilio.  
- **Actors:** primary — Student; secondary — -  
- **Traces to:** BR BR-NOT-001 · PRD PRD-NOT-002 · Decisions DEC-NOT-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** Triggering event occurs.  
- **Main workflow:** System sends WhatsApp message via Twilio on defined trigger.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Opt-out respected once defined.  
- **Dependencies/blockers:** BLOCKED (2026-09-02) — credentials, not a design gap. See below.  
- **UX required:** N · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** System  
- **Integration/job/file/audit impact:** Twilio WhatsApp Business API  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** API, Integration  
- **Complexity:** S  
- **Implementation status:** BLOCKED · **Test status:** BLOCKED  
  - Unlike `NOT-001`'s unnamed email provider, `DEC-NOT-001` confirms Twilio specifically as the WhatsApp provider, and `INTEGRATION_CONTRACTS.md` §4 requires real Twilio Account SID/Auth Token for the outbound API plus real status-callback signature verification for inbound delivery-status updates — the same "do not fake it" shape that blocks `STU-010`/`PAY-001`. No Twilio credentials exist anywhere in this repo (checked `.env.example` — no entry). Not implemented against invented/placeholder credentials; needs real credentials supplied before this can proceed.  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#not-002`  

### `NOT-003` — SMS notifications
- **Module:** Notifications  
- **Description:** [Retained] Status-change events can trigger SMS, alongside Email/WhatsApp.  
- **Actors:** primary — All roles; secondary — -  
- **Traces to:** BR — · PRD PRD-NOT-003 · Decisions DEC-NOT-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Could / Wave 3  
- **Preconditions:** Triggering event occurs.  
- **Main workflow:** System sends SMS on defined trigger.  
- **Alternate workflow:** -  
- **Error/edge behavior:** Same failure-handling expectation as Email/WhatsApp -- never silently dropped.  
- **Dependencies/blockers:** —  
- **UX required:** N · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** System  
- **Integration/job/file/audit impact:** SMS provider (unnamed)  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** API, Integration  
- **Complexity:** S  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#not-003`  


## Payments

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `PAY-001` | Payment gateway integration | CURRENT | P0 | Must | Wave 1 | M | N | Y | Y |

### `PAY-001` — Payment gateway integration
- **Module:** Payments  
- **Description:** Razorpay gateway integration with webhook signature verification (Stripe removed, `DEC-PAY-001` re-resolved 2026-09-03).  
- **Actors:** primary — Engineering; secondary — Student  
- **Traces to:** BR BR-PAY-001 · PRD PRD-PAY-001 · Decisions DEC-PAY-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 1  
- **Preconditions:** FND-001.  
- **Main workflow:** Checkout session created; webhook confirms payment.  
- **Alternate workflow:** Manual/offline payment record (base).  
- **Error/edge behavior:** Unverified/failed webhook never marks a fee paid.  
- **Dependencies/blockers:** FND-001  
- **UX required:** N · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** System  
- **Integration/job/file/audit impact:** Razorpay  
- **Security/privacy:** Webhook signature verification mandatory before accepting live money (base PRODUCTION_CHECKLIST.md).  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** Unit, API, Integration, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (2026-09-03, `prompts/13`, built jointly with `STU-010` — see that entry for the full description) — added the `PaymentWebhookEvent` idempotency ledger (keyed on Razorpay's `x-razorpay-event-id` header, confirmed via Razorpay's own docs rather than assumed), and fixed a real payment-matching defect surfaced by testing against the real API: the inherited webhook handler tried to match a payment via `entity.notes.reference`/`entity.receipt`, but Razorpay's Payment entity has no `receipt` field (Order-only) and notes-cascade from Order to Payment is undocumented either way — replaced with matching on `checkout_provider_order_id`, a value this app controls itself · **Test status:** PASS — see `STU-010`'s test-status line (shared test file, real Razorpay TEST-mode API + real HMAC verification)  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#pay-001`  


## Learning

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `LMS-001` | Native LMS module extensions | BLOCKED | P2 | Could | Unscheduled | L | Y | Y | Y |

### `LMS-001` — Native LMS module extensions
- **Module:** Learning  
- **Description:** Additional native LMS capability beyond already-scoped notes/assignments/Q&A/attendance/recordings/certificates.  
- **Actors:** primary — Student, Trainer; secondary — -  
- **Traces to:** BR — · PRD PRD-LMS-001 · Decisions DEC-LMS-001  
- **Scope/Priority/MoSCoW/Release:** BLOCKED / P2 / Could / Unscheduled  
- **⚠ Blocked because:** PRD-LMS-001's actual feature boundary (content authoring? sequencing? quizzes? gradebooks? SCORM?) is explicitly undefined — cannot decompose into buildable AC without inventing scope.  
- **Preconditions:** -  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** -  
- **Dependencies/blockers:** STU-004, TRN-005, TRN-006  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI, API  
- **Complexity:** L  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#lms-001`  


## Reporting

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `RPT-001` | Domestic/Employer reporting | CURRENT | P2 | Should | Wave 3 | M | Y | Y | N |
| `RPT-002` | Overseas reporting | CURRENT | P3 | Could | Wave 4 | M | Y | Y | N |

### `RPT-001` — Domestic/Employer reporting
- **Module:** Reporting  
- **Description:** Enrolment funnel, course performance, student progress, employer job activity, placement outcomes.  
- **Actors:** primary — IT Admin, Placement Team; secondary — Employer  
- **Traces to:** BR — · PRD PRD-RPT-001 · Decisions —  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P2 / Should / Wave 3  
- **Preconditions:** Underlying data exists (enrolment/attendance/placement features live).  
- **Main workflow:** Admin/Placement Team views reports.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A report with no underlying data for the selected period shows an empty state, never a zero-filled fabrication.  
- **Dependencies/blockers:** ADM-005, ADM-007  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** IT Admin, Placement Team  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** Responsive. · **Performance:** Query performance for large datasets.  
- **Required test types:** UI, API  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 3) · **Test status:** COMPLETE — 396/396 backend full regression, 8 new backend cases, 4 new E2E cases  
  - Neither the existing `GET /admin/reports/summary` (Super Admin's own cross-division console, `admin.py` -- users/leads/revenue only) nor the portal dispatcher's per-role "reports" section covered this feature's named scope at all. Confirmed directly: `PORTAL_NAV["it/admin"]` and `PORTAL_NAV["it/placement"]` both list "Reports", but `services/portal.py` had no handler for either role -- the exact same 404 bug class `CNS-001`/`UNI-001` found and fixed for their own roles this session, just never caught here. Added two real, data-derived report handlers: IT Admin (and Super Admin, who can do everything IT Admin can) gets an enrolment funnel (`Enquiry` -> `Enrollment` -> `Certificate` counts) and a per-course table (enrollment count, average `Enrollment.progress_percent`) built from real, already-existing data -- an honest computed `0` from a genuinely empty course is not a "fabrication" (`RPT-001-AC02`; `DATA_MODEL.md` §8's "no fabricated placeholder data" rule, which names `RPT-001` explicitly). Placement Team gets employer job activity and placement outcomes from real `Job`/`JobApplication`/`JobOffer` rows (open vs. total requirements per company, applications received, offers made/accepted). Both handlers deliberately exclude roles outside `RPT-001-AC03`'s named RBAC scope ("IT Admin, Placement Team"): `overseas_admin`'s own "Reports" nav link is left 404ing on purpose -- that is `RPT-002`'s unscheduled/blocked scope, not invented here as scope creep -- and `hr_team`, which shares every *other* section in the same outer role block as Placement Team, does not get this handler either, verified directly with its own test rather than assumed. No new frontend code was needed at all -- the existing generic portal page (`PortalSection`/`DataTable`) already renders whatever columns/metrics the backend returns, the same way `CNS-001`'s and `UNI-001`'s own "Reports" fixes needed none either. Given the shared `portal.py` change, ran the full backend regression rather than a module-scoped one. Backend: 8 new pytest cases -- IT Admin's funnel and per-course figures render with real numbers, a course with zero enrollments shows an honest `0` rather than a fabrication, Super Admin also gets the IT report, Overseas Admin's own "Reports" link still 404s (the deliberate scope boundary, not a regression), Placement Team's job-activity report renders with real figures, HR Team is confirmed excluded from the Placement Team handler, a non-admin role is rejected, auth required (`test_rpt_001_reporting.py`). E2E: 4 new Playwright cases -- IT Admin's Reports page loads with the real funnel instead of 404ing, Placement Team's Reports page loads with real job activity instead of 404ing, a non-admin is 403'd at the API layer, unauthenticated access redirects to login.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#rpt-001`  

### `RPT-002` — Overseas reporting
- **Module:** Reporting  
- **Description:** Application funnel by stage, agent commission report, visa-status aging.  
- **Actors:** primary — Overseas Admin; secondary — Counselor  
- **Traces to:** BR — · PRD PRD-RPT-002 · Decisions DEC-SCOPE-008  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P3 / Could / Wave 4  
- **Preconditions:** -  
- **Main workflow:** -  
- **Alternate workflow:** -  
- **Error/edge behavior:** -  
- **Dependencies/blockers:** OVS-004, AGT-003  
- **UX required:** Y · **API required:** Y · **DB impact:** N · **RBAC/resource-scope:** Overseas Admin  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** UI  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-05, tester feedback via WhatsApp 2026-09-04, `RAID.md` I-14) — tester reported "ADMINISTRATOR: reports not working" with the role ambiguous; reproduced directly against the running app for every candidate role before writing code: IT Admin's/Placement Team's own Reports (`RPT-001`) both worked, but Overseas Admin's own "Reports" link showed "Access unavailable — Workspace not found" — never built (`NOT_STARTED`, not a regression), the same 404 bug class `CNS-001`/`UNI-001`/`RPT-001` each already found and fixed for their own roles. Dependencies confirmed sufficient (`AGT-003` complete, `OVS-004` complete for the read side needed). Added a `services/portal.py` handler covering all three named items: application funnel by stage as `metrics` (all 7 confirmed stages, always present at a real zero — `RPT-002-AC02`), visa-status aging (division-wide) as the primary table, agent commission summary as a `panels` list (kept separate from the existing dedicated "commissions" nav section rather than duplicating it). RBAC strictly `overseas_admin` per `RPT-002-AC03` — not extended to Counselor despite being named a secondary actor, matching this codebase's "narrow to only what's confirmed" convention · **Test status:** COMPLETE — 6 new backend pytest cases (`test_rpt_002_overseas_reporting.py`; the shared dev DB's lack of test isolation and a non-unique default test `full_name` both required uuid-suffixed names and presence/delta assertions rather than exact counts), 35/35 across the other files sharing this code path, 3 new Playwright E2E cases (7/7 alongside `rpt-001-reporting.spec.ts`), confirmed live in a real browser  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#rpt-002`  


## Security

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `SEC-001` | Approval-gate audit trail `[base]` | CURRENT | P1 | Should | Wave 2 | S | N | Y | Y |
| `SEC-002` | GDPR consent capture and self-service export/delete | CURRENT | P1 | Should | Wave 2 | M | Y | Y | Y |

### `SEC-001` — Approval-gate audit trail
- **Module:** Security  
- **Description:** Agent-approval (AGT-001) and commission-payout (AGT-004) actions logged with actor, timestamp, outcome.  
- **Actors:** primary — Overseas Admin; secondary — -  
- **Traces to:** BR — · PRD NFR-AUDIT-001 · Decisions DEC-SCOPE-004, DEC-SCOPE-005  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** AGT-001, AGT-004.  
- **Main workflow:** Every approval action writes an audit record (base codebase already has AuditLog + audit helper).  
- **Alternate workflow:** -  
- **Error/edge behavior:** An audit-log write failure must not silently allow the underlying privileged action to proceed unlogged -- fail closed, not open.  
- **Dependencies/blockers:** AGT-001, AGT-004  
- **UX required:** N · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** System (write) / Overseas Admin (implicit)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Directly addresses NFR-AUDIT-001 for this specific domain.  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** Unit, API  
- **Complexity:** S  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-03, Wave 2 — unblocked by `AGT-001`+`AGT-004`) · **Test status:** COMPLETE — 365/365 backend full regression, 7 new backend cases  
  - `AGT-001`'s approve/reject and `AGT-004`'s payout-approval already wrote `AuditLog` rows sharing one `db.commit()` with the privileged write -- fail-closed by construction, confirmed by code inspection *and* a forced-failure test (monkeypatched `AuditLog` constructor), not assumed. The real, confirmed gap: `DATA_MODEL.md` §7.3 names `outcome` as a required `AuditLog` field and this feature's own Description says the same ("...logged with actor, timestamp, outcome") -- the model never had one. New migration `0020` adds `AuditLog.outcome`, set to a real business-outcome value ("approved"/"rejected"/"pending") at exactly this feature's own named call sites (`AGT-001`'s `approve_agent`/`reject_agent`, `AGT-004`'s `approve_commission_payout`) -- untouched call sites elsewhere in the codebase keep a neutral "recorded" default, a deliberate scope boundary matching the Description's own naming of only these two dependencies, not an oversight, directly tested. Also verified (not modified, since both already satisfy the requirement) `RBAC_MATRIX.md` §3's other named privileged writes: GDPR fulfil/reject (`admin.py`'s `fulfil_data_request`) and the certificate-issue override (`workflows.py`, `ADM-006`) each already share one transaction with their own audit write. "Super Admin security-log export" (also named in that control row) has no distinct write action anywhere in the codebase -- only the pre-existing read-only `GET /admin/audit` view -- so there was nothing to audit-log there; not invented as new scope beyond what's confirmed. Surfaced the new field in `GET /admin/audit`'s response and the Super Admin "Security & Audit Logs" page's column list, verified rendering live against the running stack (`curl` through both the API and the Next.js server with a real session). Given the shared `AuditLog` model/migration change, ran the full backend regression rather than a module-scoped one. Backend: 7 new pytest cases -- agent approval/rejection write the correct outcome, the payout-approval two-row audit trail's outcomes and ordering, a forced audit-write failure leaves the agent-approval write unpersisted, a forced audit-write failure leaves the payout-approval write unpersisted, an untouched call site (`auth.login`) keeps the "recorded" default, `GET /admin/audit` surfaces `outcome` (`test_sec_001_audit_trail.py`). No new UI screen (catalog: UX required N) or E2E spec (catalog: Required test types Unit/API only) -- the one frontend change (a column-list addition to an already-existing, already-tested generic table page) was verified directly against the running stack instead.
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sec-001`  

### `SEC-002` — GDPR consent capture and self-service export/delete
- **Module:** Security  
- **Description:** Explicit consent capture at data-collection points; self-service data export/delete requests.  
- **Actors:** primary — All roles; secondary — IT Admin  
- **Traces to:** BR BR-PRIV-001 · PRD NFR-PRIV-001 · Decisions DEC-PRIV-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P1 / Should / Wave 2  
- **Preconditions:** User is authenticated and requesting their own data, or an Admin is acting on a verified request.  
- **Main workflow:** User requests export/delete; system fulfills within a defined SLA (SLA itself open).  
- **Alternate workflow:** -  
- **Error/edge behavior:** Deletion respects legal/financial retention holds where applicable — retention periods still open.  
- **Dependencies/blockers:** —  
- **UX required:** Y · **API required:** Y · **DB impact:** Y · **RBAC/resource-scope:** Self (request) / IT Admin (fulfill)  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** Legal review recommended before release, per BRD Risk R-05.  
- **Accessibility/responsive:** Responsive. · **Performance:** -  
- **Required test types:** API, UI, Security  
- **Complexity:** M  
- **Implementation status:** COMPLETE (`prompts/13`, 2026-09-02, Wave 2) · **Test status:** COMPLETE — 231/231 backend, 104/104 E2E  
  - "Consent capture at data-collection points" is already satisfied by `STU-009`'s `ConsentRecord` (the enrolment agreement) — no other data-collection point is named anywhere in evidence as needing its own consent capture, so nothing new was built there; it is reused, not duplicated.
  - The genuinely net-new half: `DataSubjectRequest` (`apps/api/app/models.py`) — one table with a `type` discriminator, matching `API_CONTRACT.md` §11's single `POST /account/data-requests` endpoint family exactly (`DATA_MODEL.md` §7.4 names two same-shaped conceptual tables; implemented as one, per the approved API contract). Alembic `0016_data_subject_requests`.
  - `POST /account/data-requests` (`type=export|delete`, `Idempotency-Key` required — 422 without one; replaying the same key returns the same request rather than creating a duplicate; reusing a key for a different `type` is a 409). `GET /account/data-requests/{id}` (self-scoped — a 404 masks another user's request, never a 403 that would leak existence). `GET /account/data-requests/{id}/export` (signed download once `status=fulfilled`).
  - Export is fulfilled synchronously: a real JSON export of the requester's own profile/role-assignments/enrolments/certificates/consents is written to storage and served via `storage.presign_download` (same signed-URL mechanism as `STU-007`'s certificate download).
  - Delete is checked against the one concrete, evidence-backed retention-hold rule that exists (`DATA_MODEL.md` §8: never hard-delete a user with an existing `Payment` or `ConsentRecord`) — rejected immediately with a real, visible reason when blocked (never a silent failure), or queued `in_progress` for `IT Admin` review otherwise (`GET`/`PATCH /admin/data-requests/{id}`, `apps/api/app/api/admin.py`). Fulfilment performs a conservative, bounded redaction of the `User` row's own PII fields only (`full_name`, `phone`, `profile`, `email`, `active`) — no cascading deletes into `Enrollment`/`Payment`/`ConsentRecord`/`AuditLog`.
  - Exact retention periods and legal sign-off remain OPEN (`PRD_OPEN_ITEMS.md` item 15, `DEC-PRIV-001`) and are deliberately not invented — only presence of an existing hold is checked, never a numeric period or an automatic deletion timer.
  - Frontend: `/account/privacy` (`apps/web/app/account/privacy/page.tsx` + `DataPrivacyPanel.tsx`) — a shared route reachable from the signed-in header (`HeaderAuthActions.tsx`) rather than any one role's `PORTAL_NAV`, since this feature's actor is "All roles."
  - Backend: 10 new pytest cases (`apps/api/tests/test_sec_002_gdpr_data_requests.py`) — export fulfilled + downloadable, idempotency-key required/replayed/conflicting, a real payment blocks deletion with a visible reason, a clean account queues for admin review, admin fulfilment actually redacts the row, cross-user 404 isolation, auth required, non-admin fulfilment rejected. Full regression: 231/231 passed.
  - Frontend: 4 new Playwright cases (`apps/web/tests/e2e/sec-002-gdpr-data-requests.spec.ts`) — export request + download, a real retention-hold rejection (the seeded demo student has both a `Payment` and a `ConsentRecord`), unauthenticated access, header discoverability. Full regression: 104/104 (8 unrelated specs failed under full-suite worker contention — `adm-005`, `adm-007`, `pub-001`, `pub-004`, `stu-009`, `trn-006`, `trn-007` — and all 8 passed cleanly rerun in isolation, confirming `RAID.md` I-04/I-06 contention, not a regression).
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#sec-002`  


## Operations

| ID | Name | Scope | Pri | MoSCoW | Release | Cx | UX | API | DB |
|---|---|---|---|---|---|---|---|---|---|
| `OPS-001` | DigitalOcean deployment | CURRENT | P0 | Must | Wave 0 | L | N | N | N |
| `OPS-002` | Database migration execution and seed-data decision | CURRENT | P0 | Must | Wave 0 | S | N | N | Y |

### `OPS-001` — DigitalOcean deployment
- **Module:** Operations  
- **Description:** Retarget the existing Docker/Postgres/Redis/Celery workload from its native AWS setup onto DigitalOcean.  
- **Actors:** primary — Engineering; secondary — -  
- **Traces to:** BR — · PRD — · Decisions DEC-INFRA-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 0  
- **Preconditions:** FND-001.  
- **Main workflow:** App deployed and reachable on DigitalOcean infrastructure.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A failed deployment does not silently leave production traffic pointed at a partially-updated instance.  
- **Dependencies/blockers:** FND-001  
- **UX required:** N · **API required:** N · **DB impact:** N · **RBAC/resource-scope:** Engineering  
- **Integration/job/file/audit impact:** DigitalOcean (Managed Postgres/Redis/Spaces/App Platform or Droplets/DOKS — mapping open)  
- **Security/privacy:** TLS termination; DB/Redis not internet-accessible; secrets via env/secret manager (base security baseline).  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** Deployment/smoke  
- **Complexity:** L  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ops-001`  

### `OPS-002` — Database migration execution and seed-data decision
- **Module:** Operations  
- **Description:** Run the existing Alembic migration baseline against the DigitalOcean-hosted PostgreSQL instance; decide what to do with the base codebase's (currently empty) seed data.  
- **Actors:** primary — Engineering; secondary — -  
- **Traces to:** BR — · PRD — · Decisions DEC-ARCH-001, DEC-INFRA-001  
- **Scope/Priority/MoSCoW/Release:** CURRENT / P0 / Must / Wave 0  
- **Preconditions:** OPS-001.  
- **Main workflow:** Migrations run cleanly; seed-data decision made and executed.  
- **Alternate workflow:** -  
- **Error/edge behavior:** A failed migration does not leave the schema in a partially-applied state -- must be transactional/rollback-safe.  
- **Dependencies/blockers:** OPS-001  
- **UX required:** N · **API required:** N · **DB impact:** Y · **RBAC/resource-scope:** Engineering  
- **Integration/job/file/audit impact:** -  
- **Security/privacy:** -  
- **Accessibility/responsive:** - · **Performance:** -  
- **Required test types:** Migration/smoke  
- **Complexity:** S  
- **Implementation status:** NOT_STARTED · **Test status:** NOT_STARTED  
- **Acceptance Criteria:** see `FEATURE_ACCEPTANCE_CRITERIA.md#ops-002`  


---

**APPROVED** by user (in-session), 2026-09-01. GATE-05 satisfied for the 68 `CURRENT` features above. `BLOCKED` features are not authorized to build by this approval — see `FEATURE_QUESTIONS.md` and each feature's blocked reason above.
